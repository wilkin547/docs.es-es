---
title: Información general sobre el SDK de .NET Core
description: Obtenga información sobre el SDK de .NET Core, que es un conjunto de bibliotecas y herramientas utilizadas para crear proyectos .NET Core.
ms.date: 06/20/2016
ms.technology: dotnet-cli
ms.openlocfilehash: f23140166ada0c39d4267a4fd2ba5187b6c13c83
ms.sourcegitcommit: e6ad58812807937b03f5c581a219dcd7d1726b1d
ms.translationtype: HT
ms.contentlocale: es-ES
ms.lasthandoff: 12/10/2018
ms.locfileid: "53169324"
---
# <a name="net-core-sdk-overview"></a><span data-ttu-id="44bba-103">Información general sobre el SDK de .NET Core</span><span class="sxs-lookup"><span data-stu-id="44bba-103">.NET Core SDK Overview</span></span>

<span data-ttu-id="44bba-104">El kit de desarrollo de software (SDK) de .NET Core es un conjunto de bibliotecas y herramientas que permiten a los desarrolladores crear aplicaciones y bibliotecas de .NET Core.</span><span class="sxs-lookup"><span data-stu-id="44bba-104">.NET Core Software Development Kit (SDK) is a set of libraries and tools that allow developers to create .NET Core applications and libraries.</span></span> <span data-ttu-id="44bba-105">Este es el paquete que es más probable que adquieran los desarrolladores.</span><span class="sxs-lookup"><span data-stu-id="44bba-105">This is the package that developers will most likely acquire.</span></span> 

<span data-ttu-id="44bba-106">Incluye los componentes siguientes:</span><span class="sxs-lookup"><span data-stu-id="44bba-106">It contains the following components:</span></span>

1. <span data-ttu-id="44bba-107">Las herramientas de línea de comandos de .NET Core que se usan para compilar aplicaciones.</span><span class="sxs-lookup"><span data-stu-id="44bba-107">The .NET Core Command Line Tools that are used to build applications</span></span>
2. <span data-ttu-id="44bba-108">.NET Core (bibliotecas y entorno de ejecución) que permiten compilar y ejecutar las aplicaciones.</span><span class="sxs-lookup"><span data-stu-id="44bba-108">.NET Core (libraries and runtime) that allow applications to both be built and run</span></span>
3. <span data-ttu-id="44bba-109">El controlador `dotnet` para ejecutar los [comandos de la CLI](tools/index.md), así como también ejecutar aplicaciones.</span><span class="sxs-lookup"><span data-stu-id="44bba-109">The `dotnet` driver for running the [CLI commands](tools/index.md) as well as running applications</span></span>

## <a name="acquiring-the-net-core-sdk"></a><span data-ttu-id="44bba-110">Adquisición del SDK de .NET Core</span><span class="sxs-lookup"><span data-stu-id="44bba-110">Acquiring the .NET Core SDK</span></span>
<span data-ttu-id="44bba-111">Del mismo modo que ocurre con todas las herramientas, lo primero que debe hacer es instalar las herramientas en su máquina.</span><span class="sxs-lookup"><span data-stu-id="44bba-111">As with any tooling, the first thing is to get the tools to your machine.</span></span> <span data-ttu-id="44bba-112">Según el escenario, puede usar los instaladores nativos para instalar el SDK, o bien puede usar el script de shell de instalación.</span><span class="sxs-lookup"><span data-stu-id="44bba-112">Depending on your scenario, you can either use the native installers to install the SDK or use the installation shell script.</span></span>

<span data-ttu-id="44bba-113">Los instaladores nativos están pensados principalmente para las máquinas de los desarrolladores.</span><span class="sxs-lookup"><span data-stu-id="44bba-113">The native installers are primarily meant for developer's machines.</span></span> <span data-ttu-id="44bba-114">El SDK se distribuye mediante el uso del mecanismo de instalación nativo de cada plataforma compatible, por ejemplo, los paquetes DEB en Ubuntu o los conjuntos de MSI en Windows.</span><span class="sxs-lookup"><span data-stu-id="44bba-114">The SDK is distributed using each supported platform's native install mechanism, for instance DEB packages on Ubuntu or MSI bundles on Windows.</span></span> <span data-ttu-id="44bba-115">Estos instaladores instalarán y configurarán el entorno según sea necesario para que el usuario use el SDK inmediatamente después de la instalación.</span><span class="sxs-lookup"><span data-stu-id="44bba-115">These installers will install and set up the environment as needed for the user to use the SDK immediately after the install.</span></span> <span data-ttu-id="44bba-116">Sin embargo, también se necesitan privilegios administrativos en la máquina.</span><span class="sxs-lookup"><span data-stu-id="44bba-116">However, they also require administrative privileges on the machine.</span></span> <span data-ttu-id="44bba-117">Puede ver las instrucciones de instalación en la [Guía de instalación de .NET Core](https://aka.ms/dotnetcoregs).</span><span class="sxs-lookup"><span data-stu-id="44bba-117">You can view the installation instructions on the [.NET Core installation guide](https://aka.ms/dotnetcoregs).</span></span>

<span data-ttu-id="44bba-118">Por otro lado, los scripts de instalación no requieren privilegios administrativos.</span><span class="sxs-lookup"><span data-stu-id="44bba-118">Install scripts, on the other hand, do not require administrative privileges.</span></span> <span data-ttu-id="44bba-119">Sin embargo, tampoco instalan ningún requisito previo en la máquina; debe instalarlos todos manualmente.</span><span class="sxs-lookup"><span data-stu-id="44bba-119">However, they will also not install any prerequisites on the machine; you need to install all of the prerequisites manually.</span></span> <span data-ttu-id="44bba-120">Los scripts están pensados principalmente para configurar servidores de compilación o cuando desee instalar las herramientas sin privilegios de administración (tenga en cuenta la salvedad con respecto a los requisitos previos que ya se mencionó).</span><span class="sxs-lookup"><span data-stu-id="44bba-120">The scripts are meant mostly for setting up build servers or when you wish to install the tools without admin privileges (do note the prerequisites caveat above).</span></span> <span data-ttu-id="44bba-121">Puede encontrar más información en el [tema de referencia sobre los scripts de instalación](tools/dotnet-install-script.md).</span><span class="sxs-lookup"><span data-stu-id="44bba-121">You can find more information on the [install script reference topic](tools/dotnet-install-script.md).</span></span> <span data-ttu-id="44bba-122">Si le interesa saber cómo configurar el SDK en el servidor de compilación de integración continua, consulte el documento sobre el [SDK con servidores de integración continua](tools/using-ci-with-cli.md).</span><span class="sxs-lookup"><span data-stu-id="44bba-122">If you are interested in how to set up SDK on your CI build server you can take a look at the [SDK with CI servers](tools/using-ci-with-cli.md) document.</span></span>

<span data-ttu-id="44bba-123">De forma predeterminada, el SDK se instalará "en paralelo".</span><span class="sxs-lookup"><span data-stu-id="44bba-123">By default, the SDK will install in a "side-by-side" (SxS) manner.</span></span> <span data-ttu-id="44bba-124">Esto significa que pueden coexistir varias versiones de las herramientas de la CLI en un momento dado en una sola máquina.</span><span class="sxs-lookup"><span data-stu-id="44bba-124">This means that multiple versions of the CLI tools can coexist at any given time on a single machine.</span></span> <span data-ttu-id="44bba-125">El uso de la versión correcta se explica en detalle en la [sección de controladores](tools/index.md#driver) del tema sobre las herramientas de línea de comandos de .NET Core.</span><span class="sxs-lookup"><span data-stu-id="44bba-125">How the correct version gets used is explained in more detail in the [driver section](tools/index.md#driver) of .NET Core Command Line Tools topic.</span></span>