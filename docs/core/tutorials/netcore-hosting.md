---
title: Escritura de un host en tiempo de ejecución personalizado de .NET Core
description: Obtenga información sobre cómo hospedar el entorno de tiempo de ejecución de .NET Core desde código nativo para admitir escenarios avanzados que necesitan controlar cómo funciona dicho entorno.
author: mjrousos
ms.topic: how-to
ms.date: 12/21/2018
ms.openlocfilehash: db458e9ebb0c6ad930f82e1d67619632a493ae3b
ms.sourcegitcommit: 46cfed35d79d70e08c313b9c664c7e76babab39e
ms.translationtype: HT
ms.contentlocale: es-ES
ms.lasthandoff: 03/10/2021
ms.locfileid: "102604702"
---
# <a name="write-a-custom-net-core-host-to-control-the-net-runtime-from-your-native-code"></a><span data-ttu-id="3200f-103">Escritura de un host personalizado de .NET Core para controlar el entorno de tiempo de ejecución de .NET desde el código nativo</span><span class="sxs-lookup"><span data-stu-id="3200f-103">Write a custom .NET Core host to control the .NET runtime from your native code</span></span>

<span data-ttu-id="3200f-104">Como todo el código administrado, las aplicaciones de .NET Core se ejecutan mediante un host.</span><span class="sxs-lookup"><span data-stu-id="3200f-104">Like all managed code, .NET Core applications are executed by a host.</span></span> <span data-ttu-id="3200f-105">El host es responsable de iniciar el entorno de ejecución (incluidos componentes como el JIT y el recolector de elementos no utilizados) y de invocar puntos de entrada administrados.</span><span class="sxs-lookup"><span data-stu-id="3200f-105">The host is responsible for starting the runtime (including components like the JIT and garbage collector) and invoking managed entry points.</span></span>

<span data-ttu-id="3200f-106">Hospedar el runtime de .NET Core es un escenario avanzado y, en la mayoría de los casos, los desarrolladores de .NET Core no necesitan preocuparse sobre el hospedaje porque los procesos de compilación de .NET Core proporcionan un host predeterminado para ejecutar aplicaciones de .NET Core.</span><span class="sxs-lookup"><span data-stu-id="3200f-106">Hosting the .NET Core runtime is an advanced scenario and, in most cases, .NET Core developers don't need to worry about hosting because .NET Core build processes provide a default host to run .NET Core applications.</span></span> <span data-ttu-id="3200f-107">En cambio, en algunas circunstancias especializadas, puede ser útil hospedar explícitamente el runtime de .NET Core, como un medio para invocar código administrado en un proceso nativo o para obtener más control sobre el funcionamiento del runtime.</span><span class="sxs-lookup"><span data-stu-id="3200f-107">In some specialized circumstances, though, it can be useful to explicitly host the .NET Core runtime, either as a means of invoking managed code in a native process or in order to gain more control over how the runtime works.</span></span>

<span data-ttu-id="3200f-108">En este artículo se proporciona información general sobre los pasos necesarios para iniciar el entorno de ejecución .NET Core desde código nativo y ejecutar código administrado en él.</span><span class="sxs-lookup"><span data-stu-id="3200f-108">This article gives an overview of the steps necessary to start the .NET Core runtime from native code and execute managed code in it.</span></span>

## <a name="prerequisites"></a><span data-ttu-id="3200f-109">Requisitos previos</span><span class="sxs-lookup"><span data-stu-id="3200f-109">Prerequisites</span></span>

<span data-ttu-id="3200f-110">Como los hosts son aplicaciones nativas, este tutorial trata la creación de una aplicación de C++ para hospedar .NET Core.</span><span class="sxs-lookup"><span data-stu-id="3200f-110">Because hosts are native applications, this tutorial covers constructing a C++ application to host .NET Core.</span></span> <span data-ttu-id="3200f-111">Necesitará un entorno de desarrollo de C++ (como el que se proporciona mediante [Visual Studio](https://aka.ms/vsdownload?utm_source=mscom&utm_campaign=msdocs)).</span><span class="sxs-lookup"><span data-stu-id="3200f-111">You will need a C++ development environment (such as that provided by [Visual Studio](https://aka.ms/vsdownload?utm_source=mscom&utm_campaign=msdocs)).</span></span>

<span data-ttu-id="3200f-112">También querrá una aplicación de .NET Core sencilla con la que probar el host, por lo que debe instalar el [SDK de .NET Core](https://dotnet.microsoft.com/download) y [crear una pequeña aplicación de prueba de .NET Core](with-visual-studio.md) (como una aplicación "Hola a todos").</span><span class="sxs-lookup"><span data-stu-id="3200f-112">You will also want a simple .NET Core application to test the host with, so you should install the [.NET Core SDK](https://dotnet.microsoft.com/download) and [build a small .NET Core test app](with-visual-studio.md) (such as a 'Hello World' app).</span></span> <span data-ttu-id="3200f-113">La aplicación "Hola a todos" que se ha creado mediante la nueva plantilla de proyecto de la consola de .NET Core es suficiente.</span><span class="sxs-lookup"><span data-stu-id="3200f-113">The 'Hello World' app created by the new .NET Core console project template is sufficient.</span></span>

## <a name="hosting-apis"></a><span data-ttu-id="3200f-114">API de hospedaje</span><span class="sxs-lookup"><span data-stu-id="3200f-114">Hosting APIs</span></span>

<span data-ttu-id="3200f-115">Hay dos API distintas que pueden usarse para hospedar .NET Core.</span><span class="sxs-lookup"><span data-stu-id="3200f-115">There are two different APIs that can be used to host .NET Core.</span></span> <span data-ttu-id="3200f-116">En este artículo (y sus [ejemplos](https://github.com/dotnet/samples/tree/master/core/hosting) asociados) se describen estas dos opciones.</span><span class="sxs-lookup"><span data-stu-id="3200f-116">This article (and its associated [samples](https://github.com/dotnet/samples/tree/master/core/hosting)) covers these 2 options.</span></span>

* <span data-ttu-id="3200f-117">El método preferido para hospedar el tiempo de ejecución de .NET Core en .NET Core 3.0 y superior es con las APIs de las bibliotecas `nethost` y `hostfxr`.</span><span class="sxs-lookup"><span data-stu-id="3200f-117">The preferred method of hosting the .NET Core runtime in .NET Core 3.0 and above is with the `nethost` and `hostfxr` libraries' APIs.</span></span> <span data-ttu-id="3200f-118">Estos puntos de entrada tratan la complejidad de buscar y configurar el entorno de ejecución para la inicialización y permiten tanto el inicio de una aplicación administrada como la llamada a un método administrado estático.</span><span class="sxs-lookup"><span data-stu-id="3200f-118">These entry points handle the complexity of finding and setting up the runtime for initialization and allow both launching a managed application and calling into a static managed method.</span></span>
* <span data-ttu-id="3200f-119">El método preferido para hospedar el entorno de ejecución .NET Core antes de la versión .NET Core 3.0 es con la API de [`coreclrhost.h`](https://github.com/dotnet/runtime/blob/master/src/coreclr/hosts/inc/coreclrhost.h).</span><span class="sxs-lookup"><span data-stu-id="3200f-119">The preferred method of hosting the .NET Core runtime prior to .NET Core 3.0 is with the [`coreclrhost.h`](https://github.com/dotnet/runtime/blob/master/src/coreclr/hosts/inc/coreclrhost.h) API.</span></span> <span data-ttu-id="3200f-120">Esta API expone funciones para iniciar y detener fácilmente el entorno de ejecución e invocar código administrado (ya sea mediante el inicio de un archivo .exe administrado o una llamada a métodos estáticos administrados).</span><span class="sxs-lookup"><span data-stu-id="3200f-120">This API exposes functions for easily starting and stopping the runtime and invoking managed code (either by launching a managed exe or by calling static managed methods).</span></span>

## <a name="sample-hosts"></a><span data-ttu-id="3200f-121">Hosts de ejemplo</span><span class="sxs-lookup"><span data-stu-id="3200f-121">Sample Hosts</span></span>

<span data-ttu-id="3200f-122">Hay [hosts de ejemplo](https://github.com/dotnet/samples/tree/master/core/hosting) que muestran los pasos descritos en los tutoriales siguientes en el repositorio dotnet/samples de GitHub.</span><span class="sxs-lookup"><span data-stu-id="3200f-122">[Sample hosts](https://github.com/dotnet/samples/tree/master/core/hosting) demonstrating the steps outlined in the tutorials below are available in the dotnet/samples GitHub repository.</span></span> <span data-ttu-id="3200f-123">Los comentarios de los ejemplos asocian claramente los pasos numerados de estos tutoriales con el lugar donde se realizan en el ejemplo.</span><span class="sxs-lookup"><span data-stu-id="3200f-123">Comments in the samples clearly associate the numbered steps from these tutorials with where they're performed in the sample.</span></span> <span data-ttu-id="3200f-124">Para obtener instrucciones de descarga, vea [Ejemplos y tutoriales](../../samples-and-tutorials/index.md#view-and-download-samples).</span><span class="sxs-lookup"><span data-stu-id="3200f-124">For download instructions, see [Samples and Tutorials](../../samples-and-tutorials/index.md#view-and-download-samples).</span></span>

<span data-ttu-id="3200f-125">Tenga en cuenta que los hosts de ejemplo están pensados para usarse con fines de aprendizaje, por lo que pasan por encima de la comprobación de errores y están diseñados para enfatizar la legibilidad sobre la eficacia.</span><span class="sxs-lookup"><span data-stu-id="3200f-125">Keep in mind that the sample hosts are meant to be used for learning purposes, so they are light on error checking and are designed to emphasize readability over efficiency.</span></span>

## <a name="create-a-host-using-nethosth-and-hostfxrh"></a><span data-ttu-id="3200f-126">Creación de un host mediante `nethost.h` y `hostfxr.h`</span><span class="sxs-lookup"><span data-stu-id="3200f-126">Create a host using `nethost.h` and `hostfxr.h`</span></span>

<span data-ttu-id="3200f-127">En los siguientes pasos se explica cómo usar las bibliotecas `nethost` y `hostfxr` para iniciar el entorno de ejecución de .NET Core en una aplicación nativa y llamar a un método estático administrado.</span><span class="sxs-lookup"><span data-stu-id="3200f-127">The following steps detail how to use the `nethost` and `hostfxr` libraries to start the .NET Core runtime in a native application and call into a managed static method.</span></span> <span data-ttu-id="3200f-128">En el [ejemplo](https://github.com/dotnet/samples/tree/master/core/hosting/HostWithHostFxr) se usa el encabezado `nethost` y la biblioteca instalados con el SDK de .NET así como copias de los archivos [`coreclr_delegates.h`](https://github.com/dotnet/runtime/blob/main/src/native/corehost/coreclr_delegates.h) y [`hostfxr.h`](https://github.com/dotnet/runtime/blob/master/src/native/corehost/hostfxr.h) del repositorio [dotnet/runtime](https://github.com/dotnet/runtime).</span><span class="sxs-lookup"><span data-stu-id="3200f-128">The [sample](https://github.com/dotnet/samples/tree/master/core/hosting/HostWithHostFxr) uses the `nethost` header and library installed with the .NET SDK and copies of the [`coreclr_delegates.h`](https://github.com/dotnet/runtime/blob/main/src/native/corehost/coreclr_delegates.h) and [`hostfxr.h`](https://github.com/dotnet/runtime/blob/master/src/native/corehost/hostfxr.h) files from the [dotnet/runtime](https://github.com/dotnet/runtime) repository.</span></span>

### <a name="step-1---load-hostfxr-and-get-exported-hosting-functions"></a><span data-ttu-id="3200f-129">Paso 1: Carga de `hostfxr` y obtención de funciones de hospedaje exportadas</span><span class="sxs-lookup"><span data-stu-id="3200f-129">Step 1 - Load `hostfxr` and get exported hosting functions</span></span>

<span data-ttu-id="3200f-130">La biblioteca `nethost` proporciona la función `get_hostfxr_path` para buscar la biblioteca `hostfxr`.</span><span class="sxs-lookup"><span data-stu-id="3200f-130">The `nethost` library provides the `get_hostfxr_path` function for locating the `hostfxr` library.</span></span> <span data-ttu-id="3200f-131">La biblioteca `hostfxr` expone funciones para hospedar el entorno de ejecución de .NET Core.</span><span class="sxs-lookup"><span data-stu-id="3200f-131">The `hostfxr` library exposes functions for hosting the .NET Core runtime.</span></span> <span data-ttu-id="3200f-132">Encontrará la lista completa de funciones en [`hostfxr.h`](https://github.com/dotnet/runtime/blob/master/src/native/corehost/hostfxr.h) y en el [documento de diseño de hospedaje nativo](https://github.com/dotnet/runtime/blob/master/docs/design/features/native-hosting.md).</span><span class="sxs-lookup"><span data-stu-id="3200f-132">The full list of functions can be found in [`hostfxr.h`](https://github.com/dotnet/runtime/blob/master/src/native/corehost/hostfxr.h) and the [native hosting design document](https://github.com/dotnet/runtime/blob/master/docs/design/features/native-hosting.md).</span></span> <span data-ttu-id="3200f-133">El ejemplo y este tutorial usan lo siguiente:</span><span class="sxs-lookup"><span data-stu-id="3200f-133">The sample and this tutorial use the following:</span></span>

* <span data-ttu-id="3200f-134">`hostfxr_initialize_for_runtime_config`: Inicializa un contexto de host y se prepara para la inicialización del entorno de ejecución de .NET Core mediante la configuración del entorno de ejecución especificado.</span><span class="sxs-lookup"><span data-stu-id="3200f-134">`hostfxr_initialize_for_runtime_config`: Initializes a host context and prepares for initialization of the .NET Core runtime using the specified runtime configuration.</span></span>
* <span data-ttu-id="3200f-135">`hostfxr_get_runtime_delegate`: Obtiene un delegado para la funcionalidad del entorno de ejecución.</span><span class="sxs-lookup"><span data-stu-id="3200f-135">`hostfxr_get_runtime_delegate`: Gets a delegate for runtime functionality.</span></span>
* <span data-ttu-id="3200f-136">`hostfxr_close`: Cierra un contexto del host.</span><span class="sxs-lookup"><span data-stu-id="3200f-136">`hostfxr_close`: Closes a host context.</span></span>

<span data-ttu-id="3200f-137">La biblioteca `hostfxr` se encuentra mediante `get_hostfxr_path`.</span><span class="sxs-lookup"><span data-stu-id="3200f-137">The `hostfxr` library is found using `get_hostfxr_path`.</span></span> <span data-ttu-id="3200f-138">Después, se carga y se recuperan las exportaciones.</span><span class="sxs-lookup"><span data-stu-id="3200f-138">It is then loaded and its exports are retrieved.</span></span>

[!code-cpp[HostFxrHost#LoadHostFxr](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithHostFxr/src/NativeHost/nativehost.cpp#LoadHostFxr)]

### <a name="step-2---initialize-and-start-the-net-core-runtime"></a><span data-ttu-id="3200f-139">Paso 2: Inicialización e inicio del entorno de ejecución de .NET Core</span><span class="sxs-lookup"><span data-stu-id="3200f-139">Step 2 - Initialize and start the .NET Core runtime</span></span>

<span data-ttu-id="3200f-140">Las funciones `hostfxr_initialize_for_runtime_config` y `hostfxr_get_runtime_delegate` inicializan e inician el entorno de ejecución de .NET Core con la configuración del entorno de ejecución para el componente administrado que se va a cargar.</span><span class="sxs-lookup"><span data-stu-id="3200f-140">The `hostfxr_initialize_for_runtime_config` and `hostfxr_get_runtime_delegate` functions initialize and start the .NET Core runtime using the runtime configuration for the managed component that will be loaded.</span></span> <span data-ttu-id="3200f-141">La función `hostfxr_get_runtime_delegate` se utiliza para obtener un delegado del entorno de ejecución que permite cargar un ensamblado administrado y obtener un puntero de función para un método estático en ese ensamblado.</span><span class="sxs-lookup"><span data-stu-id="3200f-141">The `hostfxr_get_runtime_delegate` function is used to get a runtime delegate that allows loading a managed assembly and getting a function pointer to a static method in that assembly.</span></span>

[!code-cpp[HostFxrHost#Initialize](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithHostFxr/src/NativeHost/nativehost.cpp#Initialize)]

### <a name="step-3---load-managed-assembly-and-get-function-pointer-to-a-managed-method"></a><span data-ttu-id="3200f-142">Paso 3: Carga del ensamblado administrado y obtención un puntero de función a un método administrado</span><span class="sxs-lookup"><span data-stu-id="3200f-142">Step 3 - Load managed assembly and get function pointer to a managed method</span></span>

<span data-ttu-id="3200f-143">Se llama al delegado del entorno de ejecución para cargar el ensamblado administrado y obtener un puntero de función a un método administrado.</span><span class="sxs-lookup"><span data-stu-id="3200f-143">The runtime delegate is called to load the managed assembly and get a function pointer to a managed method.</span></span> <span data-ttu-id="3200f-144">El delegado necesita la ruta de acceso de ensamblado, el nombre del tipo y el nombre del método como entradas y devuelve un puntero de función que se puede utilizar para llamar al método administrado.</span><span class="sxs-lookup"><span data-stu-id="3200f-144">The delegate requires the assembly path, type name, and method name as inputs and returns a function pointer that can be used to invoke the managed method.</span></span>

[!code-cpp[HostFxrHost#LoadAndGet](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithHostFxr/src/NativeHost/nativehost.cpp#LoadAndGet)]

<span data-ttu-id="3200f-145">Al pasar `nullptr` como nombre de tipo de delegado al llamar al delegado del entorno de ejecución, el ejemplo utiliza una firma predeterminada para el método administrado:</span><span class="sxs-lookup"><span data-stu-id="3200f-145">By passing `nullptr` as the delegate type name when calling the runtime delegate, the sample uses a default signature for the managed method:</span></span>

```csharp
public delegate int ComponentEntryPoint(IntPtr args, int sizeBytes);
```

<span data-ttu-id="3200f-146">Se puede utilizar una firma diferente si se especifica el nombre del tipo de delegado al llamar al delegado en entorno de ejecución.</span><span class="sxs-lookup"><span data-stu-id="3200f-146">A different signature can be used by specifying the delegate type name when calling the runtime delegate.</span></span>

### <a name="step-4---run-managed-code"></a><span data-ttu-id="3200f-147">Paso 4: Ejecutar el código administrado</span><span class="sxs-lookup"><span data-stu-id="3200f-147">Step 4 - Run managed code!</span></span>

<span data-ttu-id="3200f-148">El host nativo ahora puede llamar al método administrado y pasarle los parámetros deseados.</span><span class="sxs-lookup"><span data-stu-id="3200f-148">The native host can now call the managed method and pass it the desired parameters.</span></span>

[!code-cpp[HostFxrHost#CallManaged](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithHostFxr/src/NativeHost/nativehost.cpp#CallManaged)]

## <a name="create-a-host-using-coreclrhosth"></a><span data-ttu-id="3200f-149">Creación de un host mediante `coreclrhost.h`</span><span class="sxs-lookup"><span data-stu-id="3200f-149">Create a host using `coreclrhost.h`</span></span>

<span data-ttu-id="3200f-150">En los siguientes pasos se explica cómo usar la API de `coreclrhost.h` para iniciar el entorno de ejecución de .NET Core en una aplicación nativa y llamar a un método estático administrado.</span><span class="sxs-lookup"><span data-stu-id="3200f-150">The following steps detail how to use the `coreclrhost.h` API to start the .NET Core runtime in a native application and call into a managed static method.</span></span> <span data-ttu-id="3200f-151">Los fragmentos de código de este documento usan algunas API específicas de Windows, pero el [host de ejemplo completo](https://github.com/dotnet/samples/tree/master/core/hosting/HostWithCoreClrHost) muestra las rutas de acceso de código de Windows y Linux.</span><span class="sxs-lookup"><span data-stu-id="3200f-151">The code snippets in this document use some Windows-specific APIs, but the [full sample host](https://github.com/dotnet/samples/tree/master/core/hosting/HostWithCoreClrHost) shows both Windows and Linux code paths.</span></span>

<span data-ttu-id="3200f-152">El [host Unix CoreRun](https://github.com/dotnet/runtime/tree/master/src/coreclr/hosts/unixcorerun) muestra un ejemplo más complejo y real de hospedaje mediante `coreclrhost.h`.</span><span class="sxs-lookup"><span data-stu-id="3200f-152">The [Unix CoreRun host](https://github.com/dotnet/runtime/tree/master/src/coreclr/hosts/unixcorerun) shows a more complex, real-world example of hosting using `coreclrhost.h`.</span></span>

### <a name="step-1---find-and-load-coreclr"></a><span data-ttu-id="3200f-153">Paso 1: Buscar y cargar CoreCLR</span><span class="sxs-lookup"><span data-stu-id="3200f-153">Step 1 - Find and load CoreCLR</span></span>

<span data-ttu-id="3200f-154">Las API del entorno de ejecución .NET Core están en *coreclr.dll* (en Windows), en *libcoreclr.so* (en Linux) o en *libcoreclr.dylib* (en macOS).</span><span class="sxs-lookup"><span data-stu-id="3200f-154">The .NET Core runtime APIs are in *coreclr.dll* (on Windows), in *libcoreclr.so* (on Linux), or in *libcoreclr.dylib* (on macOS).</span></span> <span data-ttu-id="3200f-155">El primer paso para el hospedaje de .NET Core es cargar la biblioteca de CoreCLR.</span><span class="sxs-lookup"><span data-stu-id="3200f-155">The first step to hosting .NET Core is to load the CoreCLR library.</span></span> <span data-ttu-id="3200f-156">Algunos hosts sondean diferentes rutas de acceso o usan parámetros de entrada para buscar la biblioteca, mientras que otros saben cargarla desde una ruta de acceso determinada (junto al host, por ejemplo, o desde una ubicación de todo el equipo).</span><span class="sxs-lookup"><span data-stu-id="3200f-156">Some hosts probe different paths or use input parameters to find the library while others know to load it from a certain path (next to the host, for example, or from a machine-wide location).</span></span>

<span data-ttu-id="3200f-157">Una vez encontrada, la biblioteca se carga con `LoadLibraryEx` (en Windows) o `dlopen` (en Linux o macOS).</span><span class="sxs-lookup"><span data-stu-id="3200f-157">Once found, the library is loaded with `LoadLibraryEx` (on Windows) or `dlopen` (on Linux/macOS).</span></span>

[!code-cpp[CoreClrHost#1](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithCoreClrHost/src/SampleHost.cpp#1)]

### <a name="step-2---get-net-core-hosting-functions"></a><span data-ttu-id="3200f-158">Paso 2: Obtener las funciones de hospedaje de .NET Core</span><span class="sxs-lookup"><span data-stu-id="3200f-158">Step 2 - Get .NET Core hosting functions</span></span>

<span data-ttu-id="3200f-159">CoreClrHost tiene varios métodos importantes de utilidad para el hospedaje de .NET Core:</span><span class="sxs-lookup"><span data-stu-id="3200f-159">CoreClrHost has several important methods useful for hosting .NET Core:</span></span>

* <span data-ttu-id="3200f-160">`coreclr_initialize`: Inicia el entorno de ejecución .NET Core y configura el valor AppDomain predeterminado (y único).</span><span class="sxs-lookup"><span data-stu-id="3200f-160">`coreclr_initialize`: Starts the .NET Core runtime and sets up the default (and only) AppDomain.</span></span>
* <span data-ttu-id="3200f-161">`coreclr_execute_assembly`: Ejecuta un ensamblado administrado.</span><span class="sxs-lookup"><span data-stu-id="3200f-161">`coreclr_execute_assembly`: Executes a managed assembly.</span></span>
* <span data-ttu-id="3200f-162">`coreclr_create_delegate`: Crea un puntero de función a un método administrado.</span><span class="sxs-lookup"><span data-stu-id="3200f-162">`coreclr_create_delegate`: Creates a function pointer to a managed method.</span></span>
* <span data-ttu-id="3200f-163">`coreclr_shutdown`: Cierra el entorno de ejecución .NET Core.</span><span class="sxs-lookup"><span data-stu-id="3200f-163">`coreclr_shutdown`: Shuts down the .NET Core runtime.</span></span>
* <span data-ttu-id="3200f-164">`coreclr_shutdown_2`: Es igual que `coreclr_shutdown`, pero además recupera el código de salida del código administrado.</span><span class="sxs-lookup"><span data-stu-id="3200f-164">`coreclr_shutdown_2`: Like `coreclr_shutdown`, but also retrieves the managed code's exit code.</span></span>

<span data-ttu-id="3200f-165">Después de cargar la biblioteca de CoreCLR, el siguiente paso es obtener referencias a estas funciones mediante `GetProcAddress` (en Windows) o `dlsym` (en Linux o macOS).</span><span class="sxs-lookup"><span data-stu-id="3200f-165">After loading the CoreCLR library, the next step is to get references to these functions using `GetProcAddress` (on Windows) or `dlsym` (on Linux/macOS).</span></span>

[!code-cpp[CoreClrHost#2](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithCoreClrHost/src/SampleHost.cpp#2)]

### <a name="step-3---prepare-runtime-properties"></a><span data-ttu-id="3200f-166">Paso 3: Preparar las propiedades del entorno de ejecución</span><span class="sxs-lookup"><span data-stu-id="3200f-166">Step 3 - Prepare runtime properties</span></span>

<span data-ttu-id="3200f-167">Antes de iniciar el entorno de ejecución, es necesario preparar algunas propiedades para especificar el comportamiento (especialmente las relacionadas con el cargador de ensamblados).</span><span class="sxs-lookup"><span data-stu-id="3200f-167">Before starting the runtime, it is necessary to prepare some properties to specify behavior (especially concerning the assembly loader).</span></span>

<span data-ttu-id="3200f-168">Algunas propiedades comunes incluyen:</span><span class="sxs-lookup"><span data-stu-id="3200f-168">Common properties include:</span></span>

* <span data-ttu-id="3200f-169">`TRUSTED_PLATFORM_ASSEMBLIES` Esta es una lista de rutas de acceso de ensamblado (delimitadas por ";" en Windows y ":" en Linux) que el entorno de ejecución puede resolver de forma predeterminada.</span><span class="sxs-lookup"><span data-stu-id="3200f-169">`TRUSTED_PLATFORM_ASSEMBLIES` This is a list of assembly paths (delimited by ';' on Windows and ':' on Linux) which the runtime will be able to resolve by default.</span></span> <span data-ttu-id="3200f-170">Algunos hosts tienen manifiestos codificados de forma rígida que enumeran los ensamblados que pueden cargar.</span><span class="sxs-lookup"><span data-stu-id="3200f-170">Some hosts have hard-coded manifests listing assemblies they can load.</span></span> <span data-ttu-id="3200f-171">Otros colocan cualquier biblioteca en determinadas ubicaciones (junto a *coreclr.dll*, por ejemplo) en esta lista.</span><span class="sxs-lookup"><span data-stu-id="3200f-171">Others will put any library in certain locations (next to *coreclr.dll*, for example) on this list.</span></span>
* <span data-ttu-id="3200f-172">`APP_PATHS` Esta es una lista de rutas de acceso para explorar un ensamblado si este no puede encontrarse en la lista de ensamblados de plataforma de confianza (TPA).</span><span class="sxs-lookup"><span data-stu-id="3200f-172">`APP_PATHS` This is a list of paths to probe in for an assembly if it can't be found in the trusted platform assemblies (TPA) list.</span></span> <span data-ttu-id="3200f-173">Dado que el host tiene más control sobre los ensamblados que se cargan mediante la lista TPA, es recomendable que los hosts determinen qué ensamblados esperan cargar y los muestren de forma explícita.</span><span class="sxs-lookup"><span data-stu-id="3200f-173">Because the host has more control over which assemblies are loaded using the TPA list, it is a best practice for hosts to determine which assemblies they expect to load and list them explicitly.</span></span> <span data-ttu-id="3200f-174">Pero si es necesario sondear en tiempo de ejecución, esta propiedad se lo permite.</span><span class="sxs-lookup"><span data-stu-id="3200f-174">If probing at run time is needed, however, this property can enable that scenario.</span></span>
* <span data-ttu-id="3200f-175">`APP_NI_PATHS` Esta lista es similar a APP_PATHS, excepto que son las rutas de acceso las que se sondean en busca de imágenes nativas.</span><span class="sxs-lookup"><span data-stu-id="3200f-175">`APP_NI_PATHS` This list is similar to APP_PATHS except that it's meant to be paths that will be probed for native images.</span></span>
* <span data-ttu-id="3200f-176">`NATIVE_DLL_SEARCH_DIRECTORIES` Esta propiedad es una lista de rutas de acceso que el cargador debe sondear cuando busca bibliotecas nativas llamadas mediante p/invoke.</span><span class="sxs-lookup"><span data-stu-id="3200f-176">`NATIVE_DLL_SEARCH_DIRECTORIES` This property is a list of paths the loader should probe when looking for native libraries called via p/invoke.</span></span>
* <span data-ttu-id="3200f-177">`PLATFORM_RESOURCE_ROOTS` Esta lista incluye rutas de acceso que se van a explorar para ensamblados satélite de recursos (en subdirectorios específicos de la referencia cultural).</span><span class="sxs-lookup"><span data-stu-id="3200f-177">`PLATFORM_RESOURCE_ROOTS` This list includes paths to probe in for resource satellite assemblies (in culture-specific subdirectories).</span></span>

<span data-ttu-id="3200f-178">En este host de ejemplo, la lista TPA se construye simplemente mediante la enumeración de todas las bibliotecas del directorio actual:</span><span class="sxs-lookup"><span data-stu-id="3200f-178">In this sample host, the TPA list is constructed by simply listing all libraries in the current directory:</span></span>

[!code-cpp[CoreClrHost#7](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithCoreClrHost/src/SampleHost.cpp#7)]

<span data-ttu-id="3200f-179">Dado que el ejemplo es simple, solo necesita la propiedad `TRUSTED_PLATFORM_ASSEMBLIES`:</span><span class="sxs-lookup"><span data-stu-id="3200f-179">Because the sample is simple, it only needs the `TRUSTED_PLATFORM_ASSEMBLIES` property:</span></span>

[!code-cpp[CoreClrHost#3](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithCoreClrHost/src/SampleHost.cpp#3)]

### <a name="step-4---start-the-runtime"></a><span data-ttu-id="3200f-180">Paso 4: Iniciar el entorno de ejecución</span><span class="sxs-lookup"><span data-stu-id="3200f-180">Step 4 - Start the runtime</span></span>

<span data-ttu-id="3200f-181">Las API de `coreclrhost.h` inician el tiempo de ejecución y crean el AppDomain predeterminado todos con una sola llamada.</span><span class="sxs-lookup"><span data-stu-id="3200f-181">`coreclrhost.h` APIs start the runtime and create the default AppDomain all with a single call.</span></span> <span data-ttu-id="3200f-182">La función `coreclr_initialize` toma una ruta de acceso base, un nombre y las propiedades descritas anteriormente y devuelve un manipulador al host a través del parámetro `hostHandle`.</span><span class="sxs-lookup"><span data-stu-id="3200f-182">The `coreclr_initialize` function takes a base path, name, and the properties described earlier and returns back a handle to the host via the `hostHandle` parameter.</span></span>

[!code-cpp[CoreClrHost#4](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithCoreClrHost/src/SampleHost.cpp#4)]

### <a name="step-5---run-managed-code"></a><span data-ttu-id="3200f-183">Paso 5: Ejecutar el código administrado</span><span class="sxs-lookup"><span data-stu-id="3200f-183">Step 5 - Run managed code!</span></span>

<span data-ttu-id="3200f-184">Con el entorno de ejecución iniciado, el host puede llamar al código administrado.</span><span class="sxs-lookup"><span data-stu-id="3200f-184">With the runtime started, the host can call managed code.</span></span> <span data-ttu-id="3200f-185">Esta operación se puede realizar de diferentes maneras.</span><span class="sxs-lookup"><span data-stu-id="3200f-185">This can be done in a couple of different ways.</span></span> <span data-ttu-id="3200f-186">El código de ejemplo vinculado a este tutorial usa la función `coreclr_create_delegate` para crear un delegado para un método administrado estático.</span><span class="sxs-lookup"><span data-stu-id="3200f-186">The sample code linked to this tutorial uses the `coreclr_create_delegate` function to create a delegate to a static managed method.</span></span> <span data-ttu-id="3200f-187">Esta API toma el [nombre del ensamblado](../../standard/assembly/names.md), el nombre de tipo calificado por el espacio de nombres y el nombre del método como entradas y devuelve un delegado que puede usarse para invocar al método.</span><span class="sxs-lookup"><span data-stu-id="3200f-187">This API takes the [assembly name](../../standard/assembly/names.md), namespace-qualified type name, and method name as inputs and returns a delegate that can be used to invoke the method.</span></span>

[!code-cpp[CoreClrHost#5](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithCoreClrHost/src/SampleHost.cpp#5)]

<span data-ttu-id="3200f-188">En este ejemplo, el host ya puede llamar a `managedDelegate` para ejecutar el método `ManagedWorker.DoWork`.</span><span class="sxs-lookup"><span data-stu-id="3200f-188">In this sample, the host can now call `managedDelegate` to run the `ManagedWorker.DoWork` method.</span></span>

<span data-ttu-id="3200f-189">También se puede usar la función `coreclr_execute_assembly` para iniciar un archivo ejecutable administrado.</span><span class="sxs-lookup"><span data-stu-id="3200f-189">Alternatively, the `coreclr_execute_assembly` function can be used to launch a managed executable.</span></span> <span data-ttu-id="3200f-190">Esta API toma una ruta de acceso de ensamblado y una matriz de argumentos como parámetros de entrada.</span><span class="sxs-lookup"><span data-stu-id="3200f-190">This API takes an assembly path and array of arguments as input parameters.</span></span> <span data-ttu-id="3200f-191">Carga el ensamblado en esa ruta de acceso e invoca a su método principal.</span><span class="sxs-lookup"><span data-stu-id="3200f-191">It loads the assembly at that path and invokes its main method.</span></span>

```c++
int hr = executeAssembly(
        hostHandle,
        domainId,
        argumentCount,
        arguments,
        "HelloWorld.exe",
        (unsigned int*)&exitCode);
```

### <a name="step-6---shutdown-and-clean-up"></a><span data-ttu-id="3200f-192">Paso 6: Cerrar y limpiar</span><span class="sxs-lookup"><span data-stu-id="3200f-192">Step 6 - Shutdown and clean up</span></span>

<span data-ttu-id="3200f-193">Por último, cuando el host termina de ejecutar el código administrado, el entorno de ejecución .NET Core se cierra con `coreclr_shutdown` o `coreclr_shutdown_2`.</span><span class="sxs-lookup"><span data-stu-id="3200f-193">Finally, when the host is done running managed code, the .NET Core runtime is shut down with `coreclr_shutdown` or `coreclr_shutdown_2`.</span></span>

[!code-cpp[CoreClrHost#6](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithCoreClrHost/src/SampleHost.cpp#6)]

<span data-ttu-id="3200f-194">CoreCLR no admite la reinicialización ni la descarga.</span><span class="sxs-lookup"><span data-stu-id="3200f-194">CoreCLR does not support reinitialization or unloading.</span></span> <span data-ttu-id="3200f-195">No vuelva a llamar a `coreclr_initialize` ni descargue la biblioteca de CoreCLR.</span><span class="sxs-lookup"><span data-stu-id="3200f-195">Do not call `coreclr_initialize` again or unload the CoreCLR library.</span></span>

## <a name="conclusion"></a><span data-ttu-id="3200f-196">Conclusión</span><span class="sxs-lookup"><span data-stu-id="3200f-196">Conclusion</span></span>

<span data-ttu-id="3200f-197">Una vez que se ha compilado el host, este puede probarse ejecutándose desde la línea de comandos y pasando cualquier argumento que espere el host.</span><span class="sxs-lookup"><span data-stu-id="3200f-197">Once your host is built, it can be tested by running it from the command line and passing any arguments the host expects.</span></span> <span data-ttu-id="3200f-198">Al especificar la aplicación de .NET Core para el host que se va a ejecutar, asegúrese de usar el archivo .dll que ha generado `dotnet build`.</span><span class="sxs-lookup"><span data-stu-id="3200f-198">When specifying the .NET Core app for the host to run, be sure to use the .dll that is produced by `dotnet build`.</span></span> <span data-ttu-id="3200f-199">Los archivos ejecutables (archivos .exe) que ha generado `dotnet publish` para las aplicaciones autocontenidas son realmente el host de .NET Core predeterminado (de manera que la aplicación pueda iniciarse directamente desde la línea de comandos en escenarios principales); el código de usuario se compila en un archivo .dll del mismo nombre.</span><span class="sxs-lookup"><span data-stu-id="3200f-199">Executables (.exe files) produced by `dotnet publish` for self-contained applications are actually the default .NET Core host (so that the app can be launched directly from the command line in mainline scenarios); user code is compiled into a dll of the same name.</span></span>

<span data-ttu-id="3200f-200">Si las cosas no funcionan inicialmente, vuelva a comprobar que *coreclr.dll* está disponible en la ubicación que espera el host, que todas las bibliotecas de Framework necesarias están en la lista TPA y que el valor de bits de CoreCLR (32 o 64 bits) coincide con la manera en que se ha compilado el host.</span><span class="sxs-lookup"><span data-stu-id="3200f-200">If things don't work initially, double-check that *coreclr.dll* is available in the location expected by the host, that all necessary Framework libraries are in the TPA list, and that CoreCLR's bitness (32-bit or 64-bit) matches how the host was built.</span></span>

<span data-ttu-id="3200f-201">Hospedar el runtime de .NET Core es un escenario avanzado que muchos desarrolladores no necesitarán, pero para los que necesiten iniciar el código administrado desde un proceso nativo, o que necesiten más control sobre el comportamiento del runtime de .NET Core, puede resultar muy útil.</span><span class="sxs-lookup"><span data-stu-id="3200f-201">Hosting the .NET Core runtime is an advanced scenario that many developers won't require, but for those who need to launch managed code from a native process, or who need more control over the .NET Core runtime's behavior, it can be very useful.</span></span>
