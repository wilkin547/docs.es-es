---
title: Escritura de un host en tiempo de ejecución personalizado de .NET Core
description: Obtenga información sobre cómo hospedar el entorno de tiempo de ejecución de .NET Core desde código nativo para admitir escenarios avanzados que necesitan controlar cómo funciona dicho entorno.
author: mjrousos
ms.topic: how-to
ms.date: 12/21/2018
ms.openlocfilehash: 342a0cec78303f70db3a5b31294be1d465459f55
ms.sourcegitcommit: 046a9c22487551360e20ec39fc21eef99820a254
ms.translationtype: HT
ms.contentlocale: es-ES
ms.lasthandoff: 05/14/2020
ms.locfileid: "83394852"
---
# <a name="write-a-custom-net-core-host-to-control-the-net-runtime-from-your-native-code"></a><span data-ttu-id="a829d-103">Escritura de un host personalizado de .NET Core para controlar el entorno de tiempo de ejecución de .NET desde el código nativo</span><span class="sxs-lookup"><span data-stu-id="a829d-103">Write a custom .NET Core host to control the .NET runtime from your native code</span></span>

<span data-ttu-id="a829d-104">Como todo el código administrado, las aplicaciones de .NET Core se ejecutan mediante un host.</span><span class="sxs-lookup"><span data-stu-id="a829d-104">Like all managed code, .NET Core applications are executed by a host.</span></span> <span data-ttu-id="a829d-105">El host es responsable de iniciar el entorno de ejecución (incluidos componentes como el JIT y el recolector de elementos no utilizados) y de invocar puntos de entrada administrados.</span><span class="sxs-lookup"><span data-stu-id="a829d-105">The host is responsible for starting the runtime (including components like the JIT and garbage collector) and invoking managed entry points.</span></span>

<span data-ttu-id="a829d-106">Hospedar el runtime de .NET Core es un escenario avanzado y, en la mayoría de los casos, los desarrolladores de .NET Core no necesitan preocuparse sobre el hospedaje porque los procesos de compilación de .NET Core proporcionan un host predeterminado para ejecutar aplicaciones de .NET Core.</span><span class="sxs-lookup"><span data-stu-id="a829d-106">Hosting the .NET Core runtime is an advanced scenario and, in most cases, .NET Core developers don't need to worry about hosting because .NET Core build processes provide a default host to run .NET Core applications.</span></span> <span data-ttu-id="a829d-107">En cambio, en algunas circunstancias especializadas, puede ser útil hospedar explícitamente el runtime de .NET Core, como un medio para invocar código administrado en un proceso nativo o para obtener más control sobre el funcionamiento del runtime.</span><span class="sxs-lookup"><span data-stu-id="a829d-107">In some specialized circumstances, though, it can be useful to explicitly host the .NET Core runtime, either as a means of invoking managed code in a native process or in order to gain more control over how the runtime works.</span></span>

<span data-ttu-id="a829d-108">En este artículo se proporciona información general sobre los pasos necesarios para iniciar el entorno de ejecución .NET Core desde código nativo y ejecutar código administrado en él.</span><span class="sxs-lookup"><span data-stu-id="a829d-108">This article gives an overview of the steps necessary to start the .NET Core runtime from native code and execute managed code in it.</span></span>

## <a name="prerequisites"></a><span data-ttu-id="a829d-109">Requisitos previos</span><span class="sxs-lookup"><span data-stu-id="a829d-109">Prerequisites</span></span>

<span data-ttu-id="a829d-110">Como los hosts son aplicaciones nativas, este tutorial trata la creación de una aplicación de C++ para hospedar .NET Core.</span><span class="sxs-lookup"><span data-stu-id="a829d-110">Because hosts are native applications, this tutorial covers constructing a C++ application to host .NET Core.</span></span> <span data-ttu-id="a829d-111">Necesitará un entorno de desarrollo de C++ (como el que se proporciona mediante [Visual Studio](https://aka.ms/vsdownload?utm_source=mscom&utm_campaign=msdocs)).</span><span class="sxs-lookup"><span data-stu-id="a829d-111">You will need a C++ development environment (such as that provided by [Visual Studio](https://aka.ms/vsdownload?utm_source=mscom&utm_campaign=msdocs)).</span></span>

<span data-ttu-id="a829d-112">También querrá una aplicación de .NET Core sencilla con la que probar el host, por lo que debe instalar el [SDK de .NET Core](https://dotnet.microsoft.com/download) y [crear una pequeña aplicación de prueba de .NET Core](with-visual-studio.md) (como una aplicación "Hola a todos").</span><span class="sxs-lookup"><span data-stu-id="a829d-112">You will also want a simple .NET Core application to test the host with, so you should install the [.NET Core SDK](https://dotnet.microsoft.com/download) and [build a small .NET Core test app](with-visual-studio.md) (such as a 'Hello World' app).</span></span> <span data-ttu-id="a829d-113">La aplicación "Hola a todos" que se ha creado mediante la nueva plantilla de proyecto de la consola de .NET Core es suficiente.</span><span class="sxs-lookup"><span data-stu-id="a829d-113">The 'Hello World' app created by the new .NET Core console project template is sufficient.</span></span>

## <a name="hosting-apis"></a><span data-ttu-id="a829d-114">API de hospedaje</span><span class="sxs-lookup"><span data-stu-id="a829d-114">Hosting APIs</span></span>
<span data-ttu-id="a829d-115">Hay tres API distintas que pueden usarse para hospedar .NET Core.</span><span class="sxs-lookup"><span data-stu-id="a829d-115">There are three different APIs that can be used to host .NET Core.</span></span> <span data-ttu-id="a829d-116">En este artículo (y sus [ejemplos](https://github.com/dotnet/samples/tree/master/core/hosting) asociados) se tratan todas las opciones.</span><span class="sxs-lookup"><span data-stu-id="a829d-116">This article (and its associated [samples](https://github.com/dotnet/samples/tree/master/core/hosting)) covers all options.</span></span>

* <span data-ttu-id="a829d-117">El método preferido para hospedar el tiempo de ejecución de .NET Core en .NET Core 3.0 y superior es con las APIs de las bibliotecas `nethost` y `hostfxr`.</span><span class="sxs-lookup"><span data-stu-id="a829d-117">The preferred method of hosting the .NET Core runtime in .NET Core 3.0 and above is with the `nethost` and `hostfxr` libraries' APIs.</span></span> <span data-ttu-id="a829d-118">Estos puntos de entrada tratan la complejidad de buscar y configurar el entorno de ejecución para la inicialización y permiten tanto el inicio de una aplicación administrada como la llamada a un método administrado estático.</span><span class="sxs-lookup"><span data-stu-id="a829d-118">These entry points handle the complexity of finding and setting up the runtime for initialization and allow both launching a managed application and calling into a static managed method.</span></span>
* <span data-ttu-id="a829d-119">El método preferido para hospedar el entorno de ejecución .NET Core antes de la versión .NET Core 3.0 es con [CoreClrHost.h](https://github.com/dotnet/runtime/blob/master/src/coreclr/src/hosts/inc/coreclrhost.h) API.</span><span class="sxs-lookup"><span data-stu-id="a829d-119">The preferred method of hosting the .NET Core runtime prior to .NET Core 3.0 is with the [CoreClrHost.h](https://github.com/dotnet/runtime/blob/master/src/coreclr/src/hosts/inc/coreclrhost.h) API.</span></span> <span data-ttu-id="a829d-120">Esta API expone funciones para iniciar y detener fácilmente el entorno de ejecución e invocar código administrado (ya sea mediante el inicio de un archivo .exe administrado o una llamada a métodos estáticos administrados).</span><span class="sxs-lookup"><span data-stu-id="a829d-120">This API exposes functions for easily starting and stopping the runtime and invoking managed code (either by launching a managed exe or by calling static managed methods).</span></span>
* <span data-ttu-id="a829d-121">.NET Core también se puede hospedar con la interfaz `ICLRRuntimeHost4` de [mscoree.h](https://github.com/dotnet/runtime/blob/master/src/coreclr/src/pal/prebuilt/inc/mscoree.h).</span><span class="sxs-lookup"><span data-stu-id="a829d-121">.NET Core can also be hosted with the `ICLRRuntimeHost4` interface in [mscoree.h](https://github.com/dotnet/runtime/blob/master/src/coreclr/src/pal/prebuilt/inc/mscoree.h).</span></span> <span data-ttu-id="a829d-122">Esta API se emplea desde antes que CoreClrHost.h, así que es posible que haya visto que hosts antiguos la usaban.</span><span class="sxs-lookup"><span data-stu-id="a829d-122">This API has been around longer than CoreClrHost.h, so you may have seen older hosts using it.</span></span> <span data-ttu-id="a829d-123">Aún funciona y ofrece un poco más de control sobre el proceso de hospedaje que CoreClrHost.</span><span class="sxs-lookup"><span data-stu-id="a829d-123">It still works and allows a bit more control over the hosting process than CoreClrHost.</span></span> <span data-ttu-id="a829d-124">Pero en la mayoría de los escenarios se prefiere CoreClrHost.h, ya que tiene API más sencillas.</span><span class="sxs-lookup"><span data-stu-id="a829d-124">For most scenarios, though, CoreClrHost.h is preferred now because of its simpler APIs.</span></span>

## <a name="sample-hosts"></a><span data-ttu-id="a829d-125">Hosts de ejemplo</span><span class="sxs-lookup"><span data-stu-id="a829d-125">Sample Hosts</span></span>

<span data-ttu-id="a829d-126">Hay [hosts de ejemplo](https://github.com/dotnet/samples/tree/master/core/hosting) que muestran los pasos descritos en los tutoriales siguientes en el repositorio dotnet/samples de GitHub.</span><span class="sxs-lookup"><span data-stu-id="a829d-126">[Sample hosts](https://github.com/dotnet/samples/tree/master/core/hosting) demonstrating the steps outlined in the tutorials below are available in the dotnet/samples GitHub repository.</span></span> <span data-ttu-id="a829d-127">Los comentarios de los ejemplos asocian claramente los pasos numerados de estos tutoriales con el lugar donde se realizan en el ejemplo.</span><span class="sxs-lookup"><span data-stu-id="a829d-127">Comments in the samples clearly associate the numbered steps from these tutorials with where they're performed in the sample.</span></span> <span data-ttu-id="a829d-128">Para obtener instrucciones de descarga, vea [Ejemplos y tutoriales](../../samples-and-tutorials/index.md#viewing-and-downloading-samples).</span><span class="sxs-lookup"><span data-stu-id="a829d-128">For download instructions, see [Samples and Tutorials](../../samples-and-tutorials/index.md#viewing-and-downloading-samples).</span></span>

<span data-ttu-id="a829d-129">Tenga en cuenta que los hosts de ejemplo están pensados para usarse con fines de aprendizaje, por lo que pasan por encima de la comprobación de errores y están diseñados para enfatizar la legibilidad sobre la eficacia.</span><span class="sxs-lookup"><span data-stu-id="a829d-129">Keep in mind that the sample hosts are meant to be used for learning purposes, so they are light on error checking and are designed to emphasize readability over efficiency.</span></span>

## <a name="create-a-host-using-nethosth-and-hostfxrh"></a><span data-ttu-id="a829d-130">Creación de un host con NetHost.h y HostFxr.h</span><span class="sxs-lookup"><span data-stu-id="a829d-130">Create a host using NetHost.h and HostFxr.h</span></span>

<span data-ttu-id="a829d-131">En los siguientes pasos se explica cómo usar las bibliotecas `nethost` y `hostfxr` para iniciar el entorno de ejecución de .NET Core en una aplicación nativa y llamar a un método estático administrado.</span><span class="sxs-lookup"><span data-stu-id="a829d-131">The following steps detail how to use the `nethost` and `hostfxr` libraries to start the .NET Core runtime in a native application and call into a managed static method.</span></span> <span data-ttu-id="a829d-132">En el [ejemplo](https://github.com/dotnet/samples/tree/master/core/hosting/HostWithHostFxr) se utiliza el encabezado `nethost` y la biblioteca instalados con el SDK de .NET así como copias de los archivos [`coreclr_delegates.h`](https://github.com/dotnet/core-setup/blob/master/src/corehost/cli/coreclr_delegates.h) y [`hostfxr.h`](https://github.com/dotnet/core-setup/blob/master/src/corehost/cli/hostfxr.h) del repositorio [dotnet/core-setup](https://github.com/dotnet/core-setup).</span><span class="sxs-lookup"><span data-stu-id="a829d-132">The [sample](https://github.com/dotnet/samples/tree/master/core/hosting/HostWithHostFxr) uses the `nethost` header and library installed with the .NET SDK and copies of the [`coreclr_delegates.h`](https://github.com/dotnet/core-setup/blob/master/src/corehost/cli/coreclr_delegates.h) and [`hostfxr.h`](https://github.com/dotnet/core-setup/blob/master/src/corehost/cli/hostfxr.h) files from the [dotnet/core-setup](https://github.com/dotnet/core-setup) repository.</span></span>

### <a name="step-1---load-hostfxr-and-get-exported-hosting-functions"></a><span data-ttu-id="a829d-133">Paso 1: Carga de HostFxr y obtención de funciones de hospedaje exportadas</span><span class="sxs-lookup"><span data-stu-id="a829d-133">Step 1 - Load HostFxr and get exported hosting functions</span></span>

<span data-ttu-id="a829d-134">La biblioteca `nethost` proporciona la función `get_hostfxr_path` para buscar la biblioteca `hostfxr`.</span><span class="sxs-lookup"><span data-stu-id="a829d-134">The `nethost` library provides the `get_hostfxr_path` function for locating the `hostfxr` library.</span></span> <span data-ttu-id="a829d-135">La biblioteca `hostfxr` expone funciones para hospedar el entorno de ejecución de .NET Core.</span><span class="sxs-lookup"><span data-stu-id="a829d-135">The `hostfxr` library exposes functions for hosting the .NET Core runtime.</span></span> <span data-ttu-id="a829d-136">Encontrará la lista completa de funciones en [`hostfxr.h`](https://github.com/dotnet/core-setup/blob/master/src/corehost/cli/hostfxr.h) y en el [documento de diseño de hospedaje nativo](https://github.com/dotnet/core-setup/blob/master/Documentation/design-docs/native-hosting.md).</span><span class="sxs-lookup"><span data-stu-id="a829d-136">The full list of functions can be found in [`hostfxr.h`](https://github.com/dotnet/core-setup/blob/master/src/corehost/cli/hostfxr.h) and the [native hosting design document](https://github.com/dotnet/core-setup/blob/master/Documentation/design-docs/native-hosting.md).</span></span> <span data-ttu-id="a829d-137">El ejemplo y este tutorial usan lo siguiente:</span><span class="sxs-lookup"><span data-stu-id="a829d-137">The sample and this tutorial use the following:</span></span>

* <span data-ttu-id="a829d-138">`hostfxr_initialize_for_runtime_config`: Inicializa un contexto de host y se prepara para la inicialización del entorno de ejecución de .NET Core mediante la configuración del entorno de ejecución especificado.</span><span class="sxs-lookup"><span data-stu-id="a829d-138">`hostfxr_initialize_for_runtime_config`: Initializes a host context and prepares for initialization of the .NET Core runtime using the specified runtime configuration.</span></span>
* <span data-ttu-id="a829d-139">`hostfxr_get_runtime_delegate`: Obtiene un delegado para la funcionalidad del entorno de ejecución.</span><span class="sxs-lookup"><span data-stu-id="a829d-139">`hostfxr_get_runtime_delegate`: Gets a delegate for runtime functionality.</span></span>
* <span data-ttu-id="a829d-140">`hostfxr_close`: Cierra un contexto del host.</span><span class="sxs-lookup"><span data-stu-id="a829d-140">`hostfxr_close`: Closes a host context.</span></span>

<span data-ttu-id="a829d-141">La biblioteca `hostfxr` se encuentra mediante `get_hostfxr_path`.</span><span class="sxs-lookup"><span data-stu-id="a829d-141">The `hostfxr` library is found using `get_hostfxr_path`.</span></span> <span data-ttu-id="a829d-142">Después, se carga y se recuperan las exportaciones.</span><span class="sxs-lookup"><span data-stu-id="a829d-142">It is then loaded and its exports are retrieved.</span></span>

[!code-cpp[HostFxrHost#LoadHostFxr](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithHostFxr/src/NativeHost/nativehost.cpp#LoadHostFxr)]

### <a name="step-2---initialize-and-start-the-net-core-runtime"></a><span data-ttu-id="a829d-143">Paso 2: Inicialización e inicio del entorno de ejecución de .NET Core</span><span class="sxs-lookup"><span data-stu-id="a829d-143">Step 2 - Initialize and start the .NET Core runtime</span></span>

<span data-ttu-id="a829d-144">Las funciones `hostfxr_initialize_for_runtime_config` y `hostfxr_get_runtime_delegate` inicializan e inician el entorno de ejecución de .NET Core con la configuración del entorno de ejecución para el componente administrado que se va a cargar.</span><span class="sxs-lookup"><span data-stu-id="a829d-144">The `hostfxr_initialize_for_runtime_config` and `hostfxr_get_runtime_delegate` functions initialize and start the .NET Core runtime using the runtime configuration for the managed component that will be loaded.</span></span> <span data-ttu-id="a829d-145">La función `hostfxr_get_runtime_delegate` se utiliza para obtener un delegado del entorno de ejecución que permite cargar un ensamblado administrado y obtener un puntero de función para un método estático en ese ensamblado.</span><span class="sxs-lookup"><span data-stu-id="a829d-145">The `hostfxr_get_runtime_delegate` function is used to get a runtime delegate that allows loading a managed assembly and getting a function pointer to a static method in that assembly.</span></span>

[!code-cpp[HostFxrHost#Initialize](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithHostFxr/src/NativeHost/nativehost.cpp#Initialize)]

### <a name="step-3---load-managed-assembly-and-get-function-pointer-to-a-managed-method"></a><span data-ttu-id="a829d-146">Paso 3: Carga del ensamblado administrado y obtención un puntero de función a un método administrado</span><span class="sxs-lookup"><span data-stu-id="a829d-146">Step 3 - Load managed assembly and get function pointer to a managed method</span></span>

<span data-ttu-id="a829d-147">Se llama al delegado del entorno de ejecución para cargar el ensamblado administrado y obtener un puntero de función a un método administrado.</span><span class="sxs-lookup"><span data-stu-id="a829d-147">The runtime delegate is called to load the managed assembly and get a function pointer to a managed method.</span></span> <span data-ttu-id="a829d-148">El delegado necesita la ruta de acceso de ensamblado, el nombre del tipo y el nombre del método como entradas y devuelve un puntero de función que se puede utilizar para llamar al método administrado.</span><span class="sxs-lookup"><span data-stu-id="a829d-148">The delegate requires the assembly path, type name, and method name as inputs and returns a function pointer that can be used to invoke the managed method.</span></span>

[!code-cpp[HostFxrHost#LoadAndGet](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithHostFxr/src/NativeHost/nativehost.cpp#LoadAndGet)]

<span data-ttu-id="a829d-149">Al pasar `nullptr` como nombre de tipo de delegado al llamar al delegado del entorno de ejecución, el ejemplo utiliza una firma predeterminada para el método administrado:</span><span class="sxs-lookup"><span data-stu-id="a829d-149">By passing `nullptr` as the delegate type name when calling the runtime delegate, the sample uses a default signature for the managed method:</span></span>

```csharp
public delegate int ComponentEntryPoint(IntPtr args, int sizeBytes);
```

<span data-ttu-id="a829d-150">Se puede utilizar una firma diferente si se especifica el nombre del tipo de delegado al llamar al delegado en entorno de ejecución.</span><span class="sxs-lookup"><span data-stu-id="a829d-150">A different signature can be used by specifying the delegate type name when calling the runtime delegate.</span></span>

### <a name="step-4---run-managed-code"></a><span data-ttu-id="a829d-151">Paso 4: Ejecutar el código administrado</span><span class="sxs-lookup"><span data-stu-id="a829d-151">Step 4 - Run managed code!</span></span>

<span data-ttu-id="a829d-152">El host nativo ahora puede llamar al método administrado y pasarle los parámetros deseados.</span><span class="sxs-lookup"><span data-stu-id="a829d-152">The native host can now call the managed method and pass it the desired parameters.</span></span>

[!code-cpp[HostFxrHost#CallManaged](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithHostFxr/src/NativeHost/nativehost.cpp#CallManaged)]

## <a name="create-a-host-using-coreclrhosth"></a><span data-ttu-id="a829d-153">Creación de un host mediante CoreClrHost.h</span><span class="sxs-lookup"><span data-stu-id="a829d-153">Create a host using CoreClrHost.h</span></span>

<span data-ttu-id="a829d-154">En los siguientes pasos se explica cómo usar la API CoreClrHost.h para iniciar el entorno de ejecución .NET Core en una aplicación nativa y llamar a un método administrado estático.</span><span class="sxs-lookup"><span data-stu-id="a829d-154">The following steps detail how to use the CoreClrHost.h API to start the .NET Core runtime in a native application and call into a managed static method.</span></span> <span data-ttu-id="a829d-155">Los fragmentos de código de este documento usan algunas API específicas de Windows, pero el [host de ejemplo completo](https://github.com/dotnet/samples/tree/master/core/hosting/HostWithCoreClrHost) muestra las rutas de acceso de código de Windows y Linux.</span><span class="sxs-lookup"><span data-stu-id="a829d-155">The code snippets in this document use some Windows-specific APIs, but the [full sample host](https://github.com/dotnet/samples/tree/master/core/hosting/HostWithCoreClrHost) shows both Windows and Linux code paths.</span></span>

<span data-ttu-id="a829d-156">El [host Unix CoreRun](https://github.com/dotnet/runtime/tree/master/src/coreclr/src/hosts/unixcorerun) muestra un ejemplo más complejo y real de hospedaje mediante coreclrhost.h.</span><span class="sxs-lookup"><span data-stu-id="a829d-156">The [Unix CoreRun host](https://github.com/dotnet/runtime/tree/master/src/coreclr/src/hosts/unixcorerun) shows a more complex, real-world example of hosting using coreclrhost.h.</span></span>

### <a name="step-1---find-and-load-coreclr"></a><span data-ttu-id="a829d-157">Paso 1: Buscar y cargar CoreCLR</span><span class="sxs-lookup"><span data-stu-id="a829d-157">Step 1 - Find and load CoreCLR</span></span>

<span data-ttu-id="a829d-158">Las API del entorno de ejecución .NET Core están en *coreclr.dll* (en Windows), en *libcoreclr.so* (en Linux) o en *libcoreclr.dylib* (en macOS).</span><span class="sxs-lookup"><span data-stu-id="a829d-158">The .NET Core runtime APIs are in *coreclr.dll* (on Windows), in *libcoreclr.so* (on Linux), or in *libcoreclr.dylib* (on macOS).</span></span> <span data-ttu-id="a829d-159">El primer paso para el hospedaje de .NET Core es cargar la biblioteca de CoreCLR.</span><span class="sxs-lookup"><span data-stu-id="a829d-159">The first step to hosting .NET Core is to load the CoreCLR library.</span></span> <span data-ttu-id="a829d-160">Algunos hosts sondean diferentes rutas de acceso o usan parámetros de entrada para buscar la biblioteca, mientras que otros saben cargarla desde una ruta de acceso determinada (junto al host, por ejemplo, o desde una ubicación de todo el equipo).</span><span class="sxs-lookup"><span data-stu-id="a829d-160">Some hosts probe different paths or use input parameters to find the library while others know to load it from a certain path (next to the host, for example, or from a machine-wide location).</span></span>

<span data-ttu-id="a829d-161">Una vez encontrada, la biblioteca se carga con `LoadLibraryEx` (en Windows) o `dlopen` (en Linux o macOS).</span><span class="sxs-lookup"><span data-stu-id="a829d-161">Once found, the library is loaded with `LoadLibraryEx` (on Windows) or `dlopen` (on Linux/macOS).</span></span>

[!code-cpp[CoreClrHost#1](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithCoreClrHost/src/SampleHost.cpp#1)]

### <a name="step-2---get-net-core-hosting-functions"></a><span data-ttu-id="a829d-162">Paso 2: Obtener las funciones de hospedaje de .NET Core</span><span class="sxs-lookup"><span data-stu-id="a829d-162">Step 2 - Get .NET Core hosting functions</span></span>

<span data-ttu-id="a829d-163">CoreClrHost tiene varios métodos importantes de utilidad para el hospedaje de .NET Core:</span><span class="sxs-lookup"><span data-stu-id="a829d-163">CoreClrHost has several important methods useful for hosting .NET Core:</span></span>

* <span data-ttu-id="a829d-164">`coreclr_initialize`: Inicia el entorno de ejecución .NET Core y configura el valor AppDomain predeterminado (y único).</span><span class="sxs-lookup"><span data-stu-id="a829d-164">`coreclr_initialize`: Starts the .NET Core runtime and sets up the default (and only) AppDomain.</span></span>
* <span data-ttu-id="a829d-165">`coreclr_execute_assembly`: Ejecuta un ensamblado administrado.</span><span class="sxs-lookup"><span data-stu-id="a829d-165">`coreclr_execute_assembly`: Executes a managed assembly.</span></span>
* <span data-ttu-id="a829d-166">`coreclr_create_delegate`: Crea un puntero de función a un método administrado.</span><span class="sxs-lookup"><span data-stu-id="a829d-166">`coreclr_create_delegate`: Creates a function pointer to a managed method.</span></span>
* <span data-ttu-id="a829d-167">`coreclr_shutdown`: Cierra el entorno de ejecución .NET Core.</span><span class="sxs-lookup"><span data-stu-id="a829d-167">`coreclr_shutdown`: Shuts down the .NET Core runtime.</span></span>
* <span data-ttu-id="a829d-168">`coreclr_shutdown_2`: Es igual que `coreclr_shutdown`, pero además recupera el código de salida del código administrado.</span><span class="sxs-lookup"><span data-stu-id="a829d-168">`coreclr_shutdown_2`: Like `coreclr_shutdown`, but also retrieves the managed code's exit code.</span></span>

<span data-ttu-id="a829d-169">Después de cargar la biblioteca de CoreCLR, el siguiente paso es obtener referencias a estas funciones mediante `GetProcAddress` (en Windows) o `dlsym` (en Linux o macOS).</span><span class="sxs-lookup"><span data-stu-id="a829d-169">After loading the CoreCLR library, the next step is to get references to these functions using `GetProcAddress` (on Windows) or `dlsym` (on Linux/macOS).</span></span>

[!code-cpp[CoreClrHost#2](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithCoreClrHost/src/SampleHost.cpp#2)]

### <a name="step-3---prepare-runtime-properties"></a><span data-ttu-id="a829d-170">Paso 3: Preparar las propiedades del entorno de ejecución</span><span class="sxs-lookup"><span data-stu-id="a829d-170">Step 3 - Prepare runtime properties</span></span>

<span data-ttu-id="a829d-171">Antes de iniciar el entorno de ejecución, es necesario preparar algunas propiedades para especificar el comportamiento (especialmente las relacionadas con el cargador de ensamblados).</span><span class="sxs-lookup"><span data-stu-id="a829d-171">Before starting the runtime, it is necessary to prepare some properties to specify behavior (especially concerning the assembly loader).</span></span>

<span data-ttu-id="a829d-172">Algunas propiedades comunes incluyen:</span><span class="sxs-lookup"><span data-stu-id="a829d-172">Common properties include:</span></span>

* <span data-ttu-id="a829d-173">`TRUSTED_PLATFORM_ASSEMBLIES` Esta es una lista de rutas de acceso de ensamblado (delimitadas por ";" en Windows y ":" en Linux) que el entorno de ejecución puede resolver de forma predeterminada.</span><span class="sxs-lookup"><span data-stu-id="a829d-173">`TRUSTED_PLATFORM_ASSEMBLIES` This is a list of assembly paths (delimited by ';' on Windows and ':' on Linux) which the runtime will be able to resolve by default.</span></span> <span data-ttu-id="a829d-174">Algunos hosts tienen manifiestos codificados de forma rígida que enumeran los ensamblados que pueden cargar.</span><span class="sxs-lookup"><span data-stu-id="a829d-174">Some hosts have hard-coded manifests listing assemblies they can load.</span></span> <span data-ttu-id="a829d-175">Otros colocan cualquier biblioteca en determinadas ubicaciones (junto a *coreclr.dll*, por ejemplo) en esta lista.</span><span class="sxs-lookup"><span data-stu-id="a829d-175">Others will put any library in certain locations (next to *coreclr.dll*, for example) on this list.</span></span>
* <span data-ttu-id="a829d-176">`APP_PATHS` Esta es una lista de rutas de acceso para explorar un ensamblado si este no puede encontrarse en la lista de ensamblados de plataforma de confianza (TPA).</span><span class="sxs-lookup"><span data-stu-id="a829d-176">`APP_PATHS` This is a list of paths to probe in for an assembly if it can't be found in the trusted platform assemblies (TPA) list.</span></span> <span data-ttu-id="a829d-177">Dado que el host tiene más control sobre los ensamblados que se cargan mediante la lista TPA, es recomendable que los hosts determinen qué ensamblados esperan cargar y los muestren de forma explícita.</span><span class="sxs-lookup"><span data-stu-id="a829d-177">Because the host has more control over which assemblies are loaded using the TPA list, it is a best practice for hosts to determine which assemblies they expect to load and list them explicitly.</span></span> <span data-ttu-id="a829d-178">Pero si es necesario sondear en tiempo de ejecución, esta propiedad puede habilitar ese escenario.</span><span class="sxs-lookup"><span data-stu-id="a829d-178">If probing at runtime is needed, however, this property can enable that scenario.</span></span>
* <span data-ttu-id="a829d-179">`APP_NI_PATHS` Esta lista es similar a APP_PATHS, excepto que son las rutas de acceso las que se sondean en busca de imágenes nativas.</span><span class="sxs-lookup"><span data-stu-id="a829d-179">`APP_NI_PATHS` This list is similar to APP_PATHS except that it's meant to be paths that will be probed for native images.</span></span>
* <span data-ttu-id="a829d-180">`NATIVE_DLL_SEARCH_DIRECTORIES` Esta propiedad es una lista de rutas de acceso que el cargador debe sondear cuando busca bibliotecas nativas llamadas mediante p/invoke.</span><span class="sxs-lookup"><span data-stu-id="a829d-180">`NATIVE_DLL_SEARCH_DIRECTORIES` This property is a list of paths the loader should probe when looking for native libraries called via p/invoke.</span></span>
* <span data-ttu-id="a829d-181">`PLATFORM_RESOURCE_ROOTS` Esta lista incluye rutas de acceso que se van a explorar para ensamblados satélite de recursos (en subdirectorios específicos de la referencia cultural).</span><span class="sxs-lookup"><span data-stu-id="a829d-181">`PLATFORM_RESOURCE_ROOTS` This list includes paths to probe in for resource satellite assemblies (in culture-specific subdirectories).</span></span>

<span data-ttu-id="a829d-182">En este host de ejemplo, la lista TPA se construye simplemente mediante la enumeración de todas las bibliotecas del directorio actual:</span><span class="sxs-lookup"><span data-stu-id="a829d-182">In this sample host, the TPA list is constructed by simply listing all libraries in the current directory:</span></span>

[!code-cpp[CoreClrHost#7](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithCoreClrHost/src/SampleHost.cpp#7)]

<span data-ttu-id="a829d-183">Dado que el ejemplo es simple, solo necesita la propiedad `TRUSTED_PLATFORM_ASSEMBLIES`:</span><span class="sxs-lookup"><span data-stu-id="a829d-183">Because the sample is simple, it only needs the `TRUSTED_PLATFORM_ASSEMBLIES` property:</span></span>

[!code-cpp[CoreClrHost#3](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithCoreClrHost/src/SampleHost.cpp#3)]

### <a name="step-4---start-the-runtime"></a><span data-ttu-id="a829d-184">Paso 4: Iniciar el entorno de ejecución</span><span class="sxs-lookup"><span data-stu-id="a829d-184">Step 4 - Start the runtime</span></span>

<span data-ttu-id="a829d-185">A diferencia de la API de hospedaje mscoree.h (que se describe a continuación), las API CoreCLRHost.h inician el entorno de ejecución y crean el valor AppDomain predeterminado con una sola llamada.</span><span class="sxs-lookup"><span data-stu-id="a829d-185">Unlike the mscoree.h hosting API (described below), CoreCLRHost.h APIs start the runtime and create the default AppDomain all with a single call.</span></span> <span data-ttu-id="a829d-186">La función `coreclr_initialize` toma una ruta de acceso base, un nombre y las propiedades descritas anteriormente y devuelve un manipulador al host a través del parámetro `hostHandle`.</span><span class="sxs-lookup"><span data-stu-id="a829d-186">The `coreclr_initialize` function takes a base path, name, and the properties described earlier and returns back a handle to the host via the `hostHandle` parameter.</span></span>

[!code-cpp[CoreClrHost#4](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithCoreClrHost/src/SampleHost.cpp#4)]

### <a name="step-5---run-managed-code"></a><span data-ttu-id="a829d-187">Paso 5: Ejecutar el código administrado</span><span class="sxs-lookup"><span data-stu-id="a829d-187">Step 5 - Run managed code!</span></span>

<span data-ttu-id="a829d-188">Con el entorno de ejecución iniciado, el host puede llamar al código administrado.</span><span class="sxs-lookup"><span data-stu-id="a829d-188">With the runtime started, the host can call managed code.</span></span> <span data-ttu-id="a829d-189">Esta operación se puede realizar de diferentes maneras.</span><span class="sxs-lookup"><span data-stu-id="a829d-189">This can be done in a couple of different ways.</span></span> <span data-ttu-id="a829d-190">El código de ejemplo vinculado a este tutorial usa la función `coreclr_create_delegate` para crear un delegado para un método administrado estático.</span><span class="sxs-lookup"><span data-stu-id="a829d-190">The sample code linked to this tutorial uses the `coreclr_create_delegate` function to create a delegate to a static managed method.</span></span> <span data-ttu-id="a829d-191">Esta API toma el [nombre del ensamblado](../../standard/assembly/names.md), el nombre de tipo calificado por el espacio de nombres y el nombre del método como entradas y devuelve un delegado que puede usarse para invocar al método.</span><span class="sxs-lookup"><span data-stu-id="a829d-191">This API takes the [assembly name](../../standard/assembly/names.md), namespace-qualified type name, and method name as inputs and returns a delegate that can be used to invoke the method.</span></span>

[!code-cpp[CoreClrHost#5](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithCoreClrHost/src/SampleHost.cpp#5)]

<span data-ttu-id="a829d-192">En este ejemplo, el host ya puede llamar a `managedDelegate` para ejecutar el método `ManagedWorker.DoWork`.</span><span class="sxs-lookup"><span data-stu-id="a829d-192">In this sample, the host can now call `managedDelegate` to run the `ManagedWorker.DoWork` method.</span></span>

<span data-ttu-id="a829d-193">También se puede usar la función `coreclr_execute_assembly` para iniciar un archivo ejecutable administrado.</span><span class="sxs-lookup"><span data-stu-id="a829d-193">Alternatively, the `coreclr_execute_assembly` function can be used to launch a managed executable.</span></span> <span data-ttu-id="a829d-194">Esta API toma una ruta de acceso de ensamblado y una matriz de argumentos como parámetros de entrada.</span><span class="sxs-lookup"><span data-stu-id="a829d-194">This API takes an assembly path and array of arguments as input parameters.</span></span> <span data-ttu-id="a829d-195">Carga el ensamblado en esa ruta de acceso e invoca a su método principal.</span><span class="sxs-lookup"><span data-stu-id="a829d-195">It loads the assembly at that path and invokes its main method.</span></span>

```C++
int hr = executeAssembly(
        hostHandle,
        domainId,
        argumentCount,
        arguments,
        "HelloWorld.exe",
        (unsigned int*)&exitCode);
```

### <a name="step-6---shutdown-and-clean-up"></a><span data-ttu-id="a829d-196">Paso 6: Cerrar y limpiar</span><span class="sxs-lookup"><span data-stu-id="a829d-196">Step 6 - Shutdown and clean up</span></span>

<span data-ttu-id="a829d-197">Por último, cuando el host termina de ejecutar el código administrado, el entorno de ejecución .NET Core se cierra con `coreclr_shutdown` o `coreclr_shutdown_2`.</span><span class="sxs-lookup"><span data-stu-id="a829d-197">Finally, when the host is done running managed code, the .NET Core runtime is shut down with `coreclr_shutdown` or `coreclr_shutdown_2`.</span></span>

[!code-cpp[CoreClrHost#6](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithCoreClrHost/src/SampleHost.cpp#6)]

<span data-ttu-id="a829d-198">CoreCLR no admite la reinicialización ni la descarga.</span><span class="sxs-lookup"><span data-stu-id="a829d-198">CoreCLR does not support reinitialization or unloading.</span></span> <span data-ttu-id="a829d-199">No vuelva a llamar a `coreclr_initialize` ni descargue la biblioteca de CoreCLR.</span><span class="sxs-lookup"><span data-stu-id="a829d-199">Do not call `coreclr_initialize` again or unload the CoreCLR library.</span></span>

## <a name="create-a-host-using-mscoreeh"></a><span data-ttu-id="a829d-200">Creación de un host con Mscoree.h</span><span class="sxs-lookup"><span data-stu-id="a829d-200">Create a host using Mscoree.h</span></span>

<span data-ttu-id="a829d-201">Como se ha mencionado anteriormente, CoreClrHost.h es ahora el método preferido para hospedar el entorno de ejecución .NET Core.</span><span class="sxs-lookup"><span data-stu-id="a829d-201">As mentioned previously, CoreClrHost.h is now the preferred method of hosting the .NET Core runtime.</span></span> <span data-ttu-id="a829d-202">Pero todavía se puede usar la interfaz `ICLRRuntimeHost4` si las interfaces de CoreClrHost.h no bastan (si se necesitan marcas de inicio no estándar, por ejemplo, o si se necesita un valor AppDomainManager en el dominio predeterminado).</span><span class="sxs-lookup"><span data-stu-id="a829d-202">The `ICLRRuntimeHost4` interface can still be used, though, if the CoreClrHost.h interfaces aren't sufficient (if non-standard startup flags are needed, for example, or if an AppDomainManager is needed on the default domain).</span></span> <span data-ttu-id="a829d-203">Estas instrucciones le guían durante el proceso de hospedaje de .NET Core mediante mscoree.h.</span><span class="sxs-lookup"><span data-stu-id="a829d-203">These instructions will guide you through hosting .NET Core using mscoree.h.</span></span>

<span data-ttu-id="a829d-204">El [host CoreRun](https://github.com/dotnet/runtime/tree/master/src/coreclr/src/hosts/corerun) muestra un ejemplo más complejo y real de hospedaje mediante mscoree.h.</span><span class="sxs-lookup"><span data-stu-id="a829d-204">The [CoreRun host](https://github.com/dotnet/runtime/tree/master/src/coreclr/src/hosts/corerun) shows a more complex, real-world example of hosting using mscoree.h.</span></span>

### <a name="a-note-about-mscoreeh"></a><span data-ttu-id="a829d-205">Una nota sobre mscoree.h</span><span class="sxs-lookup"><span data-stu-id="a829d-205">A note about mscoree.h</span></span>
<span data-ttu-id="a829d-206">La interfaz de hospedaje de .NET Core `ICLRRuntimeHost4` se define en [MSCOREE.IDL](https://github.com/dotnet/runtime/blob/master/src/coreclr/src/inc/MSCOREE.IDL).</span><span class="sxs-lookup"><span data-stu-id="a829d-206">The `ICLRRuntimeHost4` .NET Core hosting interface is defined in [MSCOREE.IDL](https://github.com/dotnet/runtime/blob/master/src/coreclr/src/inc/MSCOREE.IDL).</span></span> <span data-ttu-id="a829d-207">Se genera una versión de encabezado de este archivo (mscoree.h), que su host necesitará para hacer referencias, mediante MIDL cuando se compila el [runtime de .NET Core](https://github.com/dotnet/runtime/).</span><span class="sxs-lookup"><span data-stu-id="a829d-207">A header version of this file (mscoree.h), which your host will need to reference, is produced via MIDL when the [.NET Core runtime](https://github.com/dotnet/runtime/) is built.</span></span> <span data-ttu-id="a829d-208">Si no quiere compilar el runtime de .NET Core, mscoree.h también está disponible como un [encabezado pregenerado](https://github.com/dotnet/runtime/blob/master/src/coreclr/src/pal/prebuilt/inc/) en el repositorio dotnet/runtime.</span><span class="sxs-lookup"><span data-stu-id="a829d-208">If you do not want to build the .NET Core runtime, mscoree.h is also available as a [pre-built header](https://github.com/dotnet/runtime/blob/master/src/coreclr/src/pal/prebuilt/inc/) in the dotnet/runtime repository.</span></span>

### <a name="step-1---identify-the-managed-entry-point"></a><span data-ttu-id="a829d-209">Paso 1: Identificar el punto de entrada administrado</span><span class="sxs-lookup"><span data-stu-id="a829d-209">Step 1 - Identify the managed entry point</span></span>
<span data-ttu-id="a829d-210">Después de hacer referencia a los encabezados necesarios ([mscoree.h](https://github.com/dotnet/runtime/blob/master/src/coreclr/src/pal/prebuilt/inc/mscoree.h) y stdio.h, por ejemplo), una de las primeras cosas que un host de .NET Core debe hacer es localizar el punto de entrada administrado que va a usar.</span><span class="sxs-lookup"><span data-stu-id="a829d-210">After referencing necessary headers ([mscoree.h](https://github.com/dotnet/runtime/blob/master/src/coreclr/src/pal/prebuilt/inc/mscoree.h) and stdio.h, for example), one of the first things a .NET Core host must do is locate the managed entry point it will be using.</span></span> <span data-ttu-id="a829d-211">En nuestro host de ejemplo, esto se realiza simplemente tomando el primer argumento de la línea de comandos para nuestro host como la ruta de acceso a un archivo binario administrado cuyo método `main` se va a ejecutar.</span><span class="sxs-lookup"><span data-stu-id="a829d-211">In our sample host, this is done by just taking the first command-line argument to our host as the path to a managed binary whose `main` method will be executed.</span></span>

[!code-cpp[NetCoreHost#1](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithMscoree/host.cpp#1)]

### <a name="step-2---find-and-load-coreclr"></a><span data-ttu-id="a829d-212">Paso 2: Buscar y cargar CoreCLR</span><span class="sxs-lookup"><span data-stu-id="a829d-212">Step 2 - Find and load CoreCLR</span></span>
<span data-ttu-id="a829d-213">Las API del runtime de .NET Core están en *CoreCLR.dll* (en Windows).</span><span class="sxs-lookup"><span data-stu-id="a829d-213">The .NET Core runtime APIs are in *CoreCLR.dll* (on Windows).</span></span> <span data-ttu-id="a829d-214">Para obtener nuestra interfaz de hospedaje (`ICLRRuntimeHost4`), es necesario buscar y cargar *CoreCLR.dll*.</span><span class="sxs-lookup"><span data-stu-id="a829d-214">To get our hosting interface (`ICLRRuntimeHost4`), it's necessary to find and load *CoreCLR.dll*.</span></span> <span data-ttu-id="a829d-215">Depende del host definir una convención sobre cómo localizar *CoreCLR.dll*.</span><span class="sxs-lookup"><span data-stu-id="a829d-215">It is up to the host to define a convention for how it will locate *CoreCLR.dll*.</span></span> <span data-ttu-id="a829d-216">Algunos hosts esperan que el archivo esté presente en una ubicación bien conocida de todo el equipo (como *%programfiles%\dotnet\shared\Microsoft.NETCore.App\2.1.6*).</span><span class="sxs-lookup"><span data-stu-id="a829d-216">Some hosts expect the file to be present in a well-known machine-wide location (such as *%programfiles%\dotnet\shared\Microsoft.NETCore.App\2.1.6*).</span></span> <span data-ttu-id="a829d-217">Otros esperan que *CoreCLR.dll* se cargará desde una ubicación junto al propio host o a la aplicación que se va a hospedar.</span><span class="sxs-lookup"><span data-stu-id="a829d-217">Others expect that *CoreCLR.dll* will be loaded from a location next to either the host itself or the app to be hosted.</span></span> <span data-ttu-id="a829d-218">Y otros pueden consultar una variable de entorno para buscar la biblioteca.</span><span class="sxs-lookup"><span data-stu-id="a829d-218">Still others might consult an environment variable to find the library.</span></span>

<span data-ttu-id="a829d-219">En Linux o macOS, la biblioteca en tiempo de ejecución principal es *libcoreclr.so* o *libcoreclr.dylib*, respectivamente.</span><span class="sxs-lookup"><span data-stu-id="a829d-219">On Linux or macOS, the core runtime library is *libcoreclr.so* or *libcoreclr.dylib*, respectively.</span></span>

<span data-ttu-id="a829d-220">Nuestro host de ejemplo explora algunas ubicaciones comunes para *CoreCLR.dll*.</span><span class="sxs-lookup"><span data-stu-id="a829d-220">Our sample host probes a few common locations for *CoreCLR.dll*.</span></span> <span data-ttu-id="a829d-221">Una vez que lo encuentra, debe cargarse mediante `LoadLibrary` (o `dlopen` en Linux/macOS).</span><span class="sxs-lookup"><span data-stu-id="a829d-221">Once found, it must be loaded via `LoadLibrary` (or `dlopen` on Linux/macOS).</span></span>

[!code-cpp[NetCoreHost#2](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithMscoree/host.cpp#2)]

### <a name="step-3---get-an-iclrruntimehost4-instance"></a><span data-ttu-id="a829d-222">Paso 3: Obtención de una instancia de ICLRRuntimeHost4</span><span class="sxs-lookup"><span data-stu-id="a829d-222">Step 3 - Get an ICLRRuntimeHost4 Instance</span></span>
<span data-ttu-id="a829d-223">La interfaz de hospedaje de `ICLRRuntimeHost4` se recupera llamando a `GetProcAddress` (o `dlsym` en Linux/macOS) en `GetCLRRuntimeHost` y, después, invocando esa función.</span><span class="sxs-lookup"><span data-stu-id="a829d-223">The `ICLRRuntimeHost4` hosting interface is retrieved by calling `GetProcAddress` (or `dlsym` on Linux/macOS) on `GetCLRRuntimeHost`, and then invoking that function.</span></span>

[!code-cpp[NetCoreHost#3](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithMscoree/host.cpp#3)]

### <a name="step-4---set-startup-flags-and-start-the-runtime"></a><span data-ttu-id="a829d-224">Paso 4: Establecimiento de marcas de inicio e inicio del entorno de ejecución</span><span class="sxs-lookup"><span data-stu-id="a829d-224">Step 4 - Set startup flags and start the runtime</span></span>
<span data-ttu-id="a829d-225">Con `ICLRRuntimeHost4` disponible, ahora podemos especificar marcas de inicio en el runtime e iniciarlo.</span><span class="sxs-lookup"><span data-stu-id="a829d-225">With an `ICLRRuntimeHost4` in-hand, we can now specify runtime-wide startup flags and start the runtime.</span></span> <span data-ttu-id="a829d-226">Las marcas de inicio determinan qué recolector de elementos no utilizados (GC) se va a usar (simultáneo o servidor), si se usa un solo valor AppDomain o varios y qué directiva de optimización del cargador se va a usar (para la carga de ensamblados con dominio neutro).</span><span class="sxs-lookup"><span data-stu-id="a829d-226">Startup flags determine which garbage collector (GC) to use (concurrent or server), whether we will use a single AppDomain or multiple AppDomains, and what loader optimization policy to use (for domain-neutral loading of assemblies).</span></span>

[!code-cpp[NetCoreHost#4](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithMscoree/host.cpp#4)]

<span data-ttu-id="a829d-227">El runtime se inicia con una llamada a la función `Start`.</span><span class="sxs-lookup"><span data-stu-id="a829d-227">The runtime is started with a call to the `Start` function.</span></span>

```C++
hr = runtimeHost->Start();
```

### <a name="step-5---preparing-appdomain-settings"></a><span data-ttu-id="a829d-228">Paso 5: Preparar la configuración de AppDomain</span><span class="sxs-lookup"><span data-stu-id="a829d-228">Step 5 - Preparing AppDomain settings</span></span>
<span data-ttu-id="a829d-229">Una vez que se inicie el runtime, querremos configurar un AppDomain.</span><span class="sxs-lookup"><span data-stu-id="a829d-229">Once the runtime is started, we will want to set up an AppDomain.</span></span> <span data-ttu-id="a829d-230">En cambio, existen varias opciones que deben especificarse a la hora de crear un AppDomain de .NET, por lo que es necesario prepararlas primero.</span><span class="sxs-lookup"><span data-stu-id="a829d-230">There are a number of options that must be specified when creating a .NET AppDomain, however, so it's necessary to prepare those first.</span></span>

<span data-ttu-id="a829d-231">Las marcas de AppDomain especifican comportamientos de AppDomain relacionados con la seguridad y la interoperabilidad.</span><span class="sxs-lookup"><span data-stu-id="a829d-231">AppDomain flags specify AppDomain behaviors related to security and interop.</span></span> <span data-ttu-id="a829d-232">Los hosts antiguos de Silverlight usaban esta configuración en el código de usuario de espacio aislado, pero los hosts de .NET Core más modernos ejecutan el código de usuario como de plena confianza y habilitan la interoperabilidad.</span><span class="sxs-lookup"><span data-stu-id="a829d-232">Older Silverlight hosts used these settings to sandbox user code, but most modern .NET Core hosts run user code as full trust and enable interop.</span></span>

[!code-cpp[NetCoreHost#5](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithMscoree/host.cpp#5)]

<span data-ttu-id="a829d-233">Después de decidir qué marcas de AppDomain se van a usar, deben definirse sus propiedades.</span><span class="sxs-lookup"><span data-stu-id="a829d-233">After deciding which AppDomain flags to use, AppDomain properties must be defined.</span></span> <span data-ttu-id="a829d-234">Las propiedades son pares clave-valor de cadenas.</span><span class="sxs-lookup"><span data-stu-id="a829d-234">The properties are key/value pairs of strings.</span></span> <span data-ttu-id="a829d-235">Muchas de las propiedades se refieren a la manera en que AppDomain cargará ensamblados.</span><span class="sxs-lookup"><span data-stu-id="a829d-235">Many of the properties relate to how the AppDomain will load assemblies.</span></span>

<span data-ttu-id="a829d-236">Las propiedades comunes de AppDomain incluyen:</span><span class="sxs-lookup"><span data-stu-id="a829d-236">Common AppDomain properties include:</span></span>

* <span data-ttu-id="a829d-237">`TRUSTED_PLATFORM_ASSEMBLIES` Esta es una lista de rutas de acceso de ensamblado (delimitada por `;` en Windows y `:` en Linux o macOS) cuya carga debe priorizar AppDomain y a las que debe otorgar plena confianza (incluso en dominios de confianza parcial).</span><span class="sxs-lookup"><span data-stu-id="a829d-237">`TRUSTED_PLATFORM_ASSEMBLIES` This is a list of assembly paths (delimited by `;` on Windows and `:` on Linux/macOS) which the AppDomain should prioritize loading and give full trust to (even in partially trusted domains).</span></span> <span data-ttu-id="a829d-238">Esta lista está diseñada para contener ensamblados de "Framework" y otros módulos de confianza, similares a los GAC en escenarios de .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="a829d-238">This list is meant to contain 'Framework' assemblies and other trusted modules, similar to the GAC in .NET Framework scenarios.</span></span> <span data-ttu-id="a829d-239">Algunos hosts colocarán una biblioteca junto a *coreclr.dll* en esta lista, otros tienen manifiestos codificados de forma rígida que enumeran ensamblados de confianza para sus fines.</span><span class="sxs-lookup"><span data-stu-id="a829d-239">Some hosts will put any library next to *coreclr.dll* on this list, others have hard-coded manifests listing trusted assemblies for their purposes.</span></span>
* <span data-ttu-id="a829d-240">`APP_PATHS` Esta es una lista de rutas de acceso para explorar un ensamblado si este no puede encontrarse en la lista de ensamblados de plataforma de confianza (TPA).</span><span class="sxs-lookup"><span data-stu-id="a829d-240">`APP_PATHS` This is a list of paths to probe in for an assembly if it can't be found in the trusted platform assemblies (TPA) list.</span></span> <span data-ttu-id="a829d-241">Dado que el host tiene más control sobre los ensamblados que se cargan mediante la lista TPA, es recomendable que los hosts determinen qué ensamblados esperan cargar y los muestren de forma explícita.</span><span class="sxs-lookup"><span data-stu-id="a829d-241">Because the host has more control over which assemblies are loaded using the TPA list, it is a best practice for hosts to determine which assemblies they expect to load and list them explicitly.</span></span> <span data-ttu-id="a829d-242">Pero si es necesario sondear en tiempo de ejecución, esta propiedad puede habilitar ese escenario.</span><span class="sxs-lookup"><span data-stu-id="a829d-242">If probing at runtime is needed, however, this property can enable that scenario.</span></span>
* <span data-ttu-id="a829d-243">`APP_NI_PATHS` Esta lista es muy similar a APP_PATHS excepto que está diseñada para ser rutas de acceso que se explorarán para imágenes nativas.</span><span class="sxs-lookup"><span data-stu-id="a829d-243">`APP_NI_PATHS` This list is very similar to APP_PATHS except that it's meant to be paths that will be probed for native images.</span></span>
* <span data-ttu-id="a829d-244">`NATIVE_DLL_SEARCH_DIRECTORIES` Esta propiedad es una lista de rutas de acceso que el cargador debe explorar cuando busca archivos DLL nativos que se han llamado mediante p/invoke.</span><span class="sxs-lookup"><span data-stu-id="a829d-244">`NATIVE_DLL_SEARCH_DIRECTORIES` This property is a list of paths the loader should probe when looking for native DLLs called via p/invoke.</span></span>
* <span data-ttu-id="a829d-245">`PLATFORM_RESOURCE_ROOTS` Esta lista incluye rutas de acceso que se van a explorar para ensamblados satélite de recursos (en subdirectorios específicos de la referencia cultural).</span><span class="sxs-lookup"><span data-stu-id="a829d-245">`PLATFORM_RESOURCE_ROOTS` This list includes paths to probe in for resource satellite assemblies (in culture-specific subdirectories).</span></span>

<span data-ttu-id="a829d-246">En nuestro [host de ejemplo sencillo](https://github.com/dotnet/samples/tree/master/core/hosting/HostWithMscoree), estas propiedades se configuran de la siguiente manera:</span><span class="sxs-lookup"><span data-stu-id="a829d-246">In our [simple sample host](https://github.com/dotnet/samples/tree/master/core/hosting/HostWithMscoree), these properties are set up as follows:</span></span>

[!code-cpp[NetCoreHost#6](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithMscoree/host.cpp#6)]

### <a name="step-6---create-the-appdomain"></a><span data-ttu-id="a829d-247">Paso 6: Crear el AppDomain</span><span class="sxs-lookup"><span data-stu-id="a829d-247">Step 6 - Create the AppDomain</span></span>
<span data-ttu-id="a829d-248">Una vez que todas las propiedades y marcas de AppDomain están preparadas, `ICLRRuntimeHost4::CreateAppDomainWithManager` puede usarse para configurar el AppDomain.</span><span class="sxs-lookup"><span data-stu-id="a829d-248">Once all AppDomain flags and properties are prepared, `ICLRRuntimeHost4::CreateAppDomainWithManager` can be used to set up the AppDomain.</span></span> <span data-ttu-id="a829d-249">De manera opcional, esta función toma un nombre completo del ensamblado y el nombre de tipo que se va a usar como el administrador de AppDomain del dominio.</span><span class="sxs-lookup"><span data-stu-id="a829d-249">This function optionally takes a fully qualified assembly name and type name to use as the domain's AppDomain manager.</span></span> <span data-ttu-id="a829d-250">Un administrador de AppDomain puede permitir un host para controlar algunos aspectos del comportamiento de AppDomain y puede proporcionar puntos de entrada para iniciar el código administrado si el host no pretende invocar el código de usuario directamente.</span><span class="sxs-lookup"><span data-stu-id="a829d-250">An AppDomain manager can allow a host to control some aspects of AppDomain behavior and may provide entry points for launching managed code if the host doesn't intend to invoke user code directly.</span></span>

[!code-cpp[NetCoreHost#7](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithMscoree/host.cpp#7)]

### <a name="step-7---run-managed-code"></a><span data-ttu-id="a829d-251">Paso 7: Ejecutar el código administrado</span><span class="sxs-lookup"><span data-stu-id="a829d-251">Step 7 - Run managed code!</span></span>
<span data-ttu-id="a829d-252">Con un AppDomain en funcionamiento, ahora el host puede comenzar a ejecutar el código administrado.</span><span class="sxs-lookup"><span data-stu-id="a829d-252">With an AppDomain up and running, the host can now start executing managed code.</span></span> <span data-ttu-id="a829d-253">La manera más sencilla de hacer esto es usar `ICLRRuntimeHost4::ExecuteAssembly` para invocar un método de punto de entrada del ensamblado administrado.</span><span class="sxs-lookup"><span data-stu-id="a829d-253">The easiest way to do this is to use `ICLRRuntimeHost4::ExecuteAssembly` to invoke a managed assembly's entry point method.</span></span> <span data-ttu-id="a829d-254">Tenga en cuenta que esta función solo funciona en escenarios de dominio único.</span><span class="sxs-lookup"><span data-stu-id="a829d-254">Note that this function only works in single-domain scenarios.</span></span>

[!code-cpp[NetCoreHost#8](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithMscoree/host.cpp#8)]

<span data-ttu-id="a829d-255">Otra opción, si `ExecuteAssembly` no satisface las necesidades del host, es usar `CreateDelegate` para crear un puntero de función a un método administrado estático.</span><span class="sxs-lookup"><span data-stu-id="a829d-255">Another option, if `ExecuteAssembly` doesn't meet your host's needs, is to use `CreateDelegate` to create a function pointer to a static managed method.</span></span> <span data-ttu-id="a829d-256">Esto requiere que el host conozca la firma del método al que está llamando (para crear el tipo de puntero de función), pero permite a los hosts tener flexibilidad para invocar código que no sea el punto de entrada del ensamblado.</span><span class="sxs-lookup"><span data-stu-id="a829d-256">This requires the host to know the signature of the method it is calling into (in order to create the function pointer type) but allows hosts the flexibility to invoke code other than an assembly's entry point.</span></span> <span data-ttu-id="a829d-257">El nombre del ensamblado proporcionado en el segundo parámetro es el [nombre del ensamblado administrado completo](../../standard/assembly/names.md) de la biblioteca que se va a cargar.</span><span class="sxs-lookup"><span data-stu-id="a829d-257">The assembly name provided in the second parameter is the [full managed assembly name](../../standard/assembly/names.md) of the library to load.</span></span>

```C++
void *pfnDelegate = NULL;
hr = runtimeHost->CreateDelegate(
    domainId,
    L"HW, Version=1.0.0.0, Culture=neutral", // Target managed assembly
    L"ConsoleApplication.Program",           // Target managed type
    L"Main",                                 // Target entry point (static method)
    (INT_PTR*)&pfnDelegate);

((MainMethodFp*)pfnDelegate)(NULL);
```

### <a name="step-8---clean-up"></a><span data-ttu-id="a829d-258">Paso 8: Limpiar</span><span class="sxs-lookup"><span data-stu-id="a829d-258">Step 8 - Clean up</span></span>
<span data-ttu-id="a829d-259">Por último, el host debe limpiarse después descargando AppDomains, deteniendo el runtime y lanzando la referencia `ICLRRuntimeHost4`.</span><span class="sxs-lookup"><span data-stu-id="a829d-259">Finally, the host should clean up after itself by unloading AppDomains, stopping the runtime, and releasing the `ICLRRuntimeHost4` reference.</span></span>

[!code-cpp[NetCoreHost#9](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithMscoree/host.cpp#9)]

<span data-ttu-id="a829d-260">CoreCLR no admite la descarga.</span><span class="sxs-lookup"><span data-stu-id="a829d-260">CoreCLR does not support unloading.</span></span> <span data-ttu-id="a829d-261">No descargue la biblioteca de CoreCLR.</span><span class="sxs-lookup"><span data-stu-id="a829d-261">Do not unload the CoreCLR library.</span></span>

## <a name="conclusion"></a><span data-ttu-id="a829d-262">Conclusión</span><span class="sxs-lookup"><span data-stu-id="a829d-262">Conclusion</span></span>
<span data-ttu-id="a829d-263">Una vez que se ha compilado el host, este puede probarse si se ejecuta desde la línea de comandos y se pasa cualquier argumento que espere el host (como la aplicación administrada que se va a ejecutar para el host de ejemplo mscoree).</span><span class="sxs-lookup"><span data-stu-id="a829d-263">Once your host is built, it can be tested by running it from the command line and passing any arguments the host expects (like the managed app to run for the mscoree example host).</span></span> <span data-ttu-id="a829d-264">Al especificar la aplicación de .NET Core para el host que se va a ejecutar, asegúrese de usar el archivo .dll que ha generado `dotnet build`.</span><span class="sxs-lookup"><span data-stu-id="a829d-264">When specifying the .NET Core app for the host to run, be sure to use the .dll that is produced by `dotnet build`.</span></span> <span data-ttu-id="a829d-265">Los archivos ejecutables (archivos .exe) que ha generado `dotnet publish` para las aplicaciones autocontenidas son realmente el host de .NET Core predeterminado (de manera que la aplicación pueda iniciarse directamente desde la línea de comandos en escenarios principales); el código de usuario se compila en un archivo .dll del mismo nombre.</span><span class="sxs-lookup"><span data-stu-id="a829d-265">Executables (.exe files) produced by `dotnet publish` for self-contained applications are actually the default .NET Core host (so that the app can be launched directly from the command line in mainline scenarios); user code is compiled into a dll of the same name.</span></span>

<span data-ttu-id="a829d-266">Si las cosas no funcionan inicialmente, vuelva a comprobar que *coreclr.dll* está disponible en la ubicación que espera el host, que todas las bibliotecas de Framework necesarias están en la lista TPA y que el valor de bits de CoreCLR (32 o 64 bits) coincide con la manera en que se ha compilado el host.</span><span class="sxs-lookup"><span data-stu-id="a829d-266">If things don't work initially, double-check that *coreclr.dll* is available in the location expected by the host, that all necessary Framework libraries are in the TPA list, and that CoreCLR's bitness (32-bit or 64-bit) matches how the host was built.</span></span>

<span data-ttu-id="a829d-267">Hospedar el runtime de .NET Core es un escenario avanzado que muchos desarrolladores no necesitarán, pero para los que necesiten iniciar el código administrado desde un proceso nativo, o que necesiten más control sobre el comportamiento del runtime de .NET Core, puede resultar muy útil.</span><span class="sxs-lookup"><span data-stu-id="a829d-267">Hosting the .NET Core runtime is an advanced scenario that many developers won't require, but for those who need to launch managed code from a native process, or who need more control over the .NET Core runtime's behavior, it can be very useful.</span></span>
