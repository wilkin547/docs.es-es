---
title: WSTrustChannelFactory y WSTrustChannel
ms.date: 03/30/2017
ms.assetid: 96cec467-e963-4132-b18b-7d0b3a2e979f
author: BrucePerlerMS
manager: mbaldwin
ms.openlocfilehash: 7dfe18a55d8c7f56db1906cb2aa982ab043841c7
ms.sourcegitcommit: 6eac9a01ff5d70c6d18460324c016a3612c5e268
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 09/14/2018
ms.locfileid: "45597011"
---
# <a name="wstrustchannelfactory-and-wstrustchannel"></a><span data-ttu-id="f70e4-102">WSTrustChannelFactory y WSTrustChannel</span><span class="sxs-lookup"><span data-stu-id="f70e4-102">WSTrustChannelFactory and WSTrustChannel</span></span>
<span data-ttu-id="f70e4-103">Si ya está familiarizado con Windows Communication Foundation (WCF), sabe que un cliente WCF ya reconoce la federación.</span><span class="sxs-lookup"><span data-stu-id="f70e4-103">If you are already familiar with Windows Communication Foundation (WCF), you know that a WCF client is already federation aware.</span></span> <span data-ttu-id="f70e4-104">Al configurar un cliente WCF con <xref:System.ServiceModel.WSFederationHttpBinding> o un enlace personalizado similar, puede habilitar la autenticación federada en un servicio.</span><span class="sxs-lookup"><span data-stu-id="f70e4-104">By configuring a WCF client with a <xref:System.ServiceModel.WSFederationHttpBinding> or similar custom binding, you can enable federated authentication to a service.</span></span>  
  
 <span data-ttu-id="f70e4-105">WCF obtiene el token emitido por el servicio de token de seguridad (STS) en segundo plano y utiliza este token para autenticar en el servicio.</span><span class="sxs-lookup"><span data-stu-id="f70e4-105">WCF obtains the token that is issued by the security token service (STS) behind the scenes and uses this token to authenticate to the service.</span></span> <span data-ttu-id="f70e4-106">La limitación principal de este enfoque es que no hay visibilidad en las comunicaciones del cliente con el servidor.</span><span class="sxs-lookup"><span data-stu-id="f70e4-106">The main limitation to this approach is that there is no visibility into the client’s communications with the server.</span></span> <span data-ttu-id="f70e4-107">WCF genera automáticamente el token de seguridad de solicitud (RST) para el STS en función de los parámetros de token emitidos en el enlace.</span><span class="sxs-lookup"><span data-stu-id="f70e4-107">WCF automatically generates the request security token (RST) to the STS based on the issued token parameters on the binding.</span></span> <span data-ttu-id="f70e4-108">Esto significa que el cliente no puede cambiar los parámetros de RST por solicitud, inspeccionar la respuesta de token de seguridad de solicitud (RSTR) para obtener información como notificaciones de presentación ni almacenar en la memoria caché el token para su uso futuro.</span><span class="sxs-lookup"><span data-stu-id="f70e4-108">This means that the client cannot vary the RST parameters per request, inspect the request security token response (RSTR) to get information such as display claims, or cache the token for future use.</span></span>  
  
 <span data-ttu-id="f70e4-109">En la actualidad, el cliente WCF es adecuado para escenarios de federación básicos.</span><span class="sxs-lookup"><span data-stu-id="f70e4-109">Currently, the WCF client is suitable for basic federation scenarios.</span></span> <span data-ttu-id="f70e4-110">Sin embargo, uno de los escenarios principales que Windows Identity Foundation (WIF) admite requiere el control sobre el RST en un nivel que WCF no permite con facilidad.</span><span class="sxs-lookup"><span data-stu-id="f70e4-110">However, one of the major scenarios that Windows Identity Foundation (WIF) supports requires control over the RST at a level that WCF does not easily allow.</span></span> <span data-ttu-id="f70e4-111">Por consiguiente, WIF agrega características que ofrecen un mayor control sobre la comunicación con el STS.</span><span class="sxs-lookup"><span data-stu-id="f70e4-111">Therefore, WIF adds features that give you more control over communication with the STS.</span></span>  
  
 <span data-ttu-id="f70e4-112">WIF admite los escenarios de federación siguientes:</span><span class="sxs-lookup"><span data-stu-id="f70e4-112">WIF supports the following federation scenarios:</span></span>  
  
-   <span data-ttu-id="f70e4-113">Mediante un cliente WCF sin dependencias de WIF para autenticar en un servicio federado</span><span class="sxs-lookup"><span data-stu-id="f70e4-113">Using a WCF client without any WIF dependencies to authenticate to a federated service</span></span>  
  
-   <span data-ttu-id="f70e4-114">Habilitando WIF en un cliente WCF para insertar un elemento ActAs u OnBehalfOf en el RST para el STS</span><span class="sxs-lookup"><span data-stu-id="f70e4-114">Enabling WIF on a WCF client to insert an ActAs or OnBehalfOf element into the RST to the STS</span></span>  
  
-   <span data-ttu-id="f70e4-115">Mediante WIF solo para obtener un token del STS y habilitar a continuación un cliente WCF para autenticar con este token.</span><span class="sxs-lookup"><span data-stu-id="f70e4-115">Using WIF alone to obtain a token from the STS and then enable a WCF client to authenticate with this token.</span></span> <span data-ttu-id="f70e4-116">Para más información, vea el ejemplo [ClaimsAwareWebService](https://go.microsoft.com/fwlink/?LinkID=248406).</span><span class="sxs-lookup"><span data-stu-id="f70e4-116">For more information, see [ClaimsAwareWebService](https://go.microsoft.com/fwlink/?LinkID=248406) sample.</span></span>  
  
 <span data-ttu-id="f70e4-117">El primer escenario se explica por sí solo: los clientes de WCF existentes continuarán trabajando con los STS y los usuarios de confianza de WIF.</span><span class="sxs-lookup"><span data-stu-id="f70e4-117">The first scenario is self-explanatory: Existing WCF clients will continue to work with WIF relying parties and STSs.</span></span> <span data-ttu-id="f70e4-118">En este tema se describen los dos escenarios restantes.</span><span class="sxs-lookup"><span data-stu-id="f70e4-118">This topic discusses the remaining two scenarios.</span></span>  
  
## <a name="enhancing-an-existing-wcf-client-with-actas--onbehalfof"></a><span data-ttu-id="f70e4-119">Mejorar un cliente de WCF existente con ActAs/OnBehalfOf</span><span class="sxs-lookup"><span data-stu-id="f70e4-119">Enhancing an Existing WCF Client with ActAs / OnBehalfOf</span></span>  
 <span data-ttu-id="f70e4-120">En un escenario de delegación de identidad típico, un cliente llama a un servicio de nivel intermedio, que a su vez llama a un servicio back-end.</span><span class="sxs-lookup"><span data-stu-id="f70e4-120">In a typical identity delegation scenario, a client calls a middle-tier service, which then calls a back-end service.</span></span> <span data-ttu-id="f70e4-121">El servicio de nivel intermedio actúa como el cliente o en su nombre.</span><span class="sxs-lookup"><span data-stu-id="f70e4-121">The middle-tier service acts as, or acts on behalf of, the client.</span></span>  
  
> [!TIP]
>  <span data-ttu-id="f70e4-122">¿Cuál es la diferencia entre ActAs y OnBehalfOf?</span><span class="sxs-lookup"><span data-stu-id="f70e4-122">What is the difference between ActAs and OnBehalfOf?</span></span>  
>   
>  <span data-ttu-id="f70e4-123">Desde el punto de vista del protocolo WS-Trust:</span><span class="sxs-lookup"><span data-stu-id="f70e4-123">From the WS-Trust protocol standpoint:</span></span>  
>   
> 1. <span data-ttu-id="f70e4-124">Un elemento RST ActAs indica que el solicitante desea un token que contiene notificaciones acerca de dos entidades distintas: el solicitante y una entidad externa representada por el token en el elemento ActAs.</span><span class="sxs-lookup"><span data-stu-id="f70e4-124">An ActAs RST element indicates that the requestor wants a token that contains claims about two distinct entities: the requestor, and an external entity represented by the token in the ActAs element.</span></span>  
> 2. <span data-ttu-id="f70e4-125">Un elemento RST OnBehalfOf indica que el solicitante desea un token que contenga notificaciones solo acerca de una entidad: la entidad externa representada por el token en el elemento OnBehalfOf.</span><span class="sxs-lookup"><span data-stu-id="f70e4-125">An OnBehalfOf RST element indicates that the requestor wants a token that contains claims only about one entity: the external entity represented by the token in the OnBehalfOf element.</span></span>  
>   
>  <span data-ttu-id="f70e4-126">La característica ActAs se usa normalmente en escenarios que requieren delegación compuesta, donde el destinatario final del token emitido puede inspeccionar la cadena de delegación completa y ver no solo al cliente sino también a todos los intermediarios.</span><span class="sxs-lookup"><span data-stu-id="f70e4-126">The ActAs feature is typically used in scenarios that require composite delegation, where the final recipient of the issued token can inspect the entire delegation chain and see not just the client, but all intermediaries.</span></span> <span data-ttu-id="f70e4-127">Esto permite realizar el control de acceso, la auditoría y otras actividades relacionadas basadas en la cadena de delegación de identidad completa.</span><span class="sxs-lookup"><span data-stu-id="f70e4-127">This lets it perform access control, auditing and other related activities based on the entire identity delegation chain.</span></span> <span data-ttu-id="f70e4-128">La característica ActAs se utiliza normalmente en sistemas de varios niveles para autenticar y pasar información acerca de identidades entre los niveles sin tener que pasar esta información en la capa de lógica de negocios o aplicación.</span><span class="sxs-lookup"><span data-stu-id="f70e4-128">The ActAs feature is commonly used in multi-tiered systems to authenticate and pass information about identities between the tiers without having to pass this information at the application/business logic layer.</span></span>  
>   
>  <span data-ttu-id="f70e4-129">La característica OnBehalfOf se utiliza en escenarios donde solo es importante la identidad del cliente original y es en la práctica igual que la característica de suplantación de identidad disponible en Windows.</span><span class="sxs-lookup"><span data-stu-id="f70e4-129">The OnBehalfOf feature is used in scenarios where only the identity of the original client is important and is effectively the same as the identity impersonation feature available in Windows.</span></span> <span data-ttu-id="f70e4-130">Cuando se utiliza OnBehalfOf, el destinatario final del token emitido solo puede ver notificaciones acerca del cliente original y no se conserva la información sobre los intermediarios.</span><span class="sxs-lookup"><span data-stu-id="f70e4-130">When OnBehalfOf is used, the final recipient of the issued token can only see claims about the original client, and the information about intermediaries is not preserved.</span></span> <span data-ttu-id="f70e4-131">Un patrón común donde se utiliza la característica OnBehalfOf es el patrón de proxy donde el cliente no puede tener acceso al STS directamente, sino que se comunica en su lugar a través de una puerta de enlace de proxy.</span><span class="sxs-lookup"><span data-stu-id="f70e4-131">One common pattern where the OnBehalfOf feature is used is the proxy pattern where the client cannot access the STS directly but instead communicates through a proxy gateway.</span></span> <span data-ttu-id="f70e4-132">La puerta de enlace de proxy autentica al llamador y coloca la información sobre el llamador en el elemento OnBehalfOf del mensaje de RST que envía a continuación al STS real para su procesamiento.</span><span class="sxs-lookup"><span data-stu-id="f70e4-132">The proxy gateway authenticates the caller and puts information about the caller into the OnBehalfOf element of the RST message that it then sends to the real STS for processing.</span></span> <span data-ttu-id="f70e4-133">El token resultante contiene solo las notificaciones relacionadas con el cliente del proxy, lo que hace que el proxy sea completamente transparente para el receptor del token emitido. Tenga en cuenta que WIF no admite \<wsse:SecurityTokenReference> ni \<wsa:EndpointReferences> como elemento secundario de \<wst:OnBehalfOf>.</span><span class="sxs-lookup"><span data-stu-id="f70e4-133">The resulting token contains only claims related to the client of the proxy, making the proxy completely transparent to the receiver of the issued token.Note that WIF does not support \<wsse:SecurityTokenReference> or \<wsa:EndpointReferences> as a child of \<wst:OnBehalfOf>.</span></span> <span data-ttu-id="f70e4-134">La especificación WS-Trust permite tres maneras de identificar el solicitante original (en nombre del cual está actuando el proxy).</span><span class="sxs-lookup"><span data-stu-id="f70e4-134">The WS-Trust specification allows for three ways to identify the original requestor (on behalf of whom the proxy is acting).</span></span> <span data-ttu-id="f70e4-135">Estos son:</span><span class="sxs-lookup"><span data-stu-id="f70e4-135">These are:</span></span>  
>   
>  -   <span data-ttu-id="f70e4-136">Referencia del token de seguridad.</span><span class="sxs-lookup"><span data-stu-id="f70e4-136">Security token reference.</span></span> <span data-ttu-id="f70e4-137">Una referencia a un token, en el mensaje, o posiblemente recuperado fuera de banda.</span><span class="sxs-lookup"><span data-stu-id="f70e4-137">A reference to a token, either in the message, or possibly retrieved out of band).</span></span>  
> -   <span data-ttu-id="f70e4-138">Referencia de punto de conexión.</span><span class="sxs-lookup"><span data-stu-id="f70e4-138">Endpoint reference.</span></span> <span data-ttu-id="f70e4-139">Se utiliza como clave para buscar datos, de nuevo fuera de banda.</span><span class="sxs-lookup"><span data-stu-id="f70e4-139">Used as a key to look up data, again out of band.</span></span>  
> -   <span data-ttu-id="f70e4-140">Token de seguridad.</span><span class="sxs-lookup"><span data-stu-id="f70e4-140">Security token.</span></span> <span data-ttu-id="f70e4-141">Identifica el solicitante original directamente.</span><span class="sxs-lookup"><span data-stu-id="f70e4-141">Identifies the original requestor directly.</span></span>  
>   
>  <span data-ttu-id="f70e4-142">WIF solo admite tokens de seguridad, cifrados o no cifrados, como elemento secundario directo de \<wst:OnBehalfOf>.</span><span class="sxs-lookup"><span data-stu-id="f70e4-142">WIF supports only security tokens, either encrypted or unencrypted, as a direct child element of \<wst:OnBehalfOf>.</span></span>  
  
 <span data-ttu-id="f70e4-143">Esta información se ofrece a un emisor de WS-Trust mediante los elementos de token ActAs y OnBehalfOf en el RST.</span><span class="sxs-lookup"><span data-stu-id="f70e4-143">This information is conveyed to a WS-Trust issuer using the ActAs and OnBehalfOf token elements in the RST.</span></span>  
  
 <span data-ttu-id="f70e4-144">WCF expone un punto de extensibilidad en el enlace que permite que los elementos XML arbitrarios se agreguen al RST.</span><span class="sxs-lookup"><span data-stu-id="f70e4-144">WCF exposes an extensibility point on the binding that allows arbitrary XML elements to be added to the RST.</span></span> <span data-ttu-id="f70e4-145">Sin embargo, dado que el punto de extensibilidad está asociado al enlace, los escenarios que requieren que el contenido de RST varíe en cada llamada deben volver a crear el cliente para cada llamada, lo que disminuye el rendimiento.</span><span class="sxs-lookup"><span data-stu-id="f70e4-145">However, because the extensibility point is tied to the binding, scenarios that require the RST contents to vary per call must re-create the client for every call, which decreases performance.</span></span> <span data-ttu-id="f70e4-146">WIF utiliza métodos de extensión de la clase `ChannelFactory` para permitir que los desarrolladores adjunten cualquier token que se obtiene fuera de banda al RST.</span><span class="sxs-lookup"><span data-stu-id="f70e4-146">WIF uses extension methods on the `ChannelFactory` class to allow developers to attach any token that is obtained out of band to the RST.</span></span> <span data-ttu-id="f70e4-147">En el ejemplo de código siguiente se muestra cómo utilizar un token que representa al cliente (como un X.509, un nombre de usuario o un token del lenguaje de marcado de aserción de seguridad (SAML)) y adjuntarlo al RST que se envía al emisor.</span><span class="sxs-lookup"><span data-stu-id="f70e4-147">The following code example shows how to take a token that represents the client (such as an X.509, username, or Security Assertion Markup Language (SAML) token) and attach it to the RST that is sent to the issuer.</span></span>  
  
```  
IHelloService serviceChannel = channelFactory.CreateChannelActingAs<IHelloService>( clientSamlToken );  
serviceChannel.Hello("Hi!");  
```  
  
 <span data-ttu-id="f70e4-148">WIF proporciona las siguientes ventajas:</span><span class="sxs-lookup"><span data-stu-id="f70e4-148">WIF provides the following benefits:</span></span>  
  
-   <span data-ttu-id="f70e4-149">El RST se puede modificar por canal; por consiguiente, los servicios de nivel medio no tienen que volver a crear el generador de canales para cada cliente, lo que mejora el rendimiento.</span><span class="sxs-lookup"><span data-stu-id="f70e4-149">The RST can be modified per channel; therefore, middle-tier services do not have to re-create the channel factory for each client, which improves performance.</span></span>  
  
-   <span data-ttu-id="f70e4-150">Esto funciona con los clientes WCF existentes, lo que hace posible una ruta de acceso de actualización sencilla para los servicios de nivel medio de WCF existentes que desean habilitar la semántica de delegación de identidad.</span><span class="sxs-lookup"><span data-stu-id="f70e4-150">This works with existing WCF clients, which makes an easy upgrade path possible for existing WCF middle-tier services that want to enable identity delegation semantics.</span></span>  
  
 <span data-ttu-id="f70e4-151">Sin embargo, todavía no hay visibilidad en la comunicación del cliente con el STS.</span><span class="sxs-lookup"><span data-stu-id="f70e4-151">However, there is still no visibility into the client’s communication with the STS.</span></span> <span data-ttu-id="f70e4-152">Examinaremos esto en el tercer escenario.</span><span class="sxs-lookup"><span data-stu-id="f70e4-152">We’ll examine this in the third scenario.</span></span>  
  
## <a name="communicating-directly-with-an-issuer-and-using-the-issued-token-to-authenticate"></a><span data-ttu-id="f70e4-153">Comunicación directa con un emisor y uso del token emitido para autenticar</span><span class="sxs-lookup"><span data-stu-id="f70e4-153">Communicating Directly with an Issuer and Using the Issued Token to Authenticate</span></span>  
 <span data-ttu-id="f70e4-154">Para algunos escenarios avanzados, la mejora de un cliente WCF no es suficiente.</span><span class="sxs-lookup"><span data-stu-id="f70e4-154">For some advanced scenarios, enhancing a WCF client is not enough.</span></span> <span data-ttu-id="f70e4-155">Los desarrolladores que solo usan WCF suelen utilizar contratos de entrada y salida de mensajes y controlan el análisis del cliente de la respuesta del emisor de manera manual.</span><span class="sxs-lookup"><span data-stu-id="f70e4-155">Developers who use only WCF typically use Message In / Message Out contracts and handle client-side parsing of the issuer response manually.</span></span>  
  
 <span data-ttu-id="f70e4-156">WIF presenta las clases <xref:System.ServiceModel.Security.WSTrustChannelFactory> y <xref:System.ServiceModel.Security.WSTrustChannel> para permitir al cliente comunicarse directamente con un emisor de WS-Trust.</span><span class="sxs-lookup"><span data-stu-id="f70e4-156">WIF introduces the <xref:System.ServiceModel.Security.WSTrustChannelFactory> and <xref:System.ServiceModel.Security.WSTrustChannel> classes to let the client communicate directly with a WS-Trust issuer.</span></span> <span data-ttu-id="f70e4-157">Las clases <xref:System.ServiceModel.Security.WSTrustChannelFactory> y <xref:System.ServiceModel.Security.WSTrustChannel> permiten a los objetos RSTR y RST fuertemente tipados fluir entre el cliente y el emisor, como se muestra en el siguiente ejemplo de código.</span><span class="sxs-lookup"><span data-stu-id="f70e4-157">The <xref:System.ServiceModel.Security.WSTrustChannelFactory> and <xref:System.ServiceModel.Security.WSTrustChannel> classes enable strongly typed RST and RSTR objects to flow between the client and issuer, as shown in the following code example.</span></span>  
  
```  
WSTrustChannelFactory trustChannelFactory = new WSTrustChannelFactory( stsBinding, stsAddress );  
WSTrustChannel channel = (WSTrustChannel) trustChannelFactory.CreateChannel();  
RequestSecurityToken rst = new RequestSecurityToken(RequestTypes.Issue);  
rst.AppliesTo = new EndpointAddress(serviceAddress);  
RequestSecurityTokenResponse rstr = null;  
SecurityToken token = channel.Issue(rst, out rstr);  
```  
  
 <span data-ttu-id="f70e4-158">Observe que el parámetro `out` del método <xref:System.ServiceModel.Security.WSTrustChannel.Issue%2A> permite el acceso al RSTR para la inspección del cliente.</span><span class="sxs-lookup"><span data-stu-id="f70e4-158">Note that the `out` parameter on the <xref:System.ServiceModel.Security.WSTrustChannel.Issue%2A> method allows access to the RSTR for client-side inspection.</span></span>  
  
 <span data-ttu-id="f70e4-159">Hasta ahora, solo hemos visto cómo obtener un token.</span><span class="sxs-lookup"><span data-stu-id="f70e4-159">So far, we’ve only seen how to obtain a token.</span></span> <span data-ttu-id="f70e4-160">El token que se devuelve del objeto <xref:System.ServiceModel.Security.WSTrustChannel> es un `GenericXmlSecurityToken` que contiene toda la información necesaria para la autenticación en un usuario de confianza.</span><span class="sxs-lookup"><span data-stu-id="f70e4-160">The token that is returned from the <xref:System.ServiceModel.Security.WSTrustChannel> object is a `GenericXmlSecurityToken` that contains all of the information that is necessary for authentication to a relying party.</span></span> <span data-ttu-id="f70e4-161">En el ejemplo de código siguiente se muestra la forma de utilizar este token.</span><span class="sxs-lookup"><span data-stu-id="f70e4-161">The following code example shows how to use this token.</span></span>  
  
```  
IHelloService serviceChannel = channelFactory.CreateChannelWithIssuedToken<IHelloService>( token ); serviceChannel.Hello("Hi!");  
```  
  
 <span data-ttu-id="f70e4-162">El método de extensión <xref:System.ServiceModel.ChannelFactory%601.CreateChannelWithIssuedToken%2A> del objeto `ChannelFactory` indica a WIF que ha obtenido un token fuera de banda y que debe detener la llamada de WCF normal al emisor y utilizar en su lugar el token que obtuvo para autenticar al usuario de confianza.</span><span class="sxs-lookup"><span data-stu-id="f70e4-162">The <xref:System.ServiceModel.ChannelFactory%601.CreateChannelWithIssuedToken%2A> extension method on the `ChannelFactory` object indicates to WIF that you have obtained a token out of band, and that it should stop the normal WCF call to the issuer and instead use the token that you obtained to authenticate to the relying party.</span></span> <span data-ttu-id="f70e4-163">Esto tiene las ventajas siguientes:</span><span class="sxs-lookup"><span data-stu-id="f70e4-163">This has the following benefits:</span></span>  
  
-   <span data-ttu-id="f70e4-164">Proporciona control completo sobre el proceso de emisión de tokens.</span><span class="sxs-lookup"><span data-stu-id="f70e4-164">It gives you complete control over the token issuance process.</span></span>  
  
-   <span data-ttu-id="f70e4-165">Admite escenarios de ActAs/OnBehalfOf estableciendo directamente estas propiedades en el RST de salida.</span><span class="sxs-lookup"><span data-stu-id="f70e4-165">It supports ActAs / OnBehalfOf scenarios by directly setting these properties on the outgoing RST.</span></span>  
  
-   <span data-ttu-id="f70e4-166">Permite tomar decisiones de confianza dinámicas en el cliente en función del contenido de RSTR.</span><span class="sxs-lookup"><span data-stu-id="f70e4-166">It enables dynamic client-side trust decisions to be made based on the contents of the RSTR.</span></span>  
  
-   <span data-ttu-id="f70e4-167">Permite almacenar en la memoria caché y reutilizar el token que se devuelve del método <xref:System.ServiceModel.Security.WSTrustChannel.Issue%2A>.</span><span class="sxs-lookup"><span data-stu-id="f70e4-167">It lets you cache and reuse the token that is returned from the <xref:System.ServiceModel.Security.WSTrustChannel.Issue%2A> method.</span></span>  
  
-   <span data-ttu-id="f70e4-168"><xref:System.ServiceModel.Security.WSTrustChannelFactory> y <xref:System.ServiceModel.Security.WSTrustChannel> permiten controlar la semántica de almacenamiento en caché del canal, errores y recuperación según los procedimientos recomendados de WCF.</span><span class="sxs-lookup"><span data-stu-id="f70e4-168"><xref:System.ServiceModel.Security.WSTrustChannelFactory> and <xref:System.ServiceModel.Security.WSTrustChannel> allow for control of channel caching, fault, and recovery semantics according to WCF best practices.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="f70e4-169">Vea también</span><span class="sxs-lookup"><span data-stu-id="f70e4-169">See Also</span></span>  
 [<span data-ttu-id="f70e4-170">Características de WIF</span><span class="sxs-lookup"><span data-stu-id="f70e4-170">WIF Features</span></span>](../../../docs/framework/security/wif-features.md)
