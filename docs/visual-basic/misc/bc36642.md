---
description: "Obtenga más información sobre: Option Strict on requiere que cada parámetro de expresión lambda se declare con una cláusula ' as ' si no se puede inferir su tipo"
title: Option Strict On requiere que los parámetros de la expresión lambda se declaren con una cláusula 'As' si no se puede inferir su tipo.
ms.date: 07/20/2015
f1_keywords:
- bc36642
- vbc36642
helpviewer_keywords:
- BC36642
ms.assetid: 2aaa62b8-49c9-4ae8-b0f5-08e3f0b5ad10
ms.openlocfilehash: b588f54aa83fe103af5d5e19f05244a7099111d7
ms.sourcegitcommit: 10e719780594efc781b15295e499c66f316068b8
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 02/14/2021
ms.locfileid: "100436245"
---
# <a name="option-strict-on-requires-each-lambda-expression-parameter-to-be-declared-with-an-as-clause-if-its-type-cannot-be-inferred"></a><span data-ttu-id="80954-103">Option Strict On requiere que los parámetros de la expresión lambda se declaren con una cláusula 'As' si no se puede inferir su tipo.</span><span class="sxs-lookup"><span data-stu-id="80954-103">Option Strict On requires each lambda expression parameter to be declared with an 'As' clause if its type cannot be inferred</span></span>

<span data-ttu-id="80954-104">Se ha declarado un parámetro en una expresión lambda sin usar una cláusula `As` , con `Option Strict` activado.</span><span class="sxs-lookup"><span data-stu-id="80954-104">You have declared a parameter in a lambda expression without using an `As` clause, with `Option Strict` on.</span></span>  
  
```vb  
' Not valid when Option Strict is on.  
' Dim increment1 = Function (n) n + 1  
```  
  
 <span data-ttu-id="80954-105">La declaración anterior es válida si se puede deducir el tipo de `n` .</span><span class="sxs-lookup"><span data-stu-id="80954-105">The previous declaration is valid if the type of `n` can be inferred.</span></span> <span data-ttu-id="80954-106">Por ejemplo, si va a asignar la expresión lambda anterior a un delegado de función, `Del`:</span><span class="sxs-lookup"><span data-stu-id="80954-106">For example, if you are assigning the previous lambda expression to a function delegate, `Del`:</span></span>  
  
```vb  
Delegate Function Del(ByVal p As Integer) As Integer  
```  
  
 <span data-ttu-id="80954-107">Ahora el tipo de `n` puede deducirse a partir del parámetro `p`:</span><span class="sxs-lookup"><span data-stu-id="80954-107">Now the type of `n` can be inferred from parameter `p`:</span></span>  
  
```vb  
Dim increment2 as Del = Function(n) n + 1  
```  
  
 <span data-ttu-id="80954-108">**Identificador de error:** BC36642</span><span class="sxs-lookup"><span data-stu-id="80954-108">**Error ID:** BC36642</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="80954-109">Para corregir este error</span><span class="sxs-lookup"><span data-stu-id="80954-109">To correct this error</span></span>  
  
- <span data-ttu-id="80954-110">Agregar una cláusula `As` a la declaración de parámetro:</span><span class="sxs-lookup"><span data-stu-id="80954-110">Add an `As` clause to the parameter declaration:</span></span>  
  
    ```vb  
    Dim increment3 = Function (n As Integer) n + 1  
    ```  
  
## <a name="see-also"></a><span data-ttu-id="80954-111">Consulte también</span><span class="sxs-lookup"><span data-stu-id="80954-111">See also</span></span>

- [<span data-ttu-id="80954-112">Expresiones lambda</span><span class="sxs-lookup"><span data-stu-id="80954-112">Lambda Expressions</span></span>](../programming-guide/language-features/procedures/lambda-expressions.md)
