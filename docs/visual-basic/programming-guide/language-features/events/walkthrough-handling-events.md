---
title: Control de eventos (Visual Basic)
ms.date: 07/20/2015
helpviewer_keywords:
- event handling [Visual Basic], walkthroughs
- walkthroughs [Visual Basic], event handling
- variables [Visual Basic], WithEvents
- events [Visual Basic], walkthroughs
- WithEvents keyword [Visual Basic], walkthroughs
- event handlers [Visual Basic], walkthroughs
ms.assetid: f145b3fc-5ae0-4509-a2aa-1ff6934706bd
ms.openlocfilehash: aa3c1f00922cda46d3ce5a788b606a6f5da52636
ms.sourcegitcommit: 2701302a99cafbe0d86d53d540eb0fa7e9b46b36
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 04/28/2019
ms.locfileid: "64604042"
---
# <a name="walkthrough-handling-events-visual-basic"></a><span data-ttu-id="778a1-102">Tutorial: Control de eventos (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="778a1-102">Walkthrough: Handling Events (Visual Basic)</span></span>
<span data-ttu-id="778a1-103">Éste es el segundo de dos temas que muestran cómo trabajar con eventos.</span><span class="sxs-lookup"><span data-stu-id="778a1-103">This is the second of two topics that demonstrate how to work with events.</span></span> <span data-ttu-id="778a1-104">El primer tema, [Tutorial: Declarar y provocar eventos](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md), se muestra cómo declarar y provocar eventos.</span><span class="sxs-lookup"><span data-stu-id="778a1-104">The first topic, [Walkthrough: Declaring and Raising Events](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md), shows how to declare and raise events.</span></span> <span data-ttu-id="778a1-105">En esta sección se usa el formulario y la clase del tutorial para mostrar cómo controlar eventos cuando tienen lugar.</span><span class="sxs-lookup"><span data-stu-id="778a1-105">This section uses the form and class from that walkthrough to show how to handle events when they take place.</span></span>  
  
 <span data-ttu-id="778a1-106">El `Widget` ejemplo de clase utiliza instrucciones tradicionales de control de eventos.</span><span class="sxs-lookup"><span data-stu-id="778a1-106">The `Widget` class example uses traditional event-handling statements.</span></span> <span data-ttu-id="778a1-107">Visual Basic ofrece otras técnicas para trabajar con eventos.</span><span class="sxs-lookup"><span data-stu-id="778a1-107">Visual Basic provides other techniques for working with events.</span></span> <span data-ttu-id="778a1-108">Como ejercicio, puede modificar este ejemplo, para usar el `AddHandler` y `Handles` instrucciones.</span><span class="sxs-lookup"><span data-stu-id="778a1-108">As an exercise, you can modify this example to use the `AddHandler` and `Handles` statements.</span></span>  
  
### <a name="to-handle-the-percentdone-event-of-the-widget-class"></a><span data-ttu-id="778a1-109">Para controlar el evento PercentDone de la clase Widget</span><span class="sxs-lookup"><span data-stu-id="778a1-109">To handle the PercentDone event of the Widget class</span></span>  
  
1. <span data-ttu-id="778a1-110">Coloque el código siguiente en `Form1`:</span><span class="sxs-lookup"><span data-stu-id="778a1-110">Place the following code in `Form1`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#4](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#4)]  
  
     <span data-ttu-id="778a1-111">El `WithEvents` palabra clave especifica que la variable `mWidget` se usa para controlar eventos de un objeto.</span><span class="sxs-lookup"><span data-stu-id="778a1-111">The `WithEvents` keyword specifies that the variable `mWidget` is used to handle an object's events.</span></span> <span data-ttu-id="778a1-112">Especifique el tipo de objeto, proporcionando el nombre de la clase desde el que se creará el objeto.</span><span class="sxs-lookup"><span data-stu-id="778a1-112">You specify the kind of object by supplying the name of the class from which the object will be created.</span></span>  
  
     <span data-ttu-id="778a1-113">La variable `mWidget` se declara en `Form1` porque `WithEvents` variables deben ser de nivel de clase.</span><span class="sxs-lookup"><span data-stu-id="778a1-113">The variable `mWidget` is declared in `Form1` because `WithEvents` variables must be class-level.</span></span> <span data-ttu-id="778a1-114">Esto es cierto independientemente del tipo de clase en que colocarlos.</span><span class="sxs-lookup"><span data-stu-id="778a1-114">This is true regardless of the type of class you place them in.</span></span>  
  
     <span data-ttu-id="778a1-115">La variable `mblnCancel` se utiliza para cancelar la `LongTask` método.</span><span class="sxs-lookup"><span data-stu-id="778a1-115">The variable `mblnCancel` is used to cancel the `LongTask` method.</span></span>  
  
## <a name="writing-code-to-handle-an-event"></a><span data-ttu-id="778a1-116">Escribir código para controlar un evento</span><span class="sxs-lookup"><span data-stu-id="778a1-116">Writing Code to Handle an Event</span></span>  
 <span data-ttu-id="778a1-117">Tan pronto como se declara una variable utilizando `WithEvents`, el nombre de variable aparece en la lista desplegable izquierda de la clase **Editor de código**.</span><span class="sxs-lookup"><span data-stu-id="778a1-117">As soon as you declare a variable using `WithEvents`, the variable name appears in the left drop-down list of the class's **Code Editor**.</span></span> <span data-ttu-id="778a1-118">Al seleccionar `mWidget`, el `Widget` eventos de clase aparecen en la lista desplegable derecha.</span><span class="sxs-lookup"><span data-stu-id="778a1-118">When you select `mWidget`, the `Widget` class's events appear in the right drop-down list.</span></span> <span data-ttu-id="778a1-119">Al seleccionar un evento muestra el procedimiento de evento correspondiente, con el prefijo `mWidget` y un carácter de subrayado.</span><span class="sxs-lookup"><span data-stu-id="778a1-119">Selecting an event displays the corresponding event procedure, with the prefix `mWidget` and an underscore.</span></span> <span data-ttu-id="778a1-120">Todos los procedimientos de evento asociados con un `WithEvents` variable reciben el nombre de variable como prefijo.</span><span class="sxs-lookup"><span data-stu-id="778a1-120">All the event procedures associated with a `WithEvents` variable are given the variable name as a prefix.</span></span>  
  
#### <a name="to-handle-an-event"></a><span data-ttu-id="778a1-121">Para controlar un evento</span><span class="sxs-lookup"><span data-stu-id="778a1-121">To handle an event</span></span>  
  
1. <span data-ttu-id="778a1-122">Seleccione `mWidget` desde la lista desplegable izquierda en el **Editor de código**.</span><span class="sxs-lookup"><span data-stu-id="778a1-122">Select `mWidget` from the left drop-down list in the **Code Editor**.</span></span>  
  
2. <span data-ttu-id="778a1-123">Seleccione el `PercentDone` evento en la lista desplegable derecha.</span><span class="sxs-lookup"><span data-stu-id="778a1-123">Select the `PercentDone` event from the right drop-down list.</span></span> <span data-ttu-id="778a1-124">El **Editor de código** abre el `mWidget_PercentDone` procedimiento de evento.</span><span class="sxs-lookup"><span data-stu-id="778a1-124">The **Code Editor** opens the `mWidget_PercentDone` event procedure.</span></span>  
  
    > [!NOTE]
    >  <span data-ttu-id="778a1-125">El **Editor de código** es útil, pero no es necesario, para insertar nuevos controladores de eventos.</span><span class="sxs-lookup"><span data-stu-id="778a1-125">The **Code Editor** is useful, but not required, for inserting new event handlers.</span></span> <span data-ttu-id="778a1-126">En este tutorial, es más directa para copiar solo los controladores de eventos directamente en el código.</span><span class="sxs-lookup"><span data-stu-id="778a1-126">In this walkthrough, it is more direct to just copy the event handlers directly into your code.</span></span>  
  
3. <span data-ttu-id="778a1-127">Agregue el código siguiente al controlador de eventos `mWidget_PercentDone` :</span><span class="sxs-lookup"><span data-stu-id="778a1-127">Add the following code to the `mWidget_PercentDone` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#5](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#5)]  
  
     <span data-ttu-id="778a1-128">Cada vez que el `PercentDone` provoca el evento, el procedimiento de evento muestra el porcentaje completado en un `Label` control.</span><span class="sxs-lookup"><span data-stu-id="778a1-128">Whenever the `PercentDone` event is raised, the event procedure displays the percent complete in a `Label` control.</span></span> <span data-ttu-id="778a1-129">El `DoEvents` método permite la etiqueta que quiere volver a dibujar y también proporciona al usuario la oportunidad de hacer clic en el **cancelar** botón.</span><span class="sxs-lookup"><span data-stu-id="778a1-129">The `DoEvents` method allows the label to repaint, and also gives the user the opportunity to click the **Cancel** button.</span></span>  
  
4. <span data-ttu-id="778a1-130">Agregue el siguiente código para el `Button2_Click` controlador de eventos:</span><span class="sxs-lookup"><span data-stu-id="778a1-130">Add the following code for the `Button2_Click` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#6](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#6)]  
  
 <span data-ttu-id="778a1-131">Si el usuario hace clic en el **cancelar** botón mientras `LongTask` se está ejecutando, el `Button2_Click` eventos se ejecutan tan pronto como el `DoEvents` instrucción permite el procesamiento de eventos que se produzca.</span><span class="sxs-lookup"><span data-stu-id="778a1-131">If the user clicks the **Cancel** button while `LongTask` is running, the `Button2_Click` event is executed as soon as the `DoEvents` statement allows event processing to occur.</span></span> <span data-ttu-id="778a1-132">La variable de nivel de clase `mblnCancel` está establecido en `True`y el `mWidget_PercentDone` eventos, a continuación, comprueba y establece el `ByRef Cancel` argumento `True`.</span><span class="sxs-lookup"><span data-stu-id="778a1-132">The class-level variable `mblnCancel` is set to `True`, and the `mWidget_PercentDone` event then tests it and sets the `ByRef Cancel` argument to `True`.</span></span>  
  
## <a name="connecting-a-withevents-variable-to-an-object"></a><span data-ttu-id="778a1-133">Conexión a un objeto de una Variable WithEvents</span><span class="sxs-lookup"><span data-stu-id="778a1-133">Connecting a WithEvents Variable to an Object</span></span>  
 <span data-ttu-id="778a1-134">`Form1` Ahora está configurado para controlar un `Widget` eventos del objeto.</span><span class="sxs-lookup"><span data-stu-id="778a1-134">`Form1` is now set up to handle a `Widget` object's events.</span></span> <span data-ttu-id="778a1-135">Todo lo que queda es encontrar un `Widget` en algún lugar.</span><span class="sxs-lookup"><span data-stu-id="778a1-135">All that remains is to find a `Widget` somewhere.</span></span>  
  
 <span data-ttu-id="778a1-136">Al declarar una variable `WithEvents` en tiempo de diseño, no hay ningún objeto está asociado con él.</span><span class="sxs-lookup"><span data-stu-id="778a1-136">When you declare a variable `WithEvents` at design time, no object is associated with it.</span></span> <span data-ttu-id="778a1-137">Un `WithEvents` variable es igual que cualquier otra variable de objeto.</span><span class="sxs-lookup"><span data-stu-id="778a1-137">A `WithEvents` variable is just like any other object variable.</span></span> <span data-ttu-id="778a1-138">Tendrá que crear un objeto y asignar una referencia a él con el `WithEvents` variable.</span><span class="sxs-lookup"><span data-stu-id="778a1-138">You have to create an object and assign a reference to it with the `WithEvents` variable.</span></span>  
  
#### <a name="to-create-an-object-and-assign-a-reference-to-it"></a><span data-ttu-id="778a1-139">Para crear un objeto y asignar una referencia a él</span><span class="sxs-lookup"><span data-stu-id="778a1-139">To create an object and assign a reference to it</span></span>  
  
1. <span data-ttu-id="778a1-140">Seleccione **(eventos de Form1)** desde la lista desplegable izquierda en el **Editor de código**.</span><span class="sxs-lookup"><span data-stu-id="778a1-140">Select **(Form1 Events)** from the left drop-down list in the **Code Editor**.</span></span>  
  
2. <span data-ttu-id="778a1-141">Seleccione el `Load` evento en la lista desplegable derecha.</span><span class="sxs-lookup"><span data-stu-id="778a1-141">Select the `Load` event from the right drop-down list.</span></span> <span data-ttu-id="778a1-142">El **Editor de código** abre el `Form1_Load` procedimiento de evento.</span><span class="sxs-lookup"><span data-stu-id="778a1-142">The **Code Editor** opens the `Form1_Load` event procedure.</span></span>  
  
3. <span data-ttu-id="778a1-143">Agregue el siguiente código para el `Form1_Load` procedimiento de evento para crear el `Widget`:</span><span class="sxs-lookup"><span data-stu-id="778a1-143">Add the following code for the `Form1_Load` event procedure to create the `Widget`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#7](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#7)]  
  
 <span data-ttu-id="778a1-144">Cuando se ejecuta este código, Visual Basic crea un `Widget` de objetos y conecta sus eventos a los procedimientos de evento asociados `mWidget`.</span><span class="sxs-lookup"><span data-stu-id="778a1-144">When this code executes, Visual Basic creates a `Widget` object and connects its events to the event procedures associated with `mWidget`.</span></span> <span data-ttu-id="778a1-145">Desde ese punto, siempre que el `Widget` genera su `PercentDone` eventos, el `mWidget_PercentDone` se ejecuta el procedimiento de evento.</span><span class="sxs-lookup"><span data-stu-id="778a1-145">From that point on, whenever the `Widget` raises its `PercentDone` event, the `mWidget_PercentDone` event procedure is executed.</span></span>  
  
#### <a name="to-call-the-longtask-method"></a><span data-ttu-id="778a1-146">Para llamar al método LongTask</span><span class="sxs-lookup"><span data-stu-id="778a1-146">To call the LongTask method</span></span>  
  
- <span data-ttu-id="778a1-147">Agregue el código siguiente al controlador de eventos `Button1_Click` :</span><span class="sxs-lookup"><span data-stu-id="778a1-147">Add the following code to the `Button1_Click` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#8](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#8)]  
  
 <span data-ttu-id="778a1-148">Antes de la `LongTask` se llama el método, la etiqueta que muestra el porcentaje completado debe inicializarse y el nivel de clase `Boolean` marca para cancelar el método debe establecerse en `False`.</span><span class="sxs-lookup"><span data-stu-id="778a1-148">Before the `LongTask` method is called, the label that displays the percent complete must be initialized, and the class-level `Boolean` flag for canceling the method must be set to `False`.</span></span>  
  
 <span data-ttu-id="778a1-149">`LongTask` se llama con una duración de la tarea de 12,2 segundos.</span><span class="sxs-lookup"><span data-stu-id="778a1-149">`LongTask` is called with a task duration of 12.2 seconds.</span></span> <span data-ttu-id="778a1-150">El `PercentDone` evento se desencadena una vez cada una tercera parte de un segundo.</span><span class="sxs-lookup"><span data-stu-id="778a1-150">The `PercentDone` event is raised once every one-third of a second.</span></span> <span data-ttu-id="778a1-151">Cada vez que se genera el evento, el `mWidget_PercentDone` se ejecuta el procedimiento de evento.</span><span class="sxs-lookup"><span data-stu-id="778a1-151">Each time the event is raised, the `mWidget_PercentDone` event procedure is executed.</span></span>  
  
 <span data-ttu-id="778a1-152">Cuando `LongTask` se realiza, `mblnCancel` se prueba para ver si `LongTask` finalizó con normalidad, o si se ha detenido porque `mblnCancel` se estableció en `True`.</span><span class="sxs-lookup"><span data-stu-id="778a1-152">When `LongTask` is done, `mblnCancel` is tested to see if `LongTask` ended normally, or if it stopped because `mblnCancel` was set to `True`.</span></span> <span data-ttu-id="778a1-153">El porcentaje completado se actualiza solo en el caso anterior.</span><span class="sxs-lookup"><span data-stu-id="778a1-153">The percent complete is updated only in the former case.</span></span>  
  
#### <a name="to-run-the-program"></a><span data-ttu-id="778a1-154">Para ejecutar el programa</span><span class="sxs-lookup"><span data-stu-id="778a1-154">To run the program</span></span>  
  
1. <span data-ttu-id="778a1-155">Presione F5 para poner el proyecto en modo de ejecución.</span><span class="sxs-lookup"><span data-stu-id="778a1-155">Press F5 to put the project in run mode.</span></span>  
  
2. <span data-ttu-id="778a1-156">Haga clic en el **Iniciar tarea** botón.</span><span class="sxs-lookup"><span data-stu-id="778a1-156">Click the **Start Task** button.</span></span> <span data-ttu-id="778a1-157">Cada vez que el `PercentDone` provoca el evento, se actualiza la etiqueta con el porcentaje de la tarea que se ha completado.</span><span class="sxs-lookup"><span data-stu-id="778a1-157">Each time the `PercentDone` event is raised, the label is updated with the percentage of the task that is complete.</span></span>  
  
3. <span data-ttu-id="778a1-158">Haga clic en el **cancelar** botón para detener la tarea.</span><span class="sxs-lookup"><span data-stu-id="778a1-158">Click the **Cancel** button to stop the task.</span></span> <span data-ttu-id="778a1-159">Tenga en cuenta que la apariencia de la **cancelar** botón no cambia inmediatamente al hacer clic.</span><span class="sxs-lookup"><span data-stu-id="778a1-159">Notice that the appearance of the **Cancel** button does not change immediately when you click it.</span></span> <span data-ttu-id="778a1-160">El `Click` no se puede producir el evento hasta el `My.Application.DoEvents` instrucción permite el procesamiento de eventos.</span><span class="sxs-lookup"><span data-stu-id="778a1-160">The `Click` event cannot happen until the `My.Application.DoEvents` statement allows event processing.</span></span>  
  
    > [!NOTE]
    >  <span data-ttu-id="778a1-161">El `My.Application.DoEvents` método no procesa los eventos en la misma manera como lo hace el formulario.</span><span class="sxs-lookup"><span data-stu-id="778a1-161">The `My.Application.DoEvents` method does not process events in exactly the same way as the form does.</span></span> <span data-ttu-id="778a1-162">Por ejemplo, en este tutorial, debe hacer clic en el **cancelar** botón dos veces.</span><span class="sxs-lookup"><span data-stu-id="778a1-162">For example, in this walkthrough, you must click the **Cancel** button twice.</span></span> <span data-ttu-id="778a1-163">Para permitir que el formulario controlar los eventos directamente, puede usar multithreading.</span><span class="sxs-lookup"><span data-stu-id="778a1-163">To allow the form to handle the events directly, you can use multithreading.</span></span> <span data-ttu-id="778a1-164">Para obtener más información, consulte [Managed Threading](../../../../standard/threading/index.md).</span><span class="sxs-lookup"><span data-stu-id="778a1-164">For more information, see [Managed Threading](../../../../standard/threading/index.md).</span></span>
  
 <span data-ttu-id="778a1-165">Es posible instructivos para ejecutar el programa con F11 y recorrer el código de una línea a la vez.</span><span class="sxs-lookup"><span data-stu-id="778a1-165">You may find it instructive to run the program with F11 and step through the code a line at a time.</span></span> <span data-ttu-id="778a1-166">Puede ver claramente cómo entra en ejecución `LongTask`y, a continuación, brevemente vuelve a introducir `Form1` cada vez que el `PercentDone` provoca el evento.</span><span class="sxs-lookup"><span data-stu-id="778a1-166">You can clearly see how execution enters `LongTask`, and then briefly re-enters `Form1` each time the `PercentDone` event is raised.</span></span>  
  
 <span data-ttu-id="778a1-167">¿Qué ocurriría si es, mientras que la ejecución fue en el código de `Form1`, el `LongTask` se llama al método nuevo?</span><span class="sxs-lookup"><span data-stu-id="778a1-167">What would happen if, while execution was back in the code of `Form1`, the `LongTask` method were called again?</span></span> <span data-ttu-id="778a1-168">En el peor caso, podría producirse un desbordamiento de pila si `LongTask` llamadas cada vez que se generó el evento.</span><span class="sxs-lookup"><span data-stu-id="778a1-168">At worst, a stack overflow might occur if `LongTask` were called every time the event was raised.</span></span>  
  
 <span data-ttu-id="778a1-169">Puede hacer que la variable `mWidget` para controlar los eventos de otra `Widget` objeto mediante la asignación de una referencia al nuevo `Widget` a `mWidget`.</span><span class="sxs-lookup"><span data-stu-id="778a1-169">You can cause the variable `mWidget` to handle events for a different `Widget` object by assigning a reference to the new `Widget` to `mWidget`.</span></span> <span data-ttu-id="778a1-170">De hecho, puede que el código de `Button1_Click` hacer esto cada vez que haga clic en el botón.</span><span class="sxs-lookup"><span data-stu-id="778a1-170">In fact, you can make the code in `Button1_Click` do this every time you click the button.</span></span>  
  
#### <a name="to-handle-events-for-a-different-widget"></a><span data-ttu-id="778a1-171">Para controlar los eventos para un widget distinto</span><span class="sxs-lookup"><span data-stu-id="778a1-171">To handle events for a different widget</span></span>  
  
- <span data-ttu-id="778a1-172">Agregue la siguiente línea de código para el `Button1_Click` procedimiento, inmediatamente antes de la línea que dice `mWidget.LongTask(12.2, 0.33)`:</span><span class="sxs-lookup"><span data-stu-id="778a1-172">Add the following line of code to the `Button1_Click` procedure, immediately preceding the line that reads `mWidget.LongTask(12.2, 0.33)`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#9](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#9)]  
  
 <span data-ttu-id="778a1-173">El código anterior crea un nuevo `Widget` cada vez que se hace clic en el botón.</span><span class="sxs-lookup"><span data-stu-id="778a1-173">The code above creates a new `Widget` each time the button is clicked.</span></span> <span data-ttu-id="778a1-174">Tan pronto como el `LongTask` método finaliza, la referencia a la `Widget` se publica y el `Widget` se destruye.</span><span class="sxs-lookup"><span data-stu-id="778a1-174">As soon as the `LongTask` method completes, the reference to the `Widget` is released, and the `Widget` is destroyed.</span></span>  
  
 <span data-ttu-id="778a1-175">Un `WithEvents` variable puede contener sólo una referencia de objeto a la vez, por tanto, si asigna otro `Widget` objeto `mWidget`, anterior `Widget` ya no se controlarán los eventos del objeto.</span><span class="sxs-lookup"><span data-stu-id="778a1-175">A `WithEvents` variable can contain only one object reference at a time, so if you assign a different `Widget` object to `mWidget`, the previous `Widget` object's events will no longer be handled.</span></span> <span data-ttu-id="778a1-176">Si `mWidget` es la única variable de objeto que contiene una referencia a la antigua `Widget`, se destruye el objeto.</span><span class="sxs-lookup"><span data-stu-id="778a1-176">If `mWidget` is the only object variable containing a reference to the old `Widget`, the object is destroyed.</span></span> <span data-ttu-id="778a1-177">Si desea controlar eventos desde varios `Widget` objetos, utilice el `AddHandler` instrucción procesar los eventos de cada objeto por separado.</span><span class="sxs-lookup"><span data-stu-id="778a1-177">If you want to handle events from several `Widget` objects, use the `AddHandler` statement to process events from each object separately.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="778a1-178">Puede declarar tantas `WithEvents` variables que usted necesitan, pero las matrices de `WithEvents` no se admiten variables.</span><span class="sxs-lookup"><span data-stu-id="778a1-178">You can declare as many `WithEvents` variables as you need, but arrays of `WithEvents` variables are not supported.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="778a1-179">Vea también</span><span class="sxs-lookup"><span data-stu-id="778a1-179">See also</span></span>

- [<span data-ttu-id="778a1-180">Tutorial: Declarar y provocar eventos</span><span class="sxs-lookup"><span data-stu-id="778a1-180">Walkthrough: Declaring and Raising Events</span></span>](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md)
- [<span data-ttu-id="778a1-181">Eventos</span><span class="sxs-lookup"><span data-stu-id="778a1-181">Events</span></span>](../../../../visual-basic/programming-guide/language-features/events/index.md)
