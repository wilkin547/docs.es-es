---
title: Solucionar problemas de tipos de datos (Visual Basic)
ms.date: 07/20/2015
helpviewer_keywords:
- Char data type [Visual Basic], converting
- Decimal data type [Visual Basic], conversions
- data types [Visual Basic], troubleshooting
- literals [Visual Basic], default types
- type characters [Visual Basic], literal
- Mod operator [Visual Basic], in floating-point operations
- troubleshooting Visual Basic, data types
- troubleshooting data types [Visual Basic]
- floating-point numbers [Visual Basic], precision
- Boolean data type [Visual Basic], converting
- literal types [Visual Basic]
- literal type characters [Visual Basic]
- floating-point numbers [Visual Basic], imprecision
- String data type [Visual Basic], converting
- floating-point numbers [Visual Basic], comparison
- floating-point numbers
ms.assetid: 90040d67-b630-4125-a6ae-37195b079042
ms.openlocfilehash: 9bbc7f51de9899354184d051d8f1a584651dd030
ms.sourcegitcommit: 2eceb05f1a5bb261291a1f6a91c5153727ac1c19
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 09/04/2018
ms.locfileid: "43671796"
---
# <a name="troubleshooting-data-types-visual-basic"></a><span data-ttu-id="bc4c0-102">Solucionar problemas de tipos de datos (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="bc4c0-102">Troubleshooting Data Types (Visual Basic)</span></span>
<span data-ttu-id="bc4c0-103">Esta página enumera algunos problemas comunes que pueden producirse al realizar operaciones en tipos de datos intrínsecos.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-103">This page lists some common problems that can occur when you perform operations on intrinsic data types.</span></span>  
  
## <a name="floating-point-expressions-do-not-compare-as-equal"></a><span data-ttu-id="bc4c0-104">Expresiones de punto flotante no se consideran iguales</span><span class="sxs-lookup"><span data-stu-id="bc4c0-104">Floating-Point Expressions Do Not Compare as Equal</span></span>  
 <span data-ttu-id="bc4c0-105">Cuando se trabaja con números de punto flotante ([único tipo de datos](../../../../visual-basic/language-reference/data-types/single-data-type.md) y [tipo de datos Double](../../../../visual-basic/language-reference/data-types/double-data-type.md)), recuerde que se almacenan como fracciones binarias.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-105">When you work with floating-point numbers ([Single Data Type](../../../../visual-basic/language-reference/data-types/single-data-type.md) and [Double Data Type](../../../../visual-basic/language-reference/data-types/double-data-type.md)), remember that they are stored as binary fractions.</span></span> <span data-ttu-id="bc4c0-106">Esto significa que no pueden contener una representación exacta de cualquier cantidad que no sea una fracción binaria (del formulario k / (2 ^ n) donde k y n son números enteros).</span><span class="sxs-lookup"><span data-stu-id="bc4c0-106">This means they cannot hold an exact representation of any quantity that is not a binary fraction (of the form k / (2 ^ n) where k and n are integers).</span></span> <span data-ttu-id="bc4c0-107">Por ejemplo, 0,5 (= 1/2) y 0,3125 (= 5/16) pueden incluirse como valores precisos, mientras que 0,2 (= 1/5) y 0,3 (= 3/10) pueden ser solo aproximaciones.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-107">For example, 0.5 (= 1/2) and 0.3125 (= 5/16) can be held as precise values, whereas 0.2 (= 1/5) and 0.3 (= 3/10) can be only approximations.</span></span>  
  
 <span data-ttu-id="bc4c0-108">Por este motivo imprecisión, no puede confiar en los resultados exactos cuando se opera en valores de punto flotante.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-108">Because of this imprecision, you cannot rely on exact results when you operate on floating-point values.</span></span> <span data-ttu-id="bc4c0-109">En concreto, dos valores son iguales, en teoría, podrían tener representaciones ligeramente diferentes.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-109">In particular, two values that are theoretically equal might have slightly different representations.</span></span>  
  
| <span data-ttu-id="bc4c0-110">Para comparar las cantidades de punto flotante</span><span class="sxs-lookup"><span data-stu-id="bc4c0-110">To compare floating-point quantities</span></span> | 
|---| 
|<span data-ttu-id="bc4c0-111">1.  Calcular el valor absoluto de su diferencia mediante la <xref:System.Math.Abs%2A> método de la <xref:System.Math> clase en el <xref:System> espacio de nombres.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-111">1.  Calculate the absolute value of their difference by using the <xref:System.Math.Abs%2A> method of the <xref:System.Math> class in the <xref:System> namespace.</span></span><br /><span data-ttu-id="bc4c0-112">2.  Determine una diferencia máxima aceptable, de manera que puede considerar las dos cantidades para que sea igual a efectos prácticos si su diferencia no es más grande.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-112">2.  Determine an acceptable maximum difference, such that you can consider the two quantities to be equal for practical purposes if their difference is no larger.</span></span><br /><span data-ttu-id="bc4c0-113">3.  Compare el valor absoluto de la diferencia con la diferencia aceptable.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-113">3.  Compare the absolute value of the difference to the acceptable difference.</span></span>|  
  
 <span data-ttu-id="bc4c0-114">En el ejemplo siguiente se muestra una comparación correcta e incorrecta de las dos `Double` valores.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-114">The following example demonstrates both incorrect and correct comparison of two `Double` values.</span></span>  
  
 [!code-vb[VbVbalrDataTypes#10](../../../../visual-basic/language-reference/data-types/codesnippet/VisualBasic/troubleshooting-data-types_1.vb)]  
  
 <span data-ttu-id="bc4c0-115">El ejemplo anterior utiliza la <xref:System.Double.ToString%2A> método de la <xref:System.Double> estructura para que puede especificar la mayor precisión que el `CStr` usa la palabra clave.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-115">The previous example uses the <xref:System.Double.ToString%2A> method of the <xref:System.Double> structure so that it can specify better  precision than the `CStr` keyword uses.</span></span> <span data-ttu-id="bc4c0-116">El valor predeterminado es 15 dígitos, pero el formato "G17" lo amplía a 17 dígitos.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-116">The default is 15 digits, but the "G17" format extends it to 17 digits.</span></span>  
  
## <a name="mod-operator-does-not-return-accurate-result"></a><span data-ttu-id="bc4c0-117">Operador Mod no devuelve un resultado preciso</span><span class="sxs-lookup"><span data-stu-id="bc4c0-117">Mod Operator Does Not Return Accurate Result</span></span>  
 <span data-ttu-id="bc4c0-118">Debido a la imprecisión de almacenamiento de punto flotante, el [operador Mod](../../../../visual-basic/language-reference/operators/mod-operator.md) puede devolver un resultado inesperado cuando al menos uno de los operandos es de punto flotante.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-118">Because of the imprecision of floating-point storage, the [Mod Operator](../../../../visual-basic/language-reference/operators/mod-operator.md) can return an unexpected result when at least one of the operands is floating-point.</span></span>  
  
 <span data-ttu-id="bc4c0-119">El [tipo de datos Decimal](../../../../visual-basic/language-reference/data-types/decimal-data-type.md) no usa la representación de punto flotante.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-119">The [Decimal Data Type](../../../../visual-basic/language-reference/data-types/decimal-data-type.md) does not use floating-point representation.</span></span> <span data-ttu-id="bc4c0-120">Muchos números que son la inexactos en `Single` y `Double` son exactos en `Decimal` (por ejemplo, 0.2 y 0.3).</span><span class="sxs-lookup"><span data-stu-id="bc4c0-120">Many numbers that are inexact in `Single` and `Double` are exact in `Decimal` (for example 0.2 and 0.3).</span></span> <span data-ttu-id="bc4c0-121">Aunque la aritmética es más lento en `Decimal` que en punto flotante, es posible que vale la pena la disminución del rendimiento para lograr una mayor precisión.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-121">Although arithmetic is slower in `Decimal` than in floating-point, it might be worth the performance decrease to achieve better precision.</span></span>  
  
|<span data-ttu-id="bc4c0-122">Para buscar el resto entero de cantidades de punto flotante</span><span class="sxs-lookup"><span data-stu-id="bc4c0-122">To find the integer remainder of floating-point quantities</span></span>|  
|---|  
|<span data-ttu-id="bc4c0-123">1.  Declarar variables como `Decimal`.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-123">1.  Declare variables as `Decimal`.</span></span><br /><span data-ttu-id="bc4c0-124">2.  Use el carácter de tipo literal `D` para forzar que literales `Decimal`, en caso de que sus valores son demasiado grandes para el `Long` tipo de datos.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-124">2.  Use the literal type character `D` to force literals to `Decimal`, in case their values are too large for the `Long` data type.</span></span>|  
  
 <span data-ttu-id="bc4c0-125">El ejemplo siguiente muestra el potencial de imprecisión de operandos de punto flotante.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-125">The following example demonstrates the potential imprecision of floating-point operands.</span></span>  
  
 [!code-vb[VbVbalrDataTypes#11](../../../../visual-basic/language-reference/data-types/codesnippet/VisualBasic/troubleshooting-data-types_2.vb)]  
  
 <span data-ttu-id="bc4c0-126">El ejemplo anterior utiliza la <xref:System.Double.ToString%2A> método de la <xref:System.Double> estructura para que puede especificar la mayor precisión que el `CStr` usa la palabra clave.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-126">The previous example uses the <xref:System.Double.ToString%2A> method of the <xref:System.Double> structure so that it can specify better precision than the `CStr` keyword uses.</span></span> <span data-ttu-id="bc4c0-127">El valor predeterminado es 15 dígitos, pero el formato "G17" lo amplía a 17 dígitos.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-127">The default is 15 digits, but the "G17" format extends it to 17 digits.</span></span>  
  
 <span data-ttu-id="bc4c0-128">Dado que `zeroPointTwo` es `Double`, su valor para 0,2 es una fracción binaria con un valor almacenado de 0,20000000000000001.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-128">Because `zeroPointTwo` is `Double`, its value for 0.2 is an infinitely repeating binary fraction with a stored value of 0.20000000000000001.</span></span> <span data-ttu-id="bc4c0-129">Al dividir 2.0 por esta cantidad Obtiene 9,9999999999999995 con un resto de 0,19999999999999991.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-129">Dividing 2.0 by this quantity yields 9.9999999999999995 with a remainder of 0.19999999999999991.</span></span>  
  
 <span data-ttu-id="bc4c0-130">En la expresión para `decimalRemainder`, el carácter de tipo literal `D` obliga a que ambos operandos a `Decimal`, y 0.2 tiene una representación precisa.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-130">In the expression for `decimalRemainder`, the literal type character `D` forces both operands to `Decimal`, and 0.2 has a precise representation.</span></span> <span data-ttu-id="bc4c0-131">Por lo tanto, el `Mod` operador produce el resto esperado de 0,0.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-131">Therefore the `Mod` operator yields the expected remainder of 0.0.</span></span>  
  
 <span data-ttu-id="bc4c0-132">Tenga en cuenta que no es suficiente declarar `decimalRemainder` como `Decimal`.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-132">Note that it is not sufficient to declare `decimalRemainder` as `Decimal`.</span></span> <span data-ttu-id="bc4c0-133">También se deben forzar que los literales `Decimal`, o bien, usar `Double` de forma predeterminada y `decimalRemainder` recibe el mismo valor inexacto como `doubleRemainder`.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-133">You must also force the literals to `Decimal`, or they use `Double` by default and `decimalRemainder` receives the same inaccurate value as `doubleRemainder`.</span></span>  
  
## <a name="boolean-type-does-not-convert-to-numeric-type-accurately"></a><span data-ttu-id="bc4c0-134">Tipo booleano no convierte con precisión al tipo numérico</span><span class="sxs-lookup"><span data-stu-id="bc4c0-134">Boolean Type Does Not Convert to Numeric Type Accurately</span></span>  
 <span data-ttu-id="bc4c0-135">[Tipo de datos Boolean](../../../../visual-basic/language-reference/data-types/boolean-data-type.md) valores no se almacenan como números y los valores almacenados no pretende ser equivalente a números.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-135">[Boolean Data Type](../../../../visual-basic/language-reference/data-types/boolean-data-type.md) values are not stored as numbers, and the stored values are not intended to be equivalent to numbers.</span></span> <span data-ttu-id="bc4c0-136">Por compatibilidad con versiones anteriores, Visual Basic proporciona palabras clave para conversiones ([CType Function](../../../../visual-basic/language-reference/functions/ctype-function.md), `CBool`, `CInt`, y así sucesivamente) para convertir entre `Boolean` y los tipos numéricos.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-136">For compatibility with earlier versions, Visual Basic provides conversion keywords ([CType Function](../../../../visual-basic/language-reference/functions/ctype-function.md), `CBool`, `CInt`, and so on) to convert between `Boolean` and numeric types.</span></span> <span data-ttu-id="bc4c0-137">Sin embargo, otros lenguajes a veces realizan forma diferente, como hacer estas conversiones el [!INCLUDE[dnprdnshort](~/includes/dnprdnshort-md.md)] métodos.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-137">However, other languages sometimes perform these conversions differently, as do the [!INCLUDE[dnprdnshort](~/includes/dnprdnshort-md.md)] methods.</span></span>  
  
 <span data-ttu-id="bc4c0-138">Nunca debería escribir código que se basa en valores numéricos equivalentes para `True` y `False`.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-138">You should never write code that relies on equivalent numeric values for `True` and `False`.</span></span> <span data-ttu-id="bc4c0-139">Siempre que sea posible, debe restringir el uso de `Boolean` variables a los valores lógicos para que se han diseñado.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-139">Whenever possible, you should restrict usage of `Boolean` variables to the logical values for which they are designed.</span></span> <span data-ttu-id="bc4c0-140">Si debe mezclar `Boolean` y valores numéricos, asegúrese de que comprende el método de conversión que seleccione.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-140">If you must mix `Boolean` and numeric values, make sure that you understand the conversion method that you select.</span></span>  
  
### <a name="conversion-in-visual-basic"></a><span data-ttu-id="bc4c0-141">Conversión en Visual Basic</span><span class="sxs-lookup"><span data-stu-id="bc4c0-141">Conversion in Visual Basic</span></span>  
 <span data-ttu-id="bc4c0-142">Cuando se usa el `CType` o `CBool` palabras clave de conversión para convertir tipos de datos numéricos a `Boolean`, se convierte en 0 `False` y todos los demás valores se convierten en `True`.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-142">When you use the `CType` or `CBool` conversion keywords to convert numeric data types to `Boolean`, 0 becomes `False` and all other values become `True`.</span></span> <span data-ttu-id="bc4c0-143">Al convertir `Boolean` valores a tipos numéricos mediante las palabras clave de conversión, `False` se convierte en 0 y `True` se convierte en -1.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-143">When you convert `Boolean` values to numeric types by using the conversion keywords, `False` becomes 0 and `True` becomes -1.</span></span>  
  
### <a name="conversion-in-the-framework"></a><span data-ttu-id="bc4c0-144">Conversión en el marco de trabajo</span><span class="sxs-lookup"><span data-stu-id="bc4c0-144">Conversion in the Framework</span></span>  
 <span data-ttu-id="bc4c0-145">El <xref:System.Convert.ToInt32%2A> método de la <xref:System.Convert> clase en el <xref:System> convierte el espacio de nombres `True` a + 1.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-145">The <xref:System.Convert.ToInt32%2A> method of the <xref:System.Convert> class in the <xref:System> namespace converts `True` to +1.</span></span>  
  
 <span data-ttu-id="bc4c0-146">Si necesita convertir una `Boolean` valor a un tipo de datos numéricos, tenga cuidado sobre qué método de conversión que usar.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-146">If you must convert a `Boolean` value to a numeric data type, be careful about which conversion method you use.</span></span>  
  
## <a name="character-literal-generates-compiler-error"></a><span data-ttu-id="bc4c0-147">Error del compilador genera el Literal de carácter</span><span class="sxs-lookup"><span data-stu-id="bc4c0-147">Character Literal Generates Compiler Error</span></span>  
 <span data-ttu-id="bc4c0-148">En ausencia de cualquier carácter de tipo, Visual Basic se supone de forma predeterminada los tipos de datos para los literales.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-148">In the absence of any type characters, Visual Basic assumes default data types for literals.</span></span> <span data-ttu-id="bc4c0-149">El tipo predeterminado para un literal de carácter, entre comillas (`" "`), es `String`.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-149">The default type for a character literal — enclosed in quotation marks (`" "`) — is `String`.</span></span>  
  
 <span data-ttu-id="bc4c0-150">El `String` tipo de datos no se convierten en el [tipo de datos Char](../../../../visual-basic/language-reference/data-types/char-data-type.md).</span><span class="sxs-lookup"><span data-stu-id="bc4c0-150">The `String` data type does not widen to the [Char Data Type](../../../../visual-basic/language-reference/data-types/char-data-type.md).</span></span> <span data-ttu-id="bc4c0-151">Esto significa que si desea asignar un literal para una `Char` variable, debe realizar una conversión de restricción o forzar el literal para el `Char` tipo.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-151">This means that if you want to assign a literal to a `Char` variable, you must either make a narrowing conversion or force the literal to the `Char` type.</span></span>  

|<span data-ttu-id="bc4c0-152">Para crear a un carácter literal para asignar a una variable o constante</span><span class="sxs-lookup"><span data-stu-id="bc4c0-152">To create a Char literal to assign to a variable or constant</span></span>|
|---|  
|<span data-ttu-id="bc4c0-153">1.  Declarar la variable o constante como `Char`.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-153">1.  Declare the variable or constant as `Char`.</span></span><br /><span data-ttu-id="bc4c0-154">2.  Escriba el valor de carácter entre comillas (`" "`).</span><span class="sxs-lookup"><span data-stu-id="bc4c0-154">2.  Enclose the character value in quotation marks (`" "`).</span></span><br /><span data-ttu-id="bc4c0-155">3.  Siga las comillas dobles de cierre con el carácter de tipo literal `C` para forzar el literal `Char`.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-155">3.  Follow the closing double quotation mark with the literal type character `C` to force the literal to `Char`.</span></span> <span data-ttu-id="bc4c0-156">Esto es necesario si el modificador de comprobación de tipo ([Option Strict Statement](../../../../visual-basic/language-reference/statements/option-strict-statement.md)) es `On`, y es deseable en cualquier caso.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-156">This is necessary if the type checking switch ([Option Strict Statement](../../../../visual-basic/language-reference/statements/option-strict-statement.md)) is `On`, and it is desirable in any case.</span></span>|  
  
 <span data-ttu-id="bc4c0-157">El ejemplo siguiente muestra las asignaciones incorrectas y correcta de un literal a una `Char` variable.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-157">The following example demonstrates both unsuccessful and successful assignments of a literal to a `Char` variable.</span></span>  
  
 [!code-vb[VbVbalrDataTypes#12](../../../../visual-basic/language-reference/data-types/codesnippet/VisualBasic/troubleshooting-data-types_3.vb)]  
  
 <span data-ttu-id="bc4c0-158">Siempre hay un riesgo para la sesión con las conversiones de restricción, porque puede producir un error en tiempo de ejecución.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-158">There is always a risk in using narrowing conversions, because they can fail at run time.</span></span> <span data-ttu-id="bc4c0-159">Por ejemplo, una conversión de `String` a `Char` puede producir un error si el `String` valor contiene más de un carácter.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-159">For example, a conversion from `String` to `Char` can fail if the `String` value contains more than one character.</span></span> <span data-ttu-id="bc4c0-160">Por lo tanto, es mejor programación para utilizar el `C` carácter de tipo.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-160">Therefore, it is better programming to use the `C` type character.</span></span>  
  
## <a name="string-conversion-fails-at-run-time"></a><span data-ttu-id="bc4c0-161">Se produce un error de conversión de cadenas en tiempo de ejecución</span><span class="sxs-lookup"><span data-stu-id="bc4c0-161">String Conversion Fails at Run Time</span></span>  
 <span data-ttu-id="bc4c0-162">El [tipo de datos String](../../../../visual-basic/language-reference/data-types/string-data-type.md) participa en muy pocas las conversiones de ampliación.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-162">The [String Data Type](../../../../visual-basic/language-reference/data-types/string-data-type.md) participates in very few widening conversions.</span></span> <span data-ttu-id="bc4c0-163">`String` solo se amplía a sí mismo y `Object`y solo `Char` y `Char()` (un `Char` matriz) se convierten en `String`.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-163">`String` widens only to itself and `Object`, and only `Char` and `Char()` (a `Char` array) widen to `String`.</span></span> <span data-ttu-id="bc4c0-164">Esto es porque `String` variables y constantes pueden contener valores que no pueden contener otros tipos de datos.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-164">This is because `String` variables and constants can contain values that other data types cannot contain.</span></span>  
  
 <span data-ttu-id="bc4c0-165">Cuando la comprobación de tipo modificador ([Option Strict Statement](../../../../visual-basic/language-reference/statements/option-strict-statement.md)) es `On`, el compilador no permite todas las conversiones de restricción implícitas.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-165">When the type checking switch ([Option Strict Statement](../../../../visual-basic/language-reference/statements/option-strict-statement.md)) is `On`, the compiler disallows all implicit narrowing conversions.</span></span> <span data-ttu-id="bc4c0-166">Esto incluye aquellas que implican `String`.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-166">This includes those involving `String`.</span></span> <span data-ttu-id="bc4c0-167">El código todavía puede usar palabras clave de conversión, como `CStr` y [CType Function](../../../../visual-basic/language-reference/functions/ctype-function.md), qué direct el [!INCLUDE[dnprdnshort](~/includes/dnprdnshort-md.md)] para intentar la conversión.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-167">Your code can still use conversion keywords such as `CStr` and [CType Function](../../../../visual-basic/language-reference/functions/ctype-function.md), which direct the [!INCLUDE[dnprdnshort](~/includes/dnprdnshort-md.md)] to attempt the conversion.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="bc4c0-168">Se suprime el error de conversión de restricción en las conversiones de los elementos de un `For Each…Next` colección a la variable de control de bucle.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-168">The narrowing-conversion error is suppressed for conversions from the elements in a `For Each…Next` collection to the loop control variable.</span></span> <span data-ttu-id="bc4c0-169">Para obtener más información y ejemplos, vea la sección "Narrowing Conversions" en [For Each... Instrucción Next](../../../../visual-basic/language-reference/statements/for-each-next-statement.md).</span><span class="sxs-lookup"><span data-stu-id="bc4c0-169">For more information and examples, see the "Narrowing Conversions" section in [For Each...Next Statement](../../../../visual-basic/language-reference/statements/for-each-next-statement.md).</span></span>  
  
### <a name="narrowing-conversion-protection"></a><span data-ttu-id="bc4c0-170">Protección de la conversión de restricción</span><span class="sxs-lookup"><span data-stu-id="bc4c0-170">Narrowing Conversion Protection</span></span>  
 <span data-ttu-id="bc4c0-171">El inconveniente de las conversiones de restricción es que pueden producirse errores en tiempo de ejecución.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-171">The disadvantage of narrowing conversions is that they can fail at run time.</span></span> <span data-ttu-id="bc4c0-172">Por ejemplo, si un `String` variable contiene algo distinto de "True" o "False", no se puede convertir a `Boolean`.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-172">For example, if a `String` variable contains anything other than "True" or "False," it cannot be converted to `Boolean`.</span></span> <span data-ttu-id="bc4c0-173">Si contiene caracteres de puntuación, se produce un error de conversión para cualquier tipo numérico.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-173">If it contains punctuation characters, conversion to any numeric type fails.</span></span> <span data-ttu-id="bc4c0-174">A menos que sepa que su `String` variable siempre contiene valores que puede aceptar el tipo de destino, no debe intentar una conversión.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-174">Unless you know that your `String` variable always holds values that the destination type can accept, you should not try a conversion.</span></span>  
  
 <span data-ttu-id="bc4c0-175">Si necesita convertir desde `String` a otro tipo de datos, el procedimiento más seguro consiste en incluir la conversión intentada en el [intente... Catch... Finally (instrucción)](../../../../visual-basic/language-reference/statements/try-catch-finally-statement.md).</span><span class="sxs-lookup"><span data-stu-id="bc4c0-175">If you must convert from `String` to another data type, the safest procedure is to enclose the attempted conversion in the [Try...Catch...Finally Statement](../../../../visual-basic/language-reference/statements/try-catch-finally-statement.md).</span></span> <span data-ttu-id="bc4c0-176">Esto le permite tratar con un error en tiempo de ejecución.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-176">This lets you deal with a run-time failure.</span></span>  
  
### <a name="character-arrays"></a><span data-ttu-id="bc4c0-177">Matrices de caracteres</span><span class="sxs-lookup"><span data-stu-id="bc4c0-177">Character Arrays</span></span>  
 <span data-ttu-id="bc4c0-178">Una sola `Char` y una matriz de `Char` elementos ambos se convierten en `String`.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-178">A single `Char` and an array of `Char` elements both widen to `String`.</span></span> <span data-ttu-id="bc4c0-179">Sin embargo, `String` no se convierten en `Char()`.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-179">However, `String` does not widen to `Char()`.</span></span> <span data-ttu-id="bc4c0-180">Para convertir un `String` valor a un `Char` matriz, puede usar el <xref:System.String.ToCharArray%2A> método de la <xref:System.String?displayProperty=nameWithType> clase.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-180">To convert a `String` value to a `Char` array, you can use the <xref:System.String.ToCharArray%2A> method of the <xref:System.String?displayProperty=nameWithType> class.</span></span>  
  
### <a name="meaningless-values"></a><span data-ttu-id="bc4c0-181">No tiene sentidos valores</span><span class="sxs-lookup"><span data-stu-id="bc4c0-181">Meaningless Values</span></span>  
 <span data-ttu-id="bc4c0-182">En general, `String` valores no son significativos en otros tipos de datos y la conversión es muy artificial y peligrosa.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-182">In general, `String` values are not meaningful in other data types, and conversion is highly artificial and dangerous.</span></span> <span data-ttu-id="bc4c0-183">Siempre que sea posible, debe restringir el uso de `String` variables a las secuencias de caracteres para el que se ha diseñado.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-183">Whenever possible, you should restrict usage of `String` variables to the character sequences for which they are designed.</span></span> <span data-ttu-id="bc4c0-184">Nunca debe escribirse código que se basa en valores equivalentes en otros tipos.</span><span class="sxs-lookup"><span data-stu-id="bc4c0-184">You should never write code that relies on equivalent values in other types.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="bc4c0-185">Vea también</span><span class="sxs-lookup"><span data-stu-id="bc4c0-185">See Also</span></span>  
 [<span data-ttu-id="bc4c0-186">Tipos de datos</span><span class="sxs-lookup"><span data-stu-id="bc4c0-186">Data Types</span></span>](../../../../visual-basic/programming-guide/language-features/data-types/index.md)  
 [<span data-ttu-id="bc4c0-187">Caracteres de tipo</span><span class="sxs-lookup"><span data-stu-id="bc4c0-187">Type Characters</span></span>](../../../../visual-basic/programming-guide/language-features/data-types/type-characters.md)  
 [<span data-ttu-id="bc4c0-188">Tipos de valores y tipos de referencias</span><span class="sxs-lookup"><span data-stu-id="bc4c0-188">Value Types and Reference Types</span></span>](../../../../visual-basic/programming-guide/language-features/data-types/value-types-and-reference-types.md)  
 [<span data-ttu-id="bc4c0-189">Conversiones de tipos en Visual Basic</span><span class="sxs-lookup"><span data-stu-id="bc4c0-189">Type Conversions in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/data-types/type-conversions.md)  
 [<span data-ttu-id="bc4c0-190">Tipos de datos</span><span class="sxs-lookup"><span data-stu-id="bc4c0-190">Data Types</span></span>](../../../../visual-basic/language-reference/data-types/index.md)  
 [<span data-ttu-id="bc4c0-191">Funciones de conversión de tipos</span><span class="sxs-lookup"><span data-stu-id="bc4c0-191">Type Conversion Functions</span></span>](../../../../visual-basic/language-reference/functions/type-conversion-functions.md)  
 [<span data-ttu-id="bc4c0-192">Uso eficiente de tipos de datos</span><span class="sxs-lookup"><span data-stu-id="bc4c0-192">Efficient Use of Data Types</span></span>](../../../../visual-basic/programming-guide/language-features/data-types/efficient-use-of-data-types.md)
