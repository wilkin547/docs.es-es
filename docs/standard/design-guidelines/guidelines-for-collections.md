---
title: Instrucciones para colecciones
ms.date: 10/22/2008
ms.technology: dotnet-standard
ms.assetid: 297b8f1d-b11f-4dc6-960a-8e990817304e
author: KrzysztofCwalina
ms.openlocfilehash: a8e8672d71500478dbbe28512e413e8ada501f45
ms.sourcegitcommit: 9b552addadfb57fab0b9e7852ed4f1f1b8a42f8e
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 04/23/2019
ms.locfileid: "61669056"
---
# <a name="guidelines-for-collections"></a><span data-ttu-id="00ca4-102">Instrucciones para colecciones</span><span class="sxs-lookup"><span data-stu-id="00ca4-102">Guidelines for Collections</span></span>
<span data-ttu-id="00ca4-103">Cualquier tipo que se ha diseñado específicamente para manipular un grupo de objetos que tienen alguna característica común puede considerarse una colección.</span><span class="sxs-lookup"><span data-stu-id="00ca4-103">Any type designed specifically to manipulate a group of objects having some common characteristic can be considered a collection.</span></span> <span data-ttu-id="00ca4-104">Casi siempre es adecuado para estos tipos implementar <xref:System.Collections.IEnumerable> o <xref:System.Collections.Generic.IEnumerable%601>, por lo que en esta sección se considere sólo tipos que implementan una o ambas de estas interfaces para ser colecciones.</span><span class="sxs-lookup"><span data-stu-id="00ca4-104">It is almost always appropriate for such types to implement <xref:System.Collections.IEnumerable> or <xref:System.Collections.Generic.IEnumerable%601>, so in this section we only consider types implementing one or both of those interfaces to be collections.</span></span>  
  
 <span data-ttu-id="00ca4-105">**X DO NOT** usar colecciones débilmente tipadas en las API públicas.</span><span class="sxs-lookup"><span data-stu-id="00ca4-105">**X DO NOT** use weakly typed collections in public APIs.</span></span>  
  
 <span data-ttu-id="00ca4-106">El tipo de todos los valores devueltos y parámetros que representan elementos de la colección debe ser el tipo de elemento exactos, no en cualquiera de sus tipos base (Esto se aplica solo a los miembros públicos de la colección).</span><span class="sxs-lookup"><span data-stu-id="00ca4-106">The type of all return values and parameters representing collection items should be the exact item type, not any of its base types (this applies only to public members of the collection).</span></span>  
  
 <span data-ttu-id="00ca4-107">**X DO NOT** usar <xref:System.Collections.ArrayList> o <xref:System.Collections.Generic.List%601> en las API públicas.</span><span class="sxs-lookup"><span data-stu-id="00ca4-107">**X DO NOT** use <xref:System.Collections.ArrayList> or <xref:System.Collections.Generic.List%601> in public APIs.</span></span>  
  
 <span data-ttu-id="00ca4-108">Estos tipos son estructuras de datos diseñadas para usarse en la implementación interna, no en las API públicas.</span><span class="sxs-lookup"><span data-stu-id="00ca4-108">These types are data structures designed to be used in internal implementation, not in public APIs.</span></span> <span data-ttu-id="00ca4-109">`List<T>` está optimizado para rendimiento y potencia a costa de cleanness de las API y flexibilidad.</span><span class="sxs-lookup"><span data-stu-id="00ca4-109">`List<T>` is optimized for performance and power at the cost of cleanness of the APIs and flexibility.</span></span> <span data-ttu-id="00ca4-110">Por ejemplo, si devuelves `List<T>`, no nunca podrá recibir notificaciones cuando el código de cliente modifica la colección.</span><span class="sxs-lookup"><span data-stu-id="00ca4-110">For example, if you return `List<T>`, you will not ever be able to receive notifications when client code modifies the collection.</span></span> <span data-ttu-id="00ca4-111">Además, `List<T>` expone muchos miembros, como <xref:System.Collections.Generic.List%601.BinarySearch%2A>, que no son útiles o aplicable en muchos escenarios.</span><span class="sxs-lookup"><span data-stu-id="00ca4-111">Also, `List<T>` exposes many members, such as <xref:System.Collections.Generic.List%601.BinarySearch%2A>, that are not useful or applicable in many scenarios.</span></span> <span data-ttu-id="00ca4-112">Las dos secciones siguientes describen tipos (abstracciones) diseñados específicamente para su uso en las API públicas.</span><span class="sxs-lookup"><span data-stu-id="00ca4-112">The following two sections describe types (abstractions) designed specifically for use in public APIs.</span></span>  
  
 <span data-ttu-id="00ca4-113">**X DO NOT** usar `Hashtable` o `Dictionary<TKey,TValue>` en las API públicas.</span><span class="sxs-lookup"><span data-stu-id="00ca4-113">**X DO NOT** use `Hashtable` or `Dictionary<TKey,TValue>` in public APIs.</span></span>  
  
 <span data-ttu-id="00ca4-114">Estos tipos son estructuras de datos diseñadas para usarse en la implementación interna.</span><span class="sxs-lookup"><span data-stu-id="00ca4-114">These types are data structures designed to be used in internal implementation.</span></span> <span data-ttu-id="00ca4-115">Deben usar las API públicas <xref:System.Collections.IDictionary>, `IDictionary <TKey, TValue>`, o un tipo personalizado que se implementa una o ambas de las interfaces.</span><span class="sxs-lookup"><span data-stu-id="00ca4-115">Public APIs should use <xref:System.Collections.IDictionary>, `IDictionary <TKey, TValue>`, or a custom type implementing one or both of the interfaces.</span></span>  
  
 <span data-ttu-id="00ca4-116">**X DO NOT** usar <xref:System.Collections.Generic.IEnumerator%601>, <xref:System.Collections.IEnumerator>, o cualquier otro tipo que implementa alguna de estas interfaces, excepto como el tipo de valor devuelto de un `GetEnumerator` método.</span><span class="sxs-lookup"><span data-stu-id="00ca4-116">**X DO NOT** use <xref:System.Collections.Generic.IEnumerator%601>, <xref:System.Collections.IEnumerator>, or any other type that implements either of these interfaces, except as the return type of a `GetEnumerator` method.</span></span>  
  
 <span data-ttu-id="00ca4-117">Devolver los enumeradores de métodos distintos de los tipos `GetEnumerator` no se puede usar con el `foreach` instrucción.</span><span class="sxs-lookup"><span data-stu-id="00ca4-117">Types returning enumerators from methods other than `GetEnumerator` cannot be used with the `foreach` statement.</span></span>  
  
 <span data-ttu-id="00ca4-118">**X DO NOT** implementar ambas `IEnumerator<T>` y `IEnumerable<T>` en el mismo tipo.</span><span class="sxs-lookup"><span data-stu-id="00ca4-118">**X DO NOT** implement both `IEnumerator<T>` and `IEnumerable<T>` on the same type.</span></span> <span data-ttu-id="00ca4-119">Lo mismo se aplica a las interfaces no genéricas `IEnumerator` y `IEnumerable`.</span><span class="sxs-lookup"><span data-stu-id="00ca4-119">The same applies to the nongeneric interfaces `IEnumerator` and `IEnumerable`.</span></span>  
  
## <a name="collection-parameters"></a><span data-ttu-id="00ca4-120">Colección de parámetros</span><span class="sxs-lookup"><span data-stu-id="00ca4-120">Collection Parameters</span></span>  
 <span data-ttu-id="00ca4-121">**✓ DO** usar los posibles tipo especializado mínimos como un tipo de parámetro.</span><span class="sxs-lookup"><span data-stu-id="00ca4-121">**✓ DO** use the least-specialized type possible as a parameter type.</span></span> <span data-ttu-id="00ca4-122">Mayor número de miembros teniendo colecciones como parámetros utilizan la `IEnumerable<T>` interfaz.</span><span class="sxs-lookup"><span data-stu-id="00ca4-122">Most members taking collections as parameters use the `IEnumerable<T>` interface.</span></span>  
  
 <span data-ttu-id="00ca4-123">**X AVOID** con <xref:System.Collections.Generic.ICollection%601> o <xref:System.Collections.ICollection> como un parámetro para tener acceso a la `Count` propiedad.</span><span class="sxs-lookup"><span data-stu-id="00ca4-123">**X AVOID** using <xref:System.Collections.Generic.ICollection%601> or <xref:System.Collections.ICollection> as a parameter just to access the `Count` property.</span></span>  
  
 <span data-ttu-id="00ca4-124">En su lugar, considere el uso de `IEnumerable<T>` o `IEnumerable` y la comprobación de forma dinámica si el objeto implementa `ICollection<T>` o `ICollection`.</span><span class="sxs-lookup"><span data-stu-id="00ca4-124">Instead, consider using `IEnumerable<T>` or `IEnumerable` and dynamically checking whether the object implements `ICollection<T>` or `ICollection`.</span></span>  
  
## <a name="collection-properties-and-return-values"></a><span data-ttu-id="00ca4-125">Propiedades de la colección y valores devueltos</span><span class="sxs-lookup"><span data-stu-id="00ca4-125">Collection Properties and Return Values</span></span>  
 <span data-ttu-id="00ca4-126">**X DO NOT** proporcionan propiedades configurables de recopilación.</span><span class="sxs-lookup"><span data-stu-id="00ca4-126">**X DO NOT** provide settable collection properties.</span></span>  
  
 <span data-ttu-id="00ca4-127">Los usuarios pueden reemplazar el contenido de la colección por borrar la colección en primer lugar y, a continuación, agregar el nuevo contenido.</span><span class="sxs-lookup"><span data-stu-id="00ca4-127">Users can replace the contents of the collection by clearing the collection first and then adding the new contents.</span></span> <span data-ttu-id="00ca4-128">Si reemplaza toda la colección es un escenario común, considere la posibilidad de proporcionar el `AddRange` método en la colección.</span><span class="sxs-lookup"><span data-stu-id="00ca4-128">If replacing the whole collection is a common scenario, consider providing the `AddRange` method on the collection.</span></span>  
  
 <span data-ttu-id="00ca4-129">**✓ DO** usar `Collection<T>` o una subclase de `Collection<T>` para propiedades o devuelven valores que representan las colecciones de lectura/escritura.</span><span class="sxs-lookup"><span data-stu-id="00ca4-129">**✓ DO** use `Collection<T>` or a subclass of `Collection<T>` for properties or return values representing read/write collections.</span></span>  
  
 <span data-ttu-id="00ca4-130">Si `Collection<T>` no cumple el requisito de algunos (por ejemplo, la colección no debe implementar <xref:System.Collections.IList>), utilice una colección personalizada implementando `IEnumerable<T>`, `ICollection<T>`, o <xref:System.Collections.Generic.IList%601>.</span><span class="sxs-lookup"><span data-stu-id="00ca4-130">If `Collection<T>` does not meet some requirement (e.g., the collection must not implement <xref:System.Collections.IList>), use a custom collection by implementing `IEnumerable<T>`, `ICollection<T>`, or <xref:System.Collections.Generic.IList%601>.</span></span>  
  
 <span data-ttu-id="00ca4-131">**✓ DO** usar <xref:System.Collections.ObjectModel.ReadOnlyCollection%601>, una subclase de `ReadOnlyCollection<T>`, o en casos poco frecuentes `IEnumerable<T>` para propiedades o devuelven valores que representan colecciones de solo lectura.</span><span class="sxs-lookup"><span data-stu-id="00ca4-131">**✓ DO** use <xref:System.Collections.ObjectModel.ReadOnlyCollection%601>, a subclass of `ReadOnlyCollection<T>`, or in rare cases `IEnumerable<T>` for properties or return values representing read-only collections.</span></span>  
  
 <span data-ttu-id="00ca4-132">En general, se prefiere `ReadOnlyCollection<T>`.</span><span class="sxs-lookup"><span data-stu-id="00ca4-132">In general, prefer `ReadOnlyCollection<T>`.</span></span> <span data-ttu-id="00ca4-133">Si no tiene algún requisito (por ejemplo, la colección no debe implementar `IList`), utilice una colección personalizada implementando `IEnumerable<T>`, `ICollection<T>`, o `IList<T>`.</span><span class="sxs-lookup"><span data-stu-id="00ca4-133">If it does not meet some requirement (e.g., the collection must not implement `IList`), use a custom collection by implementing `IEnumerable<T>`, `ICollection<T>`, or `IList<T>`.</span></span> <span data-ttu-id="00ca4-134">Si implementa una colección de solo lectura personalizada, implementar `ICollection<T>.IsReadOnly` para devolver `true`.</span><span class="sxs-lookup"><span data-stu-id="00ca4-134">If you do implement a custom read-only collection, implement `ICollection<T>.IsReadOnly` to return `true`.</span></span>  
  
 <span data-ttu-id="00ca4-135">En caso de que esté seguro de que el único escenario en que alguna vez desea admitir iteración de solo avance, puede usar simplemente `IEnumerable<T>`.</span><span class="sxs-lookup"><span data-stu-id="00ca4-135">In cases where you are sure that the only scenario you will ever want to support is forward-only iteration, you can simply use `IEnumerable<T>`.</span></span>  
  
 <span data-ttu-id="00ca4-136">**✓ CONSIDER** mediante subclases de las colecciones genéricas de base en lugar de usar directamente las colecciones.</span><span class="sxs-lookup"><span data-stu-id="00ca4-136">**✓ CONSIDER** using subclasses of generic base collections instead of using the collections directly.</span></span>  
  
 <span data-ttu-id="00ca4-137">Esto permite para un nombre más adecuado y agregar miembros auxiliares que no están presentes en los tipos de colección base.</span><span class="sxs-lookup"><span data-stu-id="00ca4-137">This allows for a better name and for adding helper members that are not present on the base collection types.</span></span> <span data-ttu-id="00ca4-138">Esto es especialmente aplicable a las API de alto nivel.</span><span class="sxs-lookup"><span data-stu-id="00ca4-138">This is especially applicable to high-level APIs.</span></span>  
  
 <span data-ttu-id="00ca4-139">**✓ CONSIDER** devolver una subclase de `Collection<T>` o `ReadOnlyCollection<T>` de propiedades y métodos utilizados con mucha frecuencia.</span><span class="sxs-lookup"><span data-stu-id="00ca4-139">**✓ CONSIDER** returning a subclass of `Collection<T>` or `ReadOnlyCollection<T>` from very commonly used methods and properties.</span></span>  
  
 <span data-ttu-id="00ca4-140">Esto hará posible que le agregue métodos auxiliares o cambiar la implementación de la colección en el futuro.</span><span class="sxs-lookup"><span data-stu-id="00ca4-140">This will make it possible for you to add helper methods or change the collection implementation in the future.</span></span>  
  
 <span data-ttu-id="00ca4-141">**✓ CONSIDER** con una colección con clave si los elementos almacenados en la colección tienen claves únicas (nombres, Id., etcetera).</span><span class="sxs-lookup"><span data-stu-id="00ca4-141">**✓ CONSIDER** using a keyed collection if the items stored in the collection have unique keys (names, IDs, etc.).</span></span> <span data-ttu-id="00ca4-142">Colecciones con claves son colecciones que se pueden indizar por entero y una clave y se implementan normalmente mediante la herencia de `KeyedCollection<TKey,TItem>`.</span><span class="sxs-lookup"><span data-stu-id="00ca4-142">Keyed collections are collections that can be indexed by both an integer and a key and are usually implemented by inheriting from `KeyedCollection<TKey,TItem>`.</span></span>  
  
 <span data-ttu-id="00ca4-143">Colecciones con claves normalmente tienen una mayor superficie de memoria y no deben usarse si la sobrecarga de memoria supera las ventajas de tener las claves.</span><span class="sxs-lookup"><span data-stu-id="00ca4-143">Keyed collections usually have larger memory footprints and should not be used if the memory overhead outweighs the benefits of having the keys.</span></span>  
  
 <span data-ttu-id="00ca4-144">**X DO NOT** devuelven valores null de propiedades de la colección o de métodos que devuelven colecciones.</span><span class="sxs-lookup"><span data-stu-id="00ca4-144">**X DO NOT** return null values from collection properties or from methods returning collections.</span></span> <span data-ttu-id="00ca4-145">Devuelve una colección vacía o una matriz vacía en su lugar.</span><span class="sxs-lookup"><span data-stu-id="00ca4-145">Return an empty collection or an empty array instead.</span></span>  
  
 <span data-ttu-id="00ca4-146">La regla general es que deben ser null y vacías colecciones (elemento 0) o matrices tratan de la misma.</span><span class="sxs-lookup"><span data-stu-id="00ca4-146">The general rule is that null and empty (0 item) collections or arrays should be treated the same.</span></span>  
  
### <a name="snapshots-versus-live-collections"></a><span data-ttu-id="00ca4-147">Instantáneas frente a las colecciones en vivo</span><span class="sxs-lookup"><span data-stu-id="00ca4-147">Snapshots Versus Live Collections</span></span>  
 <span data-ttu-id="00ca4-148">Que representa un estado en algún momento en el tiempo de colecciones se denominan colecciones de instantánea.</span><span class="sxs-lookup"><span data-stu-id="00ca4-148">Collections representing a state at some point in time are called snapshot collections.</span></span> <span data-ttu-id="00ca4-149">Por ejemplo, una colección que contiene las filas devueltas de una consulta de base de datos sería una instantánea.</span><span class="sxs-lookup"><span data-stu-id="00ca4-149">For example, a collection containing rows returned from a database query would be a snapshot.</span></span> <span data-ttu-id="00ca4-150">Las colecciones que siempre representan el estado actual se denominan colecciones en vivo.</span><span class="sxs-lookup"><span data-stu-id="00ca4-150">Collections that always represent the current state are called live collections.</span></span> <span data-ttu-id="00ca4-151">Por ejemplo, una colección de `ComboBox` elementos es una colección en vivo.</span><span class="sxs-lookup"><span data-stu-id="00ca4-151">For example, a collection of `ComboBox` items is a live collection.</span></span>  
  
 <span data-ttu-id="00ca4-152">**X DO NOT** devuelven colecciones de instantánea de propiedades.</span><span class="sxs-lookup"><span data-stu-id="00ca4-152">**X DO NOT** return snapshot collections from properties.</span></span> <span data-ttu-id="00ca4-153">Las propiedades deben devolver colecciones en vivo.</span><span class="sxs-lookup"><span data-stu-id="00ca4-153">Properties should return live collections.</span></span>  
  
 <span data-ttu-id="00ca4-154">Captadores de propiedades deberían ser operaciones muy ligeras.</span><span class="sxs-lookup"><span data-stu-id="00ca4-154">Property getters should be very lightweight operations.</span></span> <span data-ttu-id="00ca4-155">Devuelve una instantánea requiere la creación de una copia de una colección interna en una operación o (n).</span><span class="sxs-lookup"><span data-stu-id="00ca4-155">Returning a snapshot requires creating a copy of an internal collection in an O(n) operation.</span></span>  
  
 <span data-ttu-id="00ca4-156">**✓ DO** utilizar una colección de instantáneas o activo `IEnumerable<T>` (o su subtipo) para representar las colecciones son volátiles (es decir, que puede cambiar sin modificar explícitamente la colección).</span><span class="sxs-lookup"><span data-stu-id="00ca4-156">**✓ DO** use either a snapshot collection or a live `IEnumerable<T>` (or its subtype) to represent collections that are volatile (i.e., that can change without explicitly modifying the collection).</span></span>  
  
 <span data-ttu-id="00ca4-157">En general, todas las colecciones que representa un recurso compartido (por ejemplo, los archivos en un directorio) son volátiles.</span><span class="sxs-lookup"><span data-stu-id="00ca4-157">In general, all collections representing a shared resource (e.g., files in a directory) are volatile.</span></span> <span data-ttu-id="00ca4-158">Estas colecciones son muy difícil o imposible implementar como colecciones en vivo a menos que la implementación es simplemente un enumerador de solo avance.</span><span class="sxs-lookup"><span data-stu-id="00ca4-158">Such collections are very difficult or impossible to implement as live collections unless the implementation is simply a forward-only enumerator.</span></span>  
  
## <a name="choosing-between-arrays-and-collections"></a><span data-ttu-id="00ca4-159">Elegir entre las matrices y colecciones</span><span class="sxs-lookup"><span data-stu-id="00ca4-159">Choosing Between Arrays and Collections</span></span>  
 <span data-ttu-id="00ca4-160">**✓ DO** preferir las colecciones de matrices.</span><span class="sxs-lookup"><span data-stu-id="00ca4-160">**✓ DO** prefer collections over arrays.</span></span>  
  
 <span data-ttu-id="00ca4-161">Las colecciones proporcionan más control sobre el contenido, pueden evolucionar con el tiempo y son más fáciles de utilizar.</span><span class="sxs-lookup"><span data-stu-id="00ca4-161">Collections provide more control over contents, can evolve over time, and are more usable.</span></span> <span data-ttu-id="00ca4-162">Además, usar matrices para escenarios de solo lectura no se recomienda porque el costo de clonación de la matriz es prohibitivo.</span><span class="sxs-lookup"><span data-stu-id="00ca4-162">In addition, using arrays for read-only scenarios is discouraged because the cost of cloning the array is prohibitive.</span></span> <span data-ttu-id="00ca4-163">Los estudios de facilidad de uso han demostrado que algunos desarrolladores sientan más cómodos con las API basadas en la colección.</span><span class="sxs-lookup"><span data-stu-id="00ca4-163">Usability studies have shown that some developers feel more comfortable using collection-based APIs.</span></span>  
  
 <span data-ttu-id="00ca4-164">Sin embargo, si está desarrollando la API de bajo nivel, podría ser mejor usar matrices para escenarios de lectura y escritura.</span><span class="sxs-lookup"><span data-stu-id="00ca4-164">However, if you are developing low-level APIs, it might be better to use arrays for read-write scenarios.</span></span> <span data-ttu-id="00ca4-165">Las matrices tienen una menor superficie de memoria, lo que ayuda a reducir el espacio de trabajo, y acceso a los elementos de una matriz es más rápido porque está optimizado por el tiempo de ejecución.</span><span class="sxs-lookup"><span data-stu-id="00ca4-165">Arrays have a smaller memory footprint, which helps reduce the working set, and access to elements in an array is faster because it is optimized by the runtime.</span></span>  
  
 <span data-ttu-id="00ca4-166">**✓ CONSIDER** utilizar matrices en las API de bajo nivel para minimizar el consumo de memoria y maximizar el rendimiento.</span><span class="sxs-lookup"><span data-stu-id="00ca4-166">**✓ CONSIDER** using arrays in low-level APIs to minimize memory consumption and maximize performance.</span></span>  
  
 <span data-ttu-id="00ca4-167">**✓ DO** usar matrices de bytes en lugar de colecciones de bytes.</span><span class="sxs-lookup"><span data-stu-id="00ca4-167">**✓ DO** use byte arrays instead of collections of bytes.</span></span>  
  
 <span data-ttu-id="00ca4-168">**X DO NOT** usar matrices de propiedades si la propiedad tendría que volver a una nueva matriz (p. ej., una copia de una matriz interna) cada vez que se llama al captador de propiedad.</span><span class="sxs-lookup"><span data-stu-id="00ca4-168">**X DO NOT** use arrays for properties if the property would have to return a new array (e.g., a copy of an internal array) every time the property getter is called.</span></span>  
  
## <a name="implementing-custom-collections"></a><span data-ttu-id="00ca4-169">Implementación de recopilaciones personalizadas</span><span class="sxs-lookup"><span data-stu-id="00ca4-169">Implementing Custom Collections</span></span>  
 <span data-ttu-id="00ca4-170">**✓ CONSIDER** heredar `Collection<T>`, `ReadOnlyCollection<T>`, o `KeyedCollection<TKey,TItem>` al diseñar nuevas recopilaciones.</span><span class="sxs-lookup"><span data-stu-id="00ca4-170">**✓ CONSIDER** inheriting from `Collection<T>`, `ReadOnlyCollection<T>`, or `KeyedCollection<TKey,TItem>` when designing new collections.</span></span>  
  
 <span data-ttu-id="00ca4-171">**✓ DO** implementar `IEnumerable<T>` al diseñar nuevas recopilaciones.</span><span class="sxs-lookup"><span data-stu-id="00ca4-171">**✓ DO** implement `IEnumerable<T>` when designing new collections.</span></span> <span data-ttu-id="00ca4-172">Considere la posibilidad de implementar `ICollection<T>` o incluso `IList<T>` donde tenga sentido.</span><span class="sxs-lookup"><span data-stu-id="00ca4-172">Consider implementing `ICollection<T>` or even `IList<T>` where it makes sense.</span></span>  
  
 <span data-ttu-id="00ca4-173">Al implementar este tipo de colección personalizada, siguen el patrón de API que se establece mediante la `Collection<T>` y `ReadOnlyCollection<T>` lo máximo posible.</span><span class="sxs-lookup"><span data-stu-id="00ca4-173">When implementing such custom collection, follow the API pattern established by `Collection<T>` and `ReadOnlyCollection<T>` as closely as possible.</span></span> <span data-ttu-id="00ca4-174">Es decir, implementar explícitamente los mismos miembros, nombre de los parámetros como el nombre de estas dos colecciones de ellos y así sucesivamente.</span><span class="sxs-lookup"><span data-stu-id="00ca4-174">That is, implement the same members explicitly, name the parameters like these two collections name them, and so on.</span></span>  
  
 <span data-ttu-id="00ca4-175">**✓ CONSIDER** implementar interfaces de colección no genérica (`IList` y `ICollection`) si la colección a menudo se pasará a las API teniendo estas interfaces como entrada.</span><span class="sxs-lookup"><span data-stu-id="00ca4-175">**✓ CONSIDER** implementing nongeneric collection interfaces (`IList` and `ICollection`) if the collection will often be passed to APIs taking these interfaces as input.</span></span>  
  
 <span data-ttu-id="00ca4-176">**X AVOID** implementar interfaces de colección de tipos con API complejas que no está relacionado con el concepto de una colección.</span><span class="sxs-lookup"><span data-stu-id="00ca4-176">**X AVOID** implementing collection interfaces on types with complex APIs unrelated to the concept of a collection.</span></span>  
  
 <span data-ttu-id="00ca4-177">**X DO NOT** heredar de colecciones no genéricas de base como `CollectionBase`.</span><span class="sxs-lookup"><span data-stu-id="00ca4-177">**X DO NOT** inherit from nongeneric base collections such as `CollectionBase`.</span></span> <span data-ttu-id="00ca4-178">Use `Collection<T>`, `ReadOnlyCollection<T>`, y `KeyedCollection<TKey,TItem>` en su lugar.</span><span class="sxs-lookup"><span data-stu-id="00ca4-178">Use `Collection<T>`, `ReadOnlyCollection<T>`, and `KeyedCollection<TKey,TItem>` instead.</span></span>  
  
### <a name="naming-custom-collections"></a><span data-ttu-id="00ca4-179">Nomenclatura de recopilaciones personalizadas</span><span class="sxs-lookup"><span data-stu-id="00ca4-179">Naming Custom Collections</span></span>  
 <span data-ttu-id="00ca4-180">Colecciones (los tipos que implementan `IEnumerable`) se crean principalmente por dos motivos: (1) para crear una nueva estructura de datos con operaciones específicas de la estructura y a menudo diferentes características de rendimiento que las estructuras de datos existente (por ejemplo, <xref:System.Collections.Generic.List%601>, <xref:System.Collections.Generic.LinkedList%601>, <xref:System.Collections.Generic.Stack%601>) y (2) para crear una colección especializada para que contiene un conjunto específico de elementos (por ejemplo, <xref:System.Collections.Specialized.StringCollection>).</span><span class="sxs-lookup"><span data-stu-id="00ca4-180">Collections (types that implement `IEnumerable`) are created mainly for two reasons: (1) to create a new data structure with structure-specific operations and often different performance characteristics than existing data structures (e.g.,  <xref:System.Collections.Generic.List%601>, <xref:System.Collections.Generic.LinkedList%601>, <xref:System.Collections.Generic.Stack%601>), and (2) to create a specialized collection for holding a specific set of items (e.g.,  <xref:System.Collections.Specialized.StringCollection>).</span></span> <span data-ttu-id="00ca4-181">Estructuras de datos se suelen usar en la implementación interna de aplicaciones y bibliotecas.</span><span class="sxs-lookup"><span data-stu-id="00ca4-181">Data structures are most often used in the internal implementation of applications and libraries.</span></span> <span data-ttu-id="00ca4-182">Colecciones especializadas son principalmente para exponerse en API (como tipos de propiedad y los parámetros).</span><span class="sxs-lookup"><span data-stu-id="00ca4-182">Specialized collections are mainly to be exposed in APIs (as property and parameter types).</span></span>  
  
 <span data-ttu-id="00ca4-183">**✓ DO** utilizar el sufijo "Diccionario" en los nombres de abstracciones implementar `IDictionary` o `IDictionary<TKey,TValue>`.</span><span class="sxs-lookup"><span data-stu-id="00ca4-183">**✓ DO** use the "Dictionary" suffix in names of abstractions implementing `IDictionary` or `IDictionary<TKey,TValue>`.</span></span>  
  
 <span data-ttu-id="00ca4-184">**✓ DO** utilizar el sufijo "Collection" en los nombres de tipos que implementan `IEnumerable` (o cualquiera de sus descendientes) y que representa una lista de elementos.</span><span class="sxs-lookup"><span data-stu-id="00ca4-184">**✓ DO** use the "Collection" suffix in names of types implementing `IEnumerable` (or any of its descendants) and representing a list of items.</span></span>  
  
 <span data-ttu-id="00ca4-185">**✓ DO** usar el nombre de la estructura de datos adecuado para estructuras de datos personalizadas.</span><span class="sxs-lookup"><span data-stu-id="00ca4-185">**✓ DO** use the appropriate data structure name for custom data structures.</span></span>  
  
 <span data-ttu-id="00ca4-186">**X AVOID** utilizar los sufijos de lo que implica la implementación concreta, como "LinkedList" o "Tabla hash," en los nombres de abstracciones de la colección.</span><span class="sxs-lookup"><span data-stu-id="00ca4-186">**X AVOID** using any suffixes implying particular implementation, such as "LinkedList" or "Hashtable," in names of collection abstractions.</span></span>  
  
 <span data-ttu-id="00ca4-187">**✓ CONSIDER** agregando el prefijo de nombres de la colección con el nombre del tipo de elemento.</span><span class="sxs-lookup"><span data-stu-id="00ca4-187">**✓ CONSIDER** prefixing collection names with the name of the item type.</span></span> <span data-ttu-id="00ca4-188">Por ejemplo, una colección almacenar elementos de tipo `Address` (implementar `IEnumerable<Address>`) debe denominarse `AddressCollection`.</span><span class="sxs-lookup"><span data-stu-id="00ca4-188">For example, a collection storing items of type `Address` (implementing `IEnumerable<Address>`) should be named `AddressCollection`.</span></span> <span data-ttu-id="00ca4-189">Si el tipo de elemento es una interfaz, la "I" prefijo del elemento de tipo se puede omitir.</span><span class="sxs-lookup"><span data-stu-id="00ca4-189">If the item type is an interface, the "I" prefix of the item type can be omitted.</span></span> <span data-ttu-id="00ca4-190">Por lo tanto, una colección de <xref:System.IDisposable> elementos pueden llamarse `DisposableCollection`.</span><span class="sxs-lookup"><span data-stu-id="00ca4-190">Thus, a collection of <xref:System.IDisposable> items can be called `DisposableCollection`.</span></span>  
  
 <span data-ttu-id="00ca4-191">**✓ CONSIDER** utilizar el prefijo "ReadOnly" en los nombres de colecciones de solo lectura si una colección de escritura correspondiente podría agregarse o no existe en el marco de trabajo.</span><span class="sxs-lookup"><span data-stu-id="00ca4-191">**✓ CONSIDER** using the "ReadOnly" prefix in names of read-only collections if a corresponding writeable collection might be added or already exists in the framework.</span></span>  
  
 <span data-ttu-id="00ca4-192">Por ejemplo, se debe llamar a una colección de solo lectura de cadenas `ReadOnlyStringCollection`.</span><span class="sxs-lookup"><span data-stu-id="00ca4-192">For example, a read-only collection of strings should be called `ReadOnlyStringCollection`.</span></span>  
  
 <span data-ttu-id="00ca4-193">*Portions © 2005, 2009 Microsoft Corporation. Reservados todos los derechos.*</span><span class="sxs-lookup"><span data-stu-id="00ca4-193">*Portions © 2005, 2009 Microsoft Corporation. All rights reserved.*</span></span>  
  
 <span data-ttu-id="00ca4-194">*Reimpreso con permiso de Pearson Education, Inc. de [instrucciones de diseño de Framework: Convenciones, expresiones y patrones para bibliotecas reutilizables. NET, 2ª edición](https://www.informit.com/store/framework-design-guidelines-conventions-idioms-and-9780321545619) Krzysztof Cwalina y Brad Abrams, publicada el 22 de octubre de 2008 por Addison-Wesley Professional como parte de la serie de desarrollo de Microsoft Windows.*</span><span class="sxs-lookup"><span data-stu-id="00ca4-194">*Reprinted by permission of Pearson Education, Inc. from [Framework Design Guidelines: Conventions, Idioms, and Patterns for Reusable .NET Libraries, 2nd Edition](https://www.informit.com/store/framework-design-guidelines-conventions-idioms-and-9780321545619) by Krzysztof Cwalina and Brad Abrams, published Oct 22, 2008 by Addison-Wesley Professional as part of the Microsoft Windows Development Series.*</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="00ca4-195">Vea también</span><span class="sxs-lookup"><span data-stu-id="00ca4-195">See also</span></span>

- [<span data-ttu-id="00ca4-196">Instrucciones de diseño de .NET Framework</span><span class="sxs-lookup"><span data-stu-id="00ca4-196">Framework Design Guidelines</span></span>](../../../docs/standard/design-guidelines/index.md)
- [<span data-ttu-id="00ca4-197">Instrucciones de uso</span><span class="sxs-lookup"><span data-stu-id="00ca4-197">Usage Guidelines</span></span>](../../../docs/standard/design-guidelines/usage-guidelines.md)
