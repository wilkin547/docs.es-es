---
title: Migración de Newtonsoft. JSON a System. Text. JSON-.NET
author: tdykstra
ms.author: tdykstra
ms.date: 01/10/2020
helpviewer_keywords:
- JSON serialization
- serializing objects
- serialization
- objects, serializing
ms.openlocfilehash: 01f94bcfce97da8c71b1b709baa34c2b7509a5e5
ms.sourcegitcommit: ed3f926b6cdd372037bbcc214dc8f08a70366390
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 01/16/2020
ms.locfileid: "76116687"
---
# <a name="how-to-migrate-from-newtonsoftjson-to-systemtextjson"></a><span data-ttu-id="a3def-102">Cómo migrar de Newtonsoft. JSON a System. Text. JSON</span><span class="sxs-lookup"><span data-stu-id="a3def-102">How to migrate from Newtonsoft.Json to System.Text.Json</span></span>

<span data-ttu-id="a3def-103">En este artículo se muestra cómo migrar de [Newtonsoft. JSON](https://www.newtonsoft.com/json) a <xref:System.Text.Json>.</span><span class="sxs-lookup"><span data-stu-id="a3def-103">This article shows how to migrate from [Newtonsoft.Json](https://www.newtonsoft.com/json) to <xref:System.Text.Json>.</span></span>

 <span data-ttu-id="a3def-104">`System.Text.Json` se centra principalmente en el rendimiento, la seguridad y el cumplimiento de estándares.</span><span class="sxs-lookup"><span data-stu-id="a3def-104">`System.Text.Json` focuses primarily on performance, security, and standards compliance.</span></span> <span data-ttu-id="a3def-105">Tiene algunas diferencias clave en el comportamiento predeterminado y no pretende tener paridad de características con `Newtonsoft.Json`.</span><span class="sxs-lookup"><span data-stu-id="a3def-105">It has some key differences in default behavior and doesn't aim to have feature parity with `Newtonsoft.Json`.</span></span> <span data-ttu-id="a3def-106">En algunos escenarios, `System.Text.Json` no tiene ninguna funcionalidad integrada, pero se recomiendan las soluciones alternativas.</span><span class="sxs-lookup"><span data-stu-id="a3def-106">For some scenarios, `System.Text.Json` has no built-in functionality, but there are recommended workarounds.</span></span> <span data-ttu-id="a3def-107">En otros escenarios, las soluciones alternativas no son prácticas.</span><span class="sxs-lookup"><span data-stu-id="a3def-107">For other scenarios, workarounds are impractical.</span></span> <span data-ttu-id="a3def-108">Si su aplicación depende de una característica que falta, considere la posibilidad de [presentar un problema](https://github.com/dotnet/runtime/issues/new) para averiguar si se puede Agregar compatibilidad para su escenario.</span><span class="sxs-lookup"><span data-stu-id="a3def-108">If your application depends on a missing feature, consider [filing an issue](https://github.com/dotnet/runtime/issues/new) to find out if support for your scenario can be added.</span></span>

<!-- For information about which features might be added in future releases, see the [Roadmap](https://github.com/dotnet/runtime/tree/81bf79fd9aa75305e55abe2f7e9ef3f60624a3a1/src/libraries/System.Text.Json/roadmap/README.md). [Restore this when the roadmap is updated.]-->

<span data-ttu-id="a3def-109">La mayor parte de este artículo trata sobre cómo usar la API de <xref:System.Text.Json.JsonSerializer>, pero también incluye instrucciones sobre cómo usar el <xref:System.Text.Json.JsonDocument> (que representa los tipos Document Object Model o DOM), <xref:System.Text.Json.Utf8JsonReader>y <xref:System.Text.Json.Utf8JsonWriter>.</span><span class="sxs-lookup"><span data-stu-id="a3def-109">Most of this article is about how to use the <xref:System.Text.Json.JsonSerializer> API, but it also includes guidance on how to use the <xref:System.Text.Json.JsonDocument> (which represents the Document Object Model or DOM), <xref:System.Text.Json.Utf8JsonReader>, and <xref:System.Text.Json.Utf8JsonWriter> types.</span></span> <span data-ttu-id="a3def-110">El artículo se organiza en secciones en el orden siguiente:</span><span class="sxs-lookup"><span data-stu-id="a3def-110">The article is organized into sections in the following order:</span></span>

* [<span data-ttu-id="a3def-111">Diferencias en el comportamiento **predeterminado** de JsonSerializer en comparación con Newtonsoft. JSON</span><span class="sxs-lookup"><span data-stu-id="a3def-111">Differences in **default** JsonSerializer behavior compared to Newtonsoft.Json</span></span>](#differences-in-default-jsonserializer-behavior-compared-to-newtonsoftjson)
* [<span data-ttu-id="a3def-112">Escenarios de uso de JsonSerializer que requieren soluciones alternativas</span><span class="sxs-lookup"><span data-stu-id="a3def-112">Scenarios using JsonSerializer that require workarounds</span></span>](#scenarios-using-jsonserializer-that-require-workarounds)
* [<span data-ttu-id="a3def-113">Escenarios en los que JsonSerializer no admite actualmente</span><span class="sxs-lookup"><span data-stu-id="a3def-113">Scenarios that JsonSerializer currently doesn't support</span></span>](#scenarios-that-jsonserializer-currently-doesnt-support)
* [<span data-ttu-id="a3def-114">JsonDocument y JsonElement comparado con JToken (como JObject, JArray)</span><span class="sxs-lookup"><span data-stu-id="a3def-114">JsonDocument and JsonElement compared to JToken (like JObject, JArray)</span></span>](#jsondocument-and-jsonelement-compared-to-jtoken-like-jobject-jarray)
* [<span data-ttu-id="a3def-115">Utf8JsonReader comparado con JsonTextReader</span><span class="sxs-lookup"><span data-stu-id="a3def-115">Utf8JsonReader compared to JsonTextReader</span></span>](#utf8jsonreader-compared-to-jsontextreader)
* [<span data-ttu-id="a3def-116">Utf8JsonWriter comparado con objeto JsonTextWriter</span><span class="sxs-lookup"><span data-stu-id="a3def-116">Utf8JsonWriter compared to JsonTextWriter</span></span>](#utf8jsonwriter-compared-to-jsontextwriter)

## <a name="differences-in-default-jsonserializer-behavior-compared-to-newtonsoftjson"></a><span data-ttu-id="a3def-117">Diferencias en el comportamiento predeterminado de JsonSerializer en comparación con Newtonsoft. JSON</span><span class="sxs-lookup"><span data-stu-id="a3def-117">Differences in default JsonSerializer behavior compared to Newtonsoft.Json</span></span>

<span data-ttu-id="a3def-118"><xref:System.Text.Json> es STRICT de forma predeterminada y evita cualquier adivinación o interpretación en nombre del llamador, resaltando el comportamiento determinista.</span><span class="sxs-lookup"><span data-stu-id="a3def-118"><xref:System.Text.Json> is strict by default and avoids any guessing or interpretation on the caller's behalf, emphasizing deterministic behavior.</span></span> <span data-ttu-id="a3def-119">La biblioteca se ha diseñado intencionadamente de esta manera para el rendimiento y la seguridad.</span><span class="sxs-lookup"><span data-stu-id="a3def-119">The library is intentionally designed this way for performance and security.</span></span> <span data-ttu-id="a3def-120">de forma predeterminada, `Newtonsoft.Json` es flexible.</span><span class="sxs-lookup"><span data-stu-id="a3def-120">`Newtonsoft.Json` is flexible by default.</span></span> <span data-ttu-id="a3def-121">Esta diferencia fundamental en el diseño está detrás de muchas de las siguientes diferencias específicas en el comportamiento predeterminado.</span><span class="sxs-lookup"><span data-stu-id="a3def-121">This fundamental difference in design is behind many of the following specific differences in default behavior.</span></span>

### <a name="case-insensitive-deserialization"></a><span data-ttu-id="a3def-122">Deserialización sin distinción entre mayúsculas y minúsculas</span><span class="sxs-lookup"><span data-stu-id="a3def-122">Case-insensitive deserialization</span></span> 

<span data-ttu-id="a3def-123">Durante la deserialización, `Newtonsoft.Json` realiza la coincidencia de nombres de propiedad sin distinción entre mayúsculas y minúsculas de forma predeterminada.</span><span class="sxs-lookup"><span data-stu-id="a3def-123">During deserialization, `Newtonsoft.Json` does case-insensitive property name matching by default.</span></span> <span data-ttu-id="a3def-124">El valor predeterminado de <xref:System.Text.Json> distingue entre mayúsculas y minúsculas, lo que proporciona un mejor rendimiento, ya que realiza una coincidencia exacta.</span><span class="sxs-lookup"><span data-stu-id="a3def-124">The <xref:System.Text.Json> default is case-sensitive, which gives better performance since it's doing an exact match.</span></span> <span data-ttu-id="a3def-125">Para obtener información sobre cómo hacer coincidir la coincidencia sin distinción entre mayúsculas y minúsculas, vea [coincidencia de propiedades sin distinción entre mayúsculas y minúsculas](system-text-json-how-to.md#case-insensitive-property-matching).</span><span class="sxs-lookup"><span data-stu-id="a3def-125">For information about how to do case-insensitive matching, see [Case-insensitive property matching](system-text-json-how-to.md#case-insensitive-property-matching).</span></span>

<span data-ttu-id="a3def-126">Si usa `System.Text.Json` indirectamente mediante ASP.NET Core, no es necesario hacer nada para obtener el comportamiento como `Newtonsoft.Json`.</span><span class="sxs-lookup"><span data-stu-id="a3def-126">If you're using `System.Text.Json` indirectly by using ASP.NET Core, you don't need to do anything to get behavior like `Newtonsoft.Json`.</span></span> <span data-ttu-id="a3def-127">ASP.NET Core especifica los valores para [los nombres de propiedad con grafía Camel](system-text-json-how-to.md#use-camel-case-for-all-json-property-names) y la coincidencia sin distinción entre mayúsculas y minúsculas cuando usa `System.Text.Json`.</span><span class="sxs-lookup"><span data-stu-id="a3def-127">ASP.NET Core specifies the settings for [camel-casing property names](system-text-json-how-to.md#use-camel-case-for-all-json-property-names) and case-insensitive matching when it uses `System.Text.Json`.</span></span>

### <a name="comments"></a><span data-ttu-id="a3def-128">Comentarios</span><span class="sxs-lookup"><span data-stu-id="a3def-128">Comments</span></span>

<span data-ttu-id="a3def-129">Durante la deserialización, `Newtonsoft.Json` omite los comentarios en el JSON de forma predeterminada.</span><span class="sxs-lookup"><span data-stu-id="a3def-129">During deserialization, `Newtonsoft.Json` ignores comments in the JSON by default.</span></span> <span data-ttu-id="a3def-130">El <xref:System.Text.Json> predeterminado es producir excepciones para los comentarios porque la especificación [RFC 8259](https://tools.ietf.org/html/rfc8259) no los incluye.</span><span class="sxs-lookup"><span data-stu-id="a3def-130">The <xref:System.Text.Json> default is to throw exceptions for comments because the [RFC 8259](https://tools.ietf.org/html/rfc8259) specification doesn't include them.</span></span> <span data-ttu-id="a3def-131">Para obtener información sobre cómo permitir comentarios, vea [permitir comentarios y comas finales](system-text-json-how-to.md#allow-comments-and-trailing-commas).</span><span class="sxs-lookup"><span data-stu-id="a3def-131">For information about how to allow comments, see [Allow comments and trailing commas](system-text-json-how-to.md#allow-comments-and-trailing-commas).</span></span>

### <a name="trailing-commas"></a><span data-ttu-id="a3def-132">Comas finales</span><span class="sxs-lookup"><span data-stu-id="a3def-132">Trailing commas</span></span>

<span data-ttu-id="a3def-133">Durante la deserialización, `Newtonsoft.Json` omite las comas finales de forma predeterminada.</span><span class="sxs-lookup"><span data-stu-id="a3def-133">During deserialization, `Newtonsoft.Json` ignores trailing commas by default.</span></span> <span data-ttu-id="a3def-134">También omite varias comas finales (por ejemplo, `[{"Color":"Red"},{"Color":"Green"},,]`).</span><span class="sxs-lookup"><span data-stu-id="a3def-134">It also ignores multiple trailing commas (for example, `[{"Color":"Red"},{"Color":"Green"},,]`).</span></span> <span data-ttu-id="a3def-135">El <xref:System.Text.Json> predeterminado es iniciar excepciones para las comas finales, ya que la especificación [RFC 8259](https://tools.ietf.org/html/rfc8259) no las permite.</span><span class="sxs-lookup"><span data-stu-id="a3def-135">The <xref:System.Text.Json> default is to throw exceptions for trailing commas because the [RFC 8259](https://tools.ietf.org/html/rfc8259) specification doesn't allow them.</span></span> <span data-ttu-id="a3def-136">Para obtener información sobre cómo hacer que `System.Text.Json` los acepte, consulte [permitir comentarios y comas finales](system-text-json-how-to.md#allow-comments-and-trailing-commas).</span><span class="sxs-lookup"><span data-stu-id="a3def-136">For information about how to make `System.Text.Json` accept them, see [Allow comments and trailing commas](system-text-json-how-to.md#allow-comments-and-trailing-commas).</span></span> <span data-ttu-id="a3def-137">No hay ninguna manera de permitir comas finales múltiples.</span><span class="sxs-lookup"><span data-stu-id="a3def-137">There's no way to allow multiple trailing commas.</span></span>

### <a name="json-strings-property-names-and-string-values"></a><span data-ttu-id="a3def-138">Cadenas JSON (nombres de propiedad y valores de cadena)</span><span class="sxs-lookup"><span data-stu-id="a3def-138">JSON strings (property names and string values)</span></span>

<span data-ttu-id="a3def-139">Durante la deserialización, `Newtonsoft.Json` acepta nombres de propiedad entre comillas dobles, comillas simples o sin comillas.</span><span class="sxs-lookup"><span data-stu-id="a3def-139">During deserialization, `Newtonsoft.Json` accepts property names surrounded by double quotes, single quotes, or without quotes.</span></span> <span data-ttu-id="a3def-140">Acepta valores de cadena entre comillas dobles o comillas simples.</span><span class="sxs-lookup"><span data-stu-id="a3def-140">It accepts string values surrounded by double quotes or single quotes.</span></span> <span data-ttu-id="a3def-141">Por ejemplo, `Newtonsoft.Json` acepta el siguiente código JSON:</span><span class="sxs-lookup"><span data-stu-id="a3def-141">For example, `Newtonsoft.Json` accepts the following JSON:</span></span>

```json
{
  "name1": "value",
  'name2': "value",
  name3: 'value'
}
```

<span data-ttu-id="a3def-142">`System.Text.Json` solo acepta nombres de propiedad y valores de cadena entre comillas, ya que el formato es necesario para la especificación [RFC 8259](https://tools.ietf.org/html/rfc8259) y es el único formato considerado JSON válido.</span><span class="sxs-lookup"><span data-stu-id="a3def-142">`System.Text.Json` only accepts property names and string values in double quotes because that format is required by the [RFC 8259](https://tools.ietf.org/html/rfc8259) specification and is the only format considered valid JSON.</span></span>

<span data-ttu-id="a3def-143">Un valor entre comillas simples da como resultado un [JsonException](xref:System.Text.Json.JsonException) con el siguiente mensaje:</span><span class="sxs-lookup"><span data-stu-id="a3def-143">A value enclosed in single quotes results in a [JsonException](xref:System.Text.Json.JsonException) with the following message:</span></span>

```
''' is an invalid start of a value.
```

### <a name="non-string-values-for-string-properties"></a><span data-ttu-id="a3def-144">Valores que no son de cadena para las propiedades de cadena</span><span class="sxs-lookup"><span data-stu-id="a3def-144">Non-string values for string properties</span></span>

<span data-ttu-id="a3def-145">`Newtonsoft.Json` acepta valores que no son de cadena, como un número o los literales `true` y `false`, para la deserialización de las propiedades de tipo cadena.</span><span class="sxs-lookup"><span data-stu-id="a3def-145">`Newtonsoft.Json` accepts non-string values, such as a number or the literals `true` and `false`, for deserialization to properties of type string.</span></span> <span data-ttu-id="a3def-146">A continuación se muestra un ejemplo de JSON que `Newtonsoft.Json` deserializa correctamente en la clase siguiente:</span><span class="sxs-lookup"><span data-stu-id="a3def-146">Here's an example of JSON that `Newtonsoft.Json` successfully deserializes to the following class:</span></span>

```json
{
  "String1": 1,
  "String2": true,
  "String3": false
}
```

```csharp
public class ExampleClass
{
    public string String1 { get; set; }
    public string String2 { get; set; }
    public string String3 { get; set; }
}
```

<span data-ttu-id="a3def-147">`System.Text.Json` no deserializa valores que no son de cadena en propiedades de cadena.</span><span class="sxs-lookup"><span data-stu-id="a3def-147">`System.Text.Json` doesn't deserialize non-string values into string properties.</span></span> <span data-ttu-id="a3def-148">Un valor que no sea de cadena recibido para un campo de cadena da como resultado un [JsonException](xref:System.Text.Json.JsonException) con el siguiente mensaje:</span><span class="sxs-lookup"><span data-stu-id="a3def-148">A non-string value received for a string field results in a [JsonException](xref:System.Text.Json.JsonException) with the following message:</span></span>

```
The JSON value could not be converted to System.String.
```

### <a name="converter-registration-precedence"></a><span data-ttu-id="a3def-149">Prioridad de registro del convertidor</span><span class="sxs-lookup"><span data-stu-id="a3def-149">Converter registration precedence</span></span>

<span data-ttu-id="a3def-150">La prioridad de registro de `Newtonsoft.Json` para los convertidores personalizados es la siguiente:</span><span class="sxs-lookup"><span data-stu-id="a3def-150">The `Newtonsoft.Json` registration precedence for custom converters is as follows:</span></span>

* <span data-ttu-id="a3def-151">Atributo en la propiedad</span><span class="sxs-lookup"><span data-stu-id="a3def-151">Attribute on property</span></span>
* <span data-ttu-id="a3def-152">Atributo en tipo</span><span class="sxs-lookup"><span data-stu-id="a3def-152">Attribute on type</span></span>
* <span data-ttu-id="a3def-153">Colección de [convertidores](https://www.newtonsoft.com/json/help/html/P_Newtonsoft_Json_JsonSerializerSettings_Converters.htm)</span><span class="sxs-lookup"><span data-stu-id="a3def-153">[Converters](https://www.newtonsoft.com/json/help/html/P_Newtonsoft_Json_JsonSerializerSettings_Converters.htm) collection</span></span>

<span data-ttu-id="a3def-154">Este orden significa que un convertidor personalizado de la colección `Converters` se reemplaza por un convertidor que se registra aplicando un atributo en el nivel de tipo.</span><span class="sxs-lookup"><span data-stu-id="a3def-154">This order means that a custom converter in the `Converters` collection is overridden by a converter that is registered by applying an attribute at the type level.</span></span> <span data-ttu-id="a3def-155">Los dos registros se reemplazan por un atributo en el nivel de propiedad.</span><span class="sxs-lookup"><span data-stu-id="a3def-155">Both of those registrations are overridden by an attribute at the property level.</span></span>

<span data-ttu-id="a3def-156">La prioridad de registro de <xref:System.Text.Json> para los convertidores personalizados es diferente:</span><span class="sxs-lookup"><span data-stu-id="a3def-156">The <xref:System.Text.Json> registration precedence for custom converters is different:</span></span>

* <span data-ttu-id="a3def-157">Atributo en la propiedad</span><span class="sxs-lookup"><span data-stu-id="a3def-157">Attribute on property</span></span>
* <span data-ttu-id="a3def-158"><xref:System.Text.Json.JsonSerializerOptions.Converters> colección</span><span class="sxs-lookup"><span data-stu-id="a3def-158"><xref:System.Text.Json.JsonSerializerOptions.Converters> collection</span></span>
* <span data-ttu-id="a3def-159">Atributo en tipo</span><span class="sxs-lookup"><span data-stu-id="a3def-159">Attribute on type</span></span>

<span data-ttu-id="a3def-160">La diferencia es que un convertidor personalizado de la colección `Converters` invalida un atributo en el nivel de tipo.</span><span class="sxs-lookup"><span data-stu-id="a3def-160">The difference here is that a custom converter in the `Converters` collection overrides an attribute at the type level.</span></span> <span data-ttu-id="a3def-161">La intención de este orden de prioridad es que los cambios de tiempo de ejecución invaliden las opciones de tiempo de diseño.</span><span class="sxs-lookup"><span data-stu-id="a3def-161">The intention behind this order of precedence is to make run-time changes override design-time choices.</span></span> <span data-ttu-id="a3def-162">No hay ninguna manera de cambiar la prioridad.</span><span class="sxs-lookup"><span data-stu-id="a3def-162">There's no way to change the precedence.</span></span>

<span data-ttu-id="a3def-163">Para obtener más información sobre el registro de convertidor personalizado, vea [registrar un convertidor personalizado](system-text-json-converters-how-to.md#register-a-custom-converter).</span><span class="sxs-lookup"><span data-stu-id="a3def-163">For more information about custom converter registration, see [Register a custom converter](system-text-json-converters-how-to.md#register-a-custom-converter).</span></span>

### <a name="character-escaping"></a><span data-ttu-id="a3def-164">Escape de caracteres</span><span class="sxs-lookup"><span data-stu-id="a3def-164">Character escaping</span></span>

<span data-ttu-id="a3def-165">Durante la serialización, `Newtonsoft.Json` es relativamente permisiva si se permite que los caracteres queden sin escapar.</span><span class="sxs-lookup"><span data-stu-id="a3def-165">During serialization, `Newtonsoft.Json` is relatively permissive about letting characters through without escaping them.</span></span> <span data-ttu-id="a3def-166">Es decir, no los reemplaza por `\uxxxx` donde `xxxx` es el punto de código del carácter.</span><span class="sxs-lookup"><span data-stu-id="a3def-166">That is, it doesn't replace them with `\uxxxx` where `xxxx` is the character's code point.</span></span> <span data-ttu-id="a3def-167">Donde los escapa, lo hace emitiendo una `\` antes del carácter (por ejemplo, `"` se convierte en `\"`).</span><span class="sxs-lookup"><span data-stu-id="a3def-167">Where it does escape them, it does so by emitting a `\` before the character (for example, `"` becomes `\"`).</span></span> <span data-ttu-id="a3def-168"><xref:System.Text.Json> escapa más caracteres de forma predeterminada para proporcionar protecciones de defensa en profundidad contra ataques de scripting entre sitios (XSS) o de divulgación de información, y lo hace mediante la secuencia de seis caracteres.</span><span class="sxs-lookup"><span data-stu-id="a3def-168"><xref:System.Text.Json> escapes more characters by default to provide defense-in-depth protections against cross-site scripting (XSS) or information-disclosure attacks and does so by using the six-character sequence.</span></span> <span data-ttu-id="a3def-169">`System.Text.Json` escapa a todos los caracteres no ASCII de forma predeterminada, por lo que no es necesario hacer nada si usa `StringEscapeHandling.EscapeNonAscii` en `Newtonsoft.Json`.</span><span class="sxs-lookup"><span data-stu-id="a3def-169">`System.Text.Json` escapes all non-ASCII characters by default, so you don't need to do anything if you're using `StringEscapeHandling.EscapeNonAscii` in `Newtonsoft.Json`.</span></span> <span data-ttu-id="a3def-170">de forma predeterminada, `System.Text.Json` también escapa los caracteres que distinguen entre HTML.</span><span class="sxs-lookup"><span data-stu-id="a3def-170">`System.Text.Json` also escapes HTML-sensitive characters, by default.</span></span> <span data-ttu-id="a3def-171">Para obtener información sobre cómo invalidar el comportamiento predeterminado de `System.Text.Json`, vea [personalizar la codificación de caracteres](system-text-json-how-to.md#customize-character-encoding).</span><span class="sxs-lookup"><span data-stu-id="a3def-171">For information about how to override the default `System.Text.Json` behavior, see [Customize character encoding](system-text-json-how-to.md#customize-character-encoding).</span></span>

### <a name="deserialization-of-object-properties"></a><span data-ttu-id="a3def-172">Deserialización de propiedades de objeto</span><span class="sxs-lookup"><span data-stu-id="a3def-172">Deserialization of object properties</span></span>

<span data-ttu-id="a3def-173">Cuando `Newtonsoft.Json` deserializa en `object` propiedades en POCO o en diccionarios de tipo `Dictionary<string, object>`, se:</span><span class="sxs-lookup"><span data-stu-id="a3def-173">When `Newtonsoft.Json` deserializes to `object` properties in POCOs or in dictionaries of type `Dictionary<string, object>`, it:</span></span>

* <span data-ttu-id="a3def-174">Infiere el tipo de valores primitivos en la carga JSON (excepto `null`) y devuelve los `string`almacenados, `long`, `double`, `boolean`o `DateTime` como un objeto con conversión boxing.</span><span class="sxs-lookup"><span data-stu-id="a3def-174">Infers the type of primitive values in the JSON payload (other than `null`) and returns the stored `string`, `long`, `double`, `boolean`, or `DateTime` as a boxed object.</span></span> <span data-ttu-id="a3def-175">*Los valores primitivos* son valores JSON únicos como un número JSON, una cadena, un `true`, un `false`o un `null`.</span><span class="sxs-lookup"><span data-stu-id="a3def-175">*Primitive values* are single JSON values such as a JSON number, string, `true`, `false`, or `null`.</span></span>
* <span data-ttu-id="a3def-176">Devuelve un `JObject` o `JArray` para valores complejos en la carga de JSON.</span><span class="sxs-lookup"><span data-stu-id="a3def-176">Returns a `JObject` or `JArray` for complex values in the JSON payload.</span></span> <span data-ttu-id="a3def-177">*Los valores complejos* son colecciones de pares clave-valor JSON entre llaves (`{}`) o listas de valores entre corchetes (`[]`).</span><span class="sxs-lookup"><span data-stu-id="a3def-177">*Complex values* are collections of JSON key-value pairs within braces (`{}`) or lists of values within brackets (`[]`).</span></span> <span data-ttu-id="a3def-178">Las propiedades y los valores entre llaves o corchetes pueden tener propiedades o valores adicionales.</span><span class="sxs-lookup"><span data-stu-id="a3def-178">The properties and values within the braces or brackets can have additional properties or values.</span></span>
* <span data-ttu-id="a3def-179">Devuelve una referencia nula cuando la carga útil tiene el `null` literal JSON.</span><span class="sxs-lookup"><span data-stu-id="a3def-179">Returns a null reference when the payload has the `null` JSON literal.</span></span>

<span data-ttu-id="a3def-180"><xref:System.Text.Json> almacena una `JsonElement` con conversión boxing para valores primitivos y complejos dentro de la propiedad `System.Object` o el valor del diccionario.</span><span class="sxs-lookup"><span data-stu-id="a3def-180"><xref:System.Text.Json> stores a boxed `JsonElement` for both primitive and complex values within the `System.Object` property or dictionary value.</span></span> <span data-ttu-id="a3def-181">Sin embargo, trata `null` igual que `Newtonsoft.Json` y devuelve una referencia nula cuando la carga útil tiene el literal `null` JSON.</span><span class="sxs-lookup"><span data-stu-id="a3def-181">However, it treats `null` the same as `Newtonsoft.Json` and returns a null reference when the payload has the `null` JSON literal in it.</span></span>

<span data-ttu-id="a3def-182">Para implementar la inferencia de tipos para las propiedades de `object`, cree un convertidor como el ejemplo de [Cómo escribir convertidores personalizados](system-text-json-converters-how-to.md#deserialize-inferred-types-to-object-properties).</span><span class="sxs-lookup"><span data-stu-id="a3def-182">To implement type inference for `object` properties, create a converter like the example in [How to write custom converters](system-text-json-converters-how-to.md#deserialize-inferred-types-to-object-properties).</span></span>

### <a name="maximum-depth"></a><span data-ttu-id="a3def-183">Profundidad máxima</span><span class="sxs-lookup"><span data-stu-id="a3def-183">Maximum depth</span></span>

<span data-ttu-id="a3def-184">de forma predeterminada, `Newtonsoft.Json` no tiene un límite de profundidad máximo.</span><span class="sxs-lookup"><span data-stu-id="a3def-184">`Newtonsoft.Json` doesn't have a maximum depth limit by default.</span></span> <span data-ttu-id="a3def-185">Por <xref:System.Text.Json> hay un límite predeterminado de 64 y se puede configurar mediante la configuración de <xref:System.Text.Json.JsonSerializerOptions.MaxDepth?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="a3def-185">For <xref:System.Text.Json> there's a default limit  of 64, and it's configurable by setting <xref:System.Text.Json.JsonSerializerOptions.MaxDepth?displayProperty=nameWithType>.</span></span>

### <a name="omit-null-value-properties"></a><span data-ttu-id="a3def-186">Omitir propiedades de valores NULL</span><span class="sxs-lookup"><span data-stu-id="a3def-186">Omit null-value properties</span></span>

<span data-ttu-id="a3def-187">`Newtonsoft.Json` tiene una configuración global que hace que las propiedades de valores NULL se excluyan de la serialización: [NullValueHandling. ignore](https://www.newtonsoft.com/json/help/html/T_Newtonsoft_Json_NullValueHandling.htm).</span><span class="sxs-lookup"><span data-stu-id="a3def-187">`Newtonsoft.Json` has a global setting that causes null-value properties to be excluded from serialization: [NullValueHandling.Ignore](https://www.newtonsoft.com/json/help/html/T_Newtonsoft_Json_NullValueHandling.htm).</span></span> <span data-ttu-id="a3def-188">La opción correspondiente en <xref:System.Text.Json> es <xref:System.Text.Json.JsonSerializerOptions.IgnoreNullValues%2A>.</span><span class="sxs-lookup"><span data-stu-id="a3def-188">The corresponding option in <xref:System.Text.Json> is <xref:System.Text.Json.JsonSerializerOptions.IgnoreNullValues%2A>.</span></span>

## <a name="scenarios-using-jsonserializer-that-require-workarounds"></a><span data-ttu-id="a3def-189">Escenarios de uso de JsonSerializer que requieren soluciones alternativas</span><span class="sxs-lookup"><span data-stu-id="a3def-189">Scenarios using JsonSerializer that require workarounds</span></span>

<span data-ttu-id="a3def-190">Los escenarios siguientes no son compatibles con la funcionalidad integrada, pero se proporciona código de ejemplo para las soluciones alternativas.</span><span class="sxs-lookup"><span data-stu-id="a3def-190">The following scenarios aren't supported by built-in functionality, but sample code is provided for workarounds.</span></span> <span data-ttu-id="a3def-191">La mayoría de las soluciones alternativas requieren la implementación de [convertidores personalizados](system-text-json-converters-how-to.md).</span><span class="sxs-lookup"><span data-stu-id="a3def-191">Most of the workarounds require that you implement [custom converters](system-text-json-converters-how-to.md).</span></span>

### <a name="specify-date-format"></a><span data-ttu-id="a3def-192">Especificar el formato de fecha</span><span class="sxs-lookup"><span data-stu-id="a3def-192">Specify date format</span></span>

<span data-ttu-id="a3def-193">`Newtonsoft.Json` proporciona varias maneras de controlar cómo se serializan y deserializan las propiedades de los tipos `DateTime` y `DateTimeOffset`:</span><span class="sxs-lookup"><span data-stu-id="a3def-193">`Newtonsoft.Json` provides several ways to control how properties of `DateTime` and `DateTimeOffset` types are serialized and deserialized:</span></span>

* <span data-ttu-id="a3def-194">El valor `DateTimeZoneHandling` se puede usar para serializar todos los valores de `DateTime` como fechas UTC.</span><span class="sxs-lookup"><span data-stu-id="a3def-194">The `DateTimeZoneHandling` setting can be used to serialize all `DateTime` values as UTC dates.</span></span>
* <span data-ttu-id="a3def-195">La configuración de `DateFormatString` y los convertidores de `DateTime` se pueden usar para personalizar el formato de las cadenas de fecha.</span><span class="sxs-lookup"><span data-stu-id="a3def-195">The `DateFormatString` setting and `DateTime` converters can be used to customize the format of date strings.</span></span>

<span data-ttu-id="a3def-196">En <xref:System.Text.Json>, el único formato que tiene compatibilidad integrada es ISO 8601-1:2019, ya que se ha adoptado ampliamente, no es ambiguo, y hace que los viajes de ida y vuelta sean precisamente.</span><span class="sxs-lookup"><span data-stu-id="a3def-196">In <xref:System.Text.Json>, the only format that has built-in support is ISO 8601-1:2019 since it's widely adopted, unambiguous, and makes round trips precisely.</span></span> <span data-ttu-id="a3def-197">Para usar cualquier otro formato, cree un convertidor personalizado.</span><span class="sxs-lookup"><span data-stu-id="a3def-197">To use any other format, create a custom converter.</span></span> <span data-ttu-id="a3def-198">Para obtener más información, consulte [compatibilidad con DateTime y DateTimeOffset en System. Text. JSON](../datetime/system-text-json-support.md).</span><span class="sxs-lookup"><span data-stu-id="a3def-198">For more information, see [DateTime and DateTimeOffset support in System.Text.Json](../datetime/system-text-json-support.md).</span></span>

### <a name="quoted-numbers"></a><span data-ttu-id="a3def-199">Números entre comillas</span><span class="sxs-lookup"><span data-stu-id="a3def-199">Quoted numbers</span></span>

<span data-ttu-id="a3def-200">`Newtonsoft.Json` puede serializar o deserializar los números representados por cadenas JSON (entre comillas).</span><span class="sxs-lookup"><span data-stu-id="a3def-200">`Newtonsoft.Json` can serialize or deserialize numbers represented by JSON strings (surrounded by quotes).</span></span> <span data-ttu-id="a3def-201">Por ejemplo, puede aceptar: `{"DegreesCelsius":"23"}` en lugar de `{"DegreesCelsius":23}`.</span><span class="sxs-lookup"><span data-stu-id="a3def-201">For example, it can accept: `{"DegreesCelsius":"23"}` instead of `{"DegreesCelsius":23}`.</span></span> <span data-ttu-id="a3def-202">Para habilitar ese comportamiento en <xref:System.Text.Json>, implemente un convertidor personalizado como el ejemplo siguiente.</span><span class="sxs-lookup"><span data-stu-id="a3def-202">To enable that behavior in <xref:System.Text.Json>, implement a custom converter like the following example.</span></span> <span data-ttu-id="a3def-203">El convertidor controla las propiedades definidas como `long`:</span><span class="sxs-lookup"><span data-stu-id="a3def-203">The converter handles properties defined as `long`:</span></span>

* <span data-ttu-id="a3def-204">Los serializa como cadenas JSON.</span><span class="sxs-lookup"><span data-stu-id="a3def-204">It serializes them as JSON strings.</span></span> 
* <span data-ttu-id="a3def-205">Acepta números de JSON y números entre comillas durante la deserialización.</span><span class="sxs-lookup"><span data-stu-id="a3def-205">It accepts JSON numbers and numbers within quotes while deserializing.</span></span>

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/LongToStringConverter.cs)]

<span data-ttu-id="a3def-206">Registre este convertidor personalizado [mediante un atributo](system-text-json-converters-how-to.md#registration-sample---jsonconverter-on-a-property) en propiedades de `long` individuales o [agregando el convertidor](system-text-json-converters-how-to.md#registration-sample---converters-collection) a la colección de <xref:System.Text.Json.JsonSerializerOptions.Converters>.</span><span class="sxs-lookup"><span data-stu-id="a3def-206">Register this custom converter by [using an attribute](system-text-json-converters-how-to.md#registration-sample---jsonconverter-on-a-property) on individual `long` properties or by [adding the converter](system-text-json-converters-how-to.md#registration-sample---converters-collection) to the <xref:System.Text.Json.JsonSerializerOptions.Converters> collection.</span></span>

### <a name="dictionary-with-non-string-key"></a><span data-ttu-id="a3def-207">Diccionario con clave que no es de cadena</span><span class="sxs-lookup"><span data-stu-id="a3def-207">Dictionary with non-string key</span></span>

<span data-ttu-id="a3def-208">`Newtonsoft.Json` admite colecciones de tipo `Dictionary<TKey, TValue>`.</span><span class="sxs-lookup"><span data-stu-id="a3def-208">`Newtonsoft.Json` supports collections of type `Dictionary<TKey, TValue>`.</span></span> <span data-ttu-id="a3def-209">La compatibilidad integrada con colecciones de diccionario en <xref:System.Text.Json> se limita a `Dictionary<string, TValue>`.</span><span class="sxs-lookup"><span data-stu-id="a3def-209">The built-in support for dictionary collections in <xref:System.Text.Json> is limited to `Dictionary<string, TValue>`.</span></span> <span data-ttu-id="a3def-210">Es decir, la clave debe ser una cadena.</span><span class="sxs-lookup"><span data-stu-id="a3def-210">That is, the key must be a string.</span></span>

<span data-ttu-id="a3def-211">Para admitir un diccionario con un entero o algún otro tipo como clave, cree un convertidor como el ejemplo de [Cómo escribir convertidores personalizados](system-text-json-converters-how-to.md#support-dictionary-with-non-string-key).</span><span class="sxs-lookup"><span data-stu-id="a3def-211">To support a dictionary with an integer or some other type as the key, create a converter like the example in [How to write custom converters](system-text-json-converters-how-to.md#support-dictionary-with-non-string-key).</span></span>

### <a name="polymorphic-serialization"></a><span data-ttu-id="a3def-212">Serialización polimórfica</span><span class="sxs-lookup"><span data-stu-id="a3def-212">Polymorphic serialization</span></span>

<span data-ttu-id="a3def-213">`Newtonsoft.Json` realiza automáticamente la serialización polimórfica.</span><span class="sxs-lookup"><span data-stu-id="a3def-213">`Newtonsoft.Json` automatically does polymorphic serialization.</span></span> <span data-ttu-id="a3def-214">Para obtener información sobre las capacidades de serialización polimórficas limitadas de <xref:System.Text.Json>, vea [serializar propiedades de clases derivadas](system-text-json-how-to.md#serialize-properties-of-derived-classes).</span><span class="sxs-lookup"><span data-stu-id="a3def-214">For information about the limited polymorphic serialization capabilities of <xref:System.Text.Json>, see [Serialize properties of derived classes](system-text-json-how-to.md#serialize-properties-of-derived-classes).</span></span>

<span data-ttu-id="a3def-215">La solución descrita es para definir propiedades que pueden contener clases derivadas como `object`de tipo.</span><span class="sxs-lookup"><span data-stu-id="a3def-215">The workaround described there is to define properties that may contain derived classes as type `object`.</span></span> <span data-ttu-id="a3def-216">Si eso no es posible, otra opción es crear un convertidor con un método `Write` para toda la jerarquía de tipos de herencia, como en el ejemplo de [Cómo escribir convertidores personalizados](system-text-json-converters-how-to.md#support-polymorphic-deserialization).</span><span class="sxs-lookup"><span data-stu-id="a3def-216">If that isn't possible, another option is to create a converter with a `Write` method for the whole inheritance type hierarchy like the example in [How to write custom converters](system-text-json-converters-how-to.md#support-polymorphic-deserialization).</span></span>

### <a name="polymorphic-deserialization"></a><span data-ttu-id="a3def-217">Deserialización polimórfica</span><span class="sxs-lookup"><span data-stu-id="a3def-217">Polymorphic deserialization</span></span>

<span data-ttu-id="a3def-218">`Newtonsoft.Json` tiene un valor de `TypeNameHandling` que agrega metadatos de nombre de tipo al JSON durante la serialización.</span><span class="sxs-lookup"><span data-stu-id="a3def-218">`Newtonsoft.Json` has a `TypeNameHandling` setting that adds type name metadata to the JSON while serializing.</span></span> <span data-ttu-id="a3def-219">Usa los metadatos durante la deserialización para realizar la deserialización polimórfica.</span><span class="sxs-lookup"><span data-stu-id="a3def-219">It uses the metadata while deserializing to do polymorphic deserialization.</span></span> <span data-ttu-id="a3def-220"><xref:System.Text.Json> puede hacer una gama limitada de [serialización polimórfica](system-text-json-how-to.md#serialize-properties-of-derived-classes) , pero no la deserialización polimórfica.</span><span class="sxs-lookup"><span data-stu-id="a3def-220"><xref:System.Text.Json> can do a limited range of [polymorphic serialization](system-text-json-how-to.md#serialize-properties-of-derived-classes) but not polymorphic deserialization.</span></span>

<span data-ttu-id="a3def-221">Para admitir la deserialización polimórfica, cree un convertidor como el ejemplo de [Cómo escribir convertidores personalizados](system-text-json-converters-how-to.md#support-polymorphic-deserialization).</span><span class="sxs-lookup"><span data-stu-id="a3def-221">To support polymorphic deserialization, create a converter like the example in [How to write custom converters](system-text-json-converters-how-to.md#support-polymorphic-deserialization).</span></span>

### <a name="required-properties"></a><span data-ttu-id="a3def-222">Propiedades obligatorias</span><span class="sxs-lookup"><span data-stu-id="a3def-222">Required properties</span></span>

<span data-ttu-id="a3def-223">Durante la deserialización, <xref:System.Text.Json> no inicia una excepción si no se recibe ningún valor en el JSON para una de las propiedades del tipo de destino.</span><span class="sxs-lookup"><span data-stu-id="a3def-223">During deserialization, <xref:System.Text.Json> doesn't throw an exception if no value is received in the JSON for one of the properties of the target type.</span></span> <span data-ttu-id="a3def-224">Por ejemplo, si tiene una clase `WeatherForecast`:</span><span class="sxs-lookup"><span data-stu-id="a3def-224">For example, if you have a `WeatherForecast` class:</span></span>

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/WeatherForecast.cs?name=SnippetWF)]

<span data-ttu-id="a3def-225">El siguiente JSON se deserializa sin error:</span><span class="sxs-lookup"><span data-stu-id="a3def-225">The following JSON is deserialized without error:</span></span>

```json
{
    "TemperatureCelsius": 25,
    "Summary": "Hot"
}
```

<span data-ttu-id="a3def-226">Para que se produzca un error de deserialización si no hay `Date` propiedad en JSON, implemente un convertidor personalizado.</span><span class="sxs-lookup"><span data-stu-id="a3def-226">To make deserialization fail if no `Date` property is in the JSON, implement a custom converter.</span></span> <span data-ttu-id="a3def-227">El siguiente código de convertidor de ejemplo produce una excepción si la propiedad `Date` no se establece una vez completada la deserialización:</span><span class="sxs-lookup"><span data-stu-id="a3def-227">The following sample converter code throws an exception if the `Date` property isn't set after deserialization is complete:</span></span>

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/WeatherForecastRequiredPropertyConverter.cs)]

<span data-ttu-id="a3def-228">Registre este convertidor personalizado mediante [un atributo en la clase poco](system-text-json-converters-how-to.md#registration-sample---jsonconverter-on-a-type) o [agregando el convertidor](system-text-json-converters-how-to.md#registration-sample---converters-collection) a la colección de <xref:System.Text.Json.JsonSerializerOptions.Converters>.</span><span class="sxs-lookup"><span data-stu-id="a3def-228">Register this custom converter by [using an attribute on the POCO class](system-text-json-converters-how-to.md#registration-sample---jsonconverter-on-a-type) or by [adding the converter](system-text-json-converters-how-to.md#registration-sample---converters-collection) to the <xref:System.Text.Json.JsonSerializerOptions.Converters> collection.</span></span>

<span data-ttu-id="a3def-229">Si sigue este patrón, no pase el objeto de opciones cuando llame a <xref:System.Text.Json.JsonSerializer.Serialize%2A> o <xref:System.Text.Json.JsonSerializer.Deserialize%2A>de forma recursiva.</span><span class="sxs-lookup"><span data-stu-id="a3def-229">If you follow this pattern, don't pass in the options object when recursively calling <xref:System.Text.Json.JsonSerializer.Serialize%2A> or <xref:System.Text.Json.JsonSerializer.Deserialize%2A>.</span></span> <span data-ttu-id="a3def-230">El objeto de opciones contiene la colección de <xref:System.Text.Json.JsonSerializerOptions.Converters%2A>.</span><span class="sxs-lookup"><span data-stu-id="a3def-230">The options object contains the <xref:System.Text.Json.JsonSerializerOptions.Converters%2A> collection.</span></span> <span data-ttu-id="a3def-231">Si lo pasa a `Serialize` o `Deserialize`, el convertidor personalizado llama a sí mismo, con lo que se crea un bucle infinito que produce una excepción de desbordamiento de pila.</span><span class="sxs-lookup"><span data-stu-id="a3def-231">If you pass it in to `Serialize` or `Deserialize`, the custom converter calls into itself, making an infinite loop that results in a stack overflow exception.</span></span> <span data-ttu-id="a3def-232">Si las opciones predeterminadas no son factibles, cree una nueva instancia de las opciones con la configuración que necesite.</span><span class="sxs-lookup"><span data-stu-id="a3def-232">If the default options are not feasible, create a new instance of the options with the settings that you need.</span></span> <span data-ttu-id="a3def-233">Este enfoque será lento, ya que cada instancia nueva se almacena en caché de forma independiente.</span><span class="sxs-lookup"><span data-stu-id="a3def-233">This approach will be slow since each new instance caches independently.</span></span>

<span data-ttu-id="a3def-234">El código de convertidor anterior es un ejemplo simplificado.</span><span class="sxs-lookup"><span data-stu-id="a3def-234">The preceding converter code is a simplified example.</span></span> <span data-ttu-id="a3def-235">Se requeriría lógica adicional si necesita controlar atributos (como [[JsonIgnore]](xref:System.Text.Json.Serialization.JsonIgnoreAttribute) o diferentes opciones (como codificadores personalizados).</span><span class="sxs-lookup"><span data-stu-id="a3def-235">Additional logic would be required if you need to handle attributes (such as [[JsonIgnore]](xref:System.Text.Json.Serialization.JsonIgnoreAttribute) or different options (such as custom encoders).</span></span> <span data-ttu-id="a3def-236">Además, el código de ejemplo no controla las propiedades para las que se establece un valor predeterminado en el constructor.</span><span class="sxs-lookup"><span data-stu-id="a3def-236">Also, the example code doesn't handle properties for which a default value is set in the constructor.</span></span> <span data-ttu-id="a3def-237">Y este enfoque no distingue entre los siguientes escenarios:</span><span class="sxs-lookup"><span data-stu-id="a3def-237">And this approach doesn't differentiate between the following scenarios:</span></span>

* <span data-ttu-id="a3def-238">Falta una propiedad en el JSON.</span><span class="sxs-lookup"><span data-stu-id="a3def-238">A property is missing from the JSON.</span></span>
* <span data-ttu-id="a3def-239">Una propiedad para un tipo que no acepta valores NULL está presente en el JSON, pero el valor es el predeterminado para el tipo, como cero para un `int`.</span><span class="sxs-lookup"><span data-stu-id="a3def-239">A property for a non-nullable type is present in the JSON, but the value is the default for the type, such as zero for an `int`.</span></span>
* <span data-ttu-id="a3def-240">Una propiedad para un tipo que acepta valores NULL está presente en el JSON, pero el valor es NULL.</span><span class="sxs-lookup"><span data-stu-id="a3def-240">A property for a nullable type is present in the JSON, but the value is null.</span></span>

### <a name="deserialize-null-to-non-nullable-type"></a><span data-ttu-id="a3def-241">Deserializar null en un tipo que no acepta valores NULL</span><span class="sxs-lookup"><span data-stu-id="a3def-241">Deserialize null to non-nullable type</span></span> 

<span data-ttu-id="a3def-242">`Newtonsoft.Json` no produce una excepción en el escenario siguiente:</span><span class="sxs-lookup"><span data-stu-id="a3def-242">`Newtonsoft.Json` doesn't throw an exception in the following scenario:</span></span>

* <span data-ttu-id="a3def-243">`NullValueHandling` se establece en `Ignore`y</span><span class="sxs-lookup"><span data-stu-id="a3def-243">`NullValueHandling` is set to `Ignore`, and</span></span>
* <span data-ttu-id="a3def-244">Durante la deserialización, el archivo JSON contiene un valor null para un tipo que no acepta valores NULL.</span><span class="sxs-lookup"><span data-stu-id="a3def-244">During deserialization, the JSON contains a null value for a non-nullable type.</span></span>

<span data-ttu-id="a3def-245">En el mismo escenario, <xref:System.Text.Json> produce una excepción.</span><span class="sxs-lookup"><span data-stu-id="a3def-245">In the same scenario, <xref:System.Text.Json> does throw an exception.</span></span> <span data-ttu-id="a3def-246">(La configuración de control de valores NULL correspondiente es <xref:System.Text.Json.JsonSerializerOptions.IgnoreNullValues?displayProperty=nameWithType>).</span><span class="sxs-lookup"><span data-stu-id="a3def-246">(The corresponding null handling setting is <xref:System.Text.Json.JsonSerializerOptions.IgnoreNullValues?displayProperty=nameWithType>.)</span></span>

<span data-ttu-id="a3def-247">Si es el propietario del tipo de destino, la mejor solución es hacer que la propiedad en cuestión acepte valores NULL (por ejemplo, cambiar `int` a `int?`).</span><span class="sxs-lookup"><span data-stu-id="a3def-247">If you own the target type, the best workaround is to make the property in question nullable (for example, change `int` to `int?`).</span></span>

<span data-ttu-id="a3def-248">Otra solución consiste en crear un convertidor para el tipo, como en el ejemplo siguiente, en el que se tratan los valores NULL de `DateTimeOffset` tipos:</span><span class="sxs-lookup"><span data-stu-id="a3def-248">Another workaround is to make a converter for the type, such as the following example that handles null values for `DateTimeOffset` types:</span></span>

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/DateTimeOffsetNullHandlingConverter.cs)]

<span data-ttu-id="a3def-249">Registre este convertidor personalizado [mediante un atributo en la propiedad](system-text-json-converters-how-to.md#registration-sample---jsonconverter-on-a-property) o [agregando el convertidor](system-text-json-converters-how-to.md#registration-sample---converters-collection) a la colección de <xref:System.Text.Json.JsonSerializerOptions.Converters>.</span><span class="sxs-lookup"><span data-stu-id="a3def-249">Register this custom converter by [using an attribute on the property](system-text-json-converters-how-to.md#registration-sample---jsonconverter-on-a-property) or by [adding the converter](system-text-json-converters-how-to.md#registration-sample---converters-collection) to the <xref:System.Text.Json.JsonSerializerOptions.Converters> collection.</span></span>

<span data-ttu-id="a3def-250">**Nota:** El convertidor anterior **controla los valores NULL de forma distinta** a `Newtonsoft.Json` para poco que especifican valores predeterminados.</span><span class="sxs-lookup"><span data-stu-id="a3def-250">**Note:** The preceding converter **handles null values differently** than `Newtonsoft.Json` does for POCOs that specify default values.</span></span> <span data-ttu-id="a3def-251">Por ejemplo, supongamos que el código siguiente representa el objeto de destino:</span><span class="sxs-lookup"><span data-stu-id="a3def-251">For example, suppose the following code represents your target object:</span></span>

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/WeatherForecast.cs?name=SnippetWFWithDefault)]

<span data-ttu-id="a3def-252">Y Supongamos que el siguiente código JSON se deserializa con el convertidor anterior:</span><span class="sxs-lookup"><span data-stu-id="a3def-252">And suppose the following JSON is deserialized by using the preceding converter:</span></span>

```json
{
  "Date": null,
  "TemperatureCelsius": 25,
  "Summary": null
}
```

<span data-ttu-id="a3def-253">Después de la deserialización, la propiedad `Date` tiene 1/1/0001 (`default(DateTimeOffset)`), es decir, se sobrescribe el valor establecido en el constructor.</span><span class="sxs-lookup"><span data-stu-id="a3def-253">After deserialization, the `Date` property has 1/1/0001 (`default(DateTimeOffset)`), that is, the value set in the constructor is overwritten.</span></span> <span data-ttu-id="a3def-254">Dado el mismo POCO y JSON, `Newtonsoft.Json` deserialización dejaría 1/1/2001 en la propiedad `Date`.</span><span class="sxs-lookup"><span data-stu-id="a3def-254">Given the same POCO and JSON, `Newtonsoft.Json` deserialization would leave 1/1/2001 in the `Date` property.</span></span>

### <a name="deserialize-to-immutable-classes-and-structs"></a><span data-ttu-id="a3def-255">Deserializar en clases y Structs inmutables</span><span class="sxs-lookup"><span data-stu-id="a3def-255">Deserialize to immutable classes and structs</span></span>

<span data-ttu-id="a3def-256">`Newtonsoft.Json` se puede deserializar en clases y struct inmutables, ya que puede usar constructores que tengan parámetros.</span><span class="sxs-lookup"><span data-stu-id="a3def-256">`Newtonsoft.Json` can deserialize to immutable classes and structs because it can use constructors that have parameters.</span></span> <span data-ttu-id="a3def-257"><xref:System.Text.Json> solo admite constructores públicos sin parámetros.</span><span class="sxs-lookup"><span data-stu-id="a3def-257"><xref:System.Text.Json> supports only public parameterless constructors.</span></span> <span data-ttu-id="a3def-258">Como solución alternativa, puede llamar a un constructor con parámetros en un convertidor personalizado.</span><span class="sxs-lookup"><span data-stu-id="a3def-258">As a workaround, you can call a constructor with parameters in a custom converter.</span></span>

<span data-ttu-id="a3def-259">A continuación se muestra una estructura inmutable con varios parámetros de constructor:</span><span class="sxs-lookup"><span data-stu-id="a3def-259">Here's an immutable struct with multiple constructor parameters:</span></span>

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/ImmutablePoint.cs#ImmutablePoint)]

<span data-ttu-id="a3def-260">Y este es un convertidor que serializa y deserializa este struct:</span><span class="sxs-lookup"><span data-stu-id="a3def-260">And here's a converter that serializes and deserializes this struct:</span></span>

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/ImmutablePointConverter.cs)]

<span data-ttu-id="a3def-261">Registre este convertidor personalizado [agregando el convertidor](system-text-json-converters-how-to.md#registration-sample---converters-collection) a la colección de <xref:System.Text.Json.JsonSerializerOptions.Converters>.</span><span class="sxs-lookup"><span data-stu-id="a3def-261">Register this custom converter by [adding the converter](system-text-json-converters-how-to.md#registration-sample---converters-collection) to the <xref:System.Text.Json.JsonSerializerOptions.Converters> collection.</span></span>

<span data-ttu-id="a3def-262">Para obtener un ejemplo de un convertidor similar que controla las propiedades genéricas abiertas, vea el [convertidor integrado para los pares de clave y valor](https://github.com/dotnet/runtime/blob/81bf79fd9aa75305e55abe2f7e9ef3f60624a3a1/src/libraries/System.Text.Json/src/System/Text/Json/Serialization/Converters/JsonValueConverterKeyValuePair.cs).</span><span class="sxs-lookup"><span data-stu-id="a3def-262">For an example of a similar converter that handles open generic properties, see the [built-in converter for key-value pairs](https://github.com/dotnet/runtime/blob/81bf79fd9aa75305e55abe2f7e9ef3f60624a3a1/src/libraries/System.Text.Json/src/System/Text/Json/Serialization/Converters/JsonValueConverterKeyValuePair.cs).</span></span>

### <a name="specify-constructor-to-use"></a><span data-ttu-id="a3def-263">Especificar el constructor que se va a usar</span><span class="sxs-lookup"><span data-stu-id="a3def-263">Specify constructor to use</span></span>

<span data-ttu-id="a3def-264">El atributo `Newtonsoft.Json` `[JsonConstructor]` permite especificar el constructor al que se llamará al deserializar en un POCO.</span><span class="sxs-lookup"><span data-stu-id="a3def-264">The `Newtonsoft.Json` `[JsonConstructor]` attribute lets you specify which constructor to call when deserializing to a POCO.</span></span> <span data-ttu-id="a3def-265"><xref:System.Text.Json> solo admite constructores sin parámetros.</span><span class="sxs-lookup"><span data-stu-id="a3def-265"><xref:System.Text.Json> supports only parameterless constructors.</span></span> <span data-ttu-id="a3def-266">Como solución alternativa, puede llamar a cualquier constructor que necesite en un convertidor personalizado.</span><span class="sxs-lookup"><span data-stu-id="a3def-266">As a workaround, you can call whichever constructor you need in a custom converter.</span></span> <span data-ttu-id="a3def-267">Vea el ejemplo de [deserialización en clases y Structs inmutables](#deserialize-to-immutable-classes-and-structs).</span><span class="sxs-lookup"><span data-stu-id="a3def-267">See the example for [Deserialize to immutable classes and structs](#deserialize-to-immutable-classes-and-structs).</span></span>

### <a name="conditionally-ignore-a-property"></a><span data-ttu-id="a3def-268">Omitir condicionalmente una propiedad</span><span class="sxs-lookup"><span data-stu-id="a3def-268">Conditionally ignore a property</span></span>

<span data-ttu-id="a3def-269">`Newtonsoft.Json` tiene varias maneras de omitir condicionalmente una propiedad en la serialización o deserialización:</span><span class="sxs-lookup"><span data-stu-id="a3def-269">`Newtonsoft.Json` has several ways to conditionally ignore a property on serialization or deserialization:</span></span>

* <span data-ttu-id="a3def-270">`DefaultContractResolver` permite seleccionar las propiedades que se van a incluir o excluir, en función de criterios arbitrarios.</span><span class="sxs-lookup"><span data-stu-id="a3def-270">`DefaultContractResolver` lets you select properties to include or exclude, based on arbitrary criteria.</span></span> 
* <span data-ttu-id="a3def-271">La configuración de `NullValueHandling` y `DefaultValueHandling` de `JsonSerializerSettings` permite especificar que se deben omitir todas las propiedades de valores NULL o valores predeterminados.</span><span class="sxs-lookup"><span data-stu-id="a3def-271">The `NullValueHandling` and `DefaultValueHandling` settings on `JsonSerializerSettings` let you specify that all null-value or default-value properties should be ignored.</span></span>
* <span data-ttu-id="a3def-272">La configuración de `NullValueHandling` y `DefaultValueHandling` en el atributo `[JsonProperty]` permite especificar propiedades individuales que se deben omitir cuando se establece en null o en el valor predeterminado.</span><span class="sxs-lookup"><span data-stu-id="a3def-272">The `NullValueHandling` and `DefaultValueHandling` settings on the `[JsonProperty]` attribute let you specify individual properties that should be ignored when set to null or the default value.</span></span>

<span data-ttu-id="a3def-273"><xref:System.Text.Json> proporciona las siguientes formas de omitir las propiedades durante la serialización:</span><span class="sxs-lookup"><span data-stu-id="a3def-273"><xref:System.Text.Json> provides the following ways to omit properties while serializing:</span></span>

* <span data-ttu-id="a3def-274">El atributo [[JsonIgnore]](system-text-json-how-to.md#exclude-individual-properties) de una propiedad hace que la propiedad se omita del JSON durante la serialización.</span><span class="sxs-lookup"><span data-stu-id="a3def-274">The [[JsonIgnore]](system-text-json-how-to.md#exclude-individual-properties) attribute on a property causes the property to be omitted from the JSON during serialization.</span></span>
* <span data-ttu-id="a3def-275">La opción global [IgnoreNullValues](system-text-json-how-to.md#exclude-all-null-value-properties) permite excluir todas las propiedades de valores NULL.</span><span class="sxs-lookup"><span data-stu-id="a3def-275">The [IgnoreNullValues](system-text-json-how-to.md#exclude-all-null-value-properties) global option lets you exclude all null-value properties.</span></span>
* <span data-ttu-id="a3def-276">La opción global [IgnoreReadOnlyProperties](system-text-json-how-to.md#exclude-all-read-only-properties) permite excluir todas las propiedades de solo lectura.</span><span class="sxs-lookup"><span data-stu-id="a3def-276">The [IgnoreReadOnlyProperties](system-text-json-how-to.md#exclude-all-read-only-properties) global option lets you exclude all read-only properties.</span></span>

<span data-ttu-id="a3def-277">Estas opciones **no** le permiten:</span><span class="sxs-lookup"><span data-stu-id="a3def-277">These options **don't** let you:</span></span>

* <span data-ttu-id="a3def-278">Omitir todas las propiedades que tienen el valor predeterminado para el tipo.</span><span class="sxs-lookup"><span data-stu-id="a3def-278">Ignore all properties that have the default value for the type.</span></span>
* <span data-ttu-id="a3def-279">Omitir las propiedades seleccionadas que tienen el valor predeterminado para el tipo.</span><span class="sxs-lookup"><span data-stu-id="a3def-279">Ignore selected properties that have the default value for the type.</span></span>
* <span data-ttu-id="a3def-280">Omitir las propiedades seleccionadas si su valor es NULL.</span><span class="sxs-lookup"><span data-stu-id="a3def-280">Ignore selected properties if their value is null.</span></span>
* <span data-ttu-id="a3def-281">Omitir las propiedades seleccionadas en función de criterios arbitrarios evaluados en tiempo de ejecución.</span><span class="sxs-lookup"><span data-stu-id="a3def-281">Ignore selected properties based on arbitrary criteria evaluated at run time.</span></span> 

<span data-ttu-id="a3def-282">Para esa funcionalidad, puede escribir un convertidor personalizado.</span><span class="sxs-lookup"><span data-stu-id="a3def-282">For that functionality, you can write a custom converter.</span></span> <span data-ttu-id="a3def-283">Este es un ejemplo POCO y un convertidor personalizado para ello que ilustra este enfoque:</span><span class="sxs-lookup"><span data-stu-id="a3def-283">Here's a sample POCO and a custom converter for it that illustrates this approach:</span></span>

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/WeatherForecast.cs?name=SnippetWF)]

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/WeatherForecastRuntimeIgnoreConverter.cs)]

<span data-ttu-id="a3def-284">El convertidor hace que se omita la propiedad `Summary` de la serialización si su valor es null, una cadena vacía o "N/A".</span><span class="sxs-lookup"><span data-stu-id="a3def-284">The converter causes the `Summary` property to be omitted from serialization if its value is null, an empty string, or "N/A".</span></span> 

<span data-ttu-id="a3def-285">Registre este convertidor personalizado [mediante un atributo en la clase](system-text-json-converters-how-to.md#registration-sample---jsonconverter-on-a-type) o [agregando el convertidor](system-text-json-converters-how-to.md#registration-sample---converters-collection) a la colección de <xref:System.Text.Json.JsonSerializerOptions.Converters>.</span><span class="sxs-lookup"><span data-stu-id="a3def-285">Register this custom converter by [using an attribute on the class](system-text-json-converters-how-to.md#registration-sample---jsonconverter-on-a-type) or by [adding the converter](system-text-json-converters-how-to.md#registration-sample---converters-collection) to the <xref:System.Text.Json.JsonSerializerOptions.Converters> collection.</span></span>

<span data-ttu-id="a3def-286">Este enfoque requiere lógica adicional si:</span><span class="sxs-lookup"><span data-stu-id="a3def-286">This approach requires additional logic if:</span></span>

* <span data-ttu-id="a3def-287">El POCO incluye propiedades complejas.</span><span class="sxs-lookup"><span data-stu-id="a3def-287">The POCO includes complex properties.</span></span>
* <span data-ttu-id="a3def-288">Debe controlar atributos como `[JsonIgnore]` o opciones como los codificadores personalizados.</span><span class="sxs-lookup"><span data-stu-id="a3def-288">You need to handle attributes such as `[JsonIgnore]` or options such as custom encoders.</span></span>

### <a name="callbacks"></a><span data-ttu-id="a3def-289">Devoluciones</span><span class="sxs-lookup"><span data-stu-id="a3def-289">Callbacks</span></span>

<span data-ttu-id="a3def-290">`Newtonsoft.Json` permite ejecutar código personalizado en varios puntos del proceso de serialización o deserialización:</span><span class="sxs-lookup"><span data-stu-id="a3def-290">`Newtonsoft.Json` lets you execute custom code at several points in the serialization or deserialization process:</span></span>

* <span data-ttu-id="a3def-291">OnDeserializing (al empezar a deserializar un objeto)</span><span class="sxs-lookup"><span data-stu-id="a3def-291">OnDeserializing (when beginning to deserialize an object)</span></span>
* <span data-ttu-id="a3def-292">OnDeserialized (cuando finalizó la deserialización de un objeto)</span><span class="sxs-lookup"><span data-stu-id="a3def-292">OnDeserialized (when finished deserializing an object)</span></span>
* <span data-ttu-id="a3def-293">Serialización (al principio de serializar un objeto)</span><span class="sxs-lookup"><span data-stu-id="a3def-293">OnSerializing (when beginning to serialize an object)</span></span>
* <span data-ttu-id="a3def-294">OnSerialized (al finalizar la serialización de un objeto)</span><span class="sxs-lookup"><span data-stu-id="a3def-294">OnSerialized (when finished serializing an object)</span></span>

<span data-ttu-id="a3def-295">En <xref:System.Text.Json>, puede simular devoluciones de llamada escribiendo un convertidor personalizado.</span><span class="sxs-lookup"><span data-stu-id="a3def-295">In <xref:System.Text.Json>, you can simulate callbacks by writing a custom converter.</span></span> <span data-ttu-id="a3def-296">En el ejemplo siguiente se muestra un convertidor personalizado para un POCO.</span><span class="sxs-lookup"><span data-stu-id="a3def-296">The following example shows a custom converter for a POCO.</span></span> <span data-ttu-id="a3def-297">El convertidor incluye código que muestra un mensaje en cada punto que corresponde a una devolución de llamada de `Newtonsoft.Json`.</span><span class="sxs-lookup"><span data-stu-id="a3def-297">The converter includes code that displays a message at each point that corresponds to a `Newtonsoft.Json` callback.</span></span>

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/WeatherForecastCallbacksConverter.cs)]

<span data-ttu-id="a3def-298">Registre este convertidor personalizado [mediante un atributo en la clase](system-text-json-converters-how-to.md#registration-sample---jsonconverter-on-a-type) o [agregando el convertidor](system-text-json-converters-how-to.md#registration-sample---converters-collection) a la colección de <xref:System.Text.Json.JsonSerializerOptions.Converters>.</span><span class="sxs-lookup"><span data-stu-id="a3def-298">Register this custom converter by [using an attribute on the class](system-text-json-converters-how-to.md#registration-sample---jsonconverter-on-a-type) or by [adding the converter](system-text-json-converters-how-to.md#registration-sample---converters-collection) to the <xref:System.Text.Json.JsonSerializerOptions.Converters> collection.</span></span>

<span data-ttu-id="a3def-299">Si usa un convertidor personalizado que sigue el ejemplo anterior:</span><span class="sxs-lookup"><span data-stu-id="a3def-299">If you use a custom converter that follows the preceding sample:</span></span>

* <span data-ttu-id="a3def-300">El código `OnDeserializing` no tiene acceso a la nueva instancia POCO.</span><span class="sxs-lookup"><span data-stu-id="a3def-300">The `OnDeserializing` code doesn't have access to the new POCO instance.</span></span> <span data-ttu-id="a3def-301">Para manipular la nueva instancia POCO al inicio de la deserialización, coloque ese código en el constructor POCO.</span><span class="sxs-lookup"><span data-stu-id="a3def-301">To manipulate the new POCO instance at the start of deserialization, put that code in the POCO constructor.</span></span>
* <span data-ttu-id="a3def-302">No pase el objeto de opciones cuando llame a `Serialize` o `Deserialize`de forma recursiva.</span><span class="sxs-lookup"><span data-stu-id="a3def-302">Don't pass in the options object when recursively calling `Serialize` or `Deserialize`.</span></span> <span data-ttu-id="a3def-303">El objeto de opciones contiene la colección de `Converters`.</span><span class="sxs-lookup"><span data-stu-id="a3def-303">The options object contains the `Converters` collection.</span></span> <span data-ttu-id="a3def-304">Si lo pasa a `Serialize` o `Deserialize`, se usará el convertidor, con lo que se crea un bucle infinito que produce una excepción de desbordamiento de pila.</span><span class="sxs-lookup"><span data-stu-id="a3def-304">If you pass it in to `Serialize` or `Deserialize`, the converter will be used, making an infinite loop that results in a stack overflow exception.</span></span>

## <a name="scenarios-that-jsonserializer-currently-doesnt-support"></a><span data-ttu-id="a3def-305">Escenarios en los que JsonSerializer no admite actualmente</span><span class="sxs-lookup"><span data-stu-id="a3def-305">Scenarios that JsonSerializer currently doesn't support</span></span>

<span data-ttu-id="a3def-306">Las soluciones alternativas son posibles para los escenarios siguientes, pero algunas de ellas serían relativamente difíciles de implementar.</span><span class="sxs-lookup"><span data-stu-id="a3def-306">Workarounds are possible for the following scenarios, but some of them would be relatively difficult to implement.</span></span> <span data-ttu-id="a3def-307">En este artículo no se proporcionan ejemplos de código para soluciones alternativas para estos escenarios.</span><span class="sxs-lookup"><span data-stu-id="a3def-307">This article doesn't provide code samples for workarounds for these scenarios.</span></span>

<span data-ttu-id="a3def-308">Esta no es una lista exhaustiva de las características de `Newtonsoft.Json` que no tienen ningún equivalente en `System.Text.Json`.</span><span class="sxs-lookup"><span data-stu-id="a3def-308">This is not an exhaustive list of `Newtonsoft.Json` features that have no equivalents in `System.Text.Json`.</span></span> <span data-ttu-id="a3def-309">La lista incluye muchos de los escenarios que se han solicitado en [problemas de github](https://github.com/dotnet/runtime/issues?q=is%3Aopen+is%3Aissue+label%3Aarea-System.Text.Json) o publicaciones de [stackoverflow](https://stackoverflow.com/questions/tagged/system.text.json) .</span><span class="sxs-lookup"><span data-stu-id="a3def-309">The list includes many of the scenarios that have been requested in [GitHub issues](https://github.com/dotnet/runtime/issues?q=is%3Aopen+is%3Aissue+label%3Aarea-System.Text.Json) or [StackOverflow](https://stackoverflow.com/questions/tagged/system.text.json) posts.</span></span>

<span data-ttu-id="a3def-310">Si implementa una solución alternativa para uno de estos escenarios y puede compartir el código, seleccione el botón "**esta página**" situado en la parte inferior de la página.</span><span class="sxs-lookup"><span data-stu-id="a3def-310">If you implement a workaround for one of these scenarios and can share the code, select the "**This page**" button at the bottom of the page.</span></span> <span data-ttu-id="a3def-311">Esto crea un problema de GitHub y lo agrega a los problemas que se enumeran en la parte inferior de la página.</span><span class="sxs-lookup"><span data-stu-id="a3def-311">That creates a GitHub issue and adds it to the issues that are listed at the bottom of the page.</span></span>

### <a name="types-without-built-in-support"></a><span data-ttu-id="a3def-312">Tipos sin compatibilidad integrada</span><span class="sxs-lookup"><span data-stu-id="a3def-312">Types without built-in support</span></span>

<span data-ttu-id="a3def-313"><xref:System.Text.Json> no proporciona compatibilidad integrada para los siguientes tipos:</span><span class="sxs-lookup"><span data-stu-id="a3def-313"><xref:System.Text.Json> doesn't provide built-in support for the following types:</span></span>

* <span data-ttu-id="a3def-314"><xref:System.Data.DataTable> y tipos relacionados</span><span class="sxs-lookup"><span data-stu-id="a3def-314"><xref:System.Data.DataTable> and related types</span></span>
* <span data-ttu-id="a3def-315">F#tipos, como [uniones discriminadas](../../fsharp/language-reference/discriminated-unions.md), [tipos de registros](../../fsharp/language-reference/records.md)y [tipos de registros anónimos](../../fsharp/language-reference/anonymous-records.md).</span><span class="sxs-lookup"><span data-stu-id="a3def-315">F# types, such as [discriminated unions](../../fsharp/language-reference/discriminated-unions.md), [record types](../../fsharp/language-reference/records.md), and [anonymous record types](../../fsharp/language-reference/anonymous-records.md).</span></span>
* <span data-ttu-id="a3def-316">Tipos de colección en el espacio de nombres <xref:System.Collections.Specialized></span><span class="sxs-lookup"><span data-stu-id="a3def-316">Collection types in the <xref:System.Collections.Specialized> namespace</span></span>
* <xref:System.Dynamic.ExpandoObject>
* <xref:System.TimeZoneInfo>
* <xref:System.Numerics.BigInteger>
* <xref:System.TimeSpan>
* <xref:System.DBNull>
* <xref:System.Type>
* <span data-ttu-id="a3def-317"><xref:System.ValueTuple> y sus tipos genéricos asociados</span><span class="sxs-lookup"><span data-stu-id="a3def-317"><xref:System.ValueTuple> and its associated generic types</span></span>

<span data-ttu-id="a3def-318">Se pueden implementar convertidores personalizados para tipos que no tienen compatibilidad integrada.</span><span class="sxs-lookup"><span data-stu-id="a3def-318">Custom converters can be implemented for types that don't have built-in support.</span></span>

### <a name="public-and-non-public-fields"></a><span data-ttu-id="a3def-319">Campos públicos y no públicos</span><span class="sxs-lookup"><span data-stu-id="a3def-319">Public and non-public fields</span></span>

<span data-ttu-id="a3def-320">`Newtonsoft.Json` puede serializar y deserializar los campos, así como las propiedades.</span><span class="sxs-lookup"><span data-stu-id="a3def-320">`Newtonsoft.Json` can serialize and deserialize fields as well as properties.</span></span> <span data-ttu-id="a3def-321"><xref:System.Text.Json> solo funciona con propiedades públicas.</span><span class="sxs-lookup"><span data-stu-id="a3def-321"><xref:System.Text.Json> only works with public properties.</span></span> <span data-ttu-id="a3def-322">Los convertidores personalizados pueden proporcionar esta funcionalidad.</span><span class="sxs-lookup"><span data-stu-id="a3def-322">Custom converters can provide this functionality.</span></span>

### <a name="internal-and-private-property-setters-and-getters"></a><span data-ttu-id="a3def-323">Establecedores de propiedades internos y privados y captadores</span><span class="sxs-lookup"><span data-stu-id="a3def-323">Internal and private property setters and getters</span></span>

<span data-ttu-id="a3def-324">`Newtonsoft.Json` puede usar los establecedores de propiedades privados e internos y los captadores a través del atributo `JsonProperty`.</span><span class="sxs-lookup"><span data-stu-id="a3def-324">`Newtonsoft.Json` can use private and internal property setters and getters via the `JsonProperty` attribute.</span></span> <span data-ttu-id="a3def-325"><xref:System.Text.Json> solo admite establecedores públicos.</span><span class="sxs-lookup"><span data-stu-id="a3def-325"><xref:System.Text.Json> supports only public setters.</span></span> <span data-ttu-id="a3def-326">Los convertidores personalizados pueden proporcionar esta funcionalidad.</span><span class="sxs-lookup"><span data-stu-id="a3def-326">Custom converters can provide this functionality.</span></span>

### <a name="preserve-object-references-and-handle-loops"></a><span data-ttu-id="a3def-327">Conservar referencias a objetos y procesar bucles</span><span class="sxs-lookup"><span data-stu-id="a3def-327">Preserve object references and handle loops</span></span>

<span data-ttu-id="a3def-328">De forma predeterminada, `Newtonsoft.Json` serializa por valor.</span><span class="sxs-lookup"><span data-stu-id="a3def-328">By default, `Newtonsoft.Json` serializes by value.</span></span> <span data-ttu-id="a3def-329">Por ejemplo, si un objeto contiene dos propiedades que contienen una referencia al mismo `Person` objeto, los valores de ese `Person` propiedades de objeto se duplican en JSON.</span><span class="sxs-lookup"><span data-stu-id="a3def-329">For example, if an object contains two properties that contain a reference to the same `Person` object, the values of that `Person` object's properties are duplicated in the JSON.</span></span>

<span data-ttu-id="a3def-330">`Newtonsoft.Json` tiene un valor `PreserveReferencesHandling` en `JsonSerializerSettings` que le permite serializar por referencia:</span><span class="sxs-lookup"><span data-stu-id="a3def-330">`Newtonsoft.Json` has a `PreserveReferencesHandling` setting on `JsonSerializerSettings` that lets you serialize by reference:</span></span>

* <span data-ttu-id="a3def-331">Los metadatos de un identificador se agregan al JSON creado para el primer `Person` objeto.</span><span class="sxs-lookup"><span data-stu-id="a3def-331">An identifier metadata is added to the JSON created for the first `Person` object.</span></span>
* <span data-ttu-id="a3def-332">El JSON que se crea para el segundo objeto `Person` contiene una referencia a ese identificador en lugar de los valores de propiedad.</span><span class="sxs-lookup"><span data-stu-id="a3def-332">The JSON that is created for the second `Person` object contains a reference to that identifier instead of property values.</span></span>

<span data-ttu-id="a3def-333">`Newtonsoft.Json` también tiene un valor `ReferenceLoopHandling` que le permite omitir las referencias circulares en lugar de producir una excepción.</span><span class="sxs-lookup"><span data-stu-id="a3def-333">`Newtonsoft.Json` also has a `ReferenceLoopHandling` setting that lets you ignore circular references rather than throw an exception.</span></span>

<span data-ttu-id="a3def-334"><xref:System.Text.Json> solo admite la serialización por valor y produce una excepción para las referencias circulares.</span><span class="sxs-lookup"><span data-stu-id="a3def-334"><xref:System.Text.Json> only supports serialization by value and throws an exception for circular references.</span></span>

### <a name="systemruntimeserialization-attributes"></a><span data-ttu-id="a3def-335">System. Runtime. Serialization (atributos)</span><span class="sxs-lookup"><span data-stu-id="a3def-335">System.Runtime.Serialization attributes</span></span>

<span data-ttu-id="a3def-336"><xref:System.Text.Json> no admite atributos del espacio de nombres `System.Runtime.Serialization`, como `DataMemberAttribute` y `IgnoreDataMemberAttribute`.</span><span class="sxs-lookup"><span data-stu-id="a3def-336"><xref:System.Text.Json> doesn't support attributes from the `System.Runtime.Serialization` namespace, such as `DataMemberAttribute` and `IgnoreDataMemberAttribute`.</span></span>

### <a name="octal-numbers"></a><span data-ttu-id="a3def-337">Números octales</span><span class="sxs-lookup"><span data-stu-id="a3def-337">Octal numbers</span></span>

<span data-ttu-id="a3def-338">`Newtonsoft.Json` trata los números con un cero a la izquierda como números octales.</span><span class="sxs-lookup"><span data-stu-id="a3def-338">`Newtonsoft.Json` treats numbers with a leading zero as octal numbers.</span></span> <span data-ttu-id="a3def-339"><xref:System.Text.Json> no permite ceros iniciales porque la especificación [RFC 8259](https://tools.ietf.org/html/rfc8259) no los permite.</span><span class="sxs-lookup"><span data-stu-id="a3def-339"><xref:System.Text.Json> doesn't allow leading zeroes because the [RFC 8259](https://tools.ietf.org/html/rfc8259) specification doesn't allow them.</span></span>

### <a name="populate-existing-objects"></a><span data-ttu-id="a3def-340">Rellenar objetos existentes</span><span class="sxs-lookup"><span data-stu-id="a3def-340">Populate existing objects</span></span>

<span data-ttu-id="a3def-341">El método `JsonConvert.PopulateObject` de `Newtonsoft.Json` deserializa un documento JSON a una instancia existente de una clase, en lugar de crear una nueva instancia.</span><span class="sxs-lookup"><span data-stu-id="a3def-341">The `JsonConvert.PopulateObject` method in `Newtonsoft.Json` deserializes a JSON document to an existing instance of a class, instead of creating a new instance.</span></span> <span data-ttu-id="a3def-342"><xref:System.Text.Json> crea siempre una nueva instancia del tipo de destino mediante el constructor sin parámetros público predeterminado.</span><span class="sxs-lookup"><span data-stu-id="a3def-342"><xref:System.Text.Json> always creates a new instance of the target type by using the default public parameterless constructor.</span></span> <span data-ttu-id="a3def-343">Los convertidores personalizados se pueden deserializar en una instancia existente.</span><span class="sxs-lookup"><span data-stu-id="a3def-343">Custom converters can deserialize to an existing instance.</span></span>

### <a name="reuse-rather-than-replace-properties"></a><span data-ttu-id="a3def-344">Reutilización en lugar de reemplazar propiedades</span><span class="sxs-lookup"><span data-stu-id="a3def-344">Reuse rather than replace properties</span></span>

<span data-ttu-id="a3def-345">La configuración de `ObjectCreationHandling` de `Newtonsoft.Json` permite especificar que los objetos de las propiedades deben reutilizarse en lugar de reemplazarse durante la deserialización.</span><span class="sxs-lookup"><span data-stu-id="a3def-345">The `Newtonsoft.Json` `ObjectCreationHandling` setting lets you specify that objects in properties should be reused rather than replaced during deserialization.</span></span> <span data-ttu-id="a3def-346"><xref:System.Text.Json> siempre reemplaza los objetos en las propiedades.</span><span class="sxs-lookup"><span data-stu-id="a3def-346"><xref:System.Text.Json> always replaces objects in properties.</span></span>  <span data-ttu-id="a3def-347">Los convertidores personalizados pueden proporcionar esta funcionalidad.</span><span class="sxs-lookup"><span data-stu-id="a3def-347">Custom converters can provide this functionality.</span></span>

### <a name="add-to-collections-without-setters"></a><span data-ttu-id="a3def-348">Agregar a colecciones sin establecedores</span><span class="sxs-lookup"><span data-stu-id="a3def-348">Add to collections without setters</span></span>

<span data-ttu-id="a3def-349">Durante la deserialización, `Newtonsoft.Json` agrega objetos a una colección incluso si la propiedad no tiene ningún establecedor.</span><span class="sxs-lookup"><span data-stu-id="a3def-349">During deserialization, `Newtonsoft.Json` adds objects to a collection even if the property has no setter.</span></span> <span data-ttu-id="a3def-350"><xref:System.Text.Json> omite las propiedades que no tienen establecedores.</span><span class="sxs-lookup"><span data-stu-id="a3def-350"><xref:System.Text.Json> ignores properties that don't have setters.</span></span> <span data-ttu-id="a3def-351">Los convertidores personalizados pueden proporcionar esta funcionalidad.</span><span class="sxs-lookup"><span data-stu-id="a3def-351">Custom converters can provide this functionality.</span></span>

### <a name="missingmemberhandling"></a><span data-ttu-id="a3def-352">MissingMemberHandling</span><span class="sxs-lookup"><span data-stu-id="a3def-352">MissingMemberHandling</span></span>

<span data-ttu-id="a3def-353">`Newtonsoft.Json` se pueden configurar para producir excepciones durante la deserialización si el archivo JSON incluye propiedades que faltan en el tipo de destino.</span><span class="sxs-lookup"><span data-stu-id="a3def-353">`Newtonsoft.Json` can be configured to throw exceptions during deserialization if the JSON includes properties that are missing in the target type.</span></span> <span data-ttu-id="a3def-354"><xref:System.Text.Json> omite las propiedades adicionales en el archivo JSON, excepto cuando se usa el [atributo [JsonExtensionData]](system-text-json-how-to.md#handle-overflow-json).</span><span class="sxs-lookup"><span data-stu-id="a3def-354"><xref:System.Text.Json> ignores extra properties in the JSON, except when you use the [[JsonExtensionData] attribute](system-text-json-how-to.md#handle-overflow-json).</span></span> <span data-ttu-id="a3def-355">No hay ninguna solución alternativa para la característica de miembro que falta.</span><span class="sxs-lookup"><span data-stu-id="a3def-355">There's no workaround for the missing member feature.</span></span>

### <a name="tracewriter"></a><span data-ttu-id="a3def-356">TraceWriter</span><span class="sxs-lookup"><span data-stu-id="a3def-356">TraceWriter</span></span>

<span data-ttu-id="a3def-357">`Newtonsoft.Json` le permite depurar mediante el uso de un `TraceWriter` para ver los registros generados por serialización o deserialización.</span><span class="sxs-lookup"><span data-stu-id="a3def-357">`Newtonsoft.Json` lets you debug by using a `TraceWriter` to view logs that are generated by serialization or deserialization.</span></span> <span data-ttu-id="a3def-358"><xref:System.Text.Json> no realiza el registro.</span><span class="sxs-lookup"><span data-stu-id="a3def-358"><xref:System.Text.Json> doesn't do logging.</span></span>

## <a name="jsondocument-and-jsonelement-compared-to-jtoken-like-jobject-jarray"></a><span data-ttu-id="a3def-359">JsonDocument y JsonElement comparado con JToken (como JObject, JArray)</span><span class="sxs-lookup"><span data-stu-id="a3def-359">JsonDocument and JsonElement compared to JToken (like JObject, JArray)</span></span>

<span data-ttu-id="a3def-360"><xref:System.Text.Json.JsonDocument?displayProperty=fullName> proporciona la capacidad de analizar y compilar un Document Object Model (DOM) de **solo lectura** a partir de cargas JSON existentes.</span><span class="sxs-lookup"><span data-stu-id="a3def-360"><xref:System.Text.Json.JsonDocument?displayProperty=fullName> provides the ability to parse and build a **read-only** Document Object Model (DOM) from existing JSON payloads.</span></span> <span data-ttu-id="a3def-361">DOM proporciona acceso aleatorio a los datos en una carga JSON.</span><span class="sxs-lookup"><span data-stu-id="a3def-361">The DOM provides random access to data in a JSON payload.</span></span> <span data-ttu-id="a3def-362">Se puede tener acceso a los elementos JSON que componen la carga a través del tipo de <xref:System.Text.Json.JsonElement>.</span><span class="sxs-lookup"><span data-stu-id="a3def-362">The JSON elements that compose the payload can be accessed via the <xref:System.Text.Json.JsonElement> type.</span></span> <span data-ttu-id="a3def-363">El tipo de `JsonElement` proporciona las API para convertir texto JSON en tipos comunes de .NET.</span><span class="sxs-lookup"><span data-stu-id="a3def-363">The `JsonElement` type provides APIs to convert JSON text to common .NET types.</span></span> <span data-ttu-id="a3def-364">`JsonDocument` expone una propiedad <xref:System.Text.Json.JsonDocument.RootElement>.</span><span class="sxs-lookup"><span data-stu-id="a3def-364">`JsonDocument` exposes a <xref:System.Text.Json.JsonDocument.RootElement> property.</span></span>

### <a name="jsondocument-is-idisposable"></a><span data-ttu-id="a3def-365">JsonDocument es IDisposable</span><span class="sxs-lookup"><span data-stu-id="a3def-365">JsonDocument is IDisposable</span></span>

<span data-ttu-id="a3def-366">`JsonDocument` crea una vista en memoria de los datos en un búfer agrupado.</span><span class="sxs-lookup"><span data-stu-id="a3def-366">`JsonDocument` builds an in-memory view of the data into a pooled buffer.</span></span> <span data-ttu-id="a3def-367">Por lo tanto, a diferencia de `JObject` o `JArray` de `Newtonsoft.Json`, el tipo de `JsonDocument` implementa `IDisposable` y debe usarse dentro de un bloque using.</span><span class="sxs-lookup"><span data-stu-id="a3def-367">Therefore, unlike `JObject` or `JArray` from `Newtonsoft.Json`, the `JsonDocument` type implements `IDisposable` and needs to be used inside a using block.</span></span> 

<span data-ttu-id="a3def-368">Solo devuelve un `JsonDocument` de la API si quiere transferir la propiedad de la duración y eliminar la responsabilidad del autor de la llamada.</span><span class="sxs-lookup"><span data-stu-id="a3def-368">Only return a `JsonDocument` from your API if you want to transfer lifetime ownership and dispose responsibility to the caller.</span></span> <span data-ttu-id="a3def-369">En la mayoría de los escenarios, eso no es necesario.</span><span class="sxs-lookup"><span data-stu-id="a3def-369">In most scenarios, that isn't necessary.</span></span> <span data-ttu-id="a3def-370">Si el autor de la llamada necesita trabajar con todo el documento JSON, devuelva el <xref:System.Text.Json.JsonElement.Clone%2A> del <xref:System.Text.Json.JsonDocument.RootElement%2A>, que es una <xref:System.Text.Json.JsonElement>.</span><span class="sxs-lookup"><span data-stu-id="a3def-370">If the caller needs to work with the entire JSON document, return the <xref:System.Text.Json.JsonElement.Clone%2A> of the <xref:System.Text.Json.JsonDocument.RootElement%2A>, which is a <xref:System.Text.Json.JsonElement>.</span></span> <span data-ttu-id="a3def-371">Si el autor de la llamada necesita trabajar con un elemento determinado dentro del documento JSON, devuelva el <xref:System.Text.Json.JsonElement.Clone%2A> de ese <xref:System.Text.Json.JsonElement>.</span><span class="sxs-lookup"><span data-stu-id="a3def-371">If the caller needs to work with a particular element within the JSON document, return the <xref:System.Text.Json.JsonElement.Clone%2A> of that <xref:System.Text.Json.JsonElement>.</span></span> <span data-ttu-id="a3def-372">Si devuelve el `RootElement` o un subelemento directamente sin realizar un `Clone`, el autor de la llamada no podrá tener acceso al `JsonElement` devuelto después de que se elimine el `JsonDocument` que posee el objeto.</span><span class="sxs-lookup"><span data-stu-id="a3def-372">If you return the `RootElement` or a sub-element directly without making a `Clone`, the caller won't be able to access the returned `JsonElement` after the `JsonDocument` that owns it is disposed.</span></span>

<span data-ttu-id="a3def-373">A continuación se muestra un ejemplo que requiere la creación de un `Clone`:</span><span class="sxs-lookup"><span data-stu-id="a3def-373">Here's an example that requires you to make a `Clone`:</span></span>

```csharp
public JsonElement LookAndLoad(JsonElement source)
{
    string json = File.ReadAllText(source.GetProperty("fileName").GetString());
   
    using (JsonDocument doc = JsonDocument.Parse(json))
    {
        return doc.RootElement.Clone();
    }
}
```

<span data-ttu-id="a3def-374">El código anterior espera una `JsonElement` que contiene una propiedad `fileName`.</span><span class="sxs-lookup"><span data-stu-id="a3def-374">The preceding code expects a `JsonElement` that contains a `fileName` property.</span></span> <span data-ttu-id="a3def-375">Se abre el archivo JSON y se crea un `JsonDocument`.</span><span class="sxs-lookup"><span data-stu-id="a3def-375">It opens the JSON file and creates a `JsonDocument`.</span></span> <span data-ttu-id="a3def-376">El método supone que el llamador desea trabajar con todo el documento, por lo que devuelve el `Clone` de la `RootElement`.</span><span class="sxs-lookup"><span data-stu-id="a3def-376">The method assumes that the caller wants to work with the entire document, so it returns the `Clone` of the `RootElement`.</span></span> 

<span data-ttu-id="a3def-377">Si recibe un `JsonElement` y está devolviendo un subelemento, no es necesario devolver un `Clone` del subelemento.</span><span class="sxs-lookup"><span data-stu-id="a3def-377">If you receive a `JsonElement` and are returning a sub-element, it's not necessary to return a `Clone` of the sub-element.</span></span> <span data-ttu-id="a3def-378">El autor de la llamada es responsable de mantener activo el `JsonDocument` al que pertenece el `JsonElement` pasado.</span><span class="sxs-lookup"><span data-stu-id="a3def-378">The caller is responsible for keeping alive the `JsonDocument` that the passed-in `JsonElement` belongs to.</span></span> <span data-ttu-id="a3def-379">Por ejemplo:</span><span class="sxs-lookup"><span data-stu-id="a3def-379">For example:</span></span>

```csharp
public JsonElement ReturnFileName(JsonElement source)
{
   return source.GetProperty("fileName");
}
```

### <a name="jsondocument-is-read-only"></a><span data-ttu-id="a3def-380">JsonDocument es de solo lectura</span><span class="sxs-lookup"><span data-stu-id="a3def-380">JsonDocument is read-only</span></span>

<span data-ttu-id="a3def-381">El <xref:System.Text.Json> DOM no puede Agregar, quitar o modificar elementos JSON.</span><span class="sxs-lookup"><span data-stu-id="a3def-381">The <xref:System.Text.Json> DOM can't add, remove, or modify JSON elements.</span></span> <span data-ttu-id="a3def-382">Está diseñado de esta manera para el rendimiento y para reducir las asignaciones para el análisis de los tamaños comunes de carga útil de JSON (es decir, < 1 MB).</span><span class="sxs-lookup"><span data-stu-id="a3def-382">It's designed this way for performance and to reduce allocations for parsing common JSON payload sizes (that is, < 1 MB).</span></span> <span data-ttu-id="a3def-383">Si el escenario usa actualmente un DOM modificable, una de las siguientes soluciones alternativas podría ser factible:</span><span class="sxs-lookup"><span data-stu-id="a3def-383">If your scenario currently uses a modifiable DOM, one of the following workarounds might be feasible:</span></span>

* <span data-ttu-id="a3def-384">Para compilar un `JsonDocument` desde cero (es decir, sin pasar una carga de JSON existente al método de `Parse`), escriba el texto JSON mediante el `Utf8JsonWriter` y analice la salida de ese para crear una nueva `JsonDocument`.</span><span class="sxs-lookup"><span data-stu-id="a3def-384">To build a `JsonDocument` from scratch (that is, without passing in an existing JSON payload to the `Parse` method), write the JSON text by using the `Utf8JsonWriter` and parse the output from that to make a new `JsonDocument`.</span></span>
* <span data-ttu-id="a3def-385">Para modificar un `JsonDocument`existente, úselo para escribir texto JSON, realizar cambios mientras escribe y analizar la salida de ese para crear un nuevo `JsonDocument`.</span><span class="sxs-lookup"><span data-stu-id="a3def-385">To modify an existing `JsonDocument`, use it to write JSON text, making changes while you write, and parse the output from that to make a new `JsonDocument`.</span></span>
* <span data-ttu-id="a3def-386">Para combinar documentos JSON existentes, que son equivalentes a las API `JObject.Merge` o `JContainer.Merge` de `Newtonsoft.Json`, consulte [este problema de github](https://github.com/dotnet/corefx/issues/42466#issuecomment-570475853).</span><span class="sxs-lookup"><span data-stu-id="a3def-386">To merge existing JSON documents, equivalent to the `JObject.Merge` or `JContainer.Merge` APIs from `Newtonsoft.Json`, see [this GitHub issue](https://github.com/dotnet/corefx/issues/42466#issuecomment-570475853).</span></span>

### <a name="jsonelement-is-a-union-struct"></a><span data-ttu-id="a3def-387">JsonElement es una estructura Union</span><span class="sxs-lookup"><span data-stu-id="a3def-387">JsonElement is a union struct</span></span>

<span data-ttu-id="a3def-388">`JsonDocument` expone el `RootElement` como una propiedad de tipo <xref:System.Text.Json.JsonElement>, que es un tipo de estructura de Unión que abarca cualquier elemento JSON.</span><span class="sxs-lookup"><span data-stu-id="a3def-388">`JsonDocument` exposes the `RootElement` as a property of type <xref:System.Text.Json.JsonElement>, which is a union, struct type that encompasses any JSON element.</span></span> <span data-ttu-id="a3def-389">`Newtonsoft.Json` usa tipos jerárquicos dedicados como `JObject`,`JArray`, `JToken`, etc.</span><span class="sxs-lookup"><span data-stu-id="a3def-389">`Newtonsoft.Json` uses dedicated hierarchical types like `JObject`,`JArray`, `JToken`, and so forth.</span></span> <span data-ttu-id="a3def-390">`JsonElement` es lo que puede buscar y enumerar, y puede usar `JsonElement` para materializar los elementos JSON en tipos de .NET.</span><span class="sxs-lookup"><span data-stu-id="a3def-390">`JsonElement` is what you can search and enumerate over, and you can use `JsonElement` to materialize JSON elements into .NET types.</span></span>

### <a name="how-to-search-a-jsondocument-and-jsonelement-for-sub-elements"></a><span data-ttu-id="a3def-391">Cómo buscar subelementos JsonDocument y JsonElement</span><span class="sxs-lookup"><span data-stu-id="a3def-391">How to search a JsonDocument and JsonElement for sub-elements</span></span>

<span data-ttu-id="a3def-392">Las búsquedas de tokens JSON mediante `JObject` o `JArray` de `Newtonsoft.Json` suelen ser relativamente rápidas porque son búsquedas en algunos diccionarios.</span><span class="sxs-lookup"><span data-stu-id="a3def-392">Searches for JSON tokens using `JObject` or `JArray` from `Newtonsoft.Json` tend to be relatively fast because they're lookups in some dictionary.</span></span> <span data-ttu-id="a3def-393">Por comparación, las búsquedas en `JsonElement` requieren una búsqueda secuencial de las propiedades y, por lo tanto, son relativamente lentas (por ejemplo, al usar `TryGetProperty`).</span><span class="sxs-lookup"><span data-stu-id="a3def-393">By comparison, searches on `JsonElement` require a sequential search of the properties and hence is relatively slow (for example when using `TryGetProperty`).</span></span> <span data-ttu-id="a3def-394"><xref:System.Text.Json> está diseñado para minimizar el tiempo de análisis inicial en lugar del tiempo de búsqueda.</span><span class="sxs-lookup"><span data-stu-id="a3def-394"><xref:System.Text.Json> is designed to minimize initial parse time rather than lookup time.</span></span> <span data-ttu-id="a3def-395">Por lo tanto, use los métodos siguientes para optimizar el rendimiento al buscar a través de un objeto `JsonDocument`:</span><span class="sxs-lookup"><span data-stu-id="a3def-395">Therefore, use the following approaches to optimize performance when searching through a `JsonDocument` object:</span></span>

* <span data-ttu-id="a3def-396">Use los enumeradores integrados (<xref:System.Text.Json.JsonElement.EnumerateArray%2A> y <xref:System.Text.Json.JsonElement.EnumerateObject%2A>) en lugar de crear sus propios bucles o índices.</span><span class="sxs-lookup"><span data-stu-id="a3def-396">Use the built-in enumerators (<xref:System.Text.Json.JsonElement.EnumerateArray%2A> and <xref:System.Text.Json.JsonElement.EnumerateObject%2A>) rather than doing your own indexing or loops.</span></span>
* <span data-ttu-id="a3def-397">No realice una búsqueda secuencial en todo el `JsonDocument` a través de todas las propiedades mediante `RootElement`.</span><span class="sxs-lookup"><span data-stu-id="a3def-397">Don't do a sequential search on the whole `JsonDocument` through every property by using `RootElement`.</span></span> <span data-ttu-id="a3def-398">En su lugar, busque objetos JSON anidados basados en la estructura conocida de los datos JSON.</span><span class="sxs-lookup"><span data-stu-id="a3def-398">Instead, search on nested JSON objects based on the known structure of the JSON data.</span></span> <span data-ttu-id="a3def-399">Por ejemplo, si busca una propiedad `Grade` en `Student` objetos, recorra los objetos `Student` y obtenga el valor de `Grade` para cada uno, en lugar de buscar en todos los objetos de `JsonElement` que buscan propiedades de `Grade`.</span><span class="sxs-lookup"><span data-stu-id="a3def-399">For example, if you're looking for a `Grade` property in `Student` objects, loop through the `Student` objects and get the value of `Grade` for each, rather than searching through all `JsonElement` objects looking for `Grade` properties.</span></span> <span data-ttu-id="a3def-400">Si lo hace, se producirán pasos innecesarios sobre los mismos datos.</span><span class="sxs-lookup"><span data-stu-id="a3def-400">Doing the latter will result in unnecessary passes over the same data.</span></span>

<span data-ttu-id="a3def-401">Para obtener un ejemplo de código, vea [usar JsonDocument para el acceso a los datos](system-text-json-how-to.md#use-jsondocument-for-access-to-data).</span><span class="sxs-lookup"><span data-stu-id="a3def-401">For a code example, see [Use JsonDocument for access to data](system-text-json-how-to.md#use-jsondocument-for-access-to-data).</span></span>

## <a name="utf8jsonreader-compared-to-jsontextreader"></a><span data-ttu-id="a3def-402">Utf8JsonReader comparado con JsonTextReader</span><span class="sxs-lookup"><span data-stu-id="a3def-402">Utf8JsonReader compared to JsonTextReader</span></span>

<span data-ttu-id="a3def-403"><xref:System.Text.Json.Utf8JsonReader?displayProperty=fullName> es un lector de alto rendimiento, baja asignación y de solo avance para texto JSON con codificación UTF-8, que se lee de un [> de bytes de\<ReadOnlySpan](xref:System.ReadOnlySpan%601) o de un [> de bytes de ReadOnlySequence\<](xref:System.Buffers.ReadOnlySequence%601).</span><span class="sxs-lookup"><span data-stu-id="a3def-403"><xref:System.Text.Json.Utf8JsonReader?displayProperty=fullName> is a high-performance, low allocation, forward-only reader for UTF-8 encoded JSON text, read from a [ReadOnlySpan\<byte>](xref:System.ReadOnlySpan%601) or [ReadOnlySequence\<byte>](xref:System.Buffers.ReadOnlySequence%601).</span></span> <span data-ttu-id="a3def-404">El `Utf8JsonReader` es un tipo de nivel bajo que se puede utilizar para crear analizadores y deserializadores personalizados.</span><span class="sxs-lookup"><span data-stu-id="a3def-404">The `Utf8JsonReader` is a low-level type that can be used to build custom parsers and deserializers.</span></span>

<span data-ttu-id="a3def-405">En las siguientes secciones se explican los patrones de programación recomendados para el uso de `Utf8JsonReader`.</span><span class="sxs-lookup"><span data-stu-id="a3def-405">The following sections explain recommended programming patterns for using `Utf8JsonReader`.</span></span>

### <a name="utf8jsonreader-is-a-ref-struct"></a><span data-ttu-id="a3def-406">Utf8JsonReader es un struct de referencia</span><span class="sxs-lookup"><span data-stu-id="a3def-406">Utf8JsonReader is a ref struct</span></span>

<span data-ttu-id="a3def-407">Dado que el tipo de `Utf8JsonReader` es un *struct de referencia*, tiene [ciertas limitaciones](../../csharp/language-reference/keywords/ref.md#ref-struct-types).</span><span class="sxs-lookup"><span data-stu-id="a3def-407">Because the `Utf8JsonReader` type is a *ref struct*, it has [certain limitations](../../csharp/language-reference/keywords/ref.md#ref-struct-types).</span></span> <span data-ttu-id="a3def-408">Por ejemplo, no se puede almacenar como un campo en una clase o struct que no sea un struct Ref.</span><span class="sxs-lookup"><span data-stu-id="a3def-408">For example, it can't be stored as a field on a class or struct other than a ref struct.</span></span> <span data-ttu-id="a3def-409">Para lograr un alto rendimiento, este tipo debe ser una `ref struct` porque necesita almacenar en caché la entrada [ReadOnlySpan\<byte >](xref:System.ReadOnlySpan%601), que es una estructura de referencia.</span><span class="sxs-lookup"><span data-stu-id="a3def-409">To achieve high performance, this type must be a `ref struct` since it needs to cache the input [ReadOnlySpan\<byte>](xref:System.ReadOnlySpan%601), which itself is a ref struct.</span></span> <span data-ttu-id="a3def-410">Además, este tipo es mutable ya que contiene el estado.</span><span class="sxs-lookup"><span data-stu-id="a3def-410">In addition, this type is mutable since it holds state.</span></span> <span data-ttu-id="a3def-411">Por lo tanto, **páselo por referencia** en lugar de por valor.</span><span class="sxs-lookup"><span data-stu-id="a3def-411">Therefore, **pass it by ref** rather than by value.</span></span> <span data-ttu-id="a3def-412">Si se pasa por valor, se producirá una copia de la estructura y los cambios de estado no estarán visibles para el autor de la llamada.</span><span class="sxs-lookup"><span data-stu-id="a3def-412">Passing it by value would result in a struct copy and the state changes would not be visible to the caller.</span></span> <span data-ttu-id="a3def-413">Esto difiere de `Newtonsoft.Json` debido a que el `JsonTextReader` de `Newtonsoft.Json` es una clase.</span><span class="sxs-lookup"><span data-stu-id="a3def-413">This differs from `Newtonsoft.Json` since the `Newtonsoft.Json` `JsonTextReader` is a class.</span></span> <span data-ttu-id="a3def-414">Para obtener más información sobre cómo usar las estructuras de referencia, vea [escribir código seguro C# y eficaz](../../csharp/write-safe-efficient-code.md).</span><span class="sxs-lookup"><span data-stu-id="a3def-414">For more information about how to use ref structs, see [Write safe and efficient C# code](../../csharp/write-safe-efficient-code.md).</span></span>

### <a name="read-utf-8-text"></a><span data-ttu-id="a3def-415">Leer texto UTF-8</span><span class="sxs-lookup"><span data-stu-id="a3def-415">Read UTF-8 text</span></span>

<span data-ttu-id="a3def-416">Para lograr el mejor rendimiento posible mientras usa el `Utf8JsonReader`, lea cargas de JSON ya codificadas como texto UTF-8 en lugar de como cadenas UTF-16.</span><span class="sxs-lookup"><span data-stu-id="a3def-416">To achieve the best possible performance while using the `Utf8JsonReader`, read JSON payloads already encoded as UTF-8 text rather than as UTF-16 strings.</span></span> <span data-ttu-id="a3def-417">Para obtener un ejemplo de código, vea [filtrar datos mediante Utf8JsonReader](system-text-json-how-to.md#filter-data-using-utf8jsonreader).</span><span class="sxs-lookup"><span data-stu-id="a3def-417">For a code example, see [Filter data using Utf8JsonReader](system-text-json-how-to.md#filter-data-using-utf8jsonreader).</span></span>

### <a name="read-with-a-stream-or-pipereader"></a><span data-ttu-id="a3def-418">Leer con una secuencia o PipeReader</span><span class="sxs-lookup"><span data-stu-id="a3def-418">Read with a Stream or PipeReader</span></span>

<span data-ttu-id="a3def-419">El `Utf8JsonReader` admite la lectura desde un [ReadOnlySpan\<bytes](xref:System.ReadOnlySpan%601) con codificación UTF-8 > o [\<ReadOnlySequence > byte <xref:System.IO.Pipelines.PipeReader>](xref:System.Buffers.ReadOnlySequence%601) (que es el resultado de la lectura de un).</span><span class="sxs-lookup"><span data-stu-id="a3def-419">The `Utf8JsonReader` supports reading from a UTF-8 encoded [ReadOnlySpan\<byte>](xref:System.ReadOnlySpan%601) or [ReadOnlySequence\<byte>](xref:System.Buffers.ReadOnlySequence%601) (which is the result of reading from a <xref:System.IO.Pipelines.PipeReader>).</span></span>

<span data-ttu-id="a3def-420">Para la lectura sincrónica, puede leer la carga de JSON hasta el final de la secuencia en una matriz de bytes y pasarla al lector.</span><span class="sxs-lookup"><span data-stu-id="a3def-420">For synchronous reading, you could read the JSON payload until the end of the stream into a byte array and pass that into the reader.</span></span> <span data-ttu-id="a3def-421">Para leer de una cadena (que está codificada como UTF-16), llame a <xref:System.Text.Encoding.UTF8>.<xref:System.Text.Encoding.GetBytes%2A></span><span class="sxs-lookup"><span data-stu-id="a3def-421">For reading from a string (which is encoded as UTF-16), call <xref:System.Text.Encoding.UTF8>.<xref:System.Text.Encoding.GetBytes%2A></span></span> <span data-ttu-id="a3def-422">para transcodificar primero la cadena en una matriz de bytes con codificación UTF-8.</span><span class="sxs-lookup"><span data-stu-id="a3def-422">to first transcode the string to a UTF-8 encoded byte array.</span></span> <span data-ttu-id="a3def-423">A continuación, páselo a la `Utf8JsonReader`.</span><span class="sxs-lookup"><span data-stu-id="a3def-423">Then pass that to the `Utf8JsonReader`.</span></span> 

<span data-ttu-id="a3def-424">Dado que el `Utf8JsonReader` considera que la entrada es texto JSON, se considera que una marca de orden de bytes UTF-8 (BOM) no es válida.</span><span class="sxs-lookup"><span data-stu-id="a3def-424">Since the `Utf8JsonReader` considers the input to be JSON text, a UTF-8 byte order mark (BOM) is considered invalid JSON.</span></span> <span data-ttu-id="a3def-425">El autor de la llamada debe filtrarlo antes de pasar los datos al lector.</span><span class="sxs-lookup"><span data-stu-id="a3def-425">The caller needs to filter that out before passing the data to the reader.</span></span>

<span data-ttu-id="a3def-426">Para obtener ejemplos de código, vea [usar Utf8JsonReader](system-text-json-how-to.md#use-utf8jsonreader).</span><span class="sxs-lookup"><span data-stu-id="a3def-426">For code examples, see [Use Utf8JsonReader](system-text-json-how-to.md#use-utf8jsonreader).</span></span>

### <a name="read-with-multi-segment-readonlysequence"></a><span data-ttu-id="a3def-427">Leer con ReadOnlySequence de varios segmentos</span><span class="sxs-lookup"><span data-stu-id="a3def-427">Read with multi-segment ReadOnlySequence</span></span>

<span data-ttu-id="a3def-428">Si la entrada JSON es una [> ReadOnlySpan\<byte](xref:System.ReadOnlySpan%601), se puede tener acceso a cada elemento JSON desde la propiedad `ValueSpan` en el lector a medida que avanza por el bucle de lectura.</span><span class="sxs-lookup"><span data-stu-id="a3def-428">If your JSON input is a [ReadOnlySpan\<byte>](xref:System.ReadOnlySpan%601), each JSON element can be accessed from the `ValueSpan` property on the reader as you go through the read loop.</span></span> <span data-ttu-id="a3def-429">Sin embargo, si la entrada es una [ReadOnlySequence\<byte >](xref:System.Buffers.ReadOnlySequence%601) (que es el resultado de leer de un <xref:System.IO.Pipelines.PipeReader>), algunos elementos JSON podrían ocupar varios segmentos del objeto `ReadOnlySequence<byte>`.</span><span class="sxs-lookup"><span data-stu-id="a3def-429">However, if your input is a [ReadOnlySequence\<byte>](xref:System.Buffers.ReadOnlySequence%601) (which is the result of reading from a <xref:System.IO.Pipelines.PipeReader>), some JSON elements might straddle multiple segments of the `ReadOnlySequence<byte>` object.</span></span> <span data-ttu-id="a3def-430">No se puede obtener acceso a estos elementos desde <xref:System.Text.Json.Utf8JsonReader.ValueSpan%2A> en un bloque de memoria contiguo.</span><span class="sxs-lookup"><span data-stu-id="a3def-430">These elements would not be accessible from <xref:System.Text.Json.Utf8JsonReader.ValueSpan%2A> in a contiguous memory block.</span></span> <span data-ttu-id="a3def-431">En su lugar, siempre que tenga un `ReadOnlySequence<byte>` de varios segmentos como entrada, sondee la propiedad <xref:System.Text.Json.Utf8JsonReader.HasValueSequence%2A> en el lector para averiguar cómo tener acceso al elemento JSON actual.</span><span class="sxs-lookup"><span data-stu-id="a3def-431">Instead, whenever you have a multi-segment `ReadOnlySequence<byte>` as input, poll the <xref:System.Text.Json.Utf8JsonReader.HasValueSequence%2A> property on the reader to figure out how to access the current JSON element.</span></span> <span data-ttu-id="a3def-432">Este es un patrón recomendado:</span><span class="sxs-lookup"><span data-stu-id="a3def-432">Here's a recommended pattern:</span></span>

```csharp
while (reader.Read())
{
    switch (reader.TokenType)
    {
        // ...
        ReadOnlySpan<byte> jsonElement = reader.HasValueSequence ?
            reader.ValueSequence.ToArray() :
            reader.ValueSpan;
        // ...
    }
}
```

### <a name="use-valuetextequals-for-property-name-lookups"></a><span data-ttu-id="a3def-433">Usar ValueTextEquals para las búsquedas de nombres de propiedad</span><span class="sxs-lookup"><span data-stu-id="a3def-433">Use ValueTextEquals for property name lookups</span></span>

<span data-ttu-id="a3def-434">No use <xref:System.Text.Json.Utf8JsonReader.ValueSpan%2A> para realizar comparaciones byte a Byte mediante una llamada a <xref:System.MemoryExtensions.SequenceEqual%2A> para las búsquedas de nombres de propiedad.</span><span class="sxs-lookup"><span data-stu-id="a3def-434">Don't use <xref:System.Text.Json.Utf8JsonReader.ValueSpan%2A> to do byte-by-byte comparisons by calling <xref:System.MemoryExtensions.SequenceEqual%2A> for property name lookups.</span></span> <span data-ttu-id="a3def-435">En su lugar, llame a <xref:System.Text.Json.Utf8JsonReader.ValueTextEquals%2A>, ya que ese método anula la escape de los caracteres que se van a escapar en JSON.</span><span class="sxs-lookup"><span data-stu-id="a3def-435">Call <xref:System.Text.Json.Utf8JsonReader.ValueTextEquals%2A> instead, because that method unescapes any characters that are escaped in the JSON.</span></span> <span data-ttu-id="a3def-436">Este es un ejemplo que muestra cómo buscar una propiedad denominada "Name":</span><span class="sxs-lookup"><span data-stu-id="a3def-436">Here's an example that shows how to search for a property that is named "name":</span></span>

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/ValueTextEqualsExample.cs?name=SnippetDefineUtf8Var)]

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/ValueTextEqualsExample.cs?name=SnippetUseUtf8Var&highlight=11)]

### <a name="read-null-values-into-nullable-value-types"></a><span data-ttu-id="a3def-437">Leer valores NULL en tipos de valor que aceptan valores NULL</span><span class="sxs-lookup"><span data-stu-id="a3def-437">Read null values into nullable value types</span></span>

<span data-ttu-id="a3def-438">`Newtonsoft.Json` proporciona API que devuelven <xref:System.Nullable%601>, como `ReadAsBoolean`, que controla una `TokenType` de `Null` para usted mediante la devolución de una `bool?`.</span><span class="sxs-lookup"><span data-stu-id="a3def-438">`Newtonsoft.Json` provides APIs that return <xref:System.Nullable%601>, such as `ReadAsBoolean`, which handles a `Null` `TokenType` for you by returning a `bool?`.</span></span> <span data-ttu-id="a3def-439">Las API integradas de `System.Text.Json` devuelven solo tipos de valor que no aceptan valores NULL.</span><span class="sxs-lookup"><span data-stu-id="a3def-439">The built-in `System.Text.Json` APIs return only non-nullable value types.</span></span> <span data-ttu-id="a3def-440">Por ejemplo, <xref:System.Text.Json.Utf8JsonReader.GetBoolean%2A?displayProperty=nameWithType> devuelve un `bool`.</span><span class="sxs-lookup"><span data-stu-id="a3def-440">For example, <xref:System.Text.Json.Utf8JsonReader.GetBoolean%2A?displayProperty=nameWithType> returns a `bool`.</span></span> <span data-ttu-id="a3def-441">Produce una excepción si encuentra `Null` en el archivo JSON.</span><span class="sxs-lookup"><span data-stu-id="a3def-441">It throws an exception if it finds `Null` in the JSON.</span></span> <span data-ttu-id="a3def-442">En los siguientes ejemplos se muestran dos formas de controlar valores NULL, uno devolviendo un tipo de valor que acepta valores NULL y otro devolviendo el valor predeterminado:</span><span class="sxs-lookup"><span data-stu-id="a3def-442">The following examples show two ways to handle nulls, one by returning a nullable value type and one by returning the default value:</span></span>

```csharp
public bool? ReadAsNullableBoolean()
{
    _reader.Read();
    if (_reader.TokenType == JsonTokenType.Null)
    {
        return null;
    }
    if (_reader.TokenType != JsonTokenType.True && _reader.TokenType != JsonTokenType.False)
    {
        throw new JsonException();
    }
    return _reader.GetBoolean();
}
```

```csharp
public bool ReadAsBoolean(bool defaultValue)
{
    _reader.Read();
    if (_reader.TokenType == JsonTokenType.Null)
    {
        return defaultValue;
    }
    if (_reader.TokenType != JsonTokenType.True && _reader.TokenType != JsonTokenType.False)
    {
        throw new JsonException();
    }
    return _reader.GetBoolean();
}
```

### <a name="multi-targeting"></a><span data-ttu-id="a3def-443">Compatibilidad con múltiples versiones</span><span class="sxs-lookup"><span data-stu-id="a3def-443">Multi-targeting</span></span>

<span data-ttu-id="a3def-444">Si necesita seguir usando `Newtonsoft.Json` para determinadas plataformas de destino, puede usar varias implementaciones y tener dos.</span><span class="sxs-lookup"><span data-stu-id="a3def-444">If you need to continue to use `Newtonsoft.Json` for certain target frameworks, you can multi-target and have two implementations.</span></span> <span data-ttu-id="a3def-445">Sin embargo, esto no es trivial y requeriría algunos `#ifdefs` y la duplicación de origen.</span><span class="sxs-lookup"><span data-stu-id="a3def-445">However, this is not trivial and would require some `#ifdefs` and source duplication.</span></span> <span data-ttu-id="a3def-446">Una manera de compartir todo el código posible es crear un contenedor de `ref struct` alrededor de `Utf8JsonReader` y `Newtonsoft.Json` `JsonTextReader`.</span><span class="sxs-lookup"><span data-stu-id="a3def-446">One way to share as much code as possible is to create a `ref struct` wrapper around `Utf8JsonReader` and `Newtonsoft.Json` `JsonTextReader`.</span></span> <span data-ttu-id="a3def-447">Este contenedor unificaría el área expuesta pública al aislar las diferencias de comportamiento.</span><span class="sxs-lookup"><span data-stu-id="a3def-447">This wrapper would unify the public surface area while isolating the behavioral differences.</span></span> <span data-ttu-id="a3def-448">Esto le permite aislar los cambios principalmente en la construcción del tipo, junto con pasar el nuevo tipo por referencia.</span><span class="sxs-lookup"><span data-stu-id="a3def-448">This lets you isolate the changes mainly to the construction of the type, along with passing the new type around by reference.</span></span> <span data-ttu-id="a3def-449">Este es el patrón que sigue la biblioteca [Microsoft. Extensions. DependencyModel](https://www.nuget.org/packages/Microsoft.Extensions.DependencyModel/3.1.0/) :</span><span class="sxs-lookup"><span data-stu-id="a3def-449">This is the pattern that the [Microsoft.Extensions.DependencyModel](https://www.nuget.org/packages/Microsoft.Extensions.DependencyModel/3.1.0/) library follows:</span></span>

* [<span data-ttu-id="a3def-450">UnifiedJsonReader.JsonTextReader.cs</span><span class="sxs-lookup"><span data-stu-id="a3def-450">UnifiedJsonReader.JsonTextReader.cs</span></span>](https://github.com/dotnet/runtime/blob/81bf79fd9aa75305e55abe2f7e9ef3f60624a3a1/src/installer/managed/Microsoft.Extensions.DependencyModel/UnifiedJsonReader.JsonTextReader.cs)
* [<span data-ttu-id="a3def-451">UnifiedJsonReader.Utf8JsonReader.cs</span><span class="sxs-lookup"><span data-stu-id="a3def-451">UnifiedJsonReader.Utf8JsonReader.cs</span></span>](https://github.com/dotnet/runtime/blob/81bf79fd9aa75305e55abe2f7e9ef3f60624a3a1/src/installer/managed/Microsoft.Extensions.DependencyModel/UnifiedJsonReader.Utf8JsonReader.cs)

## <a name="utf8jsonwriter-compared-to-jsontextwriter"></a><span data-ttu-id="a3def-452">Utf8JsonWriter comparado con objeto JsonTextWriter</span><span class="sxs-lookup"><span data-stu-id="a3def-452">Utf8JsonWriter compared to JsonTextWriter</span></span>

<span data-ttu-id="a3def-453"><xref:System.Text.Json.Utf8JsonWriter?displayProperty=fullName> es una forma de alto rendimiento de escribir texto JSON con codificación UTF-8 de tipos comunes de .NET como `String`, `Int32`y `DateTime`.</span><span class="sxs-lookup"><span data-stu-id="a3def-453"><xref:System.Text.Json.Utf8JsonWriter?displayProperty=fullName> is a high-performance way to write UTF-8 encoded JSON text from common .NET types like `String`, `Int32`, and `DateTime`.</span></span> <span data-ttu-id="a3def-454">El escritor es un tipo de nivel bajo que se puede utilizar para crear serializadores personalizados.</span><span class="sxs-lookup"><span data-stu-id="a3def-454">The writer is a low-level type that can be used to build custom serializers.</span></span>

<span data-ttu-id="a3def-455">En las siguientes secciones se explican los patrones de programación recomendados para el uso de `Utf8JsonWriter`.</span><span class="sxs-lookup"><span data-stu-id="a3def-455">The following sections explain recommended programming patterns for using `Utf8JsonWriter`.</span></span>

### <a name="write-with-utf-8-text"></a><span data-ttu-id="a3def-456">Escribir con texto UTF-8</span><span class="sxs-lookup"><span data-stu-id="a3def-456">Write with UTF-8 text</span></span>

<span data-ttu-id="a3def-457">Para lograr el mejor rendimiento posible mientras usa el `Utf8JsonWriter`, escriba cargas JSON ya codificadas como texto UTF-8 en lugar de como cadenas UTF-16.</span><span class="sxs-lookup"><span data-stu-id="a3def-457">To achieve the best possible performance while using the `Utf8JsonWriter`, write JSON payloads already encoded as UTF-8 text rather than as UTF-16 strings.</span></span> <span data-ttu-id="a3def-458">Utilice <xref:System.Text.Json.JsonEncodedText> para almacenar en caché y codificar previamente los nombres y valores de las propiedades de cadena conocidas como estáticas, y pasarlos al escritor, en lugar de usar literales de cadena UTF-16.</span><span class="sxs-lookup"><span data-stu-id="a3def-458">Use <xref:System.Text.Json.JsonEncodedText> to cache and pre-encode known string property names and values as statics, and pass those to the writer, rather than using UTF-16 string literals.</span></span> <span data-ttu-id="a3def-459">Esto es más rápido que el almacenamiento en caché y el uso de matrices de bytes UTF-8.</span><span class="sxs-lookup"><span data-stu-id="a3def-459">This is faster than caching and using UTF-8 byte arrays.</span></span>

<span data-ttu-id="a3def-460">Este enfoque también funciona si necesita realizar un escape personalizado.</span><span class="sxs-lookup"><span data-stu-id="a3def-460">This approach also works if you need to do custom escaping.</span></span> <span data-ttu-id="a3def-461">`System.Text.Json` no permite deshabilitar el escape mientras se escribe una cadena.</span><span class="sxs-lookup"><span data-stu-id="a3def-461">`System.Text.Json` doesn't let you disable escaping while writing a string.</span></span> <span data-ttu-id="a3def-462">Sin embargo, podría pasar su propio <xref:System.Text.Encodings.Web.JavaScriptEncoder> personalizado como una opción al escritor, o crear su propia `JsonEncodedText` que use su `JavascriptEncoder` para realizar el escape y, a continuación, escribir el `JsonEncodedText` en lugar de la cadena.</span><span class="sxs-lookup"><span data-stu-id="a3def-462">However, you could pass in your own custom <xref:System.Text.Encodings.Web.JavaScriptEncoder> as an option to the writer, or create your own `JsonEncodedText` that uses your `JavascriptEncoder` to do the escaping, and then write the `JsonEncodedText` instead of the string.</span></span> <span data-ttu-id="a3def-463">Para obtener más información, vea [personalizar la codificación de caracteres](system-text-json-how-to.md#customize-character-encoding).</span><span class="sxs-lookup"><span data-stu-id="a3def-463">For more information, see [Customize character encoding](system-text-json-how-to.md#customize-character-encoding).</span></span>

### <a name="write-raw-values"></a><span data-ttu-id="a3def-464">Escribir valores sin formato</span><span class="sxs-lookup"><span data-stu-id="a3def-464">Write raw values</span></span>

<span data-ttu-id="a3def-465">El método `Newtonsoft.Json` `WriteRawValue` escribe el JSON sin formato en el que se espera un valor.</span><span class="sxs-lookup"><span data-stu-id="a3def-465">The `Newtonsoft.Json` `WriteRawValue` method writes raw JSON where a value is expected.</span></span> <span data-ttu-id="a3def-466"><xref:System.Text.Json> no tiene ningún equivalente directo, pero esta es una solución alternativa que garantiza que solo se escribe un JSON válido:</span><span class="sxs-lookup"><span data-stu-id="a3def-466"><xref:System.Text.Json> has no direct equivalent, but here's a workaround that ensures only valid JSON is written:</span></span>

```csharp
using JsonDocument doc = JsonDocument.Parse(string);
doc.WriteTo(writer);
```

### <a name="customize-character-escaping"></a><span data-ttu-id="a3def-467">Personalizar el escape de caracteres</span><span class="sxs-lookup"><span data-stu-id="a3def-467">Customize character escaping</span></span>

<span data-ttu-id="a3def-468">El valor [StringEscapeHandling](https://www.newtonsoft.com/json/help/html/T_Newtonsoft_Json_StringEscapeHandling.htm) de `JsonTextWriter` ofrece opciones para escapar todos los caracteres que no sean ASCII **o** caracteres HTML.</span><span class="sxs-lookup"><span data-stu-id="a3def-468">The [StringEscapeHandling](https://www.newtonsoft.com/json/help/html/T_Newtonsoft_Json_StringEscapeHandling.htm) setting of `JsonTextWriter` offers options to escape all non-ASCII characters **or** HTML characters.</span></span> <span data-ttu-id="a3def-469">De forma predeterminada, `Utf8JsonWriter` escapa a todos los caracteres no ASCII **y** HTML.</span><span class="sxs-lookup"><span data-stu-id="a3def-469">By default, `Utf8JsonWriter` escapes all non-ASCII **and** HTML characters.</span></span> <span data-ttu-id="a3def-470">Este escape se hace por motivos de seguridad de defensa en profundidad.</span><span class="sxs-lookup"><span data-stu-id="a3def-470">This escaping is done for defense-in-depth security reasons.</span></span> <span data-ttu-id="a3def-471">Para especificar una directiva de escape diferente, cree una <xref:System.Text.Encodings.Web.JavaScriptEncoder> y establezca <xref:System.Text.Json.JsonWriterOptions.Encoder?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="a3def-471">To specify a different escaping policy, create a <xref:System.Text.Encodings.Web.JavaScriptEncoder> and set <xref:System.Text.Json.JsonWriterOptions.Encoder?displayProperty=nameWithType>.</span></span> <span data-ttu-id="a3def-472">Para obtener más información, vea [personalizar la codificación de caracteres](system-text-json-how-to.md#customize-character-encoding).</span><span class="sxs-lookup"><span data-stu-id="a3def-472">For more information, see [Customize character encoding](system-text-json-how-to.md#customize-character-encoding).</span></span>

### <a name="customize-json-format"></a><span data-ttu-id="a3def-473">Personalizar el formato JSON</span><span class="sxs-lookup"><span data-stu-id="a3def-473">Customize JSON format</span></span>

<span data-ttu-id="a3def-474">`JsonTextWriter` incluye la configuración siguiente, para la que `Utf8JsonWriter` no tiene ningún equivalente:</span><span class="sxs-lookup"><span data-stu-id="a3def-474">`JsonTextWriter` includes the following settings, for which `Utf8JsonWriter` has no equivalent:</span></span>

* <span data-ttu-id="a3def-475">[Indentación](https://www.newtonsoft.com/json/help/html/P_Newtonsoft_Json_JsonTextWriter_Indentation.htm) : especifica el número de caracteres que se van a aplicar sangría.</span><span class="sxs-lookup"><span data-stu-id="a3def-475">[Indentation](https://www.newtonsoft.com/json/help/html/P_Newtonsoft_Json_JsonTextWriter_Indentation.htm) - Specifies how many characters to indent.</span></span> <span data-ttu-id="a3def-476">`Utf8JsonWriter` siempre realiza la sangría de 2 caracteres.</span><span class="sxs-lookup"><span data-stu-id="a3def-476">`Utf8JsonWriter` always does 2-character indentation.</span></span>
* <span data-ttu-id="a3def-477">[IndentChar](https://www.newtonsoft.com/json/help/html/P_Newtonsoft_Json_JsonTextWriter_IndentChar.htm) : especifica el carácter que se va a utilizar para la sangría.</span><span class="sxs-lookup"><span data-stu-id="a3def-477">[IndentChar](https://www.newtonsoft.com/json/help/html/P_Newtonsoft_Json_JsonTextWriter_IndentChar.htm) - Specifies the character to use for indentation.</span></span>  <span data-ttu-id="a3def-478">`Utf8JsonWriter` usa siempre el espacio en blanco.</span><span class="sxs-lookup"><span data-stu-id="a3def-478">`Utf8JsonWriter` always uses whitespace.</span></span>
* <span data-ttu-id="a3def-479">[QuoteChar](https://www.newtonsoft.com/json/help/html/P_Newtonsoft_Json_JsonTextWriter_QuoteChar.htm) : especifica el carácter que se va a usar para rodear valores de cadena.</span><span class="sxs-lookup"><span data-stu-id="a3def-479">[QuoteChar](https://www.newtonsoft.com/json/help/html/P_Newtonsoft_Json_JsonTextWriter_QuoteChar.htm) - Specifies the character to use to surround string values.</span></span>  <span data-ttu-id="a3def-480">`Utf8JsonWriter` siempre usa comillas dobles.</span><span class="sxs-lookup"><span data-stu-id="a3def-480">`Utf8JsonWriter` always uses double quotes.</span></span>
* <span data-ttu-id="a3def-481">[QuoteName](https://www.newtonsoft.com/json/help/html/P_Newtonsoft_Json_JsonTextWriter_QuoteName.htm) : especifica si los nombres de propiedad deben encerrarse entre comillas.</span><span class="sxs-lookup"><span data-stu-id="a3def-481">[QuoteName](https://www.newtonsoft.com/json/help/html/P_Newtonsoft_Json_JsonTextWriter_QuoteName.htm) - Specifies whether or not to surround property names with quotes.</span></span>  <span data-ttu-id="a3def-482">`Utf8JsonWriter` siempre se coloca entre comillas.</span><span class="sxs-lookup"><span data-stu-id="a3def-482">`Utf8JsonWriter` always surrounds them with quotes.</span></span>

<span data-ttu-id="a3def-483">No hay ninguna solución alternativa que permita personalizar el JSON generado por `Utf8JsonWriter` de estas maneras.</span><span class="sxs-lookup"><span data-stu-id="a3def-483">There are no workarounds that would let you customize the JSON produced by `Utf8JsonWriter` in these ways.</span></span>

### <a name="write-null-values"></a><span data-ttu-id="a3def-484">Escribir valores NULL</span><span class="sxs-lookup"><span data-stu-id="a3def-484">Write null values</span></span>

<span data-ttu-id="a3def-485">Para escribir valores NULL mediante `Utf8JsonWriter`, llame a:</span><span class="sxs-lookup"><span data-stu-id="a3def-485">To write null values by using `Utf8JsonWriter`, call:</span></span>

* <span data-ttu-id="a3def-486"><xref:System.Text.Json.Utf8JsonWriter.WriteNull%2A> para escribir un par clave-valor con NULL como valor.</span><span class="sxs-lookup"><span data-stu-id="a3def-486"><xref:System.Text.Json.Utf8JsonWriter.WriteNull%2A> to write a key-value pair with null as the value.</span></span>
* <span data-ttu-id="a3def-487"><xref:System.Text.Json.Utf8JsonWriter.WriteNullValue%2A> para escribir NULL como un elemento de una matriz JSON.</span><span class="sxs-lookup"><span data-stu-id="a3def-487"><xref:System.Text.Json.Utf8JsonWriter.WriteNullValue%2A> to write null as an element of a JSON array.</span></span>

<span data-ttu-id="a3def-488">En el caso de una propiedad de cadena, si la cadena es null, <xref:System.Text.Json.Utf8JsonWriter.WriteString%2A> y <xref:System.Text.Json.Utf8JsonWriter.WriteStringValue%2A> son equivalentes a `WriteNull` y `WriteNullValue`.</span><span class="sxs-lookup"><span data-stu-id="a3def-488">For a string property, if the string is null, <xref:System.Text.Json.Utf8JsonWriter.WriteString%2A> and <xref:System.Text.Json.Utf8JsonWriter.WriteStringValue%2A> are equivalent to `WriteNull` and `WriteNullValue`.</span></span>

### <a name="write-timespan-uri-or-char-values"></a><span data-ttu-id="a3def-489">Escribir valores TimeSpan, Uri o char</span><span class="sxs-lookup"><span data-stu-id="a3def-489">Write Timespan, Uri, or char values</span></span>

<span data-ttu-id="a3def-490">`JsonTextWriter` proporciona métodos de `WriteValue` para los valores [TimeSpan](https://www.newtonsoft.com/json/help/html/M_Newtonsoft_Json_JsonTextWriter_WriteValue_18.htm), [URI](https://www.newtonsoft.com/json/help/html/M_Newtonsoft_Json_JsonTextWriter_WriteValue_22.htm)y [Char](https://www.newtonsoft.com/json/help/html/M_Newtonsoft_Json_JsonTextWriter_WriteValue_3.htm) .</span><span class="sxs-lookup"><span data-stu-id="a3def-490">`JsonTextWriter` provides `WriteValue` methods for [TimeSpan](https://www.newtonsoft.com/json/help/html/M_Newtonsoft_Json_JsonTextWriter_WriteValue_18.htm), [Uri](https://www.newtonsoft.com/json/help/html/M_Newtonsoft_Json_JsonTextWriter_WriteValue_22.htm), and [char](https://www.newtonsoft.com/json/help/html/M_Newtonsoft_Json_JsonTextWriter_WriteValue_3.htm) values.</span></span> <span data-ttu-id="a3def-491">`Utf8JsonWriter` no tiene métodos equivalentes.</span><span class="sxs-lookup"><span data-stu-id="a3def-491">`Utf8JsonWriter` doesn't have equivalent methods.</span></span> <span data-ttu-id="a3def-492">En su lugar, dé formato a estos valores como cadenas (llamando `ToString()`, por ejemplo) y llame a <xref:System.Text.Json.Utf8JsonWriter.WriteStringValue%2A>.</span><span class="sxs-lookup"><span data-stu-id="a3def-492">Instead, format these values as strings (by calling `ToString()`, for example) and call <xref:System.Text.Json.Utf8JsonWriter.WriteStringValue%2A>.</span></span>

### <a name="multi-targeting"></a><span data-ttu-id="a3def-493">Compatibilidad con múltiples versiones</span><span class="sxs-lookup"><span data-stu-id="a3def-493">Multi-targeting</span></span>

<span data-ttu-id="a3def-494">Si necesita seguir usando `Newtonsoft.Json` para determinadas plataformas de destino, puede usar varias implementaciones y tener dos.</span><span class="sxs-lookup"><span data-stu-id="a3def-494">If you need to continue to use `Newtonsoft.Json` for certain target frameworks, you can multi-target and have two implementations.</span></span> <span data-ttu-id="a3def-495">Sin embargo, esto no es trivial y requeriría algunos `#ifdefs` y la duplicación de origen.</span><span class="sxs-lookup"><span data-stu-id="a3def-495">However, this is not trivial and would require some `#ifdefs` and source duplication.</span></span> <span data-ttu-id="a3def-496">Una manera de compartir todo el código posible es crear un contenedor alrededor de `Utf8JsonWriter` y `Newtonsoft` `JsonTextWriter`.</span><span class="sxs-lookup"><span data-stu-id="a3def-496">One way to share as much code as possible is to create a wrapper around `Utf8JsonWriter` and `Newtonsoft` `JsonTextWriter`.</span></span> <span data-ttu-id="a3def-497">Este contenedor unificaría el área expuesta pública al aislar las diferencias de comportamiento.</span><span class="sxs-lookup"><span data-stu-id="a3def-497">This wrapper would unify the public surface area while isolating the behavioral differences.</span></span> <span data-ttu-id="a3def-498">Esto le permite aislar los cambios principalmente en la construcción del tipo.</span><span class="sxs-lookup"><span data-stu-id="a3def-498">This lets you isolate the changes mainly to the construction of the type.</span></span> <span data-ttu-id="a3def-499">A continuación se muestra la biblioteca [Microsoft. Extensions. DependencyModel](https://www.nuget.org/packages/Microsoft.Extensions.DependencyModel/3.1.0/) :</span><span class="sxs-lookup"><span data-stu-id="a3def-499">[Microsoft.Extensions.DependencyModel](https://www.nuget.org/packages/Microsoft.Extensions.DependencyModel/3.1.0/) library follows:</span></span>

* [<span data-ttu-id="a3def-500">UnifiedJsonWriter.JsonTextWriter.cs</span><span class="sxs-lookup"><span data-stu-id="a3def-500">UnifiedJsonWriter.JsonTextWriter.cs</span></span>](https://github.com/dotnet/runtime/blob/81bf79fd9aa75305e55abe2f7e9ef3f60624a3a1/src/installer/managed/Microsoft.Extensions.DependencyModel/UnifiedJsonWriter.JsonTextWriter.cs)
* [<span data-ttu-id="a3def-501">UnifiedJsonWriter.Utf8JsonWriter.cs</span><span class="sxs-lookup"><span data-stu-id="a3def-501">UnifiedJsonWriter.Utf8JsonWriter.cs</span></span>](https://github.com/dotnet/runtime/blob/81bf79fd9aa75305e55abe2f7e9ef3f60624a3a1/src/installer/managed/Microsoft.Extensions.DependencyModel/UnifiedJsonWriter.Utf8JsonWriter.cs)

## <a name="additional-resources"></a><span data-ttu-id="a3def-502">Recursos adicionales</span><span class="sxs-lookup"><span data-stu-id="a3def-502">Additional resources</span></span>

<!-- * [System.Text.Json roadmap](https://github.com/dotnet/runtime/blob/81bf79fd9aa75305e55abe2f7e9ef3f60624a3a1/src/libraries/System.Text.Json/roadmap/README.md)[Restore this when the roadmap is updated.]-->
* [<span data-ttu-id="a3def-503">Información general de System. Text. JSON</span><span class="sxs-lookup"><span data-stu-id="a3def-503">System.Text.Json overview</span></span>](system-text-json-overview.md)
* [<span data-ttu-id="a3def-504">Cómo usar System. Text. JSON</span><span class="sxs-lookup"><span data-stu-id="a3def-504">How to use System.Text.Json</span></span>](system-text-json-how-to.md)
* [<span data-ttu-id="a3def-505">Cómo escribir convertidores personalizados</span><span class="sxs-lookup"><span data-stu-id="a3def-505">How to write custom converters</span></span>](system-text-json-converters-how-to.md)
* [<span data-ttu-id="a3def-506">Compatibilidad con DateTime y DateTimeOffset en System. Text. JSON</span><span class="sxs-lookup"><span data-stu-id="a3def-506">DateTime and DateTimeOffset support in System.Text.Json</span></span>](../datetime/system-text-json-support.md)
* [<span data-ttu-id="a3def-507">Referencia de la API System. Text. JSON</span><span class="sxs-lookup"><span data-stu-id="a3def-507">System.Text.Json API reference</span></span>](xref:System.Text.Json)
* [<span data-ttu-id="a3def-508">Referencia de API de System. Text. JSON. Serialization</span><span class="sxs-lookup"><span data-stu-id="a3def-508">System.Text.Json.Serialization API reference</span></span>](xref:System.Text.Json.Serialization)
