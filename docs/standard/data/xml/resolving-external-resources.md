---
title: Resolver recursos externos
ms.date: 03/30/2017
ms.technology: dotnet-standard
ms.assetid: ad3fa320-4b8f-4e5c-b549-01157591007a
ms.openlocfilehash: 82e9231be8a3619f59313460f0d5e0b246eb9436
ms.sourcegitcommit: 33deec3e814238fb18a49b2a7e89278e27888291
ms.translationtype: HT
ms.contentlocale: es-ES
ms.lasthandoff: 06/02/2020
ms.locfileid: "84291492"
---
# <a name="resolving-external-resources"></a><span data-ttu-id="e0a80-102">Resolver recursos externos</span><span class="sxs-lookup"><span data-stu-id="e0a80-102">Resolving External Resources</span></span>
<span data-ttu-id="e0a80-103">La clase **XmlDocument** utiliza la propiedad **XmlResolver** de **XmlDocument** para localizar recursos que no están alineados en los datos XML, como las definiciones de tipo de documento (DTD), entidades y esquemas externos.</span><span class="sxs-lookup"><span data-stu-id="e0a80-103">The **XmlResolver** property of the **XmlDocument** is used by the **XmlDocument** class to locate resources that are not inline in the XML data, such as external document type definitions (DTDs), entities, and schemas.</span></span> <span data-ttu-id="e0a80-104">Estos elementos se pueden localizar en una red o en una unidad local, y se identifican mediante un identificador de recursos uniforme (URI).</span><span class="sxs-lookup"><span data-stu-id="e0a80-104">These items can be located on a network or on a local drive, and are identifiable by a Uniform Resource Identifier (URI).</span></span> <span data-ttu-id="e0a80-105">Esto permite a **XmlDocument** resolver los nodos **EntityReference** presentes en el documento y validar el documento de acuerdo con la DTD o el esquema externos.</span><span class="sxs-lookup"><span data-stu-id="e0a80-105">This allows the **XmlDocument** to resolve **EntityReference** nodes that are present in the document and validate the document according to the external DTD or schema.</span></span>  
  
## <a name="fully-trusted-xmldocument"></a><span data-ttu-id="e0a80-106">XmlDocument de plena confianza</span><span class="sxs-lookup"><span data-stu-id="e0a80-106">Fully-Trusted XmlDocument</span></span>  
 <span data-ttu-id="e0a80-107">La propiedad **XmlResolver** afecta a la funcionalidad del método **XmlDocument.Load**.</span><span class="sxs-lookup"><span data-stu-id="e0a80-107">The **XmlResolver** property affects the functionality of the **XmlDocument.Load** method.</span></span> <span data-ttu-id="e0a80-108">La tabla que aparece a continuación muestra cómo funciona la propiedad **XmlDocument.XmlResolver** cuando el objeto **XmlDocument** es de plena confianza.</span><span class="sxs-lookup"><span data-stu-id="e0a80-108">The table below shows how the **XmlDocument.XmlResolver** property works when the **XmlDocument** object is fully trusted.</span></span> <span data-ttu-id="e0a80-109">Esta tabla muestra los métodos **XmlDocument.Load** cuando la entrada para Load es **TextReader**, **String**, **Stream** o **URI**.</span><span class="sxs-lookup"><span data-stu-id="e0a80-109">The following table shows the **XmlDocument.Load** methods when the input to the Load is a **TextReader**, **String**, **Stream**, or **URI**.</span></span> <span data-ttu-id="e0a80-110">Esta tabla no se aplica al método **Load** si **XmlDocument** se carga de **XmlReader**.</span><span class="sxs-lookup"><span data-stu-id="e0a80-110">This table does not apply to the **Load** method if the **XmlDocument** is loaded from an **XmlReader**.</span></span>  
  
|<span data-ttu-id="e0a80-111">Propiedad XmlResolver</span><span class="sxs-lookup"><span data-stu-id="e0a80-111">XmlResolver property</span></span>|<span data-ttu-id="e0a80-112">Función</span><span class="sxs-lookup"><span data-stu-id="e0a80-112">Function</span></span>|<span data-ttu-id="e0a80-113">Notas</span><span class="sxs-lookup"><span data-stu-id="e0a80-113">Notes</span></span>|  
|--------------------------|--------------|-----------|  
|<span data-ttu-id="e0a80-114">La propiedad se establece en una clase **XmlResolver** que se ha creado previamente y que tiene las propiedades establecidas por el usuario.</span><span class="sxs-lookup"><span data-stu-id="e0a80-114">The property is set to an **XmlResolver** class that has been previously created and has properties already set on it by the user.</span></span>|<span data-ttu-id="e0a80-115">El objeto **XmlDocument** utiliza la clase **XmlResolver** proporcionada para resolver nombres de archivo y referencias de recursos externos, como las DTD, entidades y esquemas.</span><span class="sxs-lookup"><span data-stu-id="e0a80-115">The **XmlDocument** uses the **XmlResolver** that is given to resolve file names, to resolve references to external resources such as DTDs, entities, and schemas.</span></span><br /><br /> <span data-ttu-id="e0a80-116">La propiedad **XmlResolver** se utiliza también para resolver recursos externos que son necesarios cuando se agregan o se editan nodos del objeto **XmlDocument**.</span><span class="sxs-lookup"><span data-stu-id="e0a80-116">The **XmlResolver** is also used when resolving external resources that are needed when adding or editing nodes in the **XmlDocument**.</span></span>|<span data-ttu-id="e0a80-117">La propiedad **XmlResolver** que se proporciona para el objeto **XmlDocument** es el sistema de resolución que se utiliza siempre que es necesario localizar y resolver recursos externos.</span><span class="sxs-lookup"><span data-stu-id="e0a80-117">The **XmlResolver** given to the **XmlDocument** is the resolver that is used whenever external resources need to be located and resolved.</span></span>|  
|<span data-ttu-id="e0a80-118">La propiedad se establece en **null** (**Nothing** en Microsoft Visual Basic .NET).</span><span class="sxs-lookup"><span data-stu-id="e0a80-118">The property is set to **null** (**Nothing** in Microsoft Visual Basic .NET).</span></span>|<span data-ttu-id="e0a80-119">No es compatible con ninguna de las características que requiere un recurso externo, como localizar un esquema o DTD externo.</span><span class="sxs-lookup"><span data-stu-id="e0a80-119">Features that require an external resource are not supported, such as locating an external schema, or DTD.</span></span> <span data-ttu-id="e0a80-120">Tampoco resuelve entidades externas ni admite funciones de edición, como la inserción de nodos de entidad que requieren resolución.</span><span class="sxs-lookup"><span data-stu-id="e0a80-120">Nor will external entities be resolved, and performing editing functions, such as inserting entity nodes that require resolution, are not supported.</span></span>|<span data-ttu-id="e0a80-121">El objeto **XmlDocument** utiliza permisos anónimos para cargar archivos y no intenta resolver ningún otro recurso.</span><span class="sxs-lookup"><span data-stu-id="e0a80-121">The **XmlDocument** loads files as anonymous and does not attempt to resolve any other resources.</span></span>|  
|<span data-ttu-id="e0a80-122">El valor de la propiedad no se establece, aunque se deja en su estado predeterminado.</span><span class="sxs-lookup"><span data-stu-id="e0a80-122">The property is not set, but left in its default state.</span></span>|<span data-ttu-id="e0a80-123">Se crea una instancia de **XmlUrlResolver** con las credenciales NULL que utiliza el objeto **XmlDocument** al resolver nombres de archivo, localizar las DTD, entidades y esquemas externos; las credenciales **null** se utilizan al editar nodos.</span><span class="sxs-lookup"><span data-stu-id="e0a80-123">An **XmlUrlResolver** with NULL credentials will be instantiated and used by the **XmlDocument** when resolving file names, locating external DTDs, entities, and schemas, and **null** credentials are used when editing nodes.</span></span>||  
  
 <span data-ttu-id="e0a80-124">La tabla siguiente muestra el método **XmlDocument.Load** cuando la entrada para **Load** es **XmlReader** y el objeto **XmlDocument** es de plena confianza.</span><span class="sxs-lookup"><span data-stu-id="e0a80-124">The following table shows the **XmlDocument.Load** method when the input to the **Load** is an **XmlReader** and the **XmlDocument** is fully trusted.</span></span>  
  
|<span data-ttu-id="e0a80-125">Propiedad XmlResolver</span><span class="sxs-lookup"><span data-stu-id="e0a80-125">XmlResolver property</span></span>|<span data-ttu-id="e0a80-126">Función</span><span class="sxs-lookup"><span data-stu-id="e0a80-126">Function</span></span>|<span data-ttu-id="e0a80-127">Notas</span><span class="sxs-lookup"><span data-stu-id="e0a80-127">Notes</span></span>|  
|--------------------------|--------------|-----------|  
|<span data-ttu-id="e0a80-128">La clase **XmlResolver** que utiliza el objeto **XmlDocument** es la misma clase que la que utiliza **XmlReader**.</span><span class="sxs-lookup"><span data-stu-id="e0a80-128">The **XmlResolver** class used by the **XmlDocument** is the same class being used by the **XmlReader**.</span></span>|<span data-ttu-id="e0a80-129">El objeto **XmlDocument** utiliza la clase **XmlResolver** asignada a **XmlReader**.</span><span class="sxs-lookup"><span data-stu-id="e0a80-129">The **XmlDocument** uses the **XmlResolver** that was assigned to the **XmlReader**.</span></span><br /><br /> <span data-ttu-id="e0a80-130">No se puede establecer la propiedad **XmlDocument.Resolver**, independientemente del nivel de confianza de **XmlDocument** porque obtiene una clase **XmlResolver** de **XmlReader**.</span><span class="sxs-lookup"><span data-stu-id="e0a80-130">The **XmlDocument.Resolver** property cannot be set, regardless of the **XmlDocument** trust level, because it is getting an **XmlResolver** from the **XmlReader**.</span></span> <span data-ttu-id="e0a80-131">No se puede intentar invalidar la configuración **XmlReaders**' **XmlResolver** estableciendo la propiedad **XmlResolver** de **XmlDocument**.</span><span class="sxs-lookup"><span data-stu-id="e0a80-131">You cannot attempt to override the settings of the **XmlReaders**' **XmlResolver** by setting the **XmlResolver** property of the **XmlDocument**.</span></span>|<span data-ttu-id="e0a80-132">La clase **XmlReader** puede ser **XmlTextReader**, **XmlValidatingReader** o un sistema de lectura personalizado.</span><span class="sxs-lookup"><span data-stu-id="e0a80-132">The **XmlReader** can be the **XmlTextReader**, **XmlValidatingReader**, or a custom-written reader.</span></span> <span data-ttu-id="e0a80-133">Si el sistema de lectura utilizado admite la resolución de entidades, las entidades externas se resuelven.</span><span class="sxs-lookup"><span data-stu-id="e0a80-133">If the reader used supports entity resolution, external entities are resolved.</span></span> <span data-ttu-id="e0a80-134">Si el sistema de lectura que se ha pasado no admite las referencias de entidad, entonces no se resuelven.</span><span class="sxs-lookup"><span data-stu-id="e0a80-134">If the reader passed does not support entity references, then entity references are not resolved.</span></span>|  
  
## <a name="semi-trusted-xmldocument"></a><span data-ttu-id="e0a80-135">XmlDocument de confianza parcial</span><span class="sxs-lookup"><span data-stu-id="e0a80-135">Semi-Trusted XmlDocument</span></span>  
 <span data-ttu-id="e0a80-136">La tabla siguiente muestra cómo funciona la propiedad **XmlDocument.XmlResolver** cuando el objeto es de confianza parcial.</span><span class="sxs-lookup"><span data-stu-id="e0a80-136">The following table shows how the **XmlDocument.XmlResolver** property works when the object is semi-trusted.</span></span> <span data-ttu-id="e0a80-137">Esta tabla se aplica a los métodos **XmlDocument.Load** cuando la entrada para Load es **TextReader**, **String**, **Stream** o **URI**.</span><span class="sxs-lookup"><span data-stu-id="e0a80-137">This table applies to the **XmlDocument.Load** methods when the input to the Load is a **TextReader**, **String**, **Stream**, or **URI**.</span></span> <span data-ttu-id="e0a80-138">Esta tabla no se aplica al método **Load** si **XmlDocument** se carga de **XmlReader**.</span><span class="sxs-lookup"><span data-stu-id="e0a80-138">This table does not apply to the **Load** method if the **XmlDocument** is loaded from an **XmlReader**.</span></span>  
  
|<span data-ttu-id="e0a80-139">Propiedad XmlResolver</span><span class="sxs-lookup"><span data-stu-id="e0a80-139">XmlResolver property</span></span>|<span data-ttu-id="e0a80-140">Función</span><span class="sxs-lookup"><span data-stu-id="e0a80-140">Function</span></span>|<span data-ttu-id="e0a80-141">Notas</span><span class="sxs-lookup"><span data-stu-id="e0a80-141">Notes</span></span>|  
|--------------------------|--------------|-----------|  
|<span data-ttu-id="e0a80-142">En el escenario de confianza parcial, la propiedad **XmlResolver** no se puede establecer en ningún otro valor distinto de null.</span><span class="sxs-lookup"><span data-stu-id="e0a80-142">In the semi-trusted scenario, the **XmlResolver** property cannot be set to anything other than null.</span></span>|<span data-ttu-id="e0a80-143">Se crea una instancia de **XmlUrlResolver** con las credenciales **NULL** que utiliza el objeto **XmlDocument** al resolver nombres de archivo, localizar las DTD, entidades y esquemas externos; las credenciales **NULL** se utilizan al editar nodos.</span><span class="sxs-lookup"><span data-stu-id="e0a80-143">An **XmlUrlResolver** with **null** credentials will be instantiated and used by the **XmlDocument** when resolving file names, locating external DTDs, entities, and schemas, and **null** credentials are used when editing nodes.</span></span>|<span data-ttu-id="e0a80-144">Este comportamiento es idéntico al que tiene la propiedad **XmlResolver** cuando no se establece y se deja en su estado predeterminado.</span><span class="sxs-lookup"><span data-stu-id="e0a80-144">This behavior is identical to the behavior when the **XmlResolver** property is not set, but left in its default state.</span></span><br /><br /> <span data-ttu-id="e0a80-145">El objeto **XmlDocument** utiliza permisos anónimos para todas las acciones.</span><span class="sxs-lookup"><span data-stu-id="e0a80-145">The **XmlDocument** uses anonymous permissions for all actions.</span></span>|  
|<span data-ttu-id="e0a80-146">La propiedad se establece en **null** (**Nothing** en Microsoft Visual Basic .NET).</span><span class="sxs-lookup"><span data-stu-id="e0a80-146">The property is set to **null** (**Nothing** in Microsoft Visual Basic .NET).</span></span>|<span data-ttu-id="e0a80-147">No es compatible con ninguna de las características que requiere un recurso externo, como localizar un esquema o DTD externo.</span><span class="sxs-lookup"><span data-stu-id="e0a80-147">No features that require an external resource are supported, such as locating an external schema or DTD.</span></span> <span data-ttu-id="e0a80-148">Tampoco resuelve entidades externas ni admite funciones de edición, como la inserción de nodos de entidad que requieren resolución.</span><span class="sxs-lookup"><span data-stu-id="e0a80-148">Nor will external entities be resolved, and performing editing functions such as inserting entity nodes that require resolution, are not supported.</span></span>|<span data-ttu-id="e0a80-149">Cuando el valor de la propiedad es **null** el comportamiento es el mismo, independientemente de que el objeto **XmlDocument** sea de plena confianza o de confianza parcial.</span><span class="sxs-lookup"><span data-stu-id="e0a80-149">When the property is **null**, the behavior is the same regardless if the **XmlDocument** is fully trusted or semi-trusted.</span></span>|  
|<span data-ttu-id="e0a80-150">El valor de la propiedad no se establece, aunque se deja en su estado predeterminado.</span><span class="sxs-lookup"><span data-stu-id="e0a80-150">The property is not set, but left in its default state.</span></span>|<span data-ttu-id="e0a80-151">Se crea una instancia de **XmlUrlResolver** con las credenciales **NULL** que utiliza el objeto **XmlDocument** al resolver nombres de archivo, localizar las DTD, entidades y esquemas externos; las credenciales **NULL** se utilizan al editar nodos.</span><span class="sxs-lookup"><span data-stu-id="e0a80-151">An **XmlUrlResolver** with **null** credentials will be instantiated and used by the **XmlDocument** when resolving file names, locating external DTDs, entities, and schemas, and **null** credentials are used when editing nodes.</span></span>|<span data-ttu-id="e0a80-152">El objeto **XmlDocument** utiliza permisos anónimos para todas las acciones.</span><span class="sxs-lookup"><span data-stu-id="e0a80-152">The **XmlDocument** uses anonymous permissions for all actions.</span></span>|  
  
 <span data-ttu-id="e0a80-153">Esta tabla se aplica al método **XmlDocument.Load** cuando la entrada para **Load** es **XmlReader** y el objeto **XmlDocument** es de confianza parcial.</span><span class="sxs-lookup"><span data-stu-id="e0a80-153">This table applies to the **XmlDocument.Load** method when the input to the **Load** is an **XmlReader**, and the **XmlDocument** is semi-trusted.</span></span>  
  
|<span data-ttu-id="e0a80-154">Propiedad XmlResolver</span><span class="sxs-lookup"><span data-stu-id="e0a80-154">XmlResolver property</span></span>|<span data-ttu-id="e0a80-155">Función</span><span class="sxs-lookup"><span data-stu-id="e0a80-155">Function</span></span>|<span data-ttu-id="e0a80-156">Notas</span><span class="sxs-lookup"><span data-stu-id="e0a80-156">Notes</span></span>|  
|--------------------------|--------------|-----------|  
|<span data-ttu-id="e0a80-157">La clase **XmlResolver** que utiliza el objeto **XmlDocument** es la misma clase que la que utiliza **XmlReader**.</span><span class="sxs-lookup"><span data-stu-id="e0a80-157">The **XmlResolver** class used by the **XmlDocument** is the same one being used by the **XmlReader**.</span></span>|<span data-ttu-id="e0a80-158">El objeto **XmlDocument** utiliza la clase **XmlResolver** asignada a **XmlReader**.</span><span class="sxs-lookup"><span data-stu-id="e0a80-158">The **XmlDocument** uses the **XmlResolver** that was assigned to the **XmlReader**.</span></span><br /><br /> <span data-ttu-id="e0a80-159">No se puede establecer la propiedad **XmlDocument.Resolver**, independientemente del nivel de confianza de **XmlDocument** porque obtiene una clase **XmlResolver** de **XmlReader**.</span><span class="sxs-lookup"><span data-stu-id="e0a80-159">The **XmlDocument.Resolver** property cannot be set, regardless of the **XmlDocument** trust level, because it is getting an **XmlResolver** from the **XmlReader**.</span></span> <span data-ttu-id="e0a80-160">No se puede intentar invalidar la configuración de **XmlReaders** **XmlResolver** estableciendo la propiedad **XmlResolver** de **XmlDocument**.</span><span class="sxs-lookup"><span data-stu-id="e0a80-160">You cannot attempt to override the settings of the **XmlReaders** **XmlResolver** by setting the **XmlResolver** property of the **XmlDocument**.</span></span>|<span data-ttu-id="e0a80-161">La clase **XmlReader** puede ser **XmlTextReader**, validando <xref:System.Xml.XmlReader>, o un lector de escritura personalizada.</span><span class="sxs-lookup"><span data-stu-id="e0a80-161">The **XmlReader** can be the **XmlTextReader**, validating <xref:System.Xml.XmlReader>, or a custom-written reader.</span></span> <span data-ttu-id="e0a80-162">Si el sistema de lectura utilizado admite la resolución de entidades, las entidades externas se resuelven.</span><span class="sxs-lookup"><span data-stu-id="e0a80-162">If the reader used supports entity resolution, external entities are resolved.</span></span> <span data-ttu-id="e0a80-163">Si el sistema de lectura que se ha pasado no admite las referencias de entidad, entonces no se resuelven.</span><span class="sxs-lookup"><span data-stu-id="e0a80-163">If the reader passed in does not support entity references, then entity references are not resolved.</span></span>|  
  
 <span data-ttu-id="e0a80-164">El ajuste de XmlResolver para contener las credenciales correctas permite el acceso a los recursos externos.</span><span class="sxs-lookup"><span data-stu-id="e0a80-164">Setting the XmlResolver to contain the correct credentials allows access to external resources.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="e0a80-165">No hay ninguna forma de recuperar el valor de la propiedad **XmlResolver**.</span><span class="sxs-lookup"><span data-stu-id="e0a80-165">There is no way to retrieve the **XmlResolver** property.</span></span> <span data-ttu-id="e0a80-166">Esto ayuda a evitar que un usuario vuelva a utilizar una propiedad **XmlResolver** en la que se hayan establecido credenciales.</span><span class="sxs-lookup"><span data-stu-id="e0a80-166">This helps to prevent a user from reusing an **XmlResolver** on which credentials have been set.</span></span> <span data-ttu-id="e0a80-167">Además, si se utiliza una clase **XmlTextReader** o se valida <xref:System.Xml.XmlReader> para cargar **XmlDocument** y **XmlDocument** tiene un sistema de resolución establecido, los correspondientes a estos sistemas de lectura no son almacenados en caché por **XmlDocument** después de la fase **Load**, puesto que esto también presenta un riesgo de seguridad.</span><span class="sxs-lookup"><span data-stu-id="e0a80-167">Also, if an **XmlTextReader** or validating <xref:System.Xml.XmlReader> is used to load the **XmlDocument** and the **XmlDocument** has a resolver that has been set, the resolvers from these readers are not cached by the **XmlDocument** after the **Load** phase, since this also presents a security risk.</span></span>  
  
 <span data-ttu-id="e0a80-168">Para obtener más información, vea la sección Comentarios de la página de referencia de <xref:System.Xml.XmlResolver>.</span><span class="sxs-lookup"><span data-stu-id="e0a80-168">For more information, see the Remarks section of the <xref:System.Xml.XmlResolver> reference page.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="e0a80-169">Vea también</span><span class="sxs-lookup"><span data-stu-id="e0a80-169">See also</span></span>

- [<span data-ttu-id="e0a80-170">Document Object Model (DOM) para XML</span><span class="sxs-lookup"><span data-stu-id="e0a80-170">XML Document Object Model (DOM)</span></span>](xml-document-object-model-dom.md)
