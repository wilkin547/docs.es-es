---
title: 'Procedimiento: Definición de la igualdad de valores para un tipo (Guía de programación de C#)'
ms.date: 07/20/2015
helpviewer_keywords:
- overriding Equals method [C#]
- object equivalence [C#]
- Equals method [C#], overriding
- value equality [C#]
- equivalence [C#]
ms.assetid: 4084581e-b931-498b-9534-cf7ef5b68690
ms.openlocfilehash: 5eb1aaf96097d2c00cb04e24e65e01464f5f00c6
ms.sourcegitcommit: 5f236cd78cf09593c8945a7d753e0850e96a0b80
ms.translationtype: HT
ms.contentlocale: es-ES
ms.lasthandoff: 01/07/2020
ms.locfileid: "75711979"
---
# <a name="how-to-define-value-equality-for-a-type-c-programming-guide"></a><span data-ttu-id="18b27-102">Procedimiento: Definición de la igualdad de valores para un tipo (Guía de programación de C#)</span><span class="sxs-lookup"><span data-stu-id="18b27-102">How to define value equality for a type (C# Programming Guide)</span></span>

<span data-ttu-id="18b27-103">Cuando defina una clase o un struct, debe decidir si tiene sentido crear una definición personalizada de igualdad (o equivalencia) de valores para el tipo.</span><span class="sxs-lookup"><span data-stu-id="18b27-103">When you define a class or struct, you decide whether it makes sense to create a custom definition of value equality (or equivalence) for the type.</span></span> <span data-ttu-id="18b27-104">Normalmente, la igualdad de valores se implementa cuando se espera agregar objetos del tipo a una colección de algún tipo, o cuando su objetivo principal es almacenar un conjunto de campos o propiedades.</span><span class="sxs-lookup"><span data-stu-id="18b27-104">Typically, you implement value equality when objects of the type are expected to be added to a collection of some sort, or when their primary purpose is to store a set of fields or properties.</span></span> <span data-ttu-id="18b27-105">Puede basar la definición de la igualdad de valores en una comparación de todos los campos y propiedades del tipo, o bien puede basarla en un subconjunto.</span><span class="sxs-lookup"><span data-stu-id="18b27-105">You can base your definition of value equality on a comparison of all the fields and properties in the type, or you can base the definition on a subset.</span></span> <span data-ttu-id="18b27-106">En cualquier caso, y tanto en las clases como en los structs, la implementación debe cumplir las cinco garantías de equivalencia:</span><span class="sxs-lookup"><span data-stu-id="18b27-106">But in either case, and in both classes and structs, your implementation should follow the five guarantees of equivalence:</span></span>  
  
1. <span data-ttu-id="18b27-107">`x.Equals(x)` devuelve `true`.</span><span class="sxs-lookup"><span data-stu-id="18b27-107">`x.Equals(x)` returns `true`.</span></span> <span data-ttu-id="18b27-108">Esto se denomina propiedad reflexiva.</span><span class="sxs-lookup"><span data-stu-id="18b27-108">This is called the reflexive property.</span></span>  
  
2. <span data-ttu-id="18b27-109">`x.Equals(y)` devuelve el mismo valor que `y.Equals(x)`.</span><span class="sxs-lookup"><span data-stu-id="18b27-109">`x.Equals(y)` returns the same value as `y.Equals(x)`.</span></span> <span data-ttu-id="18b27-110">Esto se denomina propiedad simétrica.</span><span class="sxs-lookup"><span data-stu-id="18b27-110">This is called the symmetric property.</span></span>  
  
3. <span data-ttu-id="18b27-111">Si `(x.Equals(y) && y.Equals(z))` devuelve `true`, `x.Equals(z)` devuelve `true`.</span><span class="sxs-lookup"><span data-stu-id="18b27-111">if `(x.Equals(y) && y.Equals(z))` returns `true`, then `x.Equals(z)` returns `true`.</span></span> <span data-ttu-id="18b27-112">Esto se denomina propiedad transitiva.</span><span class="sxs-lookup"><span data-stu-id="18b27-112">This is called the transitive property.</span></span>  
  
4. <span data-ttu-id="18b27-113">Las invocaciones sucesivas de `x.Equals(y)` devuelven el mismo valor siempre y cuando los objetos a los que x e y hacen referencia no se modifiquen.</span><span class="sxs-lookup"><span data-stu-id="18b27-113">Successive invocations of `x.Equals(y)` return the same value as long as the objects referenced by x and y are not modified.</span></span>  
  
5. <span data-ttu-id="18b27-114">`x.Equals(null)` devuelve `false`.</span><span class="sxs-lookup"><span data-stu-id="18b27-114">`x.Equals(null)` returns `false`.</span></span> <span data-ttu-id="18b27-115">Pero `null.Equals(null)` produce una excepción; no cumple la regla anterior número dos.</span><span class="sxs-lookup"><span data-stu-id="18b27-115">However, `null.Equals(null)` throws an exception; it does not obey rule number two above.</span></span>  
  
 <span data-ttu-id="18b27-116">Cualquier struct que defina ya tiene una implementación predeterminada de igualdad de valor que hereda de la invalidación <xref:System.ValueType?displayProperty=nameWithType> del método <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="18b27-116">Any struct that you define already has a default implementation of value equality that it inherits from the <xref:System.ValueType?displayProperty=nameWithType> override of the <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="18b27-117">Esta implementación usa la reflexión para examinar todos los campos y propiedades del tipo.</span><span class="sxs-lookup"><span data-stu-id="18b27-117">This implementation uses reflection to examine all the fields and properties in the type.</span></span> <span data-ttu-id="18b27-118">Aunque esta implementación genera resultados correctos, es relativamente lenta en comparación con una implementación personalizada escrita específicamente para el tipo.</span><span class="sxs-lookup"><span data-stu-id="18b27-118">Although this implementation produces correct results, it is relatively slow compared to a custom implementation that you write specifically for the type.</span></span>  
  
 <span data-ttu-id="18b27-119">Los detalles de implementación para la igualdad de valores son diferentes para las clases y los structs.</span><span class="sxs-lookup"><span data-stu-id="18b27-119">The implementation details for value equality are different for classes and structs.</span></span> <span data-ttu-id="18b27-120">A pesar de ello, tanto las clases como los structs requieren los mismos pasos básicos para implementar la igualdad:</span><span class="sxs-lookup"><span data-stu-id="18b27-120">However, both classes and structs require the same basic steps for implementing equality:</span></span>  
  
1. <span data-ttu-id="18b27-121">Invalide el método [virtual](../../language-reference/keywords/virtual.md) <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="18b27-121">Override the [virtual](../../language-reference/keywords/virtual.md) <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="18b27-122">En la mayoría de los casos, la implementación de `bool Equals( object obj )` debería llamar solamente al método `Equals` específico del tipo que es la implementación de la interfaz <xref:System.IEquatable%601?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="18b27-122">In most cases, your implementation of `bool Equals( object obj )` should just call into the type-specific `Equals` method that is the implementation of the <xref:System.IEquatable%601?displayProperty=nameWithType> interface.</span></span> <span data-ttu-id="18b27-123">(Vea el paso 2).</span><span class="sxs-lookup"><span data-stu-id="18b27-123">(See step 2.)</span></span>  
  
2. <span data-ttu-id="18b27-124">Implemente la interfaz <xref:System.IEquatable%601?displayProperty=nameWithType> proporcionando un método `Equals` específico del tipo.</span><span class="sxs-lookup"><span data-stu-id="18b27-124">Implement the <xref:System.IEquatable%601?displayProperty=nameWithType> interface by providing a type-specific `Equals` method.</span></span> <span data-ttu-id="18b27-125">Aquí es donde se realiza la comparación de equivalencias propiamente dicha.</span><span class="sxs-lookup"><span data-stu-id="18b27-125">This is where the actual equivalence comparison is performed.</span></span> <span data-ttu-id="18b27-126">Por ejemplo, podría decidir que, para definir la igualdad, solo se comparen uno o dos campos del tipo.</span><span class="sxs-lookup"><span data-stu-id="18b27-126">For example, you might decide to define equality by comparing only one or two fields in your type.</span></span> <span data-ttu-id="18b27-127">No genere excepciones desde `Equals`.</span><span class="sxs-lookup"><span data-stu-id="18b27-127">Do not throw exceptions from `Equals`.</span></span> <span data-ttu-id="18b27-128">Solo para las clases: este método debe examinar únicamente los campos que se declaran en la clase.</span><span class="sxs-lookup"><span data-stu-id="18b27-128">For classes only: This method should examine only fields that are declared in the class.</span></span> <span data-ttu-id="18b27-129">Debe llamar a `base.Equals` para examinar los campos que están en la clase base.</span><span class="sxs-lookup"><span data-stu-id="18b27-129">It should call `base.Equals` to examine fields that are in the base class.</span></span> <span data-ttu-id="18b27-130">(No realice esto si el tipo hereda directamente de <xref:System.Object>, porque la implementación <xref:System.Object> de <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> realiza una comprobación de igualdad de referencia).</span><span class="sxs-lookup"><span data-stu-id="18b27-130">(Do not do this if the type inherits directly from <xref:System.Object>, because the <xref:System.Object> implementation of <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> performs a reference equality check.)</span></span>  
  
3. <span data-ttu-id="18b27-131">Opcional, pero recomendado: Sobrecargue los operadores [==](../../language-reference/operators/equality-operators.md#equality-operator-) y [!=](../../language-reference/operators/equality-operators.md#inequality-operator-).</span><span class="sxs-lookup"><span data-stu-id="18b27-131">Optional but recommended: Overload the [==](../../language-reference/operators/equality-operators.md#equality-operator-) and [!=](../../language-reference/operators/equality-operators.md#inequality-operator-) operators.</span></span>  
  
4. <span data-ttu-id="18b27-132">Invalide <xref:System.Object.GetHashCode%2A?displayProperty=nameWithType> de manera que dos objetos que tengan igualdad de valor produzcan el mismo código hash.</span><span class="sxs-lookup"><span data-stu-id="18b27-132">Override <xref:System.Object.GetHashCode%2A?displayProperty=nameWithType> so that two objects that have value equality produce the same hash code.</span></span>  
  
5. <span data-ttu-id="18b27-133">Opcional: Para admitir definiciones para "mayor que" o "menor que", implemente la interfaz <xref:System.IComparable%601> para el tipo y sobrecargue también los operadores [<=](../../language-reference/operators/comparison-operators.md#less-than-or-equal-operator-) y [>=](../../language-reference/operators/comparison-operators.md#greater-than-or-equal-operator-).</span><span class="sxs-lookup"><span data-stu-id="18b27-133">Optional: To support definitions for "greater than" or "less than," implement the <xref:System.IComparable%601> interface for your type, and also overload the [<=](../../language-reference/operators/comparison-operators.md#less-than-or-equal-operator-) and [>=](../../language-reference/operators/comparison-operators.md#greater-than-or-equal-operator-) operators.</span></span>  
  
 <span data-ttu-id="18b27-134">En el primer ejemplo que aparece más abajo se muestra una implementación de clase.</span><span class="sxs-lookup"><span data-stu-id="18b27-134">The first example that follows shows a class implementation.</span></span> <span data-ttu-id="18b27-135">En el segundo ejemplo se muestra una implementación de struct.</span><span class="sxs-lookup"><span data-stu-id="18b27-135">The second example shows a struct implementation.</span></span>  

## <a name="example"></a><span data-ttu-id="18b27-136">Ejemplo</span><span class="sxs-lookup"><span data-stu-id="18b27-136">Example</span></span>

 <span data-ttu-id="18b27-137">En el ejemplo siguiente se muestra cómo implementar la igualdad de valores en una clase (tipo de referencia).</span><span class="sxs-lookup"><span data-stu-id="18b27-137">The following example shows how to implement value equality in a class (reference type).</span></span>  
  
 [!code-csharp[csProgGuideStatements#19](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideStatements/CS/Statements.cs#19)]  
  
 <span data-ttu-id="18b27-138">En las clases (tipos de referencia), la implementación predeterminada de ambos métodos <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> realiza una comparación de igualdad de referencia, no una comprobación de igualdad de valores.</span><span class="sxs-lookup"><span data-stu-id="18b27-138">On classes (reference types), the default implementation of both <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> methods performs a reference equality comparison, not a value equality check.</span></span> <span data-ttu-id="18b27-139">Cuando un implementador invalida el método virtual, lo hace para asignarle semántica de igualdad de valores.</span><span class="sxs-lookup"><span data-stu-id="18b27-139">When an implementer overrides the virtual method, the purpose is to give it value equality semantics.</span></span>  
  
 <span data-ttu-id="18b27-140">Los operadores `==` y `!=` pueden usarse con clases, incluso si la clase no los sobrecarga,</span><span class="sxs-lookup"><span data-stu-id="18b27-140">The `==` and `!=` operators can be used with classes even if the class does not overload them.</span></span> <span data-ttu-id="18b27-141">pero el comportamiento predeterminado consiste en realizar una comprobación de igualdad de referencia.</span><span class="sxs-lookup"><span data-stu-id="18b27-141">However, the default behavior is to perform a reference equality check.</span></span> <span data-ttu-id="18b27-142">En una clase, si sobrecarga el método `Equals`, debería sobrecargar los operadores `==` y `!=`, pero no es obligatorio.</span><span class="sxs-lookup"><span data-stu-id="18b27-142">In a class, if you overload the `Equals` method, you should overload the `==` and `!=` operators, but it is not required.</span></span>  

## <a name="example"></a><span data-ttu-id="18b27-143">Ejemplo</span><span class="sxs-lookup"><span data-stu-id="18b27-143">Example</span></span>

 <span data-ttu-id="18b27-144">En el ejemplo siguiente se muestra cómo implementar la igualdad de valores en un struct (tipo de valor):</span><span class="sxs-lookup"><span data-stu-id="18b27-144">The following example shows how to implement value equality in a struct (value type):</span></span>  
  
 [!code-csharp[csProgGuideStatements#20](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideStatements/CS/Statements.cs#20)]  
  
 <span data-ttu-id="18b27-145">Para los structs, la implementación predeterminada de <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> (que es la versión invalidada de <xref:System.ValueType?displayProperty=nameWithType>) realiza una comprobación de igualdad de valor con la reflexión para comparar valores de cada campo en el tipo.</span><span class="sxs-lookup"><span data-stu-id="18b27-145">For structs, the default implementation of <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> (which is the overridden version in <xref:System.ValueType?displayProperty=nameWithType>) performs a value equality check by using reflection to compare the values of every field in the type.</span></span> <span data-ttu-id="18b27-146">Cuando un implementador invalida el método `Equals` virtual en un struct, lo hace para proporcionar un medio más eficaz de llevar a cabo la comprobación de igualdad de valores y, opcionalmente, para basar la comparación en un subconjunto de propiedades o campos del struct.</span><span class="sxs-lookup"><span data-stu-id="18b27-146">When an implementer overrides the virtual `Equals` method in a struct, the purpose is to provide a more efficient means of performing the value equality check and optionally to base the comparison on some subset of the struct's field or properties.</span></span>  
  
 <span data-ttu-id="18b27-147">Los operadores [==](../../language-reference/operators/equality-operators.md#equality-operator-) y [!=](../../language-reference/operators/equality-operators.md#inequality-operator-) no pueden funcionar en un struct a menos que el struct los sobrecargue explícitamente.</span><span class="sxs-lookup"><span data-stu-id="18b27-147">The [==](../../language-reference/operators/equality-operators.md#equality-operator-) and [!=](../../language-reference/operators/equality-operators.md#inequality-operator-) operators cannot operate on a struct unless the struct explicitly overloads them.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="18b27-148">Vea también</span><span class="sxs-lookup"><span data-stu-id="18b27-148">See also</span></span>

- [<span data-ttu-id="18b27-149">Comparaciones de igualdad</span><span class="sxs-lookup"><span data-stu-id="18b27-149">Equality comparisons</span></span>](equality-comparisons.md)
- [<span data-ttu-id="18b27-150">Guía de programación de C#</span><span class="sxs-lookup"><span data-stu-id="18b27-150">C# Programming Guide</span></span>](../index.md)
