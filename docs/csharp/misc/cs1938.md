---
title: Error del compilador CS1938
ms.date: 07/20/2015
f1_keywords:
- CS1938
helpviewer_keywords:
- CS1938
ms.assetid: fc8de996-f7a1-46e8-b07b-aea520b391b9
ms.openlocfilehash: 51a9e72e1a7fce211397b3c7224d7e69185eb41f
ms.sourcegitcommit: 5b475c1855b32cf78d2d1bbb4295e4c236f39464
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 09/24/2020
ms.locfileid: "91154592"
---
# <a name="compiler-error-cs1938"></a><span data-ttu-id="ebb97-102">Error del compilador CS1938</span><span class="sxs-lookup"><span data-stu-id="ebb97-102">Compiler Error CS1938</span></span>

<span data-ttu-id="ebb97-103">El nombre 'nombre' no está dentro del ámbito en el lado derecho de 'equals'.</span><span class="sxs-lookup"><span data-stu-id="ebb97-103">The name 'name' is not in scope on the right side of 'equals'.</span></span> <span data-ttu-id="ebb97-104">Considere la posibilidad de intercambiar las expresiones en ambos lados de 'equals'.</span><span class="sxs-lookup"><span data-stu-id="ebb97-104">Consider swapping the expressions on either side of 'equals'.</span></span>  
  
 <span data-ttu-id="ebb97-105">La palabra clave `equals` es un operador especial que se usa en una cláusula `join` para determinar la igualdad entre dos expresiones.</span><span class="sxs-lookup"><span data-stu-id="ebb97-105">The `equals` keyword is a special operator that is used in a `join` clause to determine equality between two expressions.</span></span> <span data-ttu-id="ebb97-106">La variable de rango para la secuencia de origen del lado izquierdo está dentro del ámbito en el lado izquierdo de equals y la variable de rango para el origen del lado derecho solo está dentro del ámbito en el lado izquierdo de equals.</span><span class="sxs-lookup"><span data-stu-id="ebb97-106">The range variable for the left side source sequence is in scope on the left side of equals, and the range variable for the right side source is only in scope on the left side of equals.</span></span> <span data-ttu-id="ebb97-107">Para comprobarlo, puede experimentar con IntelliSense en el siguiente ejemplo de código.</span><span class="sxs-lookup"><span data-stu-id="ebb97-107">You can verify this by experimenting with IntelliSense in the following code example.</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="ebb97-108">Para corregir este error</span><span class="sxs-lookup"><span data-stu-id="ebb97-108">To correct this error</span></span>  
  
1. <span data-ttu-id="ebb97-109">Cambie la posición de las dos variables de rango tal como se muestra en la línea comentada en el ejemplo siguiente:</span><span class="sxs-lookup"><span data-stu-id="ebb97-109">Swap the position of the two range variables as shown in the commented line in the following example:</span></span>  
  
## <a name="example"></a><span data-ttu-id="ebb97-110">Ejemplo</span><span class="sxs-lookup"><span data-stu-id="ebb97-110">Example</span></span>  

 <span data-ttu-id="ebb97-111">El código siguiente genera el error CS1938:</span><span class="sxs-lookup"><span data-stu-id="ebb97-111">The following code generates CS1938:</span></span>  
  
```csharp  
// cs1938.cs  
using System.Linq;  
class Test  
{  
    static void Main()  
    {  
        int[] sourceA = { 1, 2, 3, 4, 5 };  
        int[] sourceB = { 3, 4, 5, 6, 7 };  
  
        var query = from a in sourceA  
                    join b in sourceB on b equals a // CS1938  
                    // Try the following line instead.  
                    // join b in sourceB on a equals b  
                    select new { a, b };  
    }  
}  
```  
  
## <a name="see-also"></a><span data-ttu-id="ebb97-112">Vea también</span><span class="sxs-lookup"><span data-stu-id="ebb97-112">See also</span></span>

- [<span data-ttu-id="ebb97-113">join (cláusula)</span><span class="sxs-lookup"><span data-stu-id="ebb97-113">join clause</span></span>](../language-reference/keywords/join-clause.md)
