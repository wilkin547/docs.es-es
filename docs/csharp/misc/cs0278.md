---
title: Advertencia del compilador (nivel 2) CS0278
ms.date: 07/20/2015
f1_keywords:
- CS0278
helpviewer_keywords:
- CS0278
ms.assetid: 5418cbbe-bcec-4379-a6f6-410987beb96a
ms.openlocfilehash: e4b133f21667d79aa2765b46c86adadac663fbd0
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 03/12/2020
ms.locfileid: "79173034"
---
# <a name="compiler-warning-level-2-cs0278"></a><span data-ttu-id="3e11c-102">Advertencia del compilador (nivel 2) CS0278</span><span class="sxs-lookup"><span data-stu-id="3e11c-102">Compiler Warning (level 2) CS0278</span></span>
<span data-ttu-id="3e11c-103">'tipo' no implementa el patrón 'nombre de patrón'.</span><span class="sxs-lookup"><span data-stu-id="3e11c-103">'type' does not implement the 'pattern name' pattern.</span></span> <span data-ttu-id="3e11c-104">'nombre de método' es ambiguo con 'nombre de método'.</span><span class="sxs-lookup"><span data-stu-id="3e11c-104">'method name' is ambiguous with 'method name'.</span></span>  
  
<span data-ttu-id="3e11c-105">Hay varias instrucciones en C# que se basan en patrones definidos, como `foreach` y `using`.</span><span class="sxs-lookup"><span data-stu-id="3e11c-105">There are several statements in C# that rely on defined patterns, such as `foreach` and `using`.</span></span> <span data-ttu-id="3e11c-106">Por ejemplo, la [ `foreach` instrucción](../language-reference/keywords/foreach-in.md) se basa en la clase de colección que implementa el patrón "enumerable".</span><span class="sxs-lookup"><span data-stu-id="3e11c-106">For example, the [`foreach` statement](../language-reference/keywords/foreach-in.md) relies on the collection class implementing the "enumerable" pattern.</span></span>
  
<span data-ttu-id="3e11c-107">Puede producirse la advertencia CS0278 si el compilador no puede establecer la coincidencia debido a ambigüedades.</span><span class="sxs-lookup"><span data-stu-id="3e11c-107">CS0278 can occur if the compiler is unable to make the match due to ambiguities.</span></span> <span data-ttu-id="3e11c-108">Por ejemplo, el patrón "enumerable" necesita un método llamado `MoveNext`, y el código puede contener dos métodos denominados `MoveNext`.</span><span class="sxs-lookup"><span data-stu-id="3e11c-108">For example, the "enumerable" pattern requires that there be a method called `MoveNext`, and your code might contain two methods called `MoveNext`.</span></span> <span data-ttu-id="3e11c-109">El compilador intentará encontrar una interfaz que pueda usar, pero se recomienda que determine y resuelva la causa de la ambigüedad.</span><span class="sxs-lookup"><span data-stu-id="3e11c-109">The compiler will attempt to find an interface to use, but it is recommended that you determine and resolve the cause of the ambiguity.</span></span>  
  
## <a name="example"></a><span data-ttu-id="3e11c-110">Ejemplo</span><span class="sxs-lookup"><span data-stu-id="3e11c-110">Example</span></span>  
 <span data-ttu-id="3e11c-111">El ejemplo siguiente genera la advertencia CS0278.</span><span class="sxs-lookup"><span data-stu-id="3e11c-111">The following sample generates CS0278.</span></span>  
  
```csharp  
// CS0278.cs  
using System.Collections.Generic;  
public class myTest
{  
   public static void TestForeach<W>(W w)
      where W: IEnumerable<int>, IEnumerable<string>  
   {  
      foreach (int i in w) {}   // CS0278  
   }  
}  
```
