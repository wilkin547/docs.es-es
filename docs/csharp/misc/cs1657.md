---
description: Error del compilador CS1657
title: Error del compilador CS1657
ms.date: 07/20/2015
f1_keywords:
- CS1657
helpviewer_keywords:
- CS1657
ms.assetid: 6f0aeebe-5c90-4d5b-981c-1795d2e8fbb9
ms.openlocfilehash: 38cab70d9079f53fdd5507eb3a1d69431aeaa05c
ms.sourcegitcommit: 5b475c1855b32cf78d2d1bbb4295e4c236f39464
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 09/24/2020
ms.locfileid: "91199268"
---
# <a name="compiler-error-cs1657"></a><span data-ttu-id="8f55c-103">Error del compilador CS1657</span><span class="sxs-lookup"><span data-stu-id="8f55c-103">Compiler Error CS1657</span></span>

<span data-ttu-id="8f55c-104">No se puede pasar 'parameter' como argumento out o ref porque es 'reason'</span><span class="sxs-lookup"><span data-stu-id="8f55c-104">Cannot pass 'parameter' as a ref or out argument because 'reason''</span></span>  
  
 <span data-ttu-id="8f55c-105">Este error se produce cuando se pasa una variable como argumento [ref](../language-reference/keywords/ref.md) o [out](../language-reference/keywords/out-parameter-modifier.md) en un contexto en el que dicha variable es de solo lectura.</span><span class="sxs-lookup"><span data-stu-id="8f55c-105">This error occurs when a variable is passed as a [ref](../language-reference/keywords/ref.md) or [out](../language-reference/keywords/out-parameter-modifier.md) argument in a context in which that variable is readonly.</span></span> <span data-ttu-id="8f55c-106">Los contextos de solo lectura incluyen las variables de iteración [foreach](../language-reference/keywords/foreach-in.md) , variables [using](../language-reference/keywords/using-statement.md) y variables `fixed` .</span><span class="sxs-lookup"><span data-stu-id="8f55c-106">Readonly contexts include [foreach](../language-reference/keywords/foreach-in.md) iteration variables, [using](../language-reference/keywords/using-statement.md) variables, and `fixed` variables.</span></span> <span data-ttu-id="8f55c-107">Para resolver este error, no llame a las funciones que toman la variable `foreach`, `using` o `fixed` como parámetro `ref` o `out` en bloques `using` , instrucciones `foreach` e instrucciones `fixed` .</span><span class="sxs-lookup"><span data-stu-id="8f55c-107">To resolve this error, do not call functions that take the `foreach`, `using` or `fixed` variable as a `ref` or `out` parameter in `using` blocks, `foreach` statements, and `fixed` statements.</span></span>  
  
## <a name="example"></a><span data-ttu-id="8f55c-108">Ejemplo</span><span class="sxs-lookup"><span data-stu-id="8f55c-108">Example</span></span>  

 <span data-ttu-id="8f55c-109">El ejemplo siguiente genera la advertencia CS1657:</span><span class="sxs-lookup"><span data-stu-id="8f55c-109">The following sample generates CS1657:</span></span>  
  
```csharp  
// CS1657.cs  
using System;  
class C : IDisposable  
{  
    public int i;  
    public void Dispose() {}  
}  
  
class CMain  
{  
    static void f(ref C c)  
    {  
    }  
    static void Main()  
    {  
        using (C c = new C())  
        {  
            f(ref c);  // CS1657  
        }  
    }  
}  
```  
  
## <a name="example"></a><span data-ttu-id="8f55c-110">Ejemplo</span><span class="sxs-lookup"><span data-stu-id="8f55c-110">Example</span></span>  

 <span data-ttu-id="8f55c-111">El código siguiente ilustra el mismo problema en una instrucción `fixed` :</span><span class="sxs-lookup"><span data-stu-id="8f55c-111">The following code illustrates the same problem in a `fixed` statement:</span></span>  
  
```csharp  
// CS1657b.cs  
// compile with: /unsafe  
unsafe class C  
{  
    static void F(ref int* p)  
    {  
    }  
  
    static void Main()  
    {  
        int[] a = new int[5];  
        fixed(int* p = a) F(ref p); // CS1657  
    }  
}  
```
