---
title: 'Administración de recursos: La palabra clave use'
description: Obtenga información sobre la F# palabra clave 'use' y la función "using", que puede controlar la inicialización y la liberación de recursos.
ms.date: 05/16/2016
ms.openlocfilehash: 127877a3823faade9bc3c6aefea655c86cc348e7
ms.sourcegitcommit: fa38fe76abdc8972e37138fcb4dfdb3502ac5394
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 12/19/2018
ms.locfileid: "53613094"
---
# <a name="resource-management-the-use-keyword"></a><span data-ttu-id="ac149-103">Administración de recursos: La palabra clave use</span><span class="sxs-lookup"><span data-stu-id="ac149-103">Resource Management: The use Keyword</span></span>

<span data-ttu-id="ac149-104">Este tema describe la palabra clave `use` y `using` función, que puede controlar la inicialización y la liberación de recursos.</span><span class="sxs-lookup"><span data-stu-id="ac149-104">This topic describes the keyword `use` and the `using` function, which can control the initialization and release of resources.</span></span>

## <a name="resources"></a><span data-ttu-id="ac149-105">Recursos</span><span class="sxs-lookup"><span data-stu-id="ac149-105">Resources</span></span>

<span data-ttu-id="ac149-106">El término *recursos* se usa en más de una forma.</span><span class="sxs-lookup"><span data-stu-id="ac149-106">The term *resource* is used in more than one way.</span></span> <span data-ttu-id="ac149-107">Sí, los recursos pueden ser datos que utiliza una aplicación, como cadenas, gráficos y similares, pero en este contexto, *recursos* hace referencia a los recursos de software o del sistema operativo, como los contextos de dispositivo de gráficos, identificadores de archivo, red y las conexiones, los objetos de simultaneidad como identificadores de espera etc. de la base de datos.</span><span class="sxs-lookup"><span data-stu-id="ac149-107">Yes, resources can be data that an application uses, such as strings, graphics, and the like, but in this context, *resources* refers to software or operating system resources, such as graphics device contexts, file handles, network and database connections, concurrency objects such as wait handles, and so on.</span></span> <span data-ttu-id="ac149-108">El uso de estos recursos por aplicaciones implica la adquisición de recursos del sistema operativo o de otro proveedor de recursos, seguida de la versión posterior del recurso en el grupo para que se puede proporcionar a otra aplicación.</span><span class="sxs-lookup"><span data-stu-id="ac149-108">The use of these resources by applications involves the acquisition of the resource from the operating system or other resource provider, followed by the later release of the resource to the pool so that it can be provided to another application.</span></span> <span data-ttu-id="ac149-109">Se producen problemas cuando las aplicaciones no liberan recursos al grupo comunes.</span><span class="sxs-lookup"><span data-stu-id="ac149-109">Problems occur when applications do not release resources back to the common pool.</span></span>

## <a name="managing-resources"></a><span data-ttu-id="ac149-110">Administrar recursos</span><span class="sxs-lookup"><span data-stu-id="ac149-110">Managing Resources</span></span>

<span data-ttu-id="ac149-111">Para forma responsable y eficazmente administrar recursos en una aplicación, se deben liberar los recursos de una manera predecible y con prontitud.</span><span class="sxs-lookup"><span data-stu-id="ac149-111">To efficiently and responsibly manage resources in an application, you must release resources promptly and in a predictable manner.</span></span> <span data-ttu-id="ac149-112">.NET Framework le ayuda a hacer esto proporcionando la `System.IDisposable` interfaz.</span><span class="sxs-lookup"><span data-stu-id="ac149-112">The .NET Framework helps you do this by providing the `System.IDisposable` interface.</span></span> <span data-ttu-id="ac149-113">Un tipo que implementa `System.IDisposable` tiene la `System.IDisposable.Dispose` método, que libera los recursos correctamente.</span><span class="sxs-lookup"><span data-stu-id="ac149-113">A type that implements `System.IDisposable` has the `System.IDisposable.Dispose` method, which correctly frees resources.</span></span> <span data-ttu-id="ac149-114">Las aplicaciones bien escritas garantizar que `System.IDisposable.Dispose` se llama con prontitud cuando ya no se necesita cualquier objeto que contiene un recurso limitado.</span><span class="sxs-lookup"><span data-stu-id="ac149-114">Well-written applications guarantee that `System.IDisposable.Dispose` is called promptly when any object that holds a limited resource is no longer needed.</span></span> <span data-ttu-id="ac149-115">Afortunadamente, la mayoría de los lenguajes de .NET proporciona soporte técnico para facilitar esta tarea, y F# es ninguna excepción.</span><span class="sxs-lookup"><span data-stu-id="ac149-115">Fortunately, most .NET languages provide support to make this easier, and F# is no exception.</span></span> <span data-ttu-id="ac149-116">Hay dos construcciones de lenguaje útiles que admiten el patrón de dispose: el `use` enlace y el `using` función.</span><span class="sxs-lookup"><span data-stu-id="ac149-116">There are two useful language constructs that support the dispose pattern: the `use` binding and the `using` function.</span></span>

## <a name="use-binding"></a><span data-ttu-id="ac149-117">Usar el enlace</span><span class="sxs-lookup"><span data-stu-id="ac149-117">use Binding</span></span>

<span data-ttu-id="ac149-118">El `use` palabra clave tiene un formato similar de la `let` enlace:</span><span class="sxs-lookup"><span data-stu-id="ac149-118">The `use` keyword has a form that resembles that of the `let` binding:</span></span>

<span data-ttu-id="ac149-119">usar *valor* = *expresión*</span><span class="sxs-lookup"><span data-stu-id="ac149-119">use *value* = *expression*</span></span>

<span data-ttu-id="ac149-120">Proporciona la misma funcionalidad que un `let` enlace pero agrega una llamada a `Dispose` en el valor cuando el valor se sale del ámbito.</span><span class="sxs-lookup"><span data-stu-id="ac149-120">It provides the same functionality as a `let` binding but adds a call to `Dispose` on the value when the value goes out of scope.</span></span> <span data-ttu-id="ac149-121">Tenga en cuenta que el compilador inserta una comprobación de null en el valor, por lo que si el valor es `null`, la llamada a `Dispose` no se ha intentado.</span><span class="sxs-lookup"><span data-stu-id="ac149-121">Note that the compiler inserts a null check on the value, so that if the value is `null`, the call to `Dispose` is not attempted.</span></span>

<span data-ttu-id="ac149-122">El ejemplo siguiente muestra cómo cerrar un archivo automáticamente con el `use` palabra clave.</span><span class="sxs-lookup"><span data-stu-id="ac149-122">The following example shows how to close a file automatically by using the `use` keyword.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-2/snippet6301.fs)]

> [!NOTE]
> <span data-ttu-id="ac149-123">Puede usar `use` en expresiones de cálculo, en cuyo caso una versión personalizada de la `use` se usa la expresión.</span><span class="sxs-lookup"><span data-stu-id="ac149-123">You can use `use` in computation expressions, in which case a customized version of the `use` expression is used.</span></span> <span data-ttu-id="ac149-124">Para obtener más información, consulte [secuencias](sequences.md), [flujos de trabajo asincrónicos](asynchronous-workflows.md), y [expresiones de cálculo](computation-expressions.md).</span><span class="sxs-lookup"><span data-stu-id="ac149-124">For more information, see [Sequences](sequences.md), [Asynchronous Workflows](asynchronous-workflows.md), and [Computation Expressions](computation-expressions.md).</span></span>

## <a name="using-function"></a><span data-ttu-id="ac149-125">uso de la función</span><span class="sxs-lookup"><span data-stu-id="ac149-125">using Function</span></span>

<span data-ttu-id="ac149-126">El `using` función tiene el formato siguiente:</span><span class="sxs-lookup"><span data-stu-id="ac149-126">The `using` function has the following form:</span></span>

<span data-ttu-id="ac149-127">`using` (*expression1*) *función o expresión lambda*</span><span class="sxs-lookup"><span data-stu-id="ac149-127">`using` (*expression1*) *function-or-lambda*</span></span>

<span data-ttu-id="ac149-128">En un `using` expresión, *expression1* crea el objeto que debe eliminarse.</span><span class="sxs-lookup"><span data-stu-id="ac149-128">In a `using` expression, *expression1* creates the object that must be disposed.</span></span> <span data-ttu-id="ac149-129">El resultado de *expression1* (el objeto que se debe eliminar) se convierte en un argumento, *valor*a *función o lambda*, que es una función que espera un único restante de argumento de un tipo que coincide con el valor generado por *expression1*, o una expresión lambda que espera un argumento de ese tipo.</span><span class="sxs-lookup"><span data-stu-id="ac149-129">The result of *expression1* (the object that must be disposed) becomes an argument, *value*, to *function-or-lambda*, which is either a function that expects a single remaining argument of a type that matches the value produced by *expression1*, or a lambda expression that expects an argument of that type.</span></span> <span data-ttu-id="ac149-130">Al final de la ejecución de la función, el runtime llama a `Dispose` y libera los recursos (a menos que el valor es `null`, en cuyo caso no se intenta realizar la llamada a Dispose).</span><span class="sxs-lookup"><span data-stu-id="ac149-130">At the end of the execution of the function, the runtime calls `Dispose` and frees the resources (unless the value is `null`, in which case the call to Dispose is not attempted).</span></span>

<span data-ttu-id="ac149-131">En el ejemplo siguiente se muestra el `using` expresión con una expresión lambda.</span><span class="sxs-lookup"><span data-stu-id="ac149-131">The following example demonstrates the `using` expression with a lambda expression.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-2/snippet6302.fs)]

<span data-ttu-id="ac149-132">El ejemplo siguiente se muestra el `using` expresión con una función.</span><span class="sxs-lookup"><span data-stu-id="ac149-132">The next example shows the `using` expression with a function.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-2/snippet6303.fs)]

<span data-ttu-id="ac149-133">Tenga en cuenta que la función podría ser una función que tiene algunos argumentos ya aplicados.</span><span class="sxs-lookup"><span data-stu-id="ac149-133">Note that the function could be a function that has some arguments applied already.</span></span> <span data-ttu-id="ac149-134">El siguiente ejemplo de código muestra esto.</span><span class="sxs-lookup"><span data-stu-id="ac149-134">The following code example demonstrates this.</span></span> <span data-ttu-id="ac149-135">Se crea un archivo que contiene la cadena `XYZ`.</span><span class="sxs-lookup"><span data-stu-id="ac149-135">It creates a file that contains the string `XYZ`.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-2/snippet6304.fs)]

<span data-ttu-id="ac149-136">El `using` función y el `use` enlace son prácticamente equivalentes formas de conseguir lo mismo.</span><span class="sxs-lookup"><span data-stu-id="ac149-136">The `using` function and the `use` binding are nearly equivalent ways to accomplish the same thing.</span></span> <span data-ttu-id="ac149-137">El `using` palabra clave proporciona más control sobre cuándo `Dispose` se llama.</span><span class="sxs-lookup"><span data-stu-id="ac149-137">The `using` keyword provides more control over when `Dispose` is called.</span></span> <span data-ttu-id="ac149-138">Cuando usas `using`, `Dispose` se llama al final de la función o expresión lambda; cuando se usa el `use` palabra clave, `Dispose` se llama al final del bloque de código que lo contiene.</span><span class="sxs-lookup"><span data-stu-id="ac149-138">When you use `using`, `Dispose` is called at the end of the function or lambda expression; when you use the `use` keyword, `Dispose` is called at the end of the containing code block.</span></span> <span data-ttu-id="ac149-139">En general, es preferible usar `use` en lugar de la `using` función.</span><span class="sxs-lookup"><span data-stu-id="ac149-139">In general, you should prefer to use `use` instead of the `using` function.</span></span>

## <a name="see-also"></a><span data-ttu-id="ac149-140">Vea también</span><span class="sxs-lookup"><span data-stu-id="ac149-140">See also</span></span>

- [<span data-ttu-id="ac149-141">Referencia del lenguaje F#</span><span class="sxs-lookup"><span data-stu-id="ac149-141">F# Language Reference</span></span>](index.md)